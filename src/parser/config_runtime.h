#ifndef PARSER_CONFIG_RUNTIME_H
#define PARSER_CONFIG_RUNTIME_H

#include <stdbool.h>

bool ps_get_enable_size_compatible_nonscalar_cast(void);
void ps_set_enable_size_compatible_nonscalar_cast(bool enable);

#endif
