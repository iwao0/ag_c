// `(char[]){"..."}` — 複合リテラルのブレース内文字列
// 修正前: ブレースで包まれた文字列が1要素として扱われ、文字列ポインタの
//        下位 1 バイトのみが先頭に書き込まれていた (exit=208)
// 期待: exit=215 ('h'=104, 'o'=111)
int main(void) {
    char *s = (char[]){ "hello" };
    return s[0] + s[4];
}
