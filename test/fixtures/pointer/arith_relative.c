// ポインタの相対アクセス: *(p-1), *(p+0), *(p+1)
// arr={100,200,300}, p=arr+1, → 100+200+300=600
// 但し int 戻り値で mod 256 → 600-2*256=88
// 期待: exit=88
int main(void) {
    int arr[3];
    arr[0]=100; arr[1]=200; arr[2]=300;
    int *p = arr + 1;
    return *(p-1) + *(p+0) + *(p+1);
}
