# C11 Compiler Initial Setup - Task List

- [x] プロジェクトの初期設定
  - [x] `Makefile`の作成
  - [x] 自動テスト用スクリプト `test.sh` の作成
- [x] コンパイラ本体の初期実装 (TDDフェーズ)
  - [x] 機種依存コード用ディレクトリ (`arch`) の作成
  - [x] 単一の整数を読み込む簡易パーサー (`main.c`) と、数値を返すアセンブリ出力の実装 (`arch/arm64_apple.c`)
  - [x] `test.sh` に単一整数のテストケースを追加し、`make test` で動作確認
  - [x] `+` と `-` を解釈する簡易パーサーの実装 (`main.c`)
  - [x] 加減算用のアセンブリを出力する機能の追加 (`arch/arm64_apple.c`)
  - [x] `test.sh` に加減算のテストケースを追加し、`make test` で動作確認

- [x] 字句解析器（Tokenizer）の実装 (リファクタリング)
  - [x] `src/tokenizer/` ディレクトリの作成とヘッダ定義
  - [x] トークンの定義とトークナイズ処理の実装
  - [x] `main.c` のベタ書きパーサーをトークナイザ経由の処理に置き換え
  - [x] テストが通ることを確認

- [x] 字句解析器（Tokenizer）の単体テスト実装
  - [x] `test/` ディレクトリの作成と `test_tokenizer.c` の実装
  - [x] `tokenizer.c` 内の全関数（`tokenize`, `consume`, `expect`, `expect_number`, `at_eof` 等）の網羅的なテストケース作成
  - [x] `Makefile` を修正して `make test` または `make unittest` で単体テストを実行できるように対応
  - [x] 期待通りにすべてのテストがPassすることを確認

- [x] 四則演算（加減乗除）と括弧の実装 (TDD)
  - [x] `test.sh` に `*`, `/`, `()` のテストケースを先行追加
  - [x] `tokenizer.c` とテストコードの拡張（`*`, `/`, `(`, `)` 対応）
  - [x] トークナイズの単体テストが通ることを確認
  - [x] `src/parser/` を新設し、再帰下降構文解析のパース処理(AST構築)を実装
  - [x] `arch/arm64_apple.c` に乗算(`mul`)・除算(`sdiv`)のアセンブリ生成を追加
  - [x] 結合テストが通ることを確認

- [x] 構文解析器（Parser）の単体テスト実装
  - [x] `test/test_parser.c` の実装およびAST構築(`expr()`)の検証ケース作成
  - [x] `Makefile` を修正して `make test` または `make unittest` でパーサーの単体テストを実行可能にする
  - [x] 期待通りにすべてのテストがPassすることを確認

- [x] テスト環境のリファクタリング
  - [x] `ASSERT_TRUE` などのテストマクロを `test/test_common.h` に分離

- [x] 比較演算子の実装 (`==`, `!=`, `<`, `<=`, `>`, `>=`) (TDD)
  - [x] `test.sh` に比較演算子のテストケースを先行追加
  - [x] `test_tokenizer.c` に比較演算子のトークナイズテストを追加
  - [x] `test_parser.c` に比較演算子のパース(AST)テストを追加
  - [x] `tokenizer.c` および `parser.c` のコードの拡張
  - [x] `arch/arm64_apple.c` に比較演算のアセンブリ生成を追加 (`cmp`, `cset`)
  - [x] 単体テストおよび結合テストが通ることを確認

- [x] ローカル変数サポートの実装 (TDD)
  - [x] `test.sh` に代入式・複文（セミコロン区切り）のテストケースを先行追加
  - [x] `test_tokenizer.c` に識別子(`TK_IDENT`)・セミコロン・`=` のトークナイズテストを追加
  - [x] `test_parser.c` に代入式・変数参照・複文のAST構造テストを追加
  - [x] `tokenizer.c` を拡張（`TK_IDENT` トークン、セミコロン、`=` 対応）
  - [x] `parser.h`/`parser.c` を拡張（`ND_ASSIGN`, `ND_LVAR` ノード、`program()` / `stmt()` / `assign()` 関数）
  - [x] `ag_c.h` / `arm64_apple.c` を拡張（スタックフレーム(`stp`/`ldp`)、変数アドレス計算(`gen_lval`)）
  - [x] `main.c` を拡張（複文対応：`program()` を呼び出し、各文ごとに `gen()` を実行）
  - [x] 単体テストおよび結合テストが通ることを確認

- [x] if/else 制御構文の実装 (TDD)
  - [x] テストケース・実装コードの作成
  - [x] 単体テストおよび結合テストが通ることを確認
  - [x] `grammar.md` の更新

- [x] for/while ループの実装 (TDD)
  - [x] テストケース・実装コードの作成
  - [x] 単体テストおよび結合テストが通ることを確認
  - [x] `grammar.md` の更新

- [x] 結合テストの C 言語化
  - [x] `test/test_e2e.c` を作成（test.sh の全ケースを移植）
  - [x] `Makefile` の `test` ターゲットを更新
  - [x] 全テストが通ることを確認
- [x] `walkthrough.md` への実装完了項目の追記

- [x] return 文の実装 (TDD)

- [x] ブロック文（中括弧）の実装 (TDD)

- [x] 関数定義・呼び出しの実装 (TDD)

- [x] 複数文字変数名の実装 (TDD)

- [x] 型宣言 (int) の実装 (TDD)

- [x] ポインタ・配列の実装 (TDD)

- [x] 文字列リテラルの実装 (TDD)

## 今後の課題・リファクタリング
- [x] `token_t` を用途ごとに分割/整理する（`token_ident_t`/`token_string_t`/`token_num_t`/`token_pp_t`）
- [x] `node_t` を用途ごとに分割/整理する
- [x] `MAX_STMTS` 等による静的確保から、都度の動的メモリ確保（malloc/calloc）へと移行する

## C11準拠ギャップ解消（Tokenizer）
- [x] 大きい整数リテラルで `val` が `int` に切り詰められる問題を修正する
  - [x] `token_num_t` の整数保持を `uval` 基準で扱うように統一する
  - [x] `INT_MAX` 超えのリテラルで値が壊れないことをテストで保証する
- [x] マルチ文字文字定数（例: `'ab'`）をC仕様に合わせて受理する
  - [x] 文字定数のパースを複数文字対応に拡張する
  - [x] 既存の `'ab'` 異常系テストを仕様準拠に更新する
- [x] 文字列/文字定数のエンコーディング接頭辞を実装する
  - [x] `L"..."`, `u"..."`, `U"..."`, `u8"..."` をサポートする
  - [x] `L'c'`, `u'c'`, `U'c'` をサポートする
- [x] Universal Character Name（`\uXXXX`, `\UXXXXXXXX`）を実装する
  - [x] 識別子中のUCNを受理できるようにする
  - [x] 文字列/文字定数中のUCNを解釈できるようにする
- [x] トライグラフ置換（`??=` など）の対応方針を決めて実装する
  - [x] strict C11モードで有効化するか常時有効化するかを決定する
  - [x] 代表的なトライグラフ入力の回帰テストを追加する
- [x] `0b...`（2進整数）をC11拡張として扱うか、strict C11で禁止するかを明確化する
  - [x] 方針に合わせてTokenizer実装とテスト期待値を更新する
- [x] 隣接文字列リテラル連結（`"a" "b"`）の対応を実装する
  - [x] TokenizerまたはParserのどちらで連結するかを設計として明文化する
  - [x] 連結結果の文字列内容と長さをテストで保証する

## C11厳密準拠の追加課題（Tokenizer）
- [x] 文字列/文字定数の接頭辞ごとの型情報を保持する
  - [x] `L/u/U/u8`（文字列）と `L/u/U`（文字）をトークンに保持するフィールドを追加する
  - [x] Parser/Codegen側へ型情報を伝搬する
- [x] UCN（`\uXXXX`, `\UXXXXXXXX`）をUnicodeコードポイントとして正しく扱う
  - [x] 現在の1バイト化をやめ、必要なエンコード（少なくともUTF-8）へ変換する
  - [x] 識別子・文字列・文字定数で整合した解釈にする
- [x] C11の識別子規則に沿ったUCN検証を追加する
  - [x] 許可されないコードポイントや位置の検出を行う
  - [x] 異常系テストを追加する
- [x] 接頭辞付き文字定数の値/型の意味をC11寄りに整理する
  - [x] `L'c'`, `u'c'`, `U'c'` の型・値規則を設計し実装する
  - [x] マルチ文字定数との整合を確認する
- [x] エスケープシーケンスの実装定義範囲を見直す
  - [x] `\x` / 8進エスケープの境界動作を仕様と照らして調整する
  - [x] 既存テストの期待値を必要に応じて更新する
- [x] トライグラフ処理のモード設計を厳密化する
  - [x] 常時有効/strict時のみ有効の方針を定めて統一する
  - [x] 翻訳フェーズ順序との整合をドキュメント化する
- [x] `0b...` のデフォルト挙動を方針化する
  - [x] 「常時拡張許可」か「strict既定＋拡張モード許可」かを決める
  - [x] CLIオプション/設定で切り替え可能にする（必要なら）

## Tokenizer最適化方針（メンテナンス性重視）
- [x] 計測基盤を先に整備する
  - [x] `tokenize()` のベンチマーク（入力サイズ別）を追加する
  - [x] 指標を `tokens/sec` / `alloc回数` / `ピークメモリ` に固定する
  - [x] 最適化前のベースライン結果を記録する
    - [x] `make bench`（2026-03-16, Apple clang `-O0`）  
      1KB: `1,399,168 tokens/sec`, `alloc_count=674`, `peak_alloc_bytes=40,313`  
      16KB: `2,177,904 tokens/sec`, `alloc_count=10,370`, `peak_alloc_bytes=621,265`  
      256KB: `3,744,003 tokens/sec`, `alloc_count=165,602`, `peak_alloc_bytes=9,922,249`
- [x] Tokenizerの責務を分離する
  - [x] `src/tokenizer/` を `scanner.c` / `literals.c` / `keywords.c` / `config_runtime.c` へ段階的に分割する
    - [x] `literals.c` を追加し、文字/文字列接頭辞・UCN・escape処理を移設する
    - [x] `config_runtime.c` を追加し、strict/拡張モード設定を分離する
    - [x] `scanner.c` を追加し、空白/コメントスキップと識別子開始・継続判定を分離する
  - [x] `tokenizer.c` は全体制御フロー中心に整理する
  - [x] UCN・escape等の共通処理をユーティリティへ集約する
- [x] 判定ロジックをテーブル駆動へ移行する
  - [x] 演算子判定（1/2/3文字）をデータテーブル化する
  - [x] キーワード判定を長さ分岐＋テーブル参照へ置換する
  - [x] `is*` 系判定の重複を削減し、文字種判定の共通化を行う
  - [x] 先頭文字ベースの高速分岐（punctuator/keyword）を追加する
  - [x] 数値リテラル判定に高速パスを追加し、重い分岐を補助関数へ分離する
  - [x] `punctuator_kind_for_str` を長さ+先頭文字分岐で高速化する
- [x] メモリアロケーションを最適化する
  - [x] トークン生成を `calloc` 連発から arena/pool 方式へ移行する
  - [x] trigraph 置換バッファの全体コピーを必要時のみ実行する
  - [x] 識別子UCN変換バッファの再利用を導入する
  - [x] `make bench` 再計測（2026-03-16, Apple clang `-O0`）  
    1KB: `2,568,702 tokens/sec`, `alloc_count=3`, `peak_alloc_bytes=49,224`  
    16KB: `2,668,296 tokens/sec`, `alloc_count=37`, `peak_alloc_bytes=607,096`  
    256KB: `3,651,218 tokens/sec`, `alloc_count=590`, `peak_alloc_bytes=9,680,720`
- [x] 文字列/エスケープ処理の一貫性を高める
  - [x] Tokenizer/Codegen間でエスケープ解釈ロジックを共通化する
  - [x] prefix/char幅の型情報伝搬ルールを明文化し実装に反映する
  - [x] strict C11 と拡張モードの分岐点を設定層に集約する
- [x] 回帰防止と保守性指標を導入する
  - [x] 性能回帰テストをCIに追加する
  - [x] 可読性維持のため関数長・複雑度の基準を設定する
  - [x] 最適化前後の性能比較レポートを残す
  - [x] 追加最適化後の再計測を行う（2026-03-16, 256KB: `12,023,597 tokens/sec`）

## Tokenizer追加最適化計画（2026-03-16）
- [x] 目標設定と基準値固定
  - [x] 基準値を固定する（`256KB = 12,023,597 tokens/sec`, `alloc_count=590`）
  - [x] 次の目標値を定義する（`+10〜20%`, `alloc_count`維持）
- [x] Hot Path最終最適化
  - [x] `tokenize()` 冒頭を先頭1バイトdispatch中心に再整理する
  - [x] 数値リテラル判定の10進整数最短ルートを追加する
  - [x] `tk_skip_ignored()` の条件順を実測ベースで見直す
- [x] メモリ局所性の改善
  - [x] token構造体のフィールド順を見直し、頻出アクセスを前方へ寄せる
  - [x] arenaチャンクサイズを入力長ベースの可変戦略にする
- [x] 文字列/識別子の遅延処理
  - [x] UCNなし識別子のゼロコピー経路を強化する
  - [x] 文字列escape decodeを必要時のみ実施する
- [x] 回帰防止の強化
  - [x] ベンチ入力を3種追加する（識別子偏重/数値偏重/記号偏重）
  - [x] CIしきい値をケース別に設定する
- [x] 仕上げ
  - [x] `tokenizer_perf_report.md` に改善前後比較表を追記する
  - [x] 目標達成判定と未達項目を整理する

## Tokenizerさらなる最適化タスク（次フェーズ）
- [x] `match_punctuator()` を先頭2バイトの小テーブル化で高速化する
  - [x] 既存の最長一致仕様（3文字/2文字/1文字）を維持したまま分岐数を削減する
  - [x] 記号偏重ベンチ（punct-heavy 256KB）で改善有無を確認する
- [x] `tk_skip_ignored()` を2層化する（ASCIIホットパス + フォールバック）
  - [x] 通常ケース（空白/コメントなし）で最短復帰できるようにする
  - [x] コメント/行継続/非ASCIIをフォールバック側に集約して可読性を維持する
- [x] 数値リテラルの浮動小数点判定をさらに軽量化する
  - [x] `strtod` 呼び出し到達率を下げるための事前判定を追加する
  - [x] numeric-heavy ベンチで改善有無を確認する
- [x] 識別子・キーワード判定の分岐順を実測ベースで再配置する
  - [x] 長さ別 + 先頭文字別の判定順をホットデータに合わせて調整する
  - [x] ident-heavy ベンチで改善有無を確認する
- [x] ベンチ運用を `-O0/-O2` の2軸で定点化する
  - [x] 計測スクリプトに `-O2` 実行を追加する
  - [x] レポートに `-O0/-O2` の比較表を追加する

## Tokenizer追加最適化タスク（次ラウンド）
- [x] `token_t` 系のレイアウトをアクセス頻度ベースで再調整する
  - [x] ホットフィールドを先頭に寄せ、キャッシュミスを減らす
  - [x] ABI影響箇所（Parser/Codegen）を確認し回帰テストを通す
- [x] `match_punctuator` の2文字判定を完全テーブル参照化する
  - [x] 現在の線形探索を O(1) 参照に置き換える
  - [x] 3文字/4文字の最長一致仕様は維持する
- [x] 数値リテラル判定の事前スキャンを強化する
  - [x] `strtod` 呼び出しが不要なケースをさらに増やす
  - [x] `numeric-heavy` ベンチで改善を確認する
- [x] キーワード判定を最小完全ハッシュ化（例: gperf）で比較検証する
  - [x] 現行実装と可読性・保守性・速度を比較して採用判断する
  - [x] 不採用の場合も検証結果を `tokenizer_perf_report.md` に記録する
- [x] 分岐予測ヒント（`likely/unlikely`）をホットパス限定で導入する
  - [x] `tokenize` / `scanner` の最頻分岐に限定して適用する
  - [x] 可読性を損なう箇所には導入しない
- [x] `inline` 方針を `-O2` 前提で見直す
  - [x] 小関数のみ `static inline` を適用して過剰インラインを避ける
  - [x] `check_function_size.sh` の基準内であることを確認する
- [x] PGO（Profile Guided Optimization）導入を検証する
  - [x] 学習用ベンチ入力を定義し、`-fprofile-generate/-fprofile-use` を試す
  - [x] CIへの常時導入可否を判断する
- [x] 実コード由来コーパスでのTokenizerベンチを追加する
  - [x] 合成ベンチ（mixed/ident/numeric/punct）との乖離を可視化する
  - [x] 改善優先度を実コード寄りに再調整する

## Tokenizer最適化 継続/停止判断つき計画（2026-03-16）
- [x] フェーズ0: 現状維持ゲートを定義する
  - [x] 評価指標を `mixed/ident/numeric/punct/corpus` の `tokens/sec` と `alloc_count` に固定する
  - [x] 継続条件を「全ケースで -5% 以内」に設定する
  - [x] 打ち止め条件を「2連続で改善が ±2% 以内」に設定する
- [x] フェーズ1: 低リスク最適化のみ実施する（1テーマ1コミット）
  - [x] `tk_scan_ident_start/continue` の分岐順を実測ベースで微調整する
  - [x] 数値サフィックス判定を小テーブル化して分岐を削減する（試行したが回帰のため不採用）
  - [x] `match_punctuator` の3文字判定で頻出順の比較に調整する
- [x] フェーズ2: 判定とロールバック判断
  - [x] 各テーマ実装後に `make test bench` + perf-check を必ず実行する
  - [x] 1ケースでも -5% を超えた場合は即ロールバック候補として保留する
  - [x] 改善が誤差域（±2%）なら次テーマに進まず打ち止めを検討する（今回の連続ステップは誤差域外のため継続）
- [x] フェーズ3: 継続しない場合の方針を確定する
  - [x] Tokenizerの大規模変更（DFA化/生成器導入）は本線では実施しない
  - [x] 高コスト案は別ブランチでPoCのみ実施する

## Parser未実装タスク（2026-03-17 棚卸し）
- [x] `return;`（`void` 関数での式なし return）をサポートする
  - [x] `stmt()` の `return` 分岐で `;` 直後を許可する
  - [x] `void`/非`void` の整合チェック方針を決める（`void` は `return;` のみ許可、非`void` は式必須）
- [x] `sizeof` 演算子（`sizeof expr` / `sizeof(type)`）を実装する
  - [x] Tokenizerの `TK_SIZEOF` を Parser の `unary` に接続する
  - [x] 型名版と式版の評価ルール（少なくとも既存基本型）を定義する（`sizeof(void)` は未対応として診断）
- [x] キャスト式（`(type)expr`）を実装する
  - [x] `unary` にキャスト文法を追加する
  - [x] 数値型間キャストの最小ルール（`int/char/short/long/float/double`）を定める（第1段: `char/short` はビット幅マスク、`int/long/pointer` は no-op、`float/double` は型情報付与）
- [x] カンマ演算子（`expr , expr`）を実装する
  - [x] `expr = assign ("," assign)*` に拡張する
  - [x] 関数引数区切りとの衝突が起きないことをテストで保証する（引数は `assign` 区切りに統一）
- [x] 宣言子の拡張（1文で複数宣言）を実装する
  - [x] `int a, b=1;` のような複数 declarator をパース可能にする
  - [x] `for` 初期化部の宣言にも同じ規則を適用する
- [x] 関数プロトタイプ宣言（本体なし）を実装する
  - [x] `program` で `funcdef` と宣言文を判別する（`...);` は定義ノードを生成せず読み飛ばす）
  - [x] 宣言情報の保持方式（シンボル表）を決める（第1段: シンボル表は導入せず、パース受理のみ対応）
- [x] `goto` / ラベル文（`label:`）を実装する
  - [x] Parserでラベル定義と参照を収集し、未定義ラベル診断を追加する
  - [x] codegen用にラベル解決情報をASTに保持する
- [x] `struct` / `union` / `enum` の最小文法を実装する
  - [x] Parserで型宣言と型参照の区別を導入する
  - [x] まずはタグ宣言＋メンバ参照なしの最小セットから段階導入する
  - [x] 次優先を機能実装（Parser/Preprocessor強化）へ戻す

## Tokenizer C11差分タスク（strictデフォルトは維持しない）
- [x] 方針: strict C11モードはデフォルトOFFを維持する
  - [x] `0b...` は拡張としてデフォルト許可、strict時のみ拒否
- [x] 浮動小数点サフィックス情報をトークンに保持する
  - [x] `f/F` と `l/L` を `token_num_t` に保持する
  - [x] Parserで `long double` を `is_float=3` として扱い、Codegenは現状double経路へ lowering する方針を適用する
- [x] 接頭辞付き文字定数のマルチ文字扱いをC11寄りに整理する
  - [x] `L/u/U` 付きで複数 `c-char` をどう扱うか方針化する
  - [x] 方針に合わせてTokenizer実装と異常系テストを更新する

## Tokenizer API命名統一
- [x] `tokenizer.h` 公開APIを `tk_` 接頭辞へ完全移行する
  - [x] `src/` / `test/` の呼び出しを `tk_` 名へ置換する
  - [x] 旧API互換マクロを削除する

## Parser 構文拡張タスク（次フェーズ）
- [x] `for` 初期化子で宣言を受理する（`for (int i=0; ... )`）
  - [x] 宣言パースを共通関数化して `stmt` と `for` で再利用する
  - [x] 回帰テストを追加する（`test_stmt_for_with_decl_init`）
- [x] 単項演算子 `+` / `-` / `!` / `~` を実装する
  - [x] `unary` 規則を拡張し、既存の `*` / `&` と整合させる
  - [x] 既存ASTノードへのloweringでコード生成を成立させる（`-x`, `!x`, `~x`）
- [x] `do ... while` 文を実装する
  - [x] Parser文法の追加（`stmt` 分岐）
  - [x] e2eテストと単体テストを追加する
- [x] `break` / `continue` 文を実装する
  - [x] ループ入れ子時のジャンプ先管理を導入する
  - [x] `for` / `while` / `do while` での回帰テストを追加する
- [x] `switch` / `case` / `default` 文を実装する
  - [x] caseラベル管理と fallthrough 動作を実装する
  - [x] `break` と組み合わせた制御フローテストを追加する
- [x] `++` / `--`（前置・後置）を実装する
  - [x] lvalue制約チェックを追加する
  - [x] スカラーの主要ケースをテストする
- [x] 複合代入（`+=`, `-=`, `*=`, `/=` など）を実装する
  - [x] 既存 `assign` 規則を拡張する
  - [x] 型サイズ（`char/short/int/ptr`）に応じた代入テストを追加する
- [x] 論理演算（`&&`, `||`）を短絡評価で実装する
  - [x] 優先順位を比較演算より低く、代入より高く定義する
  - [x] 副作用を含む式で短絡が効くことをe2eで確認する
- [x] 条件演算子（`?:`）を実装する
  - [x] 右結合性を満たすように文法を設計する
  - [x] ネストケースのASTテストを追加する

## 次に実装可能なタスク（優先度順）
- [x] ビット演算（`|`, `^`, `&`）を式として実装する
  - [x] 優先順位を `==`/`<` 系より低く、`&&`/`||` より高く定義する
  - [x] `ND_BITOR` / `ND_BITXOR` / `ND_BITAND` を追加し codegen を実装する
  - [x] e2e で `mask`/`flag` 系の回帰テストを追加する
- [x] シフト演算（`<<`, `>>`）を実装する
  - [x] `add` より低く、比較演算より高い優先順位で文法に追加する
  - [x] codegen に `lsl` / `asr`（符号付き右シフト）を追加する
  - [x] 負数/境界値のテスト方針を定める（`>>` は算術右シフト `asr`、境界は `shift by 0` と高ビット領域で検証）
- [x] 剰余演算（`%`）を実装する
  - [x] `mul` 規則に `%` を追加する
  - [x] ARM64 で `sdiv` + `msub` による剰余コード生成を追加する
  - [x] `a%b` の正常系・0除算系テストを追加する（負数剰余と 0除算の実装依存ケースを追加）
- [x] 残りの複合代入（`%=`, `<<=`, `>>=`, `&=`, `^=`, `|=`）を実装する
  - [x] 既存 `assign` 拡張パターンに追従して lower する
  - [x] ビット演算/シフト実装後に e2e で一括回帰を追加する
- [x] `switch` の診断強化を行う
  - [x] `case` 値の重複検出を追加する
  - [x] `default` 重複時のエラー位置を改善する
  - [x] ネストした `switch` + `continue` の境界ケースを追加する
- [x] 文法・意味の最小型チェックを導入する
  - [x] `continue`/`break` の不正位置エラーを parser 側でも明示する
  - [x] `++/--` の対象型制約（非スカラー禁止など）を段階導入する（第1段: `float`/`double` を拒否）
  - [x] エラーメッセージの一貫化（`tk_error_tok` 経由）を行う
- [x] テスト基盤の可観測性を改善する
  - [x] `test_e2e` の失敗時に生成 `.s` の保存パスをログへ出力する
  - [x] 新カテゴリ（`bitwise`, `shift`, `switch_edge`）を追加する
  - [x] CI で parser/e2e を分離実行し、失敗原因を切り分けやすくする

## Tokenizer Doxygen整備タスク（保守性向上）
- [x] 対象範囲を確定する
  - [x] `src/tokenizer/` の公開APIと内部ヘルパーを分類する
  - [x] 公開API（ヘッダ宣言）を優先対象として明記する
- [x] コメント方針を定義する
  - [x] `@brief`, `@param`, `@return`, `@pre`, `@post`, `@note` の最小テンプレートを定める
  - [x] 副作用や実装依存のある関数には `@warning` を付与する方針を追加する
- [x] 公開ヘッダにDoxygenコメントを付与する
  - [x] `tokenizer.h` に公開関数コメントを追加する
  - [x] `token.h` に公開構造体・enumの説明を追加する
  - [x] `escape.h` / `literals.h` / `scanner.h` / `keywords.h` / `punctuator.h` / `allocator.h` の公開宣言を整備する
- [x] 実装ファイルの主要関数コメントを補完する
  - [x] `tokenizer.c` のエントリ関数と主要分岐点に関数コメントを追加する
  - [x] `literals.c` / `escape.c` のリテラル・エスケープ処理に前提条件と失敗条件を明記する
  - [x] `scanner.c` / `keywords.c` / `punctuator.c` / `allocator.c` のホットパス関数に意図を明記する
- [x] 意味整合性を確認する
  - [x] `num_kind` / `fp_kind` / strict C11関連フラグの説明が実装と一致しているか確認する
  - [x] コメントと実際のエラー挙動（例: 異常系）に齟齬がないか確認する
- [x] 品質ゲートを実施する
  - [x] `make test` を実行して回帰がないことを確認する
  - [x] 追加コメントが警告や可読性低下を招いていないかレビューする
- [x] 運用ルールをドキュメント化する
  - [x] 「Tokenizer新規関数はDoxygen必須」のルールを追記する
  - [x] 「公開API変更時はDoxygen更新必須」のルールを追記する

## Parser最適化計画（保守性 + 実行速度）
- [x] フェーズ1: 現状計測を固定する
  - [x] Parserベンチを追加し、入力サイズ別の計測値（parse time / throughput）を記録する
    - [x] 計測結果を `parser_perf_report.md` に記録する（`tokenize`/`parser` 分離計測）
  - [x] 保守性指標（関数長・分岐数・重複コード）を可視化する
  - [x] 改善目標値（例: +10%）と許容回帰閾値（例: -5%）を定義する
- [x] フェーズ2: 責務分離で保守性を上げる
  - [x] `stmt`/`decl`/`expr` をモジュール分割し、`parser.c` を段階的に縮小する
    - [x] `decl` 系（ローカル変数表・宣言パース）を `parser_decl` へ分離する
    - [x] `stmt` 系を `parser_stmt` へ分離する
    - [x] `expr` 系を `parser_expr` へ分離する
  - [x] タグ管理・ラベル管理・型ユーティリティを専用モジュールへ分離する
  - [x] 共有ヘルパーの公開範囲を整理して依存関係を単純化する
- [x] フェーズ3: ホットパス最適化を実施する
  - [x] `expr/assign/unary/primary` の分岐順を頻出ケース優先へ調整する
  - [x] `is_type_token`/サイズ判定などの繰り返し計算を局所キャッシュする
  - [x] 動的配列成長戦略を統一して `realloc` 頻度を抑える
- [x] フェーズ4: 探索構造を改善する
  - [x] `goto`/label 解決の線形探索をハッシュ化して探索コストを削減する
  - [x] タグ型参照の探索も同様に高速化する
  - [x] 構造変更後に異常系診断（未定義/重複）の精度を再確認する
- [x] フェーズ5: エラーハンドリングを整理する
  - [x] Parser診断メッセージの生成パターンを共通化する
  - [x] コンテキスト付きエラー（どの規則で失敗したか）を統一フォーマットで出す
  - [x] 既存テストのメッセージ依存箇所を必要に応じて調整する
- [x] フェーズ6: 回帰防止を強化する
  - [x] Parser性能回帰チェックをCIに追加する
  - [x] 1テーマ1コミットで段階適用し、各段階で `make build/test_parser build/test_e2e && build/test_parser && build/test_e2e` を必須化する
  - [x] 各フェーズの計測結果と判断ログを `parser_perf_report.md` に記録する

## Codegen最適化タスク（ARM64 float literal）
- [x] 浮動小数点リテラルの即値化可能判定を導入する
  - [x] 即値化可能な値は `fmov` 系でロードする（現状: `+0.0f` / `+0.0`）
  - [x] それ以外は既存どおり `float_literals` 経由でロードする

## `printf` 可変長引数ABI対応（`ag_printf`撤去）
- [x] 原因を確定する
  - [x] Apple ARM64(Darwin) における可変長引数呼び出し規約を、`clang -S` 出力で確認する
  - [x] 現行 `ag_c` 出力との差分（レジスタ渡し/スタック渡し/16byteアライメント）を整理する
  - [x] `%d` が崩れる再現ケースを最小化し、原因を「呼び出し規約不一致」で固定する
- [x] 一時ワークアラウンドを除去する
  - [x] `gen_runtime_helpers()` の生成を削除する
  - [x] `include/stdio.h` の `#define printf ag_printf` を削除し、通常の `printf` 宣言へ戻す
  - [x] `ag_printf` シンボルへの依存を全て除去する
- [x] Codegenで正しい可変長引数呼び出しを実装する
  - [x] `ND_FUNCALL` で variadic 関数呼び出し経路を追加する（まず `printf` 対象）
  - [x] variadic 引数をABIどおりスタックへ配置し、call直前のSP 16byteアラインを保証する
  - [x] 呼び出し後のスタック回復と既存式評価スタックとの整合を取る
- [x] 回帰テストを追加する
  - [x] `#include <stdio.h>` + `printf("x=%d\n", 42)` のE2Eテストを追加する
  - [x] 既存の `puts` / 通常関数呼び出しが壊れていないことを確認する
- [x] 検証とドキュメント更新
  - [x] `build/test_preprocess` と `build/test_e2e` を通す
  - [x] 仕様メモ（Darwin variadic ABI対応方針）を `walkthrough.md` へ追記する

## 固定長バッファ見直し（ハードコード長の削減）
- [x] `preprocess.c` の `#include` パス組み立てを動的化する
  - [x] `char alt[1200]` を廃止し、必要長を計算して確保する
  - [x] `char filename[1024]` を廃止し、トークン長に応じて伸長する
- [ ] 同様の固定長バッファを段階的に見直す
  - [x] `src/config/config.c` の `char line[512]` の上限仕様を明文化し、必要に応じて動的化する
  - [x] `src/parser/diag.c` の `char detail[256]` の切り捨て方針を明文化し、診断品質への影響を評価する
  - [x] `src/parser/expr.c` の `char label[32]`（`.LC` ラベル生成）の上限妥当性を確認し、必要なら `snprintf` 依存から安全な生成へ変更する

## バッファオーバーフロー観点の追加ハードニング
- [x] `src/preprocess/preprocess.c` のサイズ計算を `size_t` 化する
  - [x] `stringify_tokens()` の `cap/len` を `size_t` に変更し、加算時のオーバーフローを検出する
  - [x] `paste_tokens()` の `len_l + len_r` を安全に計算する
- [x] `test/test_e2e.c` の固定上限配列を動的化する
  - [x] `categories[64]` / `build_pids[64]` / `pids[64]` を入力件数に応じて確保する
  - [x] カテゴリ数増加時に境界外アクセスが起きないことを確認する

## セキュリティ堅牢化（2026-03-17）
- [x] プリプロセッサのファイル読み込みで `ftell` 失敗時の境界処理を強化する
  - [x] `ftell` の負値を検出し、`size_t` 変換前に失敗扱いにする
  - [x] `fread` の戻り値検証を追加する
- [x] 動的配列拡張の `realloc` 未検証箇所を安全化する
  - [x] Parser系の配列拡張を `realloc` 失敗時に即エラー終了する共通ヘルパへ統一する
  - [x] Preprocessor/Codegenの配列拡張でも同様に失敗を検知する
- [x] 文字列連結・`#include` ファイル名構築の長さ検証を強化する
  - [x] `int` 長さの負値/加算オーバーフローを検出して中断する
  - [x] 極端に大きい入力での `size_t` 桁あふれ経路を遮断する
- [x] `#include` のパストラバーサル耐性を追加する
  - [x] `../`・絶対パス・許可外ディレクトリ参照を拒否する方針を定義する
  - [x] プロジェクトルート/許可インクルードパス内に解決されることを検証してから読み込む
- [x] `#include` 再帰展開の深さ上限を導入する
  - [x] include 深さカウンタを追加し、上限超過時は診断を出して中断する
  - [x] 循環 include を検出するか、少なくとも深さ上限でDoSを防止する
- [x] Tokenizer のトークン長管理を `int` 依存から段階的に脱却する
  - [x] `p - start` 由来の長さを `size_t` で保持し、`int` へ落とす箇所に上限チェックを入れる
  - [x] 巨大トークン入力時に安全にエラー終了するテストを追加する

## Parser未実装タスク（2026-03-17 確認）
- [x] `struct/union/enum` のメンバ宣言をパース可能にする
  - [x] `struct S { int x; };` のタグ定義を受理し、型情報にメンバ数を保持する
  - [x] `union` / `enum` のタグ定義ボディを受理する（列挙子式つき含む）
  - [x] ブロック内タグ定義と既存タグ参照のスコープ整合を取る
- [x] `sizeof(void)` の扱い方針を確定する
  - [x] 仕様どおり制約違反として診断を維持するか、拡張として `1` などを返すかを決める（診断維持）
  - [x] 方針に合わせて parser 診断メッセージとテストを明文化する
- [x] `void` cast の扱いを実装する
  - [x] `(void)expr` を式文で受理できるようにする
  - [x] 値を捨てるノード表現（または既存ノードの運用）を定義する（既存ノード運用）
- [x] キャスト対応型の拡張を検討する
  - [x] 現在未対応の型（例: タグ型/一部派生型）に対するエラー方針を整理する（非スカラ型キャストは診断）
  - [x] 受理可能な型セットを文法ファイルと実装で一致させる
  - [x] `signed` / `unsigned` / `_Bool` を宣言型として受理する
  - [x] `_Bool` / `signed` / `unsigned` の基本キャストを受理する
  - [x] タグ型ポインタキャスト（`(struct S*)p`）を受理する

## Parser拡張タスク（グローバル宣言/最外部タグ）
- [x] 最外部スコープで `struct/union/enum` 宣言・定義を受理する
  - [x] `struct S;` / `struct S { int x; };` を関数定義の外でパース可能にする
  - [x] `struct S { int x; } *g;` のようなタグ定義+宣言子を最外部で受理する
- [x] 型付きグローバル宣言（最小）を受理する
  - [x] `int g;` / `int g = 1;` などを関数定義と識別してパースできるようにする
  - [x] 既存の関数定義・プロトタイプ判定を壊さないことをテストで確認する

## Parser未実装タスク（2026-03-17 再棚卸し）
- [x] 構造体/共用体メンバ参照式を実装する
  - [x] `a.b`（`.`）を `postfix` に追加する
  - [x] `p->b`（`->`）を `postfix` に追加する
  - [x] メンバ名解決と型サイズ反映（`type_size`/`deref_size`）を実装する
- [x] `enum` 列挙子を識別子として参照可能にする
  - [x] `enum E { A=1, B=2 }; return A;` のような列挙子名をシンボル表で解決する
  - [x] 明示値なし列挙子（自動インクリメント）を扱えるようにする
  - [x] 明示値は整数リテラルとして受理する（式全般は今後拡張）
- [x] `typedef` を型名として扱う
  - [x] `typedef` 宣言をパースし、型名テーブルへ登録する
  - [x] `is_type_token` 判定を拡張し、typedef名を `type` として受理する
- [x] 宣言子文法を拡張する（現在は `*` + 識別子 + 配列の最小形）
  - [x] 関数ポインタ宣言（例: `int (*fp)(int)`）をパース可能にする
  - [x] 複合型指定子（例: `unsigned long`）を段階的に受理する
  - [x] 入れ子宣言子（多段の `(` `)` を含む宣言子）を段階的に受理する
  - [x] `const`/`volatile` など修飾子付き宣言子を受理する

## Parser未実装タスク（2026-03-17 追記）
- [x] 関数ポインタの「式としての呼び出し」を実装する
  - [x] `fp(1, 2)` のような「識別子以外をcalleeに取る呼び出し」を `postfix` で受理する
  - [x] `(*fp)(1)` / `p->fp(1)` 相当の呼び出しパターンを段階導入する
- [x] 関数仮引数の宣言子を拡張する
  - [x] 仮引数で関数ポインタ宣言子を受理する
  - [x] 仮引数で配列/ポインタの派生宣言子を現状より広く受理する
- [x] キャスト/`sizeof(type)` の宣言子を拡張する
  - [x] `sizeof(int (*)(int))` のような関数ポインタ型を受理する
  - [x] 抽象宣言子（名前なし宣言子）を段階導入する
- [x] `enum` の列挙子初期化式を拡張する
  - [x] 現状の整数リテラル限定を、定数式（加減算・参照済み列挙子）へ拡張する
- [x] 構造体/共用体レイアウト計算を強化する
  - [x] 現状の単純オフセット計算を、アラインメント/パディング考慮へ拡張する

## Parser未実装タスク（2026-03-17 追加棚卸し）
- [x] 関数ポインタ値の実用経路を完成させる
  - [x] `fp = foo;` のような関数名を関数ポインタ値として解決できるようにする
  - [x] `fp(1)` の間接呼び出しで、関数シンボル由来の値経路をE2Eで保証する
- [x] `switch` / `case` の定数式を拡張する
  - [x] `case 1+2:` や `case A*2:` のような整数定数式を受理する
  - [x] 重複判定ロジックが定数式評価結果ベースで動くことを確認する
- [x] `enum` 初期化式の演算セットを拡張する
  - [x] 現状の `+,-,*,/,%,(),単項+/-` から、`<<,>>,|,&,^,~` を段階導入する
  - [x] Cの整数定数式に近い優先順位で評価できるようにする
- [ ] 宣言子の型情報保持を強化する
  - [ ] `const` / `volatile` を単なる受理に留めず、型情報として保持する
  - [ ] ポインタ段ごとの修飾子（例: `int *const p`）を保持できるようにする
- [ ] 構造体/共用体宣言の残差分を埋める
  - [x] ビットフィールド宣言（`int x:3;`）を受理する
  - [ ] 匿名struct/unionメンバの受理方針を決めて段階導入する
- [ ] キャストの未対応型を段階解消する
  - [ ] 非スカラ型へのキャスト制約（現状は診断エラー）を仕様方針に沿って実装/診断整理する
  - [ ] 「この型へのキャストは未対応です」となる型カテゴリを棚卸しして、受理範囲を拡張する

## Parser未実装タスク（2026-03-17 追加棚卸し2）
- [x] C11キーワードのParser接続を進める
  - [x] `_Alignas`, `_Alignof`, `_Atomic`, `_Generic`, `_Noreturn`, `_Static_assert`, `_Thread_local` を段階的に受理する
  - [x] `_Alignas(...)` を宣言前置指定子として受理する（アラインメント適用自体は今後）
  - [x] `_Atomic`（修飾子形式）を宣言前置指定子として受理する
  - [x] `_Atomic(type)` 形式を最小受理する（現状: 組み込み基本型まで）
  - [x] `_Generic` を式として最小受理する（現状: 基本型/ポインタ + `default`）
  - [x] `_Alignof` を式として受理する（型名のみ）
  - [x] `_Static_assert` を最外部/文内宣言として受理する（整数定数式 + 文字列）
  - [x] `_Noreturn` / `_Thread_local` を宣言前置指定子として受理する
  - [x] `_Complex`, `_Imaginary` の扱い方針（受理範囲/診断）を確定する（現状: 明示的に未対応診断）
- [x] ストレージクラス/関数指定子の意味処理を追加する
  - [x] `extern`, `static`, `auto`, `register`, `inline`, `restrict` を宣言子パースと意味解析に反映する
  - [x] 現状の「トークン化のみ」状態から、少なくとも診断一貫性を持った挙動へ移行する
- [x] 可変長引数宣言子（`...`）を実装する
  - [x] `int f(int a, ...);` と `int f(...){}` を仮引数宣言として受理する
  - [x] `...` の位置制約（末尾のみ）を診断で保証する
- [ ] 初期化子リストを実装する
  - [ ] 配列/構造体/共用体の `{ ... }` 初期化を段階導入する
  - [ ] 既存の単一式初期化と競合しない文法に整理する
- [ ] 指示付き初期化子（designated initializer）を実装する
  - [ ] `.x = 1`, `[2] = 3` を受理する
  - [ ] 重複指定や範囲外指定の診断方針を整備する
- [ ] 複合リテラルを実装する
  - [ ] `(struct S){ .x = 1 }` のような型名付き初期化式を受理する
  - [ ] 一時オブジェクトの寿命/格納戦略（最低限の実装方針）を定義する
- [x] 配列宣言子の拡張を進める
  - [x] `int a[1+2]` のような定数式サイズを受理する
  - [x] 多次元配列宣言子と仮引数配列の `static`/`restrict` を段階導入する
  - [x] 多次元配列宣言子（`int a[2][3]`）を宣言構文として受理する

## Parser未実装タスク（2026-03-17 追加棚卸し3）
- [x] 型指定子の組み合わせ受理を厳密化する
  - [x] `long long`, `long long int`, `unsigned long long` を型指定子として受理する
  - [x] `signed char`, `unsigned char`, `unsigned short`, `unsigned int` など符号付き/無し組み合わせを一貫して扱う
  - [x] 不正な組み合わせ（例: `short double`）を診断できるようにする
- [x] `enum` 定数式の対応範囲をC寄りに拡張する
  - [x] 比較演算（`==`, `!=`, `<`, `<=`, `>`, `>=`）を列挙子初期化式で受理する
  - [x] 論理演算（`&&`, `||`）と条件演算子（`?:`）を列挙子初期化式で受理する
  - [x] 上記拡張後も既存の重複 `case` 検出/列挙子解決が壊れないことを回帰テストで保証する
