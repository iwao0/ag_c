# C11 Compiler Initial Setup - Task List

- [x] プロジェクトの初期設定
  - [x] `Makefile`の作成
  - [x] 自動テスト用スクリプト `test.sh` の作成
- [x] コンパイラ本体の初期実装 (TDDフェーズ)
  - [x] 機種依存コード用ディレクトリ (`arch`) の作成
  - [x] 単一の整数を読み込む簡易パーサー (`main.c`) と、数値を返すアセンブリ出力の実装 (`arch/arm64_apple.c`)
  - [x] `test.sh` に単一整数のテストケースを追加し、`make test` で動作確認
  - [x] `+` と `-` を解釈する簡易パーサーの実装 (`main.c`)
  - [x] 加減算用のアセンブリを出力する機能の追加 (`arch/arm64_apple.c`)
  - [x] `test.sh` に加減算のテストケースを追加し、`make test` で動作確認

- [x] 字句解析器（Tokenizer）の実装 (リファクタリング)
  - [x] `src/tokenizer/` ディレクトリの作成とヘッダ定義
  - [x] トークンの定義とトークナイズ処理の実装
  - [x] `main.c` のベタ書きパーサーをトークナイザ経由の処理に置き換え
  - [x] テストが通ることを確認

- [x] 字句解析器（Tokenizer）の単体テスト実装
  - [x] `test/` ディレクトリの作成と `test_tokenizer.c` の実装
  - [x] `tokenizer.c` 内の全関数（`tokenize`, `consume`, `expect`, `expect_number`, `at_eof` 等）の網羅的なテストケース作成
  - [x] `Makefile` を修正して `make test` または `make unittest` で単体テストを実行できるように対応
  - [x] 期待通りにすべてのテストがPassすることを確認

- [x] 四則演算（加減乗除）と括弧の実装 (TDD)
  - [x] `test.sh` に `*`, `/`, `()` のテストケースを先行追加
  - [x] `tokenizer.c` とテストコードの拡張（`*`, `/`, `(`, `)` 対応）
  - [x] トークナイズの単体テストが通ることを確認
  - [x] `src/parser/` を新設し、再帰下降構文解析のパース処理(AST構築)を実装
  - [x] `arch/arm64_apple.c` に乗算(`mul`)・除算(`sdiv`)のアセンブリ生成を追加
  - [x] 結合テストが通ることを確認

- [x] 構文解析器（Parser）の単体テスト実装
  - [x] `test/test_parser.c` の実装およびAST構築(`expr()`)の検証ケース作成
  - [x] `Makefile` を修正して `make test` または `make unittest` でパーサーの単体テストを実行可能にする
  - [x] 期待通りにすべてのテストがPassすることを確認

- [x] テスト環境のリファクタリング
  - [x] `ASSERT_TRUE` などのテストマクロを `test/test_common.h` に分離

- [x] 比較演算子の実装 (`==`, `!=`, `<`, `<=`, `>`, `>=`) (TDD)
  - [x] `test.sh` に比較演算子のテストケースを先行追加
  - [x] `test_tokenizer.c` に比較演算子のトークナイズテストを追加
  - [x] `test_parser.c` に比較演算子のパース(AST)テストを追加
  - [x] `tokenizer.c` および `parser.c` のコードの拡張
  - [x] `arch/arm64_apple.c` に比較演算のアセンブリ生成を追加 (`cmp`, `cset`)
  - [x] 単体テストおよび結合テストが通ることを確認

- [x] ローカル変数サポートの実装 (TDD)
  - [x] `test.sh` に代入式・複文（セミコロン区切り）のテストケースを先行追加
  - [x] `test_tokenizer.c` に識別子(`TK_IDENT`)・セミコロン・`=` のトークナイズテストを追加
  - [x] `test_parser.c` に代入式・変数参照・複文のAST構造テストを追加
  - [x] `tokenizer.c` を拡張（`TK_IDENT` トークン、セミコロン、`=` 対応）
  - [x] `parser.h`/`parser.c` を拡張（`ND_ASSIGN`, `ND_LVAR` ノード、`program()` / `stmt()` / `assign()` 関数）
  - [x] `ag_c.h` / `arm64_apple.c` を拡張（スタックフレーム(`stp`/`ldp`)、変数アドレス計算(`gen_lval`)）
  - [x] `main.c` を拡張（複文対応：`program()` を呼び出し、各文ごとに `gen()` を実行）
  - [x] 単体テストおよび結合テストが通ることを確認

- [x] if/else 制御構文の実装 (TDD)
  - [x] テストケース・実装コードの作成
  - [x] 単体テストおよび結合テストが通ることを確認
  - [x] `grammar.md` の更新

- [x] for/while ループの実装 (TDD)
  - [x] テストケース・実装コードの作成
  - [x] 単体テストおよび結合テストが通ることを確認
  - [x] `grammar.md` の更新

- [x] 結合テストの C 言語化
  - [x] `test/test_e2e.c` を作成（test.sh の全ケースを移植）
  - [x] `Makefile` の `test` ターゲットを更新
  - [x] 全テストが通ることを確認
- [x] `walkthrough.md` への実装完了項目の追記

- [x] return 文の実装 (TDD)

- [x] ブロック文（中括弧）の実装 (TDD)

- [x] 関数定義・呼び出しの実装 (TDD)

- [x] 複数文字変数名の実装 (TDD)

- [x] 型宣言 (int) の実装 (TDD)

- [x] ポインタ・配列の実装 (TDD)

- [x] 文字列リテラルの実装 (TDD)

## 今後の課題・リファクタリング
- [x] `token_t` を用途ごとに分割/整理する（`token_ident_t`/`token_string_t`/`token_num_t`/`token_pp_t`）
- [x] `node_t` を用途ごとに分割/整理する
- [x] `MAX_STMTS` 等による静的確保から、都度の動的メモリ確保（malloc/calloc）へと移行する

## C11準拠ギャップ解消（Tokenizer）
- [x] 大きい整数リテラルで `val` が `int` に切り詰められる問題を修正する
  - [x] `token_num_t` の整数保持を `uval` 基準で扱うように統一する
  - [x] `INT_MAX` 超えのリテラルで値が壊れないことをテストで保証する
- [x] マルチ文字文字定数（例: `'ab'`）をC仕様に合わせて受理する
  - [x] 文字定数のパースを複数文字対応に拡張する
  - [x] 既存の `'ab'` 異常系テストを仕様準拠に更新する
- [x] 文字列/文字定数のエンコーディング接頭辞を実装する
  - [x] `L"..."`, `u"..."`, `U"..."`, `u8"..."` をサポートする
  - [x] `L'c'`, `u'c'`, `U'c'` をサポートする
- [x] Universal Character Name（`\uXXXX`, `\UXXXXXXXX`）を実装する
  - [x] 識別子中のUCNを受理できるようにする
  - [x] 文字列/文字定数中のUCNを解釈できるようにする
- [x] トライグラフ置換（`??=` など）の対応方針を決めて実装する
  - [x] strict C11モードで有効化するか常時有効化するかを決定する
  - [x] 代表的なトライグラフ入力の回帰テストを追加する
- [x] `0b...`（2進整数）をC11拡張として扱うか、strict C11で禁止するかを明確化する
  - [x] 方針に合わせてTokenizer実装とテスト期待値を更新する
- [x] 隣接文字列リテラル連結（`"a" "b"`）の対応を実装する
  - [x] TokenizerまたはParserのどちらで連結するかを設計として明文化する
  - [x] 連結結果の文字列内容と長さをテストで保証する

## C11厳密準拠の追加課題（Tokenizer）
- [x] 文字列/文字定数の接頭辞ごとの型情報を保持する
  - [x] `L/u/U/u8`（文字列）と `L/u/U`（文字）をトークンに保持するフィールドを追加する
  - [x] Parser/Codegen側へ型情報を伝搬する
- [x] UCN（`\uXXXX`, `\UXXXXXXXX`）をUnicodeコードポイントとして正しく扱う
  - [x] 現在の1バイト化をやめ、必要なエンコード（少なくともUTF-8）へ変換する
  - [x] 識別子・文字列・文字定数で整合した解釈にする
- [x] C11の識別子規則に沿ったUCN検証を追加する
  - [x] 許可されないコードポイントや位置の検出を行う
  - [x] 異常系テストを追加する
- [x] 接頭辞付き文字定数の値/型の意味をC11寄りに整理する
  - [x] `L'c'`, `u'c'`, `U'c'` の型・値規則を設計し実装する
  - [x] マルチ文字定数との整合を確認する
- [x] エスケープシーケンスの実装定義範囲を見直す
  - [x] `\x` / 8進エスケープの境界動作を仕様と照らして調整する
  - [x] 既存テストの期待値を必要に応じて更新する
- [x] トライグラフ処理のモード設計を厳密化する
  - [x] 常時有効/strict時のみ有効の方針を定めて統一する
  - [x] 翻訳フェーズ順序との整合をドキュメント化する
- [x] `0b...` のデフォルト挙動を方針化する
  - [x] 「常時拡張許可」か「strict既定＋拡張モード許可」かを決める
  - [x] CLIオプション/設定で切り替え可能にする（必要なら）

## Tokenizer最適化方針（メンテナンス性重視）
- [x] 計測基盤を先に整備する
  - [x] `tokenize()` のベンチマーク（入力サイズ別）を追加する
  - [x] 指標を `tokens/sec` / `alloc回数` / `ピークメモリ` に固定する
  - [x] 最適化前のベースライン結果を記録する
    - [x] `make bench`（2026-03-16, Apple clang `-O0`）  
      1KB: `1,399,168 tokens/sec`, `alloc_count=674`, `peak_alloc_bytes=40,313`  
      16KB: `2,177,904 tokens/sec`, `alloc_count=10,370`, `peak_alloc_bytes=621,265`  
      256KB: `3,744,003 tokens/sec`, `alloc_count=165,602`, `peak_alloc_bytes=9,922,249`
- [x] Tokenizerの責務を分離する
  - [x] `src/tokenizer/` を `scanner.c` / `literals.c` / `keywords.c` / `config_runtime.c` へ段階的に分割する
    - [x] `literals.c` を追加し、文字/文字列接頭辞・UCN・escape処理を移設する
    - [x] `config_runtime.c` を追加し、strict/拡張モード設定を分離する
    - [x] `scanner.c` を追加し、空白/コメントスキップと識別子開始・継続判定を分離する
  - [x] `tokenizer.c` は全体制御フロー中心に整理する
  - [x] UCN・escape等の共通処理をユーティリティへ集約する
- [x] 判定ロジックをテーブル駆動へ移行する
  - [x] 演算子判定（1/2/3文字）をデータテーブル化する
  - [x] キーワード判定を長さ分岐＋テーブル参照へ置換する
  - [x] `is*` 系判定の重複を削減し、文字種判定の共通化を行う
  - [x] 先頭文字ベースの高速分岐（punctuator/keyword）を追加する
  - [x] 数値リテラル判定に高速パスを追加し、重い分岐を補助関数へ分離する
  - [x] `punctuator_kind_for_str` を長さ+先頭文字分岐で高速化する
- [x] メモリアロケーションを最適化する
  - [x] トークン生成を `calloc` 連発から arena/pool 方式へ移行する
  - [x] trigraph 置換バッファの全体コピーを必要時のみ実行する
  - [x] 識別子UCN変換バッファの再利用を導入する
  - [x] `make bench` 再計測（2026-03-16, Apple clang `-O0`）  
    1KB: `2,568,702 tokens/sec`, `alloc_count=3`, `peak_alloc_bytes=49,224`  
    16KB: `2,668,296 tokens/sec`, `alloc_count=37`, `peak_alloc_bytes=607,096`  
    256KB: `3,651,218 tokens/sec`, `alloc_count=590`, `peak_alloc_bytes=9,680,720`
- [x] 文字列/エスケープ処理の一貫性を高める
  - [x] Tokenizer/Codegen間でエスケープ解釈ロジックを共通化する
  - [x] prefix/char幅の型情報伝搬ルールを明文化し実装に反映する
  - [x] strict C11 と拡張モードの分岐点を設定層に集約する
- [x] 回帰防止と保守性指標を導入する
  - [x] 性能回帰テストをCIに追加する
  - [x] 可読性維持のため関数長・複雑度の基準を設定する
  - [x] 最適化前後の性能比較レポートを残す
  - [x] 追加最適化後の再計測を行う（2026-03-16, 256KB: `12,023,597 tokens/sec`）

## Tokenizer追加最適化計画（2026-03-16）
- [x] 目標設定と基準値固定
  - [x] 基準値を固定する（`256KB = 12,023,597 tokens/sec`, `alloc_count=590`）
  - [x] 次の目標値を定義する（`+10〜20%`, `alloc_count`維持）
- [x] Hot Path最終最適化
  - [x] `tokenize()` 冒頭を先頭1バイトdispatch中心に再整理する
  - [x] 数値リテラル判定の10進整数最短ルートを追加する
  - [x] `tk_skip_ignored()` の条件順を実測ベースで見直す
- [x] メモリ局所性の改善
  - [x] token構造体のフィールド順を見直し、頻出アクセスを前方へ寄せる
  - [x] arenaチャンクサイズを入力長ベースの可変戦略にする
- [x] 文字列/識別子の遅延処理
  - [x] UCNなし識別子のゼロコピー経路を強化する
  - [x] 文字列escape decodeを必要時のみ実施する
- [x] 回帰防止の強化
  - [x] ベンチ入力を3種追加する（識別子偏重/数値偏重/記号偏重）
  - [x] CIしきい値をケース別に設定する
- [x] 仕上げ
  - [x] `tokenizer_perf_report.md` に改善前後比較表を追記する
  - [x] 目標達成判定と未達項目を整理する

## Tokenizerさらなる最適化タスク（次フェーズ）
- [x] `match_punctuator()` を先頭2バイトの小テーブル化で高速化する
  - [x] 既存の最長一致仕様（3文字/2文字/1文字）を維持したまま分岐数を削減する
  - [x] 記号偏重ベンチ（punct-heavy 256KB）で改善有無を確認する
- [x] `tk_skip_ignored()` を2層化する（ASCIIホットパス + フォールバック）
  - [x] 通常ケース（空白/コメントなし）で最短復帰できるようにする
  - [x] コメント/行継続/非ASCIIをフォールバック側に集約して可読性を維持する
- [x] 数値リテラルの浮動小数点判定をさらに軽量化する
  - [x] `strtod` 呼び出し到達率を下げるための事前判定を追加する
  - [x] numeric-heavy ベンチで改善有無を確認する
- [x] 識別子・キーワード判定の分岐順を実測ベースで再配置する
  - [x] 長さ別 + 先頭文字別の判定順をホットデータに合わせて調整する
  - [x] ident-heavy ベンチで改善有無を確認する
- [x] ベンチ運用を `-O0/-O2` の2軸で定点化する
  - [x] 計測スクリプトに `-O2` 実行を追加する
  - [x] レポートに `-O0/-O2` の比較表を追加する

## Tokenizer追加最適化タスク（次ラウンド）
- [x] `token_t` 系のレイアウトをアクセス頻度ベースで再調整する
  - [x] ホットフィールドを先頭に寄せ、キャッシュミスを減らす
  - [x] ABI影響箇所（Parser/Codegen）を確認し回帰テストを通す
- [x] `match_punctuator` の2文字判定を完全テーブル参照化する
  - [x] 現在の線形探索を O(1) 参照に置き換える
  - [x] 3文字/4文字の最長一致仕様は維持する
- [x] 数値リテラル判定の事前スキャンを強化する
  - [x] `strtod` 呼び出しが不要なケースをさらに増やす
  - [x] `numeric-heavy` ベンチで改善を確認する
- [x] キーワード判定を最小完全ハッシュ化（例: gperf）で比較検証する
  - [x] 現行実装と可読性・保守性・速度を比較して採用判断する
  - [x] 不採用の場合も検証結果を `tokenizer_perf_report.md` に記録する
- [x] 分岐予測ヒント（`likely/unlikely`）をホットパス限定で導入する
  - [x] `tokenize` / `scanner` の最頻分岐に限定して適用する
  - [x] 可読性を損なう箇所には導入しない
- [x] `inline` 方針を `-O2` 前提で見直す
  - [x] 小関数のみ `static inline` を適用して過剰インラインを避ける
  - [x] `check_function_size.sh` の基準内であることを確認する
- [x] PGO（Profile Guided Optimization）導入を検証する
  - [x] 学習用ベンチ入力を定義し、`-fprofile-generate/-fprofile-use` を試す
  - [x] CIへの常時導入可否を判断する
- [x] 実コード由来コーパスでのTokenizerベンチを追加する
  - [x] 合成ベンチ（mixed/ident/numeric/punct）との乖離を可視化する
  - [x] 改善優先度を実コード寄りに再調整する

## Tokenizer最適化 継続/停止判断つき計画（2026-03-16）
- [x] フェーズ0: 現状維持ゲートを定義する
  - [x] 評価指標を `mixed/ident/numeric/punct/corpus` の `tokens/sec` と `alloc_count` に固定する
  - [x] 継続条件を「全ケースで -5% 以内」に設定する
  - [x] 打ち止め条件を「2連続で改善が ±2% 以内」に設定する
- [x] フェーズ1: 低リスク最適化のみ実施する（1テーマ1コミット）
  - [x] `tk_scan_ident_start/continue` の分岐順を実測ベースで微調整する
  - [x] 数値サフィックス判定を小テーブル化して分岐を削減する（試行したが回帰のため不採用）
  - [x] `match_punctuator` の3文字判定で頻出順の比較に調整する
- [x] フェーズ2: 判定とロールバック判断
  - [x] 各テーマ実装後に `make test bench` + perf-check を必ず実行する
  - [x] 1ケースでも -5% を超えた場合は即ロールバック候補として保留する
  - [x] 改善が誤差域（±2%）なら次テーマに進まず打ち止めを検討する（今回の連続ステップは誤差域外のため継続）
- [x] フェーズ3: 継続しない場合の方針を確定する
  - [x] Tokenizerの大規模変更（DFA化/生成器導入）は本線では実施しない
  - [x] 高コスト案は別ブランチでPoCのみ実施する

## Parser未実装タスク（2026-03-17 棚卸し）
- [x] `return;`（`void` 関数での式なし return）をサポートする
  - [x] `stmt()` の `return` 分岐で `;` 直後を許可する
  - [x] `void`/非`void` の整合チェック方針を決める（`void` は `return;` のみ許可、非`void` は式必須）
- [x] `sizeof` 演算子（`sizeof expr` / `sizeof(type)`）を実装する
  - [x] Tokenizerの `TK_SIZEOF` を Parser の `unary` に接続する
  - [x] 型名版と式版の評価ルール（少なくとも既存基本型）を定義する（`sizeof(void)` は未対応として診断）
- [x] キャスト式（`(type)expr`）を実装する
  - [x] `unary` にキャスト文法を追加する
  - [x] 数値型間キャストの最小ルール（`int/char/short/long/float/double`）を定める（第1段: `char/short` はビット幅マスク、`int/long/pointer` は no-op、`float/double` は型情報付与）
- [x] カンマ演算子（`expr , expr`）を実装する
  - [x] `expr = assign ("," assign)*` に拡張する
  - [x] 関数引数区切りとの衝突が起きないことをテストで保証する（引数は `assign` 区切りに統一）
- [x] 宣言子の拡張（1文で複数宣言）を実装する
  - [x] `int a, b=1;` のような複数 declarator をパース可能にする
  - [x] `for` 初期化部の宣言にも同じ規則を適用する
- [x] 関数プロトタイプ宣言（本体なし）を実装する
  - [x] `program` で `funcdef` と宣言文を判別する（`...);` は定義ノードを生成せず読み飛ばす）
  - [x] 宣言情報の保持方式（シンボル表）を決める（第1段: シンボル表は導入せず、パース受理のみ対応）
- [x] `goto` / ラベル文（`label:`）を実装する
  - [x] Parserでラベル定義と参照を収集し、未定義ラベル診断を追加する
  - [x] codegen用にラベル解決情報をASTに保持する
- [x] `struct` / `union` / `enum` の最小文法を実装する
  - [x] Parserで型宣言と型参照の区別を導入する
  - [x] まずはタグ宣言＋メンバ参照なしの最小セットから段階導入する
  - [x] 次優先を機能実装（Parser/Preprocessor強化）へ戻す

## Tokenizer C11差分タスク（strictデフォルトは維持しない）
- [x] 方針: strict C11モードはデフォルトOFFを維持する
  - [x] `0b...` は拡張としてデフォルト許可、strict時のみ拒否
- [x] 浮動小数点サフィックス情報をトークンに保持する
  - [x] `f/F` と `l/L` を `token_num_t` に保持する
  - [x] Parserで `long double` を `is_float=3` として扱い、Codegenは現状double経路へ lowering する方針を適用する
- [x] 接頭辞付き文字定数のマルチ文字扱いをC11寄りに整理する
  - [x] `L/u/U` 付きで複数 `c-char` をどう扱うか方針化する
  - [x] 方針に合わせてTokenizer実装と異常系テストを更新する

## Tokenizer API命名統一
- [x] `tokenizer.h` 公開APIを `tk_` 接頭辞へ完全移行する
  - [x] `src/` / `test/` の呼び出しを `tk_` 名へ置換する
  - [x] 旧API互換マクロを削除する

## Parser 構文拡張タスク（次フェーズ）
- [x] `for` 初期化子で宣言を受理する（`for (int i=0; ... )`）
  - [x] 宣言パースを共通関数化して `stmt` と `for` で再利用する
  - [x] 回帰テストを追加する（`test_stmt_for_with_decl_init`）
- [x] 単項演算子 `+` / `-` / `!` / `~` を実装する
  - [x] `unary` 規則を拡張し、既存の `*` / `&` と整合させる
  - [x] 既存ASTノードへのloweringでコード生成を成立させる（`-x`, `!x`, `~x`）
- [x] `do ... while` 文を実装する
  - [x] Parser文法の追加（`stmt` 分岐）
  - [x] e2eテストと単体テストを追加する
- [x] `break` / `continue` 文を実装する
  - [x] ループ入れ子時のジャンプ先管理を導入する
  - [x] `for` / `while` / `do while` での回帰テストを追加する
- [x] `switch` / `case` / `default` 文を実装する
  - [x] caseラベル管理と fallthrough 動作を実装する
  - [x] `break` と組み合わせた制御フローテストを追加する
- [x] `++` / `--`（前置・後置）を実装する
  - [x] lvalue制約チェックを追加する
  - [x] スカラーの主要ケースをテストする
- [x] 複合代入（`+=`, `-=`, `*=`, `/=` など）を実装する
  - [x] 既存 `assign` 規則を拡張する
  - [x] 型サイズ（`char/short/int/ptr`）に応じた代入テストを追加する
- [x] 論理演算（`&&`, `||`）を短絡評価で実装する
  - [x] 優先順位を比較演算より低く、代入より高く定義する
  - [x] 副作用を含む式で短絡が効くことをe2eで確認する
- [x] 条件演算子（`?:`）を実装する
  - [x] 右結合性を満たすように文法を設計する
  - [x] ネストケースのASTテストを追加する

## 次に実装可能なタスク（優先度順）
- [x] ビット演算（`|`, `^`, `&`）を式として実装する
  - [x] 優先順位を `==`/`<` 系より低く、`&&`/`||` より高く定義する
  - [x] `ND_BITOR` / `ND_BITXOR` / `ND_BITAND` を追加し codegen を実装する
  - [x] e2e で `mask`/`flag` 系の回帰テストを追加する
- [x] シフト演算（`<<`, `>>`）を実装する
  - [x] `add` より低く、比較演算より高い優先順位で文法に追加する
  - [x] codegen に `lsl` / `asr`（符号付き右シフト）を追加する
  - [x] 負数/境界値のテスト方針を定める（`>>` は算術右シフト `asr`、境界は `shift by 0` と高ビット領域で検証）
- [x] 剰余演算（`%`）を実装する
  - [x] `mul` 規則に `%` を追加する
  - [x] ARM64 で `sdiv` + `msub` による剰余コード生成を追加する
  - [x] `a%b` の正常系・0除算系テストを追加する（負数剰余と 0除算の実装依存ケースを追加）
- [x] 残りの複合代入（`%=`, `<<=`, `>>=`, `&=`, `^=`, `|=`）を実装する
  - [x] 既存 `assign` 拡張パターンに追従して lower する
  - [x] ビット演算/シフト実装後に e2e で一括回帰を追加する
- [x] `switch` の診断強化を行う
  - [x] `case` 値の重複検出を追加する
  - [x] `default` 重複時のエラー位置を改善する
  - [x] ネストした `switch` + `continue` の境界ケースを追加する
- [x] 文法・意味の最小型チェックを導入する
  - [x] `continue`/`break` の不正位置エラーを parser 側でも明示する
  - [x] `++/--` の対象型制約（非スカラー禁止など）を段階導入する（第1段: `float`/`double` を拒否）
  - [x] エラーメッセージの一貫化（`tk_error_tok` 経由）を行う
- [x] テスト基盤の可観測性を改善する
  - [x] `test_e2e` の失敗時に生成 `.s` の保存パスをログへ出力する
  - [x] 新カテゴリ（`bitwise`, `shift`, `switch_edge`）を追加する
  - [x] CI で parser/e2e を分離実行し、失敗原因を切り分けやすくする

## Tokenizer Doxygen整備タスク（保守性向上）
- [x] 対象範囲を確定する
  - [x] `src/tokenizer/` の公開APIと内部ヘルパーを分類する
  - [x] 公開API（ヘッダ宣言）を優先対象として明記する
- [x] コメント方針を定義する
  - [x] `@brief`, `@param`, `@return`, `@pre`, `@post`, `@note` の最小テンプレートを定める
  - [x] 副作用や実装依存のある関数には `@warning` を付与する方針を追加する
- [x] 公開ヘッダにDoxygenコメントを付与する
  - [x] `tokenizer.h` に公開関数コメントを追加する
  - [x] `token.h` に公開構造体・enumの説明を追加する
  - [x] `escape.h` / `literals.h` / `scanner.h` / `keywords.h` / `punctuator.h` / `allocator.h` の公開宣言を整備する
- [x] 実装ファイルの主要関数コメントを補完する
  - [x] `tokenizer.c` のエントリ関数と主要分岐点に関数コメントを追加する
  - [x] `literals.c` / `escape.c` のリテラル・エスケープ処理に前提条件と失敗条件を明記する
  - [x] `scanner.c` / `keywords.c` / `punctuator.c` / `allocator.c` のホットパス関数に意図を明記する
- [x] 意味整合性を確認する
  - [x] `num_kind` / `fp_kind` / strict C11関連フラグの説明が実装と一致しているか確認する
  - [x] コメントと実際のエラー挙動（例: 異常系）に齟齬がないか確認する
- [x] 品質ゲートを実施する
  - [x] `make test` を実行して回帰がないことを確認する
  - [x] 追加コメントが警告や可読性低下を招いていないかレビューする
- [x] 運用ルールをドキュメント化する
  - [x] 「Tokenizer新規関数はDoxygen必須」のルールを追記する
  - [x] 「公開API変更時はDoxygen更新必須」のルールを追記する

## Parser最適化計画（保守性 + 実行速度）
- [x] フェーズ1: 現状計測を固定する
  - [x] Parserベンチを追加し、入力サイズ別の計測値（parse time / throughput）を記録する
    - [x] 計測結果を `parser_perf_report.md` に記録する（`tokenize`/`parser` 分離計測）
  - [x] 保守性指標（関数長・分岐数・重複コード）を可視化する
  - [x] 改善目標値（例: +10%）と許容回帰閾値（例: -5%）を定義する
- [x] フェーズ2: 責務分離で保守性を上げる
  - [x] `stmt`/`decl`/`expr` をモジュール分割し、`parser.c` を段階的に縮小する
    - [x] `decl` 系（ローカル変数表・宣言パース）を `parser_decl` へ分離する
    - [x] `stmt` 系を `parser_stmt` へ分離する
    - [x] `expr` 系を `parser_expr` へ分離する
  - [x] タグ管理・ラベル管理・型ユーティリティを専用モジュールへ分離する
  - [x] 共有ヘルパーの公開範囲を整理して依存関係を単純化する
- [x] フェーズ3: ホットパス最適化を実施する
  - [x] `expr/assign/unary/primary` の分岐順を頻出ケース優先へ調整する
  - [x] `is_type_token`/サイズ判定などの繰り返し計算を局所キャッシュする
  - [x] 動的配列成長戦略を統一して `realloc` 頻度を抑える
- [x] フェーズ4: 探索構造を改善する
  - [x] `goto`/label 解決の線形探索をハッシュ化して探索コストを削減する
  - [x] タグ型参照の探索も同様に高速化する
  - [x] 構造変更後に異常系診断（未定義/重複）の精度を再確認する
- [x] フェーズ5: エラーハンドリングを整理する
  - [x] Parser診断メッセージの生成パターンを共通化する
  - [x] コンテキスト付きエラー（どの規則で失敗したか）を統一フォーマットで出す
  - [x] 既存テストのメッセージ依存箇所を必要に応じて調整する
- [x] フェーズ6: 回帰防止を強化する
  - [x] Parser性能回帰チェックをCIに追加する
  - [x] 1テーマ1コミットで段階適用し、各段階で `make build/test_parser build/test_e2e && build/test_parser && build/test_e2e` を必須化する
  - [x] 各フェーズの計測結果と判断ログを `parser_perf_report.md` に記録する

## Codegen最適化タスク（ARM64 float literal）
- [x] 浮動小数点リテラルの即値化可能判定を導入する
  - [x] 即値化可能な値は `fmov` 系でロードする（現状: `+0.0f` / `+0.0`）
  - [x] それ以外は既存どおり `float_literals` 経由でロードする

## `printf` 可変長引数ABI対応（`ag_printf`撤去）
- [x] 原因を確定する
  - [x] Apple ARM64(Darwin) における可変長引数呼び出し規約を、`clang -S` 出力で確認する
  - [x] 現行 `ag_c` 出力との差分（レジスタ渡し/スタック渡し/16byteアライメント）を整理する
  - [x] `%d` が崩れる再現ケースを最小化し、原因を「呼び出し規約不一致」で固定する
- [x] 一時ワークアラウンドを除去する
  - [x] `gen_runtime_helpers()` の生成を削除する
  - [x] `include/stdio.h` の `#define printf ag_printf` を削除し、通常の `printf` 宣言へ戻す
  - [x] `ag_printf` シンボルへの依存を全て除去する
- [x] Codegenで正しい可変長引数呼び出しを実装する
  - [x] `ND_FUNCALL` で variadic 関数呼び出し経路を追加する（まず `printf` 対象）
  - [x] variadic 引数をABIどおりスタックへ配置し、call直前のSP 16byteアラインを保証する
  - [x] 呼び出し後のスタック回復と既存式評価スタックとの整合を取る
- [x] 回帰テストを追加する
  - [x] `#include <stdio.h>` + `printf("x=%d\n", 42)` のE2Eテストを追加する
  - [x] 既存の `puts` / 通常関数呼び出しが壊れていないことを確認する
- [x] 検証とドキュメント更新
  - [x] `build/test_preprocess` と `build/test_e2e` を通す
  - [x] 仕様メモ（Darwin variadic ABI対応方針）を `walkthrough.md` へ追記する

## 固定長バッファ見直し（ハードコード長の削減）
- [x] `preprocess.c` の `#include` パス組み立てを動的化する
  - [x] `char alt[1200]` を廃止し、必要長を計算して確保する
  - [x] `char filename[1024]` を廃止し、トークン長に応じて伸長する
- [x] 同様の固定長バッファを段階的に見直す
  - [x] `src/config/config.c` の `char line[512]` の上限仕様を明文化し、必要に応じて動的化する
  - [x] `src/parser/diag.c` の `char detail[256]` の切り捨て方針を明文化し、診断品質への影響を評価する
  - [x] `src/parser/expr.c` の `char label[32]`（`.LC` ラベル生成）の上限妥当性を確認し、必要なら `snprintf` 依存から安全な生成へ変更する

## バッファオーバーフロー観点の追加ハードニング
- [x] `src/preprocess/preprocess.c` のサイズ計算を `size_t` 化する
  - [x] `stringify_tokens()` の `cap/len` を `size_t` に変更し、加算時のオーバーフローを検出する
  - [x] `paste_tokens()` の `len_l + len_r` を安全に計算する
- [x] `test/test_e2e.c` の固定上限配列を動的化する
  - [x] `categories[64]` / `build_pids[64]` / `pids[64]` を入力件数に応じて確保する
  - [x] カテゴリ数増加時に境界外アクセスが起きないことを確認する

## セキュリティ堅牢化（2026-03-17）
- [x] プリプロセッサのファイル読み込みで `ftell` 失敗時の境界処理を強化する
  - [x] `ftell` の負値を検出し、`size_t` 変換前に失敗扱いにする
  - [x] `fread` の戻り値検証を追加する
- [x] 動的配列拡張の `realloc` 未検証箇所を安全化する
  - [x] Parser系の配列拡張を `realloc` 失敗時に即エラー終了する共通ヘルパへ統一する
  - [x] Preprocessor/Codegenの配列拡張でも同様に失敗を検知する
- [x] 文字列連結・`#include` ファイル名構築の長さ検証を強化する
  - [x] `int` 長さの負値/加算オーバーフローを検出して中断する
  - [x] 極端に大きい入力での `size_t` 桁あふれ経路を遮断する
- [x] `#include` のパストラバーサル耐性を追加する
  - [x] `../`・絶対パス・許可外ディレクトリ参照を拒否する方針を定義する
  - [x] プロジェクトルート/許可インクルードパス内に解決されることを検証してから読み込む
- [x] `#include` 再帰展開の深さ上限を導入する
  - [x] include 深さカウンタを追加し、上限超過時は診断を出して中断する
  - [x] 循環 include を検出するか、少なくとも深さ上限でDoSを防止する
- [x] Tokenizer のトークン長管理を `int` 依存から段階的に脱却する
  - [x] `p - start` 由来の長さを `size_t` で保持し、`int` へ落とす箇所に上限チェックを入れる
  - [x] 巨大トークン入力時に安全にエラー終了するテストを追加する

## Parser未実装タスク（2026-03-17 確認）
- [x] `struct/union/enum` のメンバ宣言をパース可能にする
  - [x] `struct S { int x; };` のタグ定義を受理し、型情報にメンバ数を保持する
  - [x] `union` / `enum` のタグ定義ボディを受理する（列挙子式つき含む）
  - [x] ブロック内タグ定義と既存タグ参照のスコープ整合を取る
- [x] `sizeof(void)` の扱い方針を確定する
  - [x] 仕様どおり制約違反として診断を維持するか、拡張として `1` などを返すかを決める（診断維持）
  - [x] 方針に合わせて parser 診断メッセージとテストを明文化する
- [x] `void` cast の扱いを実装する
  - [x] `(void)expr` を式文で受理できるようにする
  - [x] 値を捨てるノード表現（または既存ノードの運用）を定義する（既存ノード運用）
- [x] キャスト対応型の拡張を検討する
  - [x] 現在未対応の型（例: タグ型/一部派生型）に対するエラー方針を整理する（非スカラ型キャストは診断）
  - [x] 受理可能な型セットを文法ファイルと実装で一致させる
  - [x] `signed` / `unsigned` / `_Bool` を宣言型として受理する
  - [x] `_Bool` / `signed` / `unsigned` の基本キャストを受理する
  - [x] タグ型ポインタキャスト（`(struct S*)p`）を受理する

## Parser拡張タスク（グローバル宣言/最外部タグ）
- [x] 最外部スコープで `struct/union/enum` 宣言・定義を受理する
  - [x] `struct S;` / `struct S { int x; };` を関数定義の外でパース可能にする
  - [x] `struct S { int x; } *g;` のようなタグ定義+宣言子を最外部で受理する
- [x] 型付きグローバル宣言（最小）を受理する
  - [x] `int g;` / `int g = 1;` などを関数定義と識別してパースできるようにする
  - [x] 既存の関数定義・プロトタイプ判定を壊さないことをテストで確認する

## Parser未実装タスク（2026-03-17 再棚卸し）
- [x] 構造体/共用体メンバ参照式を実装する
  - [x] `a.b`（`.`）を `postfix` に追加する
  - [x] `p->b`（`->`）を `postfix` に追加する
  - [x] メンバ名解決と型サイズ反映（`type_size`/`deref_size`）を実装する
- [x] `enum` 列挙子を識別子として参照可能にする
  - [x] `enum E { A=1, B=2 }; return A;` のような列挙子名をシンボル表で解決する
  - [x] 明示値なし列挙子（自動インクリメント）を扱えるようにする
  - [x] 明示値は整数リテラルとして受理する（式全般は今後拡張）
- [x] `typedef` を型名として扱う
  - [x] `typedef` 宣言をパースし、型名テーブルへ登録する
  - [x] `is_type_token` 判定を拡張し、typedef名を `type` として受理する
- [x] 宣言子文法を拡張する（現在は `*` + 識別子 + 配列の最小形）
  - [x] 関数ポインタ宣言（例: `int (*fp)(int)`）をパース可能にする
  - [x] 複合型指定子（例: `unsigned long`）を段階的に受理する
  - [x] 入れ子宣言子（多段の `(` `)` を含む宣言子）を段階的に受理する
  - [x] `const`/`volatile` など修飾子付き宣言子を受理する

## Parser未実装タスク（2026-03-17 追記）
- [x] 関数ポインタの「式としての呼び出し」を実装する
  - [x] `fp(1, 2)` のような「識別子以外をcalleeに取る呼び出し」を `postfix` で受理する
  - [x] `(*fp)(1)` / `p->fp(1)` 相当の呼び出しパターンを段階導入する
- [x] 関数仮引数の宣言子を拡張する
  - [x] 仮引数で関数ポインタ宣言子を受理する
  - [x] 仮引数で配列/ポインタの派生宣言子を現状より広く受理する
- [x] キャスト/`sizeof(type)` の宣言子を拡張する
  - [x] `sizeof(int (*)(int))` のような関数ポインタ型を受理する
  - [x] 抽象宣言子（名前なし宣言子）を段階導入する
- [x] `enum` の列挙子初期化式を拡張する
  - [x] 現状の整数リテラル限定を、定数式（加減算・参照済み列挙子）へ拡張する
- [x] 構造体/共用体レイアウト計算を強化する
  - [x] 現状の単純オフセット計算を、アラインメント/パディング考慮へ拡張する

## Parser未実装タスク（2026-03-17 追加棚卸し）
- [x] 関数ポインタ値の実用経路を完成させる
  - [x] `fp = foo;` のような関数名を関数ポインタ値として解決できるようにする
  - [x] `fp(1)` の間接呼び出しで、関数シンボル由来の値経路をE2Eで保証する
- [x] `switch` / `case` の定数式を拡張する
  - [x] `case 1+2:` や `case A*2:` のような整数定数式を受理する
  - [x] 重複判定ロジックが定数式評価結果ベースで動くことを確認する
- [x] `enum` 初期化式の演算セットを拡張する
  - [x] 現状の `+,-,*,/,%,(),単項+/-` から、`<<,>>,|,&,^,~` を段階導入する
  - [x] Cの整数定数式に近い優先順位で評価できるようにする
- [x] 宣言子の型情報保持を強化する
  - [x] `const` / `volatile` を単なる受理に留めず、型情報として保持する
  - [x] ポインタ段ごとの修飾子（例: `int *const p`）を保持できるようにする
  - [x] 先頭ポインタ段の修飾子（`int *const p`, `int *volatile p`）を型情報として保持する
- [x] 構造体/共用体宣言の残差分を埋める
  - [x] ビットフィールド宣言（`int x:3;`）を受理する
  - [x] 匿名struct/unionメンバの受理方針を決めて段階導入する
- [x] キャストの未対応型を段階解消する
  - [x] 非スカラ型へのキャスト制約（現状は診断エラー）を仕様方針に沿って実装/診断整理する
  - [x] 「この型へのキャストは未対応です」となる型カテゴリを棚卸しする（現状: 非スカラ型、対応外派生型）

## Parser未実装タスク（2026-03-17 追加棚卸し2）
- [x] C11キーワードのParser接続を進める
  - [x] `_Alignas`, `_Alignof`, `_Atomic`, `_Generic`, `_Noreturn`, `_Static_assert`, `_Thread_local` を段階的に受理する
  - [x] `_Alignas(...)` を宣言前置指定子として受理する（アラインメント適用自体は今後）
  - [x] `_Atomic`（修飾子形式）を宣言前置指定子として受理する
  - [x] `_Atomic(type)` 形式を最小受理する（現状: 組み込み基本型まで）
  - [x] `_Generic` を式として最小受理する（現状: 基本型/ポインタ + `default`）
  - [x] `_Alignof` を式として受理する（型名のみ）
  - [x] `_Static_assert` を最外部/文内宣言として受理する（整数定数式 + 文字列）
  - [x] `_Noreturn` / `_Thread_local` を宣言前置指定子として受理する
  - [x] `_Complex`, `_Imaginary` の扱い方針（受理範囲/診断）を確定する（現状: 明示的に未対応診断）
- [x] ストレージクラス/関数指定子の意味処理を追加する
  - [x] `extern`, `static`, `auto`, `register`, `inline`, `restrict` を宣言子パースと意味解析に反映する
  - [x] 現状の「トークン化のみ」状態から、少なくとも診断一貫性を持った挙動へ移行する
- [x] 可変長引数宣言子（`...`）を実装する
  - [x] `int f(int a, ...);` と `int f(...){}` を仮引数宣言として受理する
  - [x] `...` の位置制約（末尾のみ）を診断で保証する
- [x] 初期化子リストを実装する
  - [x] スカラの波括弧初期化（`int x={3};`）を最小受理する
  - [x] 配列/構造体/共用体の `{ ... }` 初期化を段階導入する（現状: 非スカラの再帰初期化は未対応）
  - [x] 配列の `{ ... }` 初期化（`int a[3]={1,2,3};`）を最小受理する
  - [x] `char a[N] = "..."` 形式の配列初期化を最小受理する
  - [x] 構造体の `={...}` を宣言順メンバ代入として最小実装する（現状: 非スカラメンバは未対応）
  - [x] 共用体の `={...}` を先頭メンバ代入として最小実装する（現状: 1要素のみ / 非スカラメンバは未対応）
  - [x] 既存の単一式初期化と競合しない文法に整理する（スカラ/ポインタは単一式、配列/集約体は波括弧のみ）
- [x] 指示付き初期化子（designated initializer）を実装する
  - [x] `.x = 1`, `[2] = 3` を最小受理する（現状: 単純形のみ）
  - [x] 重複指定や範囲外指定の最小診断を導入する
- [x] 複合リテラルを実装する
  - [x] 型名付き初期化式（`(int){3}`, `(struct S){...}`）を最小受理する
  - [x] 一時オブジェクトの寿命/格納戦略（最低限の実装方針）を定義する
  - [x] 現状方針: 非`static`複合リテラルは隠しローカル変数へloweringし、`(init, obj)` として評価する（ブロック寿命の最小実装）
- [x] 配列宣言子の拡張を進める
  - [x] `int a[1+2]` のような定数式サイズを受理する
  - [x] 多次元配列宣言子と仮引数配列の `static`/`restrict` を段階導入する
  - [x] 多次元配列宣言子（`int a[2][3]`）を宣言構文として受理する

## Parser未実装タスク（2026-03-17 追加棚卸し3）
- [x] 型指定子の組み合わせ受理を厳密化する
  - [x] `long long`, `long long int`, `unsigned long long` を型指定子として受理する
  - [x] `signed char`, `unsigned char`, `unsigned short`, `unsigned int` など符号付き/無し組み合わせを一貫して扱う
  - [x] 不正な組み合わせ（例: `short double`）を診断できるようにする
- [x] `enum` 定数式の対応範囲をC寄りに拡張する
  - [x] 比較演算（`==`, `!=`, `<`, `<=`, `>`, `>=`）を列挙子初期化式で受理する
  - [x] 論理演算（`&&`, `||`）と条件演算子（`?:`）を列挙子初期化式で受理する
  - [x] 上記拡張後も既存の重複 `case` 検出/列挙子解決が壊れないことを回帰テストで保証する

## Parser未実装タスク（2026-03-18 追記）
- [x] `sizeof(void)` の方針を拡張実装する
  - [x] 現状の未対応診断（`sizeof(void) はサポートしていません`）を維持するか、拡張受理に切り替えるかを決める
  - [x] 方針に合わせて parser/e2e テストを更新する
- [x] 非スカラ型キャストの対応範囲を拡張する
  - [x] `struct` / `union` へのキャストを禁止のままにするか、対応するかを仕様として確定する（方針: 値キャストは診断エラー）
  - [x] `この型へのキャストは未対応です` となる型カテゴリ（派生型含む）を段階的に解消する（`_Complex/_Imaginary` cast の最小受理を追加）
- [x] `_Atomic(type)` の受理範囲を拡張する
  - [x] 現状未対応の派生宣言子（例: `_Atomic(int*)`）を受理できるようにする
  - [x] 既存の `_Atomic` 修飾子形式との整合性をテストで保証する
- [x] `_Complex` / `_Imaginary` を段階実装する
  - [x] 現状の未対応診断から、最小受理する型範囲（例: `_Complex double`）を定義する
  - [x] unsupported のまま残す範囲は診断メッセージとテストを明確化する
- [x] 集約型初期化子の未対応ケースを解消する
  - [x] 構造体初期化で非スカラメンバ（入れ子構造体/共用体）を受理する
  - [x] 共用体初期化で非スカラメンバ（入れ子構造体/共用体）を受理する
  - [x] 構造体/共用体メンバが配列の場合の初期化を受理する
  - [x] 共用体初期化の複数要素指定（現状1要素のみ）の扱いを仕様化する（方針: 制約違反として診断エラー）

## Parser未実装タスク（2026-03-18 追加棚卸し5）
- [x] 非スカラ値キャストの実装方針を再検討する
  - [x] 現状診断エラーの `struct` / `union` 値キャストを、将来実装対象に含めるかを仕様として確定する（現状: 非対応を維持）
  - [x] 継続して未対応とする場合は、制約違反としての扱い（エラー文言/テスト）を C 準拠観点で明文化する（`struct` / `union` の診断テストを固定）
- [x] 集約型の単一式初期化（`={expr}`）を拡張する
  - [x] 現状未対応の `int a[2]=1` / `struct S s=1` / `union U u=1` に対する扱い（禁止維持 or 段階受理）を仕様化する（現状: `union U u=1` は先頭メンバ初期化として受理、`int a[2]=1` は禁止、`struct S s=1` は同型オブジェクトのみ受理）
  - [x] 少なくとも「同型オブジェクトからの初期化」をどう扱うか（例: `struct S s=t;`）を決めてテスト化する（現状: 同一タグ型ローカル変数からのメンバコピーとして受理）
- [x] `_Complex` / `_Imaginary` の意味処理を拡張する
  - [x] 現状の「受理のみ」から、演算・キャスト・初期化における型意味（実部/虚部を含む）を段階実装する（現状方針: 実装内部では対応する実数スカラへ縮退して扱う）
  - [x] 最小受理範囲外のケースは未対応診断として整理し、異常系テストを追加する
- [x] 構造体/共用体メンバ初期化の残差分を解消する
  - [x] `src/parser/decl.c` の診断（現行: `構造体/共用体初期化は現在 1/2/4/8 byte スカラのみ対応です`）に到達する型カテゴリを棚卸しする（現状: 配列/入れ子 struct/union は対応済み。実質的には将来導入する非1/2/4/8byteカテゴリが対象）
  - [x] 関数型/不完全型など、現在も初期化不能なメンバケースの扱いを仕様として固定する（方針: 宣言時に診断エラー、メッセージをテストで固定）

## Parser未実装タスク（2026-03-19 追加棚卸し6）
- [x] 非スカラ値キャスト（`struct`/`union`）の実装可否を再検討する
  - [x] 現状の診断エラー運用（`[cast] struct/union 値へのキャストは未対応です`）を維持するか、段階実装へ進めるかを再決定する（現状: 診断エラー維持）
  - [x] 実装する場合の最小スコープ（同型間のみ許可 / メモリコピーlowering など）を定義する（現状: 未着手、方針保留）
- [x] `_Complex` / `_Imaginary` の表現を「実数スカラ縮退」から拡張する
  - [x] 実部/虚部を区別した内部表現を導入するか、縮退方針を正式仕様として固定するかを決める（現状: 実数スカラ縮退を正式方針とする）
  - [x] 方針に合わせて演算規則（加減乗除・比較・代入変換）とテストを整備する（現状: 縮退方針で既存演算規則を適用）
- [x] 集約型の単一式初期化の残り（配列/構造体）を拡張する
  - [x] `int a[2]=1` の扱いを仕様化する（現状: 要素0へ代入 + 残り0埋めとして受理）
  - [x] `struct S s=1` の許容条件（非許容維持 / 同型コピー以外の段階受理）を明確化する（現状: 同型ローカル変数コピーのみ受理、その他単一式は診断）
- [x] `decl.c` の「非1/2/4/8byteカテゴリ」初期化方針を具体化する
  - [x] `構造体/共用体初期化は現在 1/2/4/8 byte スカラのみ対応です` に実際に到達する型カテゴリ（将来型拡張含む）を一覧化する（現状実装では将来拡張型が主対象）
  - [x] バイトコピーで吸収する範囲と診断維持する範囲を定義する（現状: 同型コピー経路はバイトコピー、その他は診断）

## Parser未実装タスク（2026-03-19 追加棚卸し7）
- [x] 非スカラ値キャスト（`struct` / `union`）をどう扱うか最終決定する
  - [x] 現状の診断エラー維持を継続するか、段階実装へ進めるかを確定する（現状: 診断エラー維持を継続）
  - [x] 実装に進む場合の最小許容範囲（同型間のみ等）を定義する（現状: 段階実装へは進まず、非スカラ値castは一律未対応診断）
- [x] 構造体単一式初期化の対応範囲を拡張する
  - [x] 現状「同型ローカル変数コピーのみ受理」をどこまで広げるかを決める（例: 条件演算子結果や一時値）（現状: `,` 演算子の最終値が同型ローカル変数なら受理）
  - [x] 非対応を維持するケースは診断仕様を明文化する（例: 最終値が lvalue でない一時値）（現状: 条件演算子結果/複合リテラル最終値は `同型オブジェクトのみ対応` 診断で固定）
- [x] 共用体初期化子の複数要素指定を扱うか決める
  - [x] 現状1要素のみ受理の制約を維持するか、拡張するかを仕様化する（現状: C11制約違反として1要素制約を維持）
  - [x] 拡張する場合の上書き規則と診断ルールを定義する（現状: 拡張しない方針。2要素目以降は `共用体初期化子は現状1要素のみ対応です` で診断固定）
- [x] `_Complex` / `_Imaginary` の内部表現を再検討する
  - [x] 現状の実数スカラ縮退を維持するか、実部/虚部を持つ表現へ拡張するかを確定する（現状: 実数スカラ縮退を維持）
  - [x] 方針に応じた演算・代入・比較の意味規則をテスト込みで整理する（現状: 縮退方針で既存規則適用、`test_parser` / `test_e2e` で固定）
- [x] 将来型拡張時の初期化ポリシーを具体化する
  - [x] 非 `1/2/4/8` byteカテゴリに到達した場合の初期化方式（バイトコピー or 診断）を型ごとに決める（現状: メンバ直接初期化は診断維持、同型コピー経路は byte copy を継続）
  - [x] 方針を `decl.c` 診断メッセージとテストへ反映する（現状: 診断を `1/2/4/8 byte スカラのみ対応` に明文化。既存診断系テスト運用に準拠）

## Parser未実装タスク（2026-03-19 追加棚卸し8）
- [x] 非 `1/2/4/8` byte スカラメンバ初期化の段階実装を検討する
  - [x] `decl.c` の `構造体/共用体初期化は現在 1/2/4/8 byte スカラのみ対応です` に到達する具体型（例: 将来導入する拡張実数型）を再確認する（現状: 既存型では実質未到達。将来拡張型が対象）
  - [x] 対応する場合は store lowering 方式（直接 store / byte copy）を型ごとに定義する（現状: `1/2/4/8` は直接 store、それ以外は同型copy経路のみ byte copy）
- [x] 配列メンバ初期化の非波括弧単一式対応可否を決める
  - [x] 現状の `'{...}' または文字列リテラルのみ対応` を維持するか、`=expr` の段階受理に進むかを仕様化する（現状: 制約違反として禁止を維持）
  - [x] 方針に応じて異常系診断テスト（エラーメッセージ固定）を追加する（先頭配列メンバを `struct/union` で非波括弧指定するケースを固定）
- [x] 非スカラ値castの将来拡張条件を明文化する
  - [x] 現状の一律診断維持に加えて、「着手するなら何を満たした時か」（同型限定・ABI固定など）を条件として追記する（条件: 同型限定・ABI/表現固定・lowering方針確定時のみ着手）
  - [x] 条件が未充足の間は現行診断を維持する回帰テストを追加する（同型に見える `struct` cast でも現行診断を維持するテストを追加）

## Parser未実装タスク（2026-03-19 追加棚卸し9）
- [x] `expr.c` の汎用 cast 未対応カテゴリ（`この型へのキャストは未対応です`）を棚卸しする
  - [x] 現在この診断に到達する型の具体例（派生型/修飾付き型を含む）をテスト付きで一覧化する（現状: 代表的失敗系は `struct/union` 専用診断・cast構文診断へ分岐し、本汎用診断への到達は未確認）
  - [x] 診断維持か段階受理かを型カテゴリごとに方針化する（方針: 汎用診断は将来型拡張の保険として維持し、既知カテゴリは専用診断/受理へ寄せる）
- [x] 構造体/共用体初期化の「`{...}` 必須」制約の適用範囲を再整理する
  - [x] 現状の例外経路（同型コピー、共用体先頭メンバ初期化）と制約本文との対応をドキュメント化する（現状: 構造体は同型単一式コピーを例外許可、共用体は同型コピーまたは先頭メンバ初期化を例外許可）
  - [x] 方針に沿って診断文言を「制約違反」観点で統一するかを決める（現状: 既存診断文言を維持し、仕様文書側で制約違反扱いとして説明）
- [x] 将来導入型（`1/2/4/8` 以外）向けの回帰テスト導入方法を設計する
  - [x] 現状実装で到達困難な分岐を、テスト専用フックなしで検証する方法（型導入 or 代替ケース）を決める（方針: 現行は非到達を否定回帰テストで固定し、将来型導入時に陽性診断テストへ置換）
  - [x] 決定した方法に基づき parser テストを追加する（`[decl] 構造体/共用体初期化は現在 1/2/4/8 byte スカラのみ対応です` が既存失敗系に誤出力しないことを固定）

## Parser未実装タスク（高頻度ユースケース優先）
- [x] 構造体/共用体メンバ参照の後続添字アクセスを受理する
  - [x] `s.a[0]` のような `.` / `->` の直後に `[]` が続く postfix 連鎖を parser で受理する
  - [x] `test_parser` に回帰テストを追加し、`';' 期待 (actual: '[')` 回帰を防止する
- [x] 構造体初期化の brace elision（波括弧省略）対応を拡張する
  - [x] `struct S { int a[2]; int z; }; struct S s={1,2,3};` のような平坦な初期化子列を受理する（現状: 配列メンバに対する最小brace elisionを受理）
  - [x] 既存の designator/重複チェックとの整合を回帰テストで固定する（`{1,2,.a={...}}` 系で同一メンバ重複診断を固定）
- [x] 構造体の単一式初期化で条件演算子結果を受理する
  - [x] `struct S s = cond ? a : b;` を同型オブジェクト条件で受理する
  - [x] 非同型/一時値ケースの診断仕様を維持し、メッセージを固定する（`(t,1)` などの非同型最終値は既存診断を維持）
- [x] 共用体初期化子の複数指定を段階受理するか再検討する
  - [x] `union U u={.x=1,.y=2};` の扱い（禁止維持 / 最終指定優先）を仕様化する（現状: designator連鎖のみ受理、後勝ち）
  - [x] 方針に応じて診断・受理ケースをテストへ反映する（`.x,.y` 受理と、非designator連鎖の診断維持を固定）
- [x] `struct/union` 値 cast の段階受理可否を再評価する
  - [x] 実運用コードで頻出するパターン（同型 cast 等）を対象に、最小スコープを定義する（現状: 同型 `struct/union` value cast を no-op 受理）
  - [x] 受理しない範囲は現行診断維持をテストで固定する（非同型/スカラ→非スカラ cast は診断維持）
- [x] `この型へのキャストは未対応です` の到達型を可視化する
  - [x] 再現コードを列挙し、どの型カテゴリで落ちるかを一覧化する（調査結果: `struct/union` 値castは専用診断、`_Atomic` 系 cast は段階受理を進めた結果、主に専用診断または受理へ分岐）
  - [x] 優先度の高い型カテゴリから段階解消計画を作成する（第1段: `long double` cast 型名受理【完了】、第2段: `_Atomic(T)` cast 型名受理【完了】、第3段: 派生型cast（`const int`）受理と不正ストレージ指定子（`_Thread_local`）専用診断化）
- [x] cast 型名の cv/restrict 修飾子を受理する
  - [x] `(const int)`, `(volatile int)`, `(restrict int*)` を parser で受理する
  - [x] cast 型名で不正なストレージ指定（`_Thread_local`）に cast 文脈の専用診断を出す
- [x] cast 型名で `_Atomic` 型指定子（`_Atomic int` 形式）を受理する
  - [x] `(_Atomic int)` / `(_Atomic const int)` を parser で受理する
  - [x] 既存の `_Atomic(T)` 受理と共存することを確認する（回帰）
  - [x] 入れ子 `_Atomic(_Atomic(T))` cast 型名も受理する（`[primary]` への漏れを防止）

## Parser未実装タスク（2026-03-19 追加棚卸し10）
- [x] `struct/union` 値 cast の受理範囲を再拡張する
  - [x] 現状の「同一タグ型 no-op cast のみ受理」から、同一レイアウト互換や一時値を含む最小拡張範囲を決める（第1段: ternary 分岐が同一タグ型なら受理）
  - [x] 非受理ケースの診断を C 制約ベースで整理し、`test_parser` 異常系を追加する（非同一タグ `struct/union` cast の診断固定を追加）
- [x] 構造体の単一式初期化（同型）で rvalue/一時値を段階受理する
  - [x] `struct S s = (cond ? a : b);` のような同型値だが `lvar` でないケースの受理可否を仕様化する（ternary 分岐の複合リテラル/同型 lvar 混在を受理）
  - [x] 受理/非受理のどちらでも AST lowering 方針（copy/byte-copy）と診断をテストで固定する（受理ケース回帰と既存非受理診断を維持）
- [x] 共用体初期化子の複数要素制約を再評価する
  - [x] 現状 `1` 要素制約（`共用体初期化子は現状1要素のみ対応です`）を維持するか、C の上書き規則に寄せるかを決める（方針: designator 連鎖のみ段階受理、非 designator 連鎖は診断維持）
  - [x] 方針に応じて `{.x=1,.y=2}` 系と非 designator 連鎖の挙動を仕様・テストに反映する（受理ケースを parser テストへ追加）
- [x] 配列メンバ初期化の単一式（非波括弧）を段階受理する
  - [x] 現状の `'{...}' または文字列リテラルのみ対応` を維持するか、`struct S { int a[2]; }; struct S s={1,2};` 以外の単一式指定を拡張するか決める（方針: 配列lvarの同長コピーを段階受理）
  - [x] 採用方針に合わせて `decl.c` の診断/受理経路を更新し、回帰テストを追加する（`struct S s={src,7};` を受理固定）
  - [x] 文字列リテラルによる配列メンバ初期化（`struct S s={"ab",7};`）を parser 受理ケースとして追加し、`test_parser` で回帰固定
- [x] cast 型名の宣言指定子サポートを拡張する
  - [x] `(_Atomic volatile int)` や typedef 由来の複合指定子で、現状パース段で落ちる組み合わせを棚卸しする（現状: `(int const)` 形式がパース段で失敗）
  - [x] 受理する指定子セット（type-qualifier / type-specifier の順序自由度）を C11 に近づける（第5段: `(signed char)` / `(unsigned char)` の受理まで完了）

## Parser未実装タスク（2026-03-19 追加棚卸し11）
- [x] `struct` 配列メンバの単一式初期化（`s={src,7}`）を codegen まで正しく実行できるようにする
  - [x] parser受理は追加済みだが、生成コードの実行で `EXIT:139` になる回帰を解消する
  - [x] `test_e2e` に実行時回帰（期待値 `18`）を再導入し、安定化を確認する
- [x] `struct` 配列メンバの文字列初期化（`s={"ab",7}`）を codegen まで正しく実行できるようにする
  - [x] parser受理は追加済みだが、生成コードの実行で `EXIT:139` になる回帰を解消する
  - [x] `test_e2e` に実行時回帰（期待値 `202`）を導入して固定する
- [x] 非スカラ値 cast の未対応範囲を段階的に解消する
  - [x] 依然として診断になる `struct/union` 非同型 cast の受理方針（維持/拡張）を再定義する（方針: 同種かつ同サイズは段階受理）
  - [x] 拡張する場合は最小スコープ（同型一時値、レイアウト互換判定、lowering）を実装する（第1段: `struct/union` の同種・同サイズ値 cast を no-op 受理）
- [x] cast の汎用未対応分岐（`[cast] この型へのキャストは未対応です`）の到達経路を整理する
  - [x] 現在は実質非到達のため、到達しうる型カテゴリを明確化するか分岐を削除する（方針: 汎用文言を廃止し、ガード用の `cast 型指定子の解釈に失敗しました` へ置換）
  - [x] 専用診断へ寄せる場合は異常系テストを更新する（既存の「汎用文言が出ないこと」テストを維持）
- [x] `decl.c` の `1/2/4/8 byte` 限定分岐に到達する実型での検証基盤を整備する
  - [x] 将来型導入時に陽性テストへ置換できるよう、到達性確認の手順をドキュメント化する
  - [x] 到達性確認手順（現行実装）
    - [x] 手順1: `test/test_parser.c` の `test_parse_invalid_diagnostics` で既存否定回帰（`[decl] 構造体/共用体初期化は現在 1/2/4/8 byte スカラのみ対応です` が誤出力されないこと）を維持する
    - [x] 手順2: 新しい非 `1/2/4/8` byte スカラ型を導入したら、同セクションに陽性ケース（当該診断に到達する入力）を追加して否定回帰を置換する
    - [x] 手順3: `make -s DIAG_LANG=ja build/test_parser && build/test_parser` を必須確認として運用する
- [x] 多段ポインタ参照（`int **pp; return **pp;`）を codegen まで正しく実行できるようにする
  - [x] parser受理は可能だが、最小ケース（`int x=42; int *p=&x; int **pp=&p; return **pp;`）で `EXIT:139` となる不具合を解消する
  - [x] `test_e2e` に多段ポインタ参照の実行時回帰テストを追加して固定する

## Parser未実装タスク（2026-03-19 追加棚卸し12）
- [x] 非スカラ値 cast の未対応ケース（スカラ/ポインタ→`struct`/`union`）を段階解消する
  - [x] 現状診断（`[cast] struct/union 値へのキャストは未対応です（非スカラ型）`）となるケースは、当面は診断維持（受理しない）方針とする
  - [x] 受理する場合は最小lowering（byte copy など）を実装し、`test_parser`/`test_e2e` に回帰を追加する（第2段: `struct` へのスカラ/ポインタ cast 受理を追加）
  - [x] 第1段: `union` へのスカラ cast（例: `(union U)7`）を「先頭メンバ初期化」lowering で受理し、`test_e2e` 回帰を追加する
  - [x] 第2段: `struct` へのスカラ/ポインタ cast を最小 lowering（先頭メンバ初期化）で受理し、拒否ケースは「非スカラ源のみ」に整理する
- [x] `struct` 単一式初期化の対応範囲を拡張する
  - [x] 現状未対応の `struct S s=1` は禁止維持とする（`[decl] 構造体の単一式初期化は同型オブジェクトのみ対応です`）
  - [x] 診断文言を `test_parser` で固定する
- [x] `union` 配列メンバの非波括弧初期化（例: `union U { int a[2]; int z; }; union U u={1,2};`）の扱いを明確化する
  - [x] 方針: 段階受理する（先頭配列メンバへの brace elision として扱う）
  - [x] parser/e2e の回帰を整備する（`test_e2e` に受理ケースを追加、compile-fail 期待を削除）
  - [x] 現段階は parse 受理を優先し、実行時の値評価詳細は別タスクで追う（`test_e2e` は parse-only ケースで固定）

## Parser未実装タスク（2026-03-19 追加棚卸し13）
- [x] `union` 先頭配列メンバの非波括弧初期化を実行時意味まで固定する
  - [x] `union U { int a[2]; int z; }; union U u={1,2};` の値評価方針（先頭配列メンバの連続初期化）を明文化する
  - [x] codegen/e2e の期待値テストを追加し、parse-only ケースを実行検証へ置換する
- [x] 非スカラ cast の lowering を次段階へ進める
  - [x] `struct` へのスカラ/ポインタ cast を段階受理し、config でON/OFF可能にする
  - [x] `union` と同様の一時オブジェクト経由 lowering を `struct` 側にも適用する
- [x] cast 後 postfix 連鎖の回帰を拡充する
  - [x] `((union U)&x).p` 以外の連鎖（`->`, `[]`, `++/--`）について parser/e2e を追加し、意味差分を固定する
  - [x] `->` 連鎖の回帰を `test_e2e` に追加する（`((union U*)&u)->a[1]`）

## Parser次フェーズ候補（2026-03-19）
- [x] 最適化: cast lowering 生成ノードの簡素化
  - [x] `struct/union` cast の一時オブジェクト生成で冗長な `ND_COMMA` 連鎖を削減する
  - [x] 最適化後も `test_parser` / `test_e2e` の意味互換を維持する
- [x] C準拠強化: 非スカラ cast の適用範囲を再評価する
  - [x] 現在の先頭メンバ初期化 lowering を C 制約観点で許容する範囲と非許容範囲に分けて仕様化する
  - [x] 非許容ケースは明示診断へ寄せる
- [x] 診断改善: cast/initializer のメッセージ精度向上
  - [x] `struct/union` cast 失敗時に「失敗理由（非スカラ源・無効設定・型不整合）」を区別できるようにする
  - [x] `union` 非波括弧初期化の診断を文脈別（配列メンバ/非配列メンバ）に分割する
- [x] 設定検証: parser config の組み合わせテストを体系化する
  - [x] `enable_*` フラグの ON/OFF 組み合わせを最小網羅で固定する
  - [x] cast 系設定は scalar だけでなく pointer cast ケースも ON/OFF で固定する
  - [x] ドキュメント（README/grammar/walkthrough）に設定マトリクスを追加する

## Parser未実装タスク（2026-03-19 追加棚卸し14）
- [x] `sizeof` / `_Alignof` のタグ型 type-name（`struct/union`）を受理する
  - [x] `sizeof(struct S)` が式フォールバックで失敗する回帰（`[primary] 数値が必要です`）を解消する
  - [x] `test_parser` / `test_e2e` に `sizeof(struct S)` / `_Alignof(struct S)` の回帰を追加して固定する

## Parser未実装タスク（2026-03-19 追加棚卸し15）
- [x] 入れ子 designator（`.a[1]=...`）を初期化子で受理する
  - [x] `struct S { int a[2]; }; struct S s={.a[1]=3};` が `=` 期待で失敗する回帰を解消する
  - [x] `union U { int a[2]; int z; }; union U u={.a[1]=3};` でも同様に受理/診断方針を固定する
- [x] 配列の複合リテラル postfix（`((int[2]){1,2})[1]`）を受理する
  - [x] 現状の `[primary] 数値が必要です` 回帰を解消する
  - [x] `test_parser` / `test_e2e` に配列複合リテラル添字アクセスの回帰を追加する
- [x] 構造体複合リテラルのアドレス経由メンバ参照（`(&(struct S){3})->x`）を受理する
  - [x] 現状の `'->' の左辺は構造体/共用体ポインタ` 診断回帰を解消する
  - [x] 複合リテラルの一時オブジェクト型情報（ポインタ/タグ）の伝搬を整理し、回帰テストで固定する

## Preprocessor未実装タスク（2026-03-20 棚卸し）
- [x] 定義済みマクロ（`__LINE__`, `__FILE__`, `__DATE__`, `__TIME__`, `__STDC__`, `__STDC_VERSION__`）を実装する
  - [x] プリプロセス時に展開される組み込みマクロとして登録する仕組みを導入する
  - [x] `__LINE__` は展開時点のトークン行番号を整数リテラルとして生成する
  - [x] `__FILE__` は展開時点のファイル名を文字列リテラルとして生成する
  - [x] `__DATE__` / `__TIME__` はコンパイル開始時刻から生成する
  - [x] `__STDC__` は `1`、`__STDC_VERSION__` は `201112L` として定義する
  - [x] `test_preprocess` に展開結果の回帰テストを追加する
- [x] `#pragma once` を実装する
  - [x] インクルード済みファイルパスを記録し、2回目以降の `#pragma once` 付きファイルを読み飛ばす
  - [x] `#pragma` が未知の場合は無視する（C11 準拠の黙過）方針を明文化する
  - [x] `test_preprocess` に `#pragma once` の回帰テストを追加する
- [x] `#line` ディレクティブを実装する
  - [x] `#line N` / `#line N "file"` でトークンの行番号・ファイル名を上書きできるようにする
  - [x] `__LINE__` / `__FILE__` の展開結果に反映されることをテストで確認する

## ビットフィールド実装タスク（2026-03-20 棚卸し）
- [x] ビットフィールドのパッキングレイアウト計算を実装する
  - [x] 現状は幅情報を読み捨てフルサイズで計算していた（`parser.c` / `stmt.c`）
  - [x] ビットフィールド幅をメンバ情報として `psx_ctx_add_tag_member_bf` に保持する
  - [x] 同一ストレージユニット内への連続ビットパッキングを計算し、`struct` サイズに反映する
  - [x] パッキング境界（型サイズ境界での新ユニット開始）をC11規則に沿って実装する
- [x] ビットフィールドへの読み書きコード生成を実装する
  - [x] `ND_DEREF` の読み出し時に ARM64 `ubfx`/`sbfx` でビット抽出する
  - [x] `ND_ASSIGN` の書き込み時に ARM64 `bfi` で read-modify-write を生成する
  - [x] 符号付き/無し両方のビットフィールドで正しい符号拡張を行う
  - [x] `test_e2e` にビットフィールドの読み書き回帰テストを追加する

## _Alignas 意味処理タスク（2026-03-20 棚卸し）
- [x] `_Alignas` によるローカル変数のスタックアライメント強制を実装する
  - [x] 現状は `skip_balanced_group` で読み飛ばしているだけ（`parser.c` 行 74-81）
  - [x] `_Alignas(N)` の値をパースして宣言ノードに保持する（`g_last_alignas_value` / `psx_take_alignas_value`）
  - [x] `psx_decl_register_lvar_sized_align` でスタックオフセット計算時に指定アラインメントへ切り上げる
  - [x] `test_e2e` に `_Alignas(16)` 付き変数のアライメント確認テストを追加する（alignas/lvar_align）
- [x] `_Alignas` による構造体メンバのアライメント指定を実装する
  - [x] `struct { _Alignas(8) int x; };` でメンバオフセットが指定値の倍数になることを保証する（parser.c / stmt.c）
  - [x] `sizeof` が正しい値を返すことをテストで確認する（alignas/struct_member: sizeof(S)==16）

## 標準ヘッダスタブ整備タスク（2026-03-20 棚卸し）
- [x] `<stddef.h>` を追加する
  - [x] `size_t`（`unsigned long`）、`ptrdiff_t`（`long`）、`NULL`（`(void*)0`）を定義する
  - [x] `offsetof(type, member)` を組み込み演算子として実装する、またはマクロで定義する
  - [x] `test_e2e` に `sizeof(size_t)` 等の確認テストを追加する
- [x] `<stdbool.h>` を追加する
  - [x] `bool`（`_Bool`）、`true`（`1`）、`false`（`0`）を定義する（`_Bool` 自体はすでに実装済み）
  - [x] `test_e2e` に `bool` 型変数を使う回帰テストを追加する
- [x] `<stdint.h>` を追加する
  - [x] `int8_t` / `uint8_t` / `int16_t` / `uint16_t` / `int32_t` / `uint32_t` / `int64_t` / `uint64_t` を `typedef` で定義する
  - [x] `INT_MAX` / `UINT_MAX` 等の定数マクロを定義する
  - [x] `test_e2e` に固定幅整数型を使う回帰テストを追加する
- [x] `<stdarg.h>` を追加する（可変長引数関数内での `va_list` 使用）
  - [x] Apple ARM64 ABI に準拠した `va_list` / `va_start` / `va_arg` / `va_end` のマクロ実装方針を決める
  - [x] `ag_c` コンパイルコード内で `va_arg` を使う最小ケース（`int` 引数取り出し）を e2e で確認する（stdarg/va_arg_int: my_sum(3,10,20,12)==42）

## Codegen未実装タスク（構造体/共用体 ABI、2026-03-20 棚卸し）
- [x] Apple ARM64 calling convention の構造体引数渡しを実装する
  - [x] 現状は小サイズ構造体をレジスタ渡ししているが、ABI 規約（HFA/HVA / スタック渡し境界）との整合を確認する
    - ≤8B: 1 レジスタ渡し、9-16B: 2 レジスタ渡し（stp/ldp）、>16B: アドレス渡し（byref）— ARM64 ABI 準拠
  - [x] 16 バイト超の構造体を呼び出し元がスタックに配置して先頭アドレスを渡す経路を実装する
    - 呼び出し側: `add x0, x29, #offset` でアドレスを渡す。被呼び出し側: `is_byref_param` フラグ付き lvar として受け取り、ND_DEREF で透過的にメンバアクセス
  - [x] 呼び出し側・被呼び出し側双方の対称性を `test_e2e` で確認する
    - struct_arg/{small_sum, small_member, mid_sum, large_sum} の 4 テストを追加
- [x] 構造体/共用体を戻り値として返す ABI を実装する（≤8B スコープ）
  - [x] ≤8B 構造体: x0 に packed value で返す経路が既存スタックマシンと整合していることを確認（コード変更不要）
  - [x] パーサーが `struct Tag func(...)` 形式を関数定義として認識するよう修正する（`ps_program` に `is_tag_return_function_signature` ルックアヘッドを追加）
  - [x] `funcdef()` 内で struct/union 戻り値型を消費し `ret_token_kind=TK_STRUCT` として設定する
  - [x] `parse_struct_copy_initializer()` に非 lvar RHS（関数呼び出し結果）からの ≤8B 代入フォールバックを追加する
  - [x] `test_e2e` に struct_ret/{make_and_sum, return_member, chain_call} の 3 テストを追加して 309 テスト通過を確認
  - [x] 9–16B 戻り値: x0/x1 ペアで返す経路（呼び出し側・被呼び出し側双方の 2 レジスタハンドリング）
  - [x] >16B 戻り値: `x8` レジスタ（hidden pointer）経由で返す経路を実装する

## VLA（可変長配列）最小実装タスク（2026-03-20 棚卸し）
- [x] VLA 宣言を受理してスタック動的確保に lowering する最小実装を行う
  - [x] 現状は `DIAG_ERR_PARSER_ARRAY_SIZE_CONSTEXPR_REQUIRED` でエラーになる → 非定数サイズを検出して VLA 経路へ分岐
  - [x] 整数変数サイズの 1 次元配列（`int a[n];`）を最小スコープとして受理する
  - [x] ARM64 で `alloca` 相当のスタック動的確保（`sub sp, sp, x_size` + bic アライメント）を生成する
  - [x] VLA サイズ式を宣言時点で 1 度だけ評価し、ベースポインタをフレームスロットに保存、要素アクセスはポインタ経由
  - [x] `test_e2e` に VLA 基本アクセス・ループ代入・関数引数サイズの 3 テストを追加する（vla/basic_elem, loop_fill, param_size）
  - [x] `sizeof(vla)` のランタイム評価（C11 での定義済み動作）を実装する
  - [x] 2 次元 VLA を実装する（`int a[n][M]` 定数内側、`int a[n][m]` 両方可変）
    - [x] `lvar_t` に `outer_stride` / `vla_row_stride_frame_off` フィールドを追加
    - [x] `node_mem_t` に `inner_deref_size` / `vla_row_stride_frame_off` フィールドを追加
    - [x] `decl.c` VLA 分岐で内側次元を解析：定数内側は `outer_stride = M*elem`、可変内側は 24B フレームスロット
    - [x] `expr.c primary()` で `deref_size = outer_stride`、`inner_deref_size = elem_size`（多次元時）を設定
    - [x] `expr.c postfix()` でサブスクリプト条件を `deref_size > 0` に変更、`inner_deref_size` を伝播、実行時ストライドは `LVAR(vla_row_stride_frame_off)` で乗算
    - [x] `arm64_apple.c ND_VLA_ALLOC` に 2D runtime 分岐（rhs=row_stride_expr → frame保存 → n×stride = total bytes）を追加
    - [x] `test_e2e` に `vla_2d/{const_inner_read, const_inner_loop, runtime_inner_read, runtime_inner_loop}` の 4 テストを追加して 313 テスト通過を確認
  - [x] 仮引数 VLA 宣言子（`int f(int n, int a[n])`）を実装する
    - [x] `parse_param_declarator_name` に `out_is_array_declarator` フラグを追加し `[...]` 後置を検出
    - [x] funcdef で `param_elem_size` を取得し、配列宣言子時に `size=8, elem_size=実際のサイズ` でポインタとして登録
    - [x] `sizeof(a)` が 8（ポインタサイズ）を返すことを確認（C11 6.7.6.3p7 準拠）
    - [x] `test_e2e` に `vla_param/{basic_access, sizeof_is_ptr, write_through}` の 3 テストを追加して 316 テスト通過を確認

## パーサーバックログ候補（2026-03-20 棚卸し）

### `__func__` 定義済み識別子
- [x] `__func__` を実装する（C99/C11 で各関数本体に暗黙定義される `const char[]`）
  - [x] `g_current_funcname` を `expr.c` に追加し、`funcdef()` で関数名をセット（`psx_expr_set_current_funcname`）
  - [x] `primary()` で `__func__` トークンを認識し、現在の関数名文字列リテラル（`.LC%d`）に展開する
  - [x] `test_e2e` に func_name/{first_char_main, first_char_helper, each_func_distinct} の 3 テストを追加（312 テスト通過）

### VLA 拡張
- [x] 多次元 VLA（`int a[n][m]`）を実装する（「VLA（可変長配列）最小実装タスク」セクションで実装済み）
- [x] 仮引数 VLA 宣言子（`int f(int n, int a[n])`）を実装する（「VLA（可変長配列）最小実装タスク」セクションで実装済み）

### `inline` 関数
- [x] `inline` 指定子のセマンティクスを実装する
  - [x] `inline` キーワードは `is_decl_prefix_token` / `skip_cv_qualifiers` に含まれており、`psx_consume_type_kind()` 経由で自動スキップ → 追加実装不要
  - [x] 単一翻訳単位では通常の関数定義と同様にコード生成される（inline expansion は省略）
  - [x] `extern inline` / `static inline` も同様に `skip_cv_qualifiers` でスキップ → 通常関数として出力
  - [x] `test_e2e` に `inline_func/{basic_inline, static_inline, extern_inline}` の 3 テストを追加して 319 テスト通過を確認

### `extern` 宣言・暫定定義
- [x] 関数スコープの `extern` 変数宣言を実装する（`extern int g;` をローカルスコープで使用）
  - [x] 外部シンボルをアドレスロードで参照するコードを生成する
- [x] グローバル変数の暫定定義（tentative definition）を実装する
  - [x] 初期化子なしのグローバル変数宣言を暫定定義として扱い、翻訳単位末に `.comm` セクションへ出力する

## グローバル変数 + extern 宣言タスク（2026-03-21 棚卸し）
- [x] グローバル変数のコード生成を実装する
  - [x] `ND_GVAR` AST ノード型を追加し、`adrp`/`add @PAGE`/`@PAGEOFF` によるシンボルアドレス参照を生成する
  - [x] `global_var_t` リンクリストでグローバル変数を管理する（tentative / initialized / extern）
  - [x] `ps_program()` でトップレベル変数宣言をパースし `global_vars` テーブルに登録する
  - [x] `primary()` でローカル変数未発見時に `global_vars` フォールバック → `ND_GVAR` ノードを返す
  - [x] tentative 定義は `.comm` ディレクティブ、初期化済みは `.data` セクションで出力する
  - [x] ローカルスコープの `extern int g;` をサポートする（`skip_cv_qualifiers` で `extern` フラグ検出）
  - [x] `test_e2e` に `global_var/{tentative_rw, tentative_multi_func, initialized, initialized_modified, local_extern}` の 5 テストを追加して 319 テスト通過を確認

### 前置インクリメント/デクリメントの lvalue 扱い
- [x] `++(++x)` / `*(p++)` 等の連鎖を制約に従って診断する
  - `++(++x)`, `(x++)++`, `--(--x)` → 「左辺値である必要があります」エラーで診断済み
  - `*(p++)` → コンパイル成功（C11 で合法な式、正しい動作）

## Codegen バックログ候補（2026-03-20 棚卸し）

### 構造体/共用体 ABI 拡張
- [x] 構造体/共用体を戻り値として返す ABI を 9–16B・>16B へ拡張する
  - [x] 9–16B 戻り値: x0/x1 ペアで返す経路を実装する（テスト3件追加、333テスト通過）
  - [x] >16B 戻り値: x8 に呼び出し元が確保したバッファアドレスを渡す indirect return を実装する（テスト3件追加、336テスト通過）

### 浮動小数点リテラル拡張
- [x] 16進浮動小数点リテラル（`0x1.8p+3` 形式）を実装する
  - [x] トークナイザで `0x[0-9a-fA-F]+'.'[0-9a-fA-F]*'p'[+-]?[0-9]+` パターンを認識する（`has_hex_float_marker` + `strtod` で実装済み）
  - [x] `strtod` / `strtof` を用いて変換し、既存浮動小数点リテラル経路に接続する（f/F/l/L サフィックスも対応済み）
  - [x] `test_e2e` に hex float の回帰テストを追加する（4件追加、340テスト通過）

### `#pragma pack`
- [ ] `#pragma pack(push, N)` / `#pragma pack(pop)` を実装する
  - [ ] プリプロセッサで `#pragma pack(...)` を認識し、現在のパック値をスタック管理する
  - [ ] 構造体メンバオフセット計算でパック値を上限として使用する
  - [ ] `test_e2e` に `#pragma pack(1)` を使った構造体レイアウト回帰テストを追加する

## 型修飾・制約チェック バックログ候補（2026-03-20 棚卸し）

### `const` 正確性（const-correctness）
- [ ] `const` 変数への代入を診断する（現状: 未チェック）
  - [ ] `const int x = 1; x = 2;` を diagnostic error とする
  - [ ] `int *p = const_ptr;` のような暗黙の const 外れを警告/エラーとする
  - [ ] `test_e2e` の診断テストに const 違反ケースを追加する

### `restrict` 修飾子セマンティクス
- [ ] `restrict` の制約チェック方針を仕様化する
  - [ ] 現状: パーサーで受理するのみ、意味処理なし
  - [ ] 方針として「意味処理は実施しない（optimizer 向け hint として将来対応）」と明文化する

## 標準ヘッダ追加バックログ候補（2026-03-20 棚卸し）
- [x] `<limits.h>` を追加する（`INT_MAX`, `LONG_MAX` 等 — 現状は `<stdint.h>` 側のみ）
  - [x] `include/limits.h` に `CHAR_BIT`, `SCHAR_MIN/MAX`, `CHAR_MIN/MAX`, `SHRT_MIN/MAX`, `INT_MIN/MAX`, `UINT_MAX`, `LONG_MIN/MAX`, `ULONG_MAX`, `LLONG_MIN/MAX`, `ULLONG_MAX` を定義
  - [x] `test_e2e` に `limits_int_max`, `limits_int_min`, `limits_char_bit` の 3 テストを追加して 330 テスト通過を確認
- [ ] `<float.h>` を追加する（`FLT_MAX`, `DBL_EPSILON` 等の浮動小数点定数）
- [ ] `<string.h>` スタブを追加する（`memcpy`, `memset`, `strlen` 等の宣言のみ）
- [ ] `<stdio.h>` スタブを追加する（`printf`, `fprintf`, `sprintf` 等の宣言のみ）
- [ ] `<stdlib.h>` スタブを追加する（`malloc`, `free`, `exit`, `atoi` 等の宣言のみ）

## Codegen レジスタ割り付け最適化（2026-03-21 棚卸し）

現状の codegen はスタックマシン方式で、すべての中間値をメモリ（ソフトウェアスタック）経由で受け渡している。
`str x0, [sp, #-16]!` / `ldr x0, [sp], #16` が式評価のたびに発生し、不要な load/store が大量に生成される。

### フェーズ1: ピープホール最適化（簡易、効果大）
- [ ] 連続する `str x0, [sp, #-16]!` → `ldr x0, [sp], #16` パターンを検出して除去する
  - 出力バッファを後方参照し、直前の str と対になる ldr を削除する
  - 二項演算（add, sub, mul, div 等）の前後で発生する冗長な push/pop が主な対象
- [ ] `ldr x1, [sp], #16` → `ldr x0, [sp], #16` の連続を `ldp x0, x1, [sp], #32` に置換する

### フェーズ2: 線形スキャン レジスタ割り付け（中規模、本格的な改善）
- [ ] AST を走査して式ノードに「結果レジスタ番号」を割り当てるパスを追加する
  - 利用可能レジスタ: x9〜x15（x0-x7 は ABI 引数/戻り値、x16-x18 は ABI 予約、x19-x28 は callee-saved）
  - 式の深さが7を超えた場合のみスタックにスピルする
- [ ] gen_expr を修正し、割り当て済みレジスタ間で直接演算するコードを生成する
  - 例: `a + b` → `ldr x9, [x29, #off_a]` / `ldr x10, [x29, #off_b]` / `add x9, x9, x10`（スタック経由なし）
