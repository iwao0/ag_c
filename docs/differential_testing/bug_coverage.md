# 差分テスト バグカバレッジ表

clang との差分テスト（同一 C ソースを ag_c と clang でコンパイルして exit code を比較）で
炙り出した miscompile / コンパイルエラーの **チェック済み領域** を管理する。同じ領域を
何度も探さないための索引。

最終更新: 2026-08-01（tokenizer・parser・演算子制約のNative/Wasm拒否parityまで）

## 凡例（状態）
- ✅ **済**: チェック済みで現状 green（差分なし）。
- 🔧 **修正済**: バグを発見し修正＋回帰 fixture 登録済み（`test/fixtures/probes_found_bugs/`）。
- ⚠️ **未対応**: バグを確認済みだが未修正（既知の制約）。
- ⬜ **未チェック**: まだ系統的に差分テストしていない。

## 使い方
1. 新しい領域を差分テストする前にこの表を見て、未チェック（⬜）や未対応（⚠️）を優先する。
2. バグを修正したら行を 🔧 に更新し、fixture 名 / コミットを記入する。
3. 「型 × 宣言経路 × 使用文脈」で取りこぼしが多い（例: unsigned を直しても int だけ／local だけ／
   scalar だけ、になりがち）。1 つ直したら long/char/short・global・typedef・配列要素・
   ポインタ deref・struct メンバまで広げて確認し、各セルを更新する。
4. 手法・再現手順は `HANDOFF.md` の「作業のやり方」を参照。

---

## カバレッジ表

### 整数型の幅 / 符号 / キャスト
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| sub-int (char/short) の符号拡張・ゼロ拡張 | 🔧 | cast_short_char_sign_extend ほか | インライン使用で sign/zero-extend |
| (int)/(unsigned)/(signed) キャストの符号更新 | 🔧 | 5b474f7, 1317698 | 全経路網羅済み |
| (int)/(long) の 32/64bit 切り詰め・拡張 | 🔧 | 1c8e358, 8ddafa6 | (long)unsigned の zext 含む |
| long/long long リテラルの i64 型付け | 🔧 | 365d8c0 | |
| i32 比較の 32bit 幅 / sub-int 戻り値切り詰め | 🔧 | int_cmp_width_and_subint_return | |
| unsigned char/short 戻り値のゼロ拡張 | 🔧 | 1b5e1df | |
| unsigned char/short 戻り値の整数昇格（比較の符号） | 🔧 | unsigned_subint_return_promote | funcall に is_unsigned を立てていたため `f()>-1` が unsigned 比較で誤判定。char/short は signed int 昇格なので除外、unsigned int/long のみ保持 |
| typedef 経由 unsigned char/short (`uint8_t`) のロード符号性 | 🔧 | stdheader/stdint_uint8 | adjust_local_decl_spec_from_typedef が typedef の unsigned を上書きして捨て、1byte ロードが ldrsb に。200→-56 (exit truncation で偽装通過) |
| typedef 経由 unsigned char/short の**戻り値**ゼロ拡張 | 🔧 | typedef_unsigned_subint_return | resolve_func_ret_typedef が typedef の unsigned を捨て、sub-int 戻り値が符号拡張 (`u8 f(){return 200;}`→-56)。上記ローカルと同根の戻り型版 |
| typedef unsigned を struct **メンバ**型に使ったときのロード符号性 | 🔧 | typedef_unsigned_struct_member | struct_layout の typedef メンバ分岐が td_isu を member_is_unsigned に反映せず符号拡張 |
| unsigned char/short **配列メンバ** `s.x[i]` のゼロ拡張 | 🔧 | unsigned_char_array_member | build_member_deref_node が配列メンバに pointee_is_unsigned を立てず（is_bool と非対称）。サイズ1配列は array_len=0 に潰れるため subscript で base is_unsigned も伝播 |
| Wasm object の global unsigned sub-int scalar/array data | 🔧 | test_wasm32_object: global_unsigned_data | file-scope `unsigned char` / `unsigned short` scalar と配列初期化の object bytes と、参照時の `i32.load8_u` / `i32.load16_u` を固定 |
| Wasm object の global signed sub-int scalar/array data | 🔧 | test_wasm32_object: global_signed_data | file-scope `signed char` / `short` scalar と配列初期化の object bytes と、参照時の `i32.load8_s` / `i32.load16_s` を固定 |
| `unsigned long`/`unsigned char` 戻りの符号性追跡 | ✅ | unsigned_long_return_signedness | 旧 ⚠️。再検証で shift/divide/比較いずれも clang 一致＝再現せず。回帰 fixture 化 |
| 混在幅比較（片側 i32・片側 i64） | ✅ | mixed_width_comparison | 旧 ⚠️。混在は 64bit で sign/zero-extend して比較され正しい（miscompile でない）。回帰 fixture 化 |
| **runtime負値式のmixed-width通常算術変換** `long long x=-12; x==-9-runtime_int` | 🔧 | mixed_width_negative_runtime_conversion, aggregate_value_abi_xtu_boundaries | binary loweringが左右の値を通常算術変換後の共通型へ明示変換せず、ARM64でruntime `i32`演算結果をW-registerに置いたまま`i64` operandと演算・比較していた。負の`i32`は上位32bitがzeroになり、同じbit patternの正の`4294967284LL`として扱われる。TypeShapeとDataLayoutから共通整数型をMIRで一度だけ分類し、非shiftの加減乗算・bitwise・等価/関係比較前にsignedはSEXT、unsignedはZEXT、縮小時はTRUNCする。左右順序、全比較・四則演算・剰余・bitwise、runtime式の単一評価、cross-TU large aggregate member検査をClang strictおよびnative/WAT/objectで固定する |
| **同一IR幅でrankとsignednessが競合するruntime通常算術変換** `long long -7 + unsigned long 3` | 🧪 | mixed_rank_runtime_arithmetic_boundaries | `long + unsigned int`は全値を表現できるsigned long、同じ64-bitの`long long + unsigned long`はunsigned long long、`int + unsigned long`はunsigned longになるrank規則を型とruntime値の両方で固定する。左右順序、加減乗除・剰余・bitwise・比較、compound assignmentの演算後格納変換、関数呼出しoperandの単一評価をClangおよびnative/WAT/objectで照合する |
| **byte pointer＋wide offsetから復元したtyped lvalueのcompound assignment** `*(unsigned int *)(bytes + offset) += a - b` | 🧪 | cast_lvalue_mixed_rank_compound_boundaries | 実際の`unsigned int`配列objectをcharacter pointerで走査し、整列済みsubobject位置を元型pointerへ戻してcompound assignmentする定義済み境界を固定する。64-bit unsigned式でoffsetを計算し、RHSの`unsigned int - unsigned long long`を通常算術変換した後、加減乗算結果をlvalueの32-bit unsigned型へ戻す。compound式の結果値、隣接subobject不変、address/RHS関数の単一評価を検査し、未整列dereferenceという未定義動作を含むc-testsuite 00217の意図をClang strict・Native・WAT・Wasm objectで安全に保持する |
| 複合代入の sub-int / unsigned wrap | ✅ | integer_runtime_arithmetic_boundaries | unsigned char/short の代入後変換と wrap を local・配列・member・pointer dereference で固定 |
| 連鎖代入 `b=a=E` の sub-int lvalue 経由の値変換 | 🔧 | chained_assign_narrow_lvalue | 代入式の値が lvalue 型へ変換されず、`b=a=300`(a=uchar) で b が 300 のまま。格納後に再ロードして返す (lvar/gvar/deref/member) |
| 三項 `c?int:char` の sub-int 分岐の値 | 🔧 | ternary_subint_branch | char 分岐を 4 バイト slot に strb で書き上位 garbage→merge ldrsw が誤値。結果型へ retag して full-width store (signed/unsigned 双方正しい) |
| シフト境界・左右独立の整数昇格・unsigned/signed 右シフト | ✅ | integer_runtime_arithmetic_boundaries, shift_integer_promotion_runtime_boundaries | 32/64-bit最上位bit、符号なし論理右shift、targetの符号付き算術右shift、sub-int/bit-field/_Boolの整数昇格、左辺と異なる幅・signednessの動的shift count、結果型がpromoted leftだけで決まること、複合代入の格納時変換、operandの単一評価をClang strictおよびnative/WAT/objectで固定 |
| **シフト結果型 = promoted left operand** `((unsigned)1 << (long)1)` | 🔧 | shift_left_operand_type | C11 6.5.7p3。結果型を左右最大幅にしていたため右辺 `long` で結果が 64bit 化し c-testsuite 00200 が不一致。ND_SHL/ND_SHR の型幅・unsigned 伝播・IR result_ty を promoted 左辺基準へ。併せて `(long)1` / `(unsigned long)1` の cast 結果幅を保持し `sizeof((long)1)==8` を固定 |
| **符号なし整数定数式の整数昇格・通常の算術変換** `~0u >> 1`, `UINT_MAX > -1`, `1u / -1`, `1ULL << 63` | 🔧 | unsigned_integer_constant_expressions / unsigned_long_long_constant_boundaries / compile_fail: switch_duplicate_case_unsigned_{shift,comparison,long_conversion}, unsigned_long_long_{divzero_constant,negative_shift_count,shift_width} | Syntax定数評価と静的HIR初期化評価が生の `long long` 演算を使い、unsigned右シフト、mixed signed/unsigned比較・除算・剰余を誤評価していた。各Syntax式のcanonical `QualType`をresolver内に保持し、`TypeShape + DataLayout`による整数昇格・通常の算術変換・結果型正規化へ統一。32/64-bitの最上位bit・加算/乗算wrap、`_Static_assert`、enum、配列境界、bit-field幅、`_Alignas`、case重複、global/static local/aggregate初期化を網羅する。ゼロ除算・負shift count・型幅以上のshiftはshould_reject正本をNative/Wasm object compile-failへ登録し、E3010/E3064まで固定する |
| **cast・`sizeof`・文字定数を含む整数定数式** `(unsigned char)257`, `(_Bool)-27`, `sizeof(int)-5`, `'A'+'\\n'` | 🧪 | constant_cast_sizeof_character / compile_fail: static_assert_sizeof_vla | 型付き定数評価の派生境界。unsigned char/shortの切り詰め、`_Bool`正規化、固定長`sizeof`が返す64-bit unsigned `size_t`の減算wrap、通常/escape/NUL文字定数を、直接evaluator・enum・`_Static_assert`・配列境界・bit-field幅・global/static初期化・native/Wasm実行で固定。VLAへの`sizeof`は実行時値のため`_Static_assert`では拒否する |
| **`sizeof` / `_Alignof` の未評価operand・結果型・型制約** `sizeof(1/0)`, `sizeof(*(int*)0)`, `_Alignof(int[(effect++, n)])` | 🧪 | unevaluated_type_query_boundaries, type_query_result_type_boundaries / compile_fail: sizeof_{incomplete_array_type,incomplete_array_expression,flexible_array_member,bitfield,generic_selected_bitfield,generic_selected_function,incomplete_enum}_rejected, alignof_{void,incomplete,incomplete_array,function}_type_rejected | 通常operandの除算・null dereference・post-incrementは評価せず、VLA `sizeof`のboundだけ実行時評価する。VLA型の`_Alignof`はboundを評価せず、結果をenum・`_Static_assert`・配列境界・bit-field幅・`_Alignas`で利用できることを固定する。両演算子の結果がtargetの`size_t`型であること、cv-qualified・atomic・register配列の有効な問い合わせをNative/WAT/Wasm objectで照合し、`void`・未完成record/配列/enum・flexible array member・関数型・bit-fieldへの適用はClang strict同様に拒否する。should_reject正本をcompile-fail registryから参照し、E3064/E3117/E3118のNative/Wasm診断ID parityまで固定する |
| **短絡・条件演算子内のVLA `sizeof`評価** `cond ? sizeof(int[(left++, n)]) : sizeof(int[(right++, m)])`, `0 && sizeof(VLA)` | 🧪 | vla_sizeof_short_circuit_boundaries | VLA型の`sizeof`は実行時式だが、条件演算子の未選択枝と`&&`/`||`の短絡された右辺には到達しない。選択されたboundだけを1回評価し、短絡されない論理右辺は通常どおり1回評価することをClang strictとの比較およびnative/WAT/objectで固定する |
| **VLA scopeへの再入とbound再評価** `repeat: { int values[next_bound()]; ... goto repeat; }` | 🧪 | vla_scope_reentry_boundaries | VLAを含むblockから一度退出して後方`goto`またはloopの次反復で再入すると、bound式を毎回1回評価して新しいVLA instanceを作る。2回のgoto再入で異なる長さを捕捉し、4回のloop反復で各instanceの`sizeof`・先頭/末尾access・書込みが新しいboundへ従うことをClang strictとの比較およびnative/WAT/objectで固定する |
| **VLA scope退出時のstorage解放** loop `continue` / `break`・後方`goto` | 🔧 | vla_scope_storage_release | `IR_VLA_ALLOC`が関数終了までstackを戻さず、同じ宣言へ再入するたびに約4KiBを累積して20,000反復でnativeがexit 139になっていた。target-independent MIRへ`stack_save`/`stack_restore`を追加し、loop body・`for`全体・switch・後方labelのCFG境界でcheckpointを復元する。`for`/`while`/`do-while`の通常末尾、`continue`、`break`、後方`goto`について同時生存量は1 instanceのままになることをClang strictおよびnative/WAT/objectで固定する |
| **連続するlexical scopeのVLA storage解放** `{ int a[n]; } { int b[m]; }` | 🔧 | vla_sequential_scope_storage_release | 合成statement列とsource compound statementが同じ`PSX_HIR_BLOCK`だったため、通常のblock退出ではdynamic stackを復元できず、24個の非重複VLAが累積してnativeはexit 139、WATはout-of-boundsになっていた。source scopeを`PSX_HIR_SCOPE`として分離し、VM宣言前後のstack状態をsemantic lifetime IDへ対応付ける。各blockで同時に1個だけ生存することをClang strictおよびnative/WAT/objectで固定する |
| **goto先のVLA lifetime checkpoint** label前後の複数VLA・未実行label | 🔧 | vla_goto_lifetime_checkpoint_boundaries | label実行時にだけcheckpointを保存すると、labelを飛び越えた後の後方`goto`が未初期化checkpointを使う。またlabelより前のVLAを保持しつつ後のVLAだけを破棄する位置も表せない。semanticが検証済みのVM宣言chainをTyped HIRへ保持し、goto元とlabel位置のlifetime ID差から復元先を選ぶ。先行VLAの内容保持、label間VLAの再宣言、最初に飛び越されたlabelへのgotoをClang strictおよびnative/WAT/objectで固定する |
| **`for`初期宣言VLAのlifetime** `for (unsigned char bytes[n]; ...; ...)` | 🧪 | vla_for_initializer_lifetime_boundaries | `for`初期宣言のVLAはloop本体の各反復で再生成せず、`continue`後も同一instanceと捕捉済みboundを保持する。一方、loopのnormal終了・`break`・外側scopeへの`goto`ではstorageを解放する。大容量VLAを複数回生成して累積解放漏れも検出し、Clang strictおよびnative/WAT/objectで固定する |
| **for-init/body二段VLA lifetime** 初期宣言VLAを保持したbody VLAの`continue`・normal退出・`break`・外側`goto` | 🧪 | vla_for_init_body_lifetime_boundaries | for-init VLAとbody-local VLAが同時にliveな二段checkpointで、`continue`はbody VLAだけを解放し、初期宣言VLAの内容・`sizeof`・captured boundを次反復まで保持する。normal終了、`break`、外側scopeへの`goto`では両storageを解放し、大容量VLAの反復で各bound評価回数とstack累積漏れをClang strictおよびnative/WAT/objectで固定する |
| **switch内VLAの退出と外側VLA保持** case内VLAから`break` / 外側loopへの`continue` / `goto` / 通常退出 | 🧪 | vla_switch_exit_lifetime_boundaries | switch caseのlexical scopeで確保したVLAは各退出経路で解放し、switchより外側で生存中のVLAはcheckpoint復元後も内容と`sizeof`を保持する。大容量の内側VLAを全4経路で反復してstack累積漏れを検出し、Clang strictおよびnative/WAT/objectで固定する |
| **switch case fallthroughのVLA lifetime** case-local VLAを破棄して次caseへfallthrough | 🔧 | vla_switch_fallthrough_lifetime_boundaries | switch終了時に使うstack checkpointをcase dispatch後に保存していたため、選択caseへ直接分岐する経路では未初期化checkpointを復元してnativeがSIGBUSになった。checkpoint保存をdispatch前へ移して全case/default経路を支配させ、各case-local VLAだけをfallthrough時に解放しながらswitch外側VLAを保持する。先頭・中間・末尾caseを入口にした大容量VLA列をClang strictおよびnative/WAT/objectで固定する |
| **nested switchのVLA lifetime階層** loop body・outer case・inner caseの三段checkpoint | 🧪 | vla_nested_switch_lifetime_boundaries | nested switchでinner `break`とinner/outer fallthroughが外側caseおよびloop bodyのVLAを保持し、二重switch内の`continue`は全段を解放して外側loopへ移ることを確認する。outer/innerの先頭・中間・default入口を切り替え、各bound評価回数・生存中VLAの内容・反復時のstack累積漏れをClang strictおよびnative/WAT/objectで固定する |
| **if/else分岐のVLA lifetime合流** 両armの通常退出・`continue`・`break` | 🧪 | vla_if_branch_lifetime_boundaries | if/elseの各lexical scopeで確保したVLAはmerge前に解放し、分岐外で生存中のVLAは内容・`sizeof`・captured boundを保持する。merge後の兄弟scopeで大容量VLAを再利用し、両armからの`continue`と`break`では分岐内VLAとloop body VLAをまとめて解放する。Wasm 64 KiBを超える反復数で累積漏れを検出し、Clang strictおよびnative/WAT/objectで固定する |
| **nested gotoのVLA lifetime階層** 外側VLAを保持したまま内側・兄弟scopeへ後方再入 | 🧪 | vla_nested_goto_lifetime_boundaries | 外側scopeのVLAを生存させたまま、後方`goto`で内側VLA scopeを繰り返し退出・再入する。内側storageは各edgeで解放し、外側VLAの内容・`sizeof`・captured boundは保持する。連続する兄弟scopeの異なるVLAも各反復でboundを再評価しつつ同時生存させず、Clang strictおよびnative/WAT/objectで固定する |
| **関数return時のVLA lifetime** live VLAからscalar / complex / aggregate / `void` return | 🧪 | vla_return_lifetime_boundaries | VLAを保持したまま関数からreturnするとき、return値をstack復元前にmaterializeし、関数epilogueでcallerのdynamic stackを必ず復元する。整数scalar、専用loweringを通る`double _Complex`、外側・内側VLAが同時にliveなaggregate return、明示的`void return`を大容量VLA付きで反復呼出しし、bound評価回数・return値・stack累積漏れをClang strictおよびnative/WAT/objectで固定する |
| **nested loopのVLA lifetime** 外側VLAを保持した内側`for` / `while` / `do-while`の`continue`・`break` | 🧪 | vla_nested_loop_lifetime_boundaries | loop target checkpointを二段積んだ状態で、内側loopの`continue`と`break`は内側VLAだけを解放し、外側loop bodyのVLAは内容・`sizeof`・captured boundを維持する。3種類のloopそれぞれで大容量の内側VLAを反復確保し、外側・内側bound評価回数とstack累積漏れをClang strictおよびnative/WAT/objectで固定する |
| **VLA initializer禁止の診断・pipeline停止** `int values[count] = {0};` | 🔧 | should_reject / compile_fail: vla_initializer | direct Typed HIR preflightがVLA initializerを検出しても診断なしで失敗を返し、callerの`E0006`内部不変条件違反へ落ちていた。重複していた事前判定を削除してlocal declaration resolutionへ一本化し、共通pipelineが`E3064`を発行した直後に必ず失敗を返すようにする。Clang strictとの比較、Native/Wasm should_reject、両compile-fail経路で固定する。 |
| **ローカル不完全配列の定義・初期化診断** `int values[];`, `int values[] = 1;`, `int values[] = source;` | 🔧 | should_reject / compile_fail: {local,static_local}_incomplete_array_{without,scalar,array}_initializer | direct Typed HIR preflightがautomatic未指定長配列を共通local declaration pipelineより前に除外し、automatic/staticの長さを補完できない式initializerも拒否理由を記録しなかったため、`E0006`へ落ちていた。initializerなしは不完全配列のまま共通pipelineへ渡して`E3064`を発行し、initializer-listまたは互換な文字列以外の式は型不一致`E3099`として記録する。両storage classの6境界をClang strict、Native/Wasm should_reject、Native/Wasm object compile-failの診断ID parityで固定する。 |
| **固定長ローカル配列の式initializer制約** `int values[2] = 1;`, `int values[2] = source;` | 🔧 | should_reject / compile_fail: {local,static_local}_fixed_array_{scalar,array}_initializer | automatic配列の式initializerは不一致時に拒否理由を記録せず`E0006`へ落ち、同じ配列型の通常の識別子式ならC11にないobject copyとして受理していた。文字配列への互換な文字列literal以外の標準C式initializerは型不一致`E3099`として拒否し、既存の配列compound-literal初期化拡張だけを`ND_COMPOUND_LITERAL`かつ同一配列型に限定して維持する。automatic/staticの4境界をClang strict、Native/Wasm should_reject、Native/Wasm object compile-failの診断ID parityで固定する。 |
| **file-scope配列式initializer・配列単純代入の残余境界** fixed/incomplete array、string literal、mismatched compound literal | 🧪 | should_reject / compile_fail: file_scope_{fixed,incomplete}_array_{scalar,array}_initializer, local_fixed_array_mismatched_compound_literal_initializer, assignment_{fixed_array,string_to_array,incomplete_array} | file-scopeでも配列の式initializerは許可せず、fixed arrayは`E3099`、initializerからboundを推論できないincomplete arrayは`E3064`で拒否する。automaticの配列compound-literal初期化拡張は同一配列型だけに限定し、型が異なるcompound literalを`E3099`で拒否する。初期化時だけ許可される文字列literalを含め、fixed/incomplete arrayは単純代入のmodifiable lvalueにならないため`E3098`で拒否する。8境界をClang strict、Native/Wasm should_reject、Native/Wasm object compile-failの診断ID parityで固定する。 |
| **initializer要素過多の診断分類** `{1, 2}` for scalar/array/struct/union | 🔧 | should_reject / compile_fail: excess_{scalar,array,struct,union}_{initializer_{file_scope,local,static_local},compound_literal_initializer} | flat initializer plannerがleafを使い切った後にunion以外の失敗種別を設定せず、file/local/compound literalのarray・struct・scalarが`E0006`内部不変条件違反へ落ちていた。余剰要素tokenを保持してobject kindごとにscalar=`E3025`、array=`E3027`、struct=`E3032`、union=`E3036`へ分類する。file-scope scalarの汎用`E3064`も`E3025`へ統一し、4種類×4文脈の16境界をClang strict、Native/Wasm should_reject、Native/Wasm object compile-failで固定する。 |
| **scalar initializerの過剰なbrace階層** `int value = {{7}};`, `(int){{7}}` | 🔧 | scalar_subobject_brace_init / should_reject / compile_fail: scalar_initializer_excess_braces_{file_scope,local,static_local}, scalar_compound_literal_excess_braces, pointer_initializer_excess_braces_file_scope, scalar_initializer_recursive_excess_braces | scalar objectを直接初期化するinitializer-listの唯一の要素がさらにinitializer-listである場合はC11制約違反だが、automatic objectとcompound literalは受理し、static localは`E0006`、file-scopeは`E3064`へ分岐していた。local flat plannerとAST非依存のstatic classifierへ専用失敗種別を追加し、すべて`E3025`へ統一する。一方、aggregateのscalar子要素を一重braceで初期化する合法形は正例fixtureで維持し、Clang strict、Native/Wasm should_reject、WAT/Wasm objectで固定する。 |
| **`_Generic` の未選択associationと定数式** `_Generic(1, int:7, default:runtime())` | 🧪 | generic_unselected_constant_expression / compile_fail: generic_{void,incomplete,function,vla}_association_type, generic_{incomplete_array,implicit_incomplete_tag}_association_type, generic_unselected_{undefined_identifier,incomplete_compound_literal} | 制御式と未選択associationの副作用・関数呼出しは評価せず、選択式だけでenum・`_Static_assert`・global/aggregate初期化の整数定数式を構成する。一方、未選択associationも意味解析し、association型は完全な非VM object型に限定して`void`・未完成record/array・暗黙宣言された未完成tag・関数・VLA型を拒否し、未選択式内の識別子とcompound literalも名前・型制約を満たす必要がある。8境界はshould_reject正本をcompile-fail registryから参照し、E3043/E3064/E3066/E3114のNative/Wasm診断ID parityまで固定する |
| **`_Generic`選択結果の値カテゴリ・修飾保持** `_Generic(0, int: constant, default: mutable) = 3` | 🧪 | should_reject: assign_generic_selected_const | generic selectionの結果は選択association式の値カテゴリと型修飾を保持する。選択されたconst lvalueをmodifiable lvalueとして代入できないことをClang strictと照合し、E3077のNative/Wasm診断ID parityを固定する |
| **`_Generic` associationの互換型重複** `typedef int integer`, `int (*)(int)` / `int (*)(const int)` | 🧪 | function_pointer_constraint_positive_boundaries, nested_callback_{return_qualifier,parameter_adjustment,prototype_variadic,return_function_prototype,parameter_qualifier_adjustment,function_pointer_parameter_qualifier}_boundaries / compile_fail: generic_duplicate_{typedef_association,positive_enum_compatible_type,negative_enum_compatible_type,qualified_parameter_function_pointer,array_adjusted_function_pointer,function_adjusted_parameter_pointer,nested_callback_parameter_pointer_qualifier,nested_callback_array_adjustment,nested_callback_function_adjustment,nested_callback_unprototyped_int,nested_callback_return_function_unprototyped_int,nested_callback_restrict_array_adjustment,nested_callback_atomic_array_adjustment,nested_callback_function_pointer_parameter_qualifier} | association typeは互いに互換であってはならない。typedefと基底型、enumと実装上のcompatible integer type、値parameterの最上位修飾を除いた型、array parameterをpointerへ調整した型、function parameterをfunction pointerへ調整した型、およびconsumer関数のparameter内へ再帰したcallbackのpointer最上位修飾除去・array/function parameter調整後の型はそれぞれ同じ型になる。array declarator内の`restrict`とpointer側`_Atomic`もparameter調整後の最上位修飾として除去し、callbackが受け取るfunction pointer parameterの`const`/`volatile`も同様に除去する。さらにparameter未指定callbackとdefault argument promotionで不変な`int` prototypeは、callbackが返すfunction pointerの内側を含めて互換なため、これらを重複指定するとE3105で拒否する。一方、同じ調整後型を単独associationとして選択する合法形はpositive fixtureで固定する。14件はshould_reject正本をcompile-fail registryから参照してNative/Wasm診断ID parityを固定する |
| **条件演算子の未選択operandと定数式適格性** `1 ? 7 : (1/0)`, `1 ? 3 : runtime()`, `1 ? 2 : (3,4)` | 🔧 | conditional_unselected_constant_expression / should_reject: conditional_{unselected_call,selected_comma}_enum, logical_{unselected_call,selected_comma}_static_assert / compile_fail: conditional_{struct_condition,distinct_struct_types,incompatible_object_pointers,void_scalar_operands} | 条件演算子・論理短絡は未選択operandを評価しないためゼロ除算を実行せず、静的初期化では未選択関数呼出しも許容する。一方、整数定数式は未選択側を含む全operandの構文種別に制約があり、enumや`_Static_assert`では関数呼出しを含められない。comma演算子は未評価subexpression内だけ許容され、選択・評価される位置では拒否する。Syntax evaluatorを「評価位置を追跡するoperand適格性検査」と「短絡する値評価」に分け、ネストした条件式・論理短絡と両branchの型制約もClang strict比較で固定する。4件のICE拒否境界はshould_reject正本からNative/Wasm object compile-failへ登録する |
| **条件演算子のscalar条件・共通型制約** struct条件・nonzero integer/pointer・distinct struct・void/scalar・pointer-to-array element qualifier | 🧪 | should_reject / compile_fail: conditional_{struct_condition,pointer_nonzero_integer,different_structs,distinct_struct_types,void_scalar_operands,differently_qualified_array_pointers} | 第1operandはscalar型、第2・第3operandは算術共通型、同じaggregate型、`void`同士、または規定されたpointer合成規則を満たす必要がある。struct条件、pointerと非zero整数、異なるstruct型、`void`とscalar、配列elementの深いconst差を持つpointer-to-arrayをClang strictと照合し、E3100/E3101のNative/Wasm object診断parityを固定する。既存のobject/function/nested callback/atomic pointer不一致fixtureも同じ共通型境界として維持する |
| **enum/caseの整数定数式operand制約** `(1, 2)`, `1.0`, `(int)(1.0 + 2.0)` | 🧪 | should_reject: enum_{comma,float}_constant, enum_floating_expression_cast, case_{comma,float}_constant | enumeratorとcase labelは整数定数式を要求する。comma演算子、直接の浮動小数定数、複合浮動小数式を整数へcastした値をClang strictと同様に拒否し、E3064のNative/Wasm診断ID parityを固定する |
| **comma演算子operandの通常変換と単一評価** `(effect(), array)`, `(effect(), function)(arg)`, `(effect(), atomic_value)` | 🧪 | comma_operand_conversion_boundaries, atomic_comma_operand_conversion_boundaries | comma演算子は左operandを1回評価し、右operandには通常のarray/function designator変換とlvalue変換を適用する。固定長・多次元・VLA・文字列のpointer decayとsubscript、function designatorの呼出し、const/volatile scalar読出し、aggregate値コピーに加え、atomic integer・wide integer・pointer・aggregateのloadと対応する非atomic値型への変換をClang strictおよびnative/WAT/objectで固定する。右operandのatomic object選択も1回だけ評価し、`_Generic`制御式内ではcomma式全体が未評価のため左operandも実行しない |
| **条件演算子のatomic operand値変換とbranch単一評価** `choose ? atomic_left : atomic_right` | 🧪 | atomic_conditional_value_conversion_boundaries | 条件演算子の第2・第3operandにあるatomic lvalueを対応する非atomic値へ変換してから共通結果型を決める。int・64-bit unsigned・float/double混在・float/double complex混在・atomic pointer object・atomic aggregateについて結果型と実値を固定し、実行時には選択branchのobject selectorだけを1回評価して未選択branchを評価しないことをClang strictおよびnative/WAT/objectで検証する |
| **ポインタ修飾の暗黙変換制約** `const int *` → `int *`, `int **` → `const int **`, `int **` → `void **` | 🧪 | function_argument_return_constraints, function_pointer_constraint_positive_boundaries, array_pointer_qualification_constraints / compile_fail: assign_discards_const_pointer, assignment_discards_const_pointer, {argument,return}_discards_const_pointer, {add_const,discard_const}_through_double_pointer, object_double_pointer_to_void_double_pointer | mutable pointerからconst pointee pointer、object pointerと`void *`の単段変換は維持する。一方、初期化・代入・実引数・returnでconstを破棄する変換、多段ポインタの内側だけへconstを追加・除去する変換、object二重pointerから`void **`への変換はClang C11 strict同様に拒否する |
| **代入変換の基本category制約** string/pointer・integer/pointer・aggregate/scalar・void値・Atomic pointer複合代入 | 🧪 | should_reject / compile_fail: assign_{string_to_int,int_to_ptr_implicit,struct_to_int,void_func_to_int}, atomic_pointer_compound_assignment, void_value_used | 初期化・単純代入・複合代入で、文字列pointerとinteger、非zero integerとpointer、aggregateとscalar、値を持たない`void`式を暗黙互換にしない。Atomic pointerの複合代入にもpointer算術のoperand制約を適用し、6件をClang strict、Native/Wasm should_reject、Native/Wasm object compile-failのE3099診断ID parityで固定する |
| **plain/Atomic pointeeとarray element qualifierのpointer変換制約** assignment/call/return/equality/relational/subtraction | 🧪 | should_reject: assignment_plain_to_atomic_pointer, call_{plain_to_atomic,atomic_to_plain}_pointer, return_{plain_to_atomic,atomic_to_plain}_pointer, binary_atomic_plain_pointer_{equality,relational,subtraction}, {initializer,call,return}_const_array_pointer_to_plain | plain pointeeとAtomic pointeeは同じstorage幅でも別型であり、初期化だけでなく後続代入、実引数、return、等価・順序比較、pointer減算で暗黙互換にしない。pointer-to-arrayではelementの`const`を深いqualifierとして保持し、初期化・実引数・returnでの破棄を拒否する。12件をClang strictと照合し、E3078/E3099/E3108/E3109/E3120/E3121/E3122のNative/Wasm object診断parityを固定する |
| **enum pointerの代入互換性とtag identity** enum pointer vs compatible integer pointer・distinct enum | 🧪 | should_reject / compile_fail: assign_enum_{incompatible_integer_pointer,incompatible_nested_integer_pointer,negative_incompatible_nested_unsigned_pointer}, assign_distinct_enum_pointer, enum_{incompatible_integer_pointer,incompatible_nested_integer_pointer,negative_incompatible_nested_unsigned_pointer}, distinct_enum_pointer_compatibility | enum値が実装選択の整数表現を持っても、pointer変換ではenum tag identityとpointer階層を保持する。positive enum対`int *`、negative enum対`unsigned int **`、distinct enum pointerの代入をE3099で拒否し、`_Static_assert`によるtag identity不一致をE3012で拒否する。両境界をClang strict・Native・Wasm objectで固定する |
| **関数呼び出しtarget・引数数・固定引数変換制約** non-function call・too few/many・pointer/integer/aggregate・nested qualifier・variadic | 🧪 | should_reject / compile_fail: call_nonfunction_object, too_{few,many}_args, argument_discards_const_pointer, call_argument_{incompatible_pointer,nonzero_integer_to_pointer,pointer_to_integer,incompatible_struct,struct_to_integer,integer_to_struct,adds_nested_const,discards_nested_const,nested_const_pointer}, stdio_file_pointer_argument, variadic_fixed_argument_{incompatible,discards_const} | 呼び出しtargetはfunctionまたはfunction pointer型を要求し、prototypeの固定引数は必要数を満たして通常の代入互換変換に従わなければならない。引数不足・過剰、単段・多段pointerの互換性とqualifier、非zero整数とpointer、pointerとinteger、異なるaggregate型・aggregateとscalar、公開`FILE *`型を独立fixtureでClang strictと照合する。variadic関数でも`...`より前の固定parameterには同じ制約を適用し、E3102/E3103/E3120/E3121のNative/Wasm object診断parityを固定する。既存の単段const破棄fixtureと登録を再利用し、同内容だった古いインライン登録を削除する |
| **return値の代入互換変換制約** pointer/integer・distinct aggregate・qualifier破棄 | 🧪 | should_reject: return_wrong_type_ptr, return_discards_const_pointer, return_{incompatible_pointer,pointer_to_integer,incompatible_struct,integer_to_struct,struct_to_integer}, atomic_rvalue_return_to_plain_type | expression付きreturnは関数の戻り型へ代入互換変換できなければならない。非zero整数とpointer、pointerとinteger、互換でないobject pointer、異なるaggregate型、aggregateとscalarの両方向、およびAtomic rvalueから対応するplain型への暗黙returnを独立fixtureでClang strictと照合し、E3108のNative/Wasm object診断parityを固定する。const pointee破棄は既存のE3109 fixtureと登録を再利用し、同内容だった古いインライン登録を削除する |
| **function pointerとobject/void pointerのcategory制約** `void *p = function`, `function == p`, `c ? function : p` | 🧪 | conditional_operator_constraints, function_argument_return_constraints, function_pointer_constraint_positive_boundaries / compile_fail: function_pointer_to_void_pointer, void_pointer_to_function_pointer, compare_function_and_void_pointers, conditional_function_and_void_pointers, conditional_object_function_pointer | C11では`void *`の汎用変換はobject/incomplete pointerだけに適用され、function pointerとの暗黙代入・比較・条件演算子での合成はできない。compatible function pointer同士とnull pointer constantとの演算は維持し、object pointerと`void *`の代入・引数・return・条件演算子は合法側fixtureで固定し、Clang strictとag_cの拒否をNative/Wasm objectの診断IDまで固定する |
| **function pointer signature互換性** `int (*)(int)` ↔ `double (*)(double)` | 🧪 | function_argument_return_constraints, conditional_operator_constraints, function_pointer_constraint_positive_boundaries / compile_fail: {assign,compare,argument,return}_incompatible_function_pointer, conditional_incompatible_function_pointers | function pointerの代入・等価比較・実引数・return・条件演算子は、戻り値とparameterを含むcompatible function typeを要求する。値parameterの最上位修飾、array parameterからpointer parameter、function parameterからfunction pointerへの調整を含むcompatible prototypeを各文脈で維持し、戻り値・parameter型が異なるsignatureをE3099/E3122/E3120/E3108/E3101で拒否する |
| **function戻り型の最上位修飾と互換性** `int(void)` ↔ `const int(void)`, `int *(void)` ↔ `int *const(void)` | 🧪 | function_return_qualifier_compatibility_boundaries, nested_callback_return_qualifier_boundaries / compile_fail: function_return_qualifier_{redeclaration,pointer}_mismatch, function_pointer_return_qualifier_redeclaration_mismatch, nested_callback_{return_qualifier,pointer_return_qualifier,return_pointee_qualifier}_{redeclaration,pointer}_mismatch | 値parameterの最上位修飾と異なり、functionの戻り型に付く`const`/`volatile`はfunction typeの互換性に残る。scalarのplain/const/volatile/const volatileに加え、pointer戻りのtop-level `const`とpointee `const`を`_Generic`で別function pointer型として選択し、対応するpointerから呼べる合法形を固定する。さらにconsumer関数のparameter内へ再帰したcallback戻り型でもscalarのplain/const/volatile、pointer戻りのtop-level `const`、pointee `const`を区別する一方、consumer自身のpointer parameterに付くtop-level `const`は正しく除去して対応するcallback呼出しを実行する。直接およびnested callbackの各戻り型修飾が異なる再宣言と暗黙function pointer初期化はClang strict同様に拒否する |
| **nested callback戻りpointerの修飾区別** `int *volatile callback(void)` / `_Atomic(int *) callback(void)` / `_Atomic(int) *callback(void)` | 🧪 | nested_callback_return_pointer_qualifier_boundaries / compile_fail: nested_callback_return_{volatile_pointer,restrict_pointer,atomic_pointer,atomic_pointee}_pointer_mismatch | consumer parameter内のcallback戻り型でも、pointer自体の`volatile`・`restrict`・Atomic wrapperと、atomic pointeeへのpointerを別の再帰位置に保持する。plain pointerを含む5種類を`_Generic`で区別し、対応するconsumerとcallbackの二段間接呼出しで値を検証する。同じpointer幅でもplain戻り型のconsumer function pointerへ暗黙初期化できないことをE3099で固定する |
| **nested callback戻りfunction pointerの修飾区別** `int (*const factory(void))(int)` / `_Atomic(int (*)(int)) factory(void)` | 🧪 | nested_callback_return_function_pointer_qualifier_boundaries / compile_fail: nested_callback_return_function_{const_pointer,volatile_pointer,atomic_pointer}_mismatch | consumer parameter内のfactory callbackが返すfunction pointerについても、pointer自体の`const`・`volatile`とAtomic wrapperをplain function pointerから区別する。4種類を`_Generic`で選択し、consumer→factory→targetの三段間接呼出しをNative/WAT/objectで実行する。同じfunction table index表現でもplain factoryを受け取るconsumer function pointerへ暗黙初期化できないことをE3099で固定する。`restrict`はpointer-to-functionへ適用できないため合法群には含めない |
| **nested callback内function-pointer parameter修飾** `int callback(int (*const target)(int), int)` / `_Atomic(int (*)(int)) target` | 🧪 | nested_callback_function_pointer_parameter_qualifier_boundaries / compile_fail: generic_duplicate_nested_callback_function_pointer_parameter_qualifier, nested_callback_atomic_function_pointer_parameter_{redeclaration,pointer}_mismatch | consumerが受け取るcallbackのさらに内側にあるfunction pointer parameterでも、top-level `const`/`volatile`はfunction type互換性から除去し、plain callbackと互換にする。consumer→callback→targetの三段間接呼出しと`_Generic`重複拒否を固定する。一方、`_Atomic(function pointer)` parameterはplain function pointerと別型として保持し、consumer再宣言とfunction pointer初期化を拒否する |
| **nested callbackのqualified aggregate returnとABI** `const struct Pair callback(void)` / `volatile struct Packet callback(void)` | 🧪 | nested_callback_qualified_aggregate_return_boundaries / compile_fail: nested_callback_{const_small,volatile_large}_aggregate_return_pointer_mismatch, nested_callback_const_aggregate_return_redeclaration_mismatch | callback戻りrecordのtop-level `const`/`volatile`をconsumer function typeへ再帰的に保持する。register相当のsmall pairとhidden return領域を使うlarge packetのplain/const/volatile 6種類を`_Generic`で区別し、consumer経由のaggregate copyと全member値をClang strictおよびNative/WAT/objectで固定する。同じaggregate ABIでも修飾が異なるconsumer function pointer初期化と再宣言を拒否する |
| **nested callbackのAtomic aggregate return** `_Atomic(struct Pair) callback(void)` | 🧪 | nested_callback_atomic_aggregate_return_boundaries / compile_fail: nested_callback_atomic_aggregate_return_{pointer,redeclaration}_mismatch | 8-byte recordを返すcallbackのAtomic wrapperをplain aggregate returnと区別し、consumer function type、`_Generic`選択、atomic aggregate初期化・load、consumer経由の間接呼出しをClang strictおよびNative/WAT/objectで固定する。同じaggregate return ABIでもplain callbackを受けるconsumer function pointerへの初期化とconsumer再宣言を拒否する |
| **nested callbackのpadded Atomic aggregate return** `_Atomic(struct Words3) callback(void)` | 🧪 | nested_callback_large_atomic_aggregate_return_boundaries / compile_fail: nested_callback_large_atomic_aggregate_return_{pointer,redeclaration}_mismatch | 12-byte recordが16-byteのAtomic storageへ拡張されるcallback returnについて、plain returnとの型識別、consumer経由の間接呼出し、atomic初期化・store・load、前後canaryをClang strictおよびNative/WAT/objectで固定する。NativeとWasm32で異なるalignmentを含む隠れた戻り領域／padding境界を検査し、plain callback consumerとのfunction pointer初期化と再宣言の不一致を拒否する |
| **nested callbackのAtomic complex return** `_Atomic(float/double complex) callback(void)` | 🧪 | nested_callback_atomic_complex_return_boundaries / compile_fail: nested_callback_atomic_complex_return_{pointer,redeclaration}_mismatch | 8-byteのfloat complexと16-byteのdouble complexについて、plain／Atomic callbackをconsumer function typeへ再帰的に保持し、4種類の`_Generic`識別、consumer経由の間接呼出し、atomic初期化・store・load、実部・虚部と前後canaryをClang strictおよびNative/WAT/objectで固定する。NativeとWasm32でalignmentが異なるdouble complexを含め、plain callback consumerとのfunction pointer初期化と再宣言の不一致を拒否する |
| **nested callbackのAtomic union return** `_Atomic(union T) callback(void)` | 🧪 | nested_callback_atomic_union_return_boundaries / compile_fail: nested_callback_atomic_union_return_{pointer,redeclaration}_mismatch | 4-byteのsmall unionと16-byteのwide unionについて、plain／Atomic callbackをconsumer function typeへ再帰的に保持し、4種類の`_Generic`識別、consumer経由の間接呼出し、atomic初期化・store・load、active memberの値と前後canaryをClang strictおよびNative/WAT/objectで固定する。NativeとWasm32でalignmentが異なるwide unionを含め、plain callback consumerとのfunction pointer初期化と再宣言の不一致を拒否する |
| **nested callbackのAtomic enum return互換性** `_Atomic(enum E) callback(void)` | 🧪 | nested_callback_atomic_enum_return_boundaries / compile_fail: nested_callback_atomic_distinct_enum_return_pointer_mismatch, nested_callback_atomic_enum_signedness_return_redeclaration_mismatch | signed／unsigned compatible enumをAtomic wrapper内のcallback returnへ置き、対応する`_Atomic(int)`／`_Atomic(unsigned int)` callbackとconsumer function typeの再帰位置でも互換にする。plain／Atomic consumerの`_Generic`識別、enum callbackとcompatible integer callbackの両方からの間接呼出し、atomic初期化・loadをClang strictおよびNative/WAT/objectで固定する。同じcompatible integer型を持つdistinct enum同士と、unsigned-compatible enum対signed intの再宣言は拒否する |
| **nested callbackのAtomic sub-integer return** `_Atomic(_Bool/signed char/unsigned short) callback(void)` | 🧪 | nested_callback_atomic_subinteger_return_boundaries / compile_fail: nested_callback_atomic_subinteger_{return_pointer,signedness_return_redeclaration}_mismatch | 1-byteの`_Bool`／signed charと2-byteのunsigned shortについて、plain／Atomic callbackをconsumer function typeへ再帰的に保持し、6種類の`_Generic`識別、consumer経由の間接呼出し、`_Bool`正規化、符号拡張・ゼロ拡張、atomic初期化・store・load、隣接canaryをClang strictおよびNative/WAT/objectで固定する。plain `_Bool` callbackとのfunction pointer初期化と、同じ2-byte ABIでもsignednessが異なるconsumer再宣言を拒否する |
| **nested callbackのAtomic floating return** `_Atomic(float/double/long double) callback(void)` | 🧪 | nested_callback_atomic_floating_return_boundaries / compile_fail: nested_callback_atomic_floating_{return_pointer,rank_return_redeclaration}_mismatch | float、double、long doubleについて、plain／Atomic callbackをconsumer function typeへ再帰的に保持し、6種類の`_Generic`識別、consumer経由の間接呼出し、atomic初期化・store・load、fractional valueと隣接canaryをClang strictおよびNative/WAT/objectで固定する。plain double callbackとのfunction pointer初期化と、同じ8-byte ABIになるtargetでもdouble対long doubleのconsumer再宣言を拒否する |
| **nested callbackのAtomic wide-integer return** `_Atomic(long/long long) callback(void)` | 🧪 | nested_callback_atomic_wide_integer_return_boundaries / compile_fail: nested_callback_atomic_long_{return_pointer,rank_return_redeclaration,signedness_return_pointer}_mismatch | signed／unsignedのlongとlong longについて、plain／Atomic callbackをconsumer function typeへ再帰的に保持し、8種類の`_Generic`識別、consumer経由の間接呼出し、32-bit範囲外を含む値、atomic初期化・store・load、隣接canaryをClang strictおよびNative/WAT/objectで固定する。現在のNative／Wasm32 targetでlongとlong longがともに64-bit storageとなる一方、異なるCのrankを保つ境界も検査し、plain対Atomic、long対long long、signed対unsignedのconsumer function type不一致を拒否する |
| **nested callbackのpadded Atomic aggregate parameter** `callback(_Atomic(struct Words3))` | 🔧 | nested_callback_large_atomic_aggregate_parameter_boundaries / compile_fail: nested_callback_large_atomic_aggregate_parameter_{pointer,redeclaration}_mismatch | 12-byte recordを16-byte Atomic storageのcallback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの`_Generic`識別、consumer経由の間接呼出し、parameter object上のatomic load・store、by-value copy、呼出し元の前後canaryをClang strictおよびNative/WAT/objectで固定する。parameter storage plannerがTypeIdだけで12 byteを確保していたため、16-byte atomic storeが直後のlocal先頭memberを上書きして戻り値42を25へ破損していた。QualTypeのDataLayoutでparameter slotを16 byte・Native 16-byte alignment（Wasm32は8-byte alignment）として計画し、plain callback consumerとのfunction pointer初期化と再宣言の不一致を拒否する |
| **nested callbackのpadded Atomic union parameter** `callback(_Atomic(union Words3))` | 🧪 | nested_callback_large_atomic_union_parameter_boundaries / compile_fail: nested_callback_large_atomic_union_parameter_{pointer,redeclaration}_mismatch | 12-byte unionを16-byte Atomic storageのcallback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの`_Generic`識別、consumer経由の間接呼出し、parameter object上のatomic load・store、by-value copy、呼出し元の前後canaryをClang strictおよびNative/WAT/objectで固定する。QualTypeによるparameter storage計画がrecord kindに依存せずunion identityでも16 byte・Native 16-byte alignment（Wasm32は8-byte alignment）を維持することを確認し、plain callback consumerとのfunction pointer初期化と再宣言の不一致を拒否する |
| **nested callbackのAtomic complex parameter** `callback(_Atomic(float/double complex))` | 🧪 | nested_callback_atomic_complex_parameter_boundaries / compile_fail: nested_callback_atomic_complex_parameter_{pointer,redeclaration}_mismatch | float complexとdouble complexをAtomic callback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの4種類の`_Generic`識別、consumer経由の間接呼出し、parameter objectの実アドレスalignment、atomic load・store、by-value copy、呼出し元の前後canaryをClang strictおよびNative/WAT/objectで固定する。8-byte Atomic float complexと、Nativeでは16-byte alignment・Wasm32では8-byte alignmentとなる16-byte Atomic double complexのstorageを維持し、plain callback consumerとのfunction pointer初期化と再宣言の不一致を拒否する |
| **nested callbackのAtomic enum parameter** `callback(_Atomic(enum E))` | 🧪 | nested_callback_atomic_enum_parameter_boundaries / compile_fail: nested_callback_atomic_distinct_enum_parameter_pointer_mismatch, nested_callback_atomic_enum_signedness_parameter_redeclaration_mismatch | signed／unsigned compatible enumをAtomic callback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの4種類の`_Generic`識別、consumer経由の間接呼出し、parameter object上のatomic load・store、by-value copy、呼出し元の前後canaryをClang strictおよびNative/WAT/objectで固定する。Atomic enum callback parameterと対応する`_Atomic(int)`／`_Atomic(unsigned int)` callback parameterを互換にする一方、同じcompatible integer型を持つdistinct enum同士とunsigned-compatible enum対signed intのconsumer型不一致を拒否する |
| **nested callbackのAtomic sub-integer parameter** `callback(_Atomic(_Bool/char/short))` | 🧪 | nested_callback_atomic_subinteger_parameter_boundaries / compile_fail: nested_callback_atomic_subinteger_{parameter_pointer,signedness_parameter_redeclaration}_mismatch | 1-byteの`_Bool`／signed charと2-byteのunsigned shortをAtomic callback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの6種類の`_Generic`識別、consumer経由の間接呼出し、`_Bool`正規化、signed charの符号拡張、unsigned shortのゼロ拡張、parameter object上のatomic load・store、by-value copy、呼出し元の隣接canaryをClang strictおよびNative/WAT/objectで固定する。plain `_Bool` callbackとのfunction pointer初期化と、同じ2-byte storageでもsignednessが異なるconsumer再宣言を拒否する |
| **nested callbackのAtomic floating parameter** `callback(_Atomic(float/double/long double))` | 🧪 | nested_callback_atomic_floating_parameter_boundaries / compile_fail: nested_callback_atomic_floating_{parameter_pointer,rank_parameter_redeclaration}_mismatch | float、double、long doubleをAtomic callback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの6種類の`_Generic`識別、consumer経由の間接呼出し、fractional value、parameter object上のatomic load・store、by-value copy、呼出し元の前後canaryをClang strictおよびNative/WAT/objectで固定する。plain double callbackとのfunction pointer初期化と、同じ8-byte ABIとなるtargetでもdouble対long doubleのconsumer再宣言を拒否する |
| **nested callbackのAtomic wide-integer parameter** `callback(_Atomic(long/long long))` | 🧪 | nested_callback_atomic_wide_integer_parameter_boundaries / compile_fail: nested_callback_atomic_long_{parameter_pointer,rank_parameter_redeclaration,signedness_parameter_pointer}_mismatch | signed／unsignedのlongとlong longをAtomic callback仮引数としてconsumer function type内へ再帰的に保持し、plain／Atomic consumerの8種類の`_Generic`識別、consumer経由の間接呼出し、32-bit範囲外の値、parameter object上のatomic load・store、by-value copy、呼出し元の前後canaryをClang strictおよびNative/WAT/objectで固定する。現在のNative／Wasm32 targetで全型が8-byte storageとなってもCのrankとsignednessを保持し、plain対Atomic、long対long long、signed対unsignedのconsumer型不一致を拒否する |
| **nested callbackのAtomic pointer parameter** `callback(_Atomic(int *))` | 🧪 | nested_callback_atomic_pointer_parameter_boundaries / compile_fail: nested_callback_atomic_pointer_{parameter_pointer,pointee_parameter_redeclaration}_mismatch | plain data pointer、Atomic pointer、pointer-to-Atomic-pointeeをcallback仮引数としてconsumer function type内へ再帰的に保持し、3種類の`_Generic`識別、consumer経由の間接呼出し、Atomic pointer parameter object上のload・store、by-value copy、呼出し元の前後canary、およびAtomic pointeeへのload・storeをClang strictおよびNative/WAT/objectで固定する。同じtarget pointer幅でもplain pointer対Atomic pointerと、Atomic pointer対pointer-to-Atomic-pointeeのconsumer型不一致を拒否する |
| **Atomic aggregate callbackのfunction type互換性** assignment/member/conditional/pointer-to-pointer/variadic/return | 🧪 | should_reject: atomic_aggregate_callback_{assignment,member_initializer,conditional,pointer_to_pointer,return}, atomic_aggregate_variadic_callback_assignment, call_argument_atomic_function_pointer | callback parameterのAtomic aggregate wrapperは同じaggregate storage形でもplain parameterと互換にせず、初期化・member初期化・条件演算子・多段pointer・return・実引数で保持する。variadicnessもfunction typeの一部として固定し、E3099/E3101/E3108/E3120のNative/Wasm object診断parityをClang strictと照合する |
| **nested callback parameterの調整と再帰互換性** `int consume(int callback(int values[static 2]))` | 🧪 | nested_callback_parameter_adjustment_boundaries / compile_fail: generic_duplicate_nested_callback_{array,function}_adjustment, nested_callback_array_element_qualifier_{redeclaration,pointer}_mismatch | consumer関数のparameter内にあるcallback function typeでも、array parameterはpointerへ、function parameterはfunction pointerへ再帰的に調整する。調整前後のtypedefを使ったconsumer再宣言・function pointer保存・条件演算子・間接呼出しをClang strictおよびNative/WAT/objectで固定し、`_Generic`へ両方を列挙した互換重複はE3105で拒否する。一方、array elementの`const`は調整後pointerのpointee修飾として保持し、違いがあるconsumer再宣言とfunction pointer初期化を拒否する |
| **nested callback parameterのrestrict/Atomic調整** `int callback(int values[static const restrict 1])` / `int values[_Atomic 1]` | 🧪 | nested_callback_parameter_qualifier_adjustment_boundaries / compile_fail: generic_duplicate_nested_callback_{restrict,atomic}_array_adjustment, nested_callback_atomic_array_element_{redeclaration,pointer}_mismatch | callback function typeがconsumer parameter内へ再帰した場合も、array declarator内の`const restrict`およびpointer側`_Atomic`はadjusted pointerの最上位修飾としてfunction type互換性から除去し、plain `int *` parameterと互換にする。再宣言・保存済みfunction pointer・条件演算子・間接呼出しと`_Generic`重複拒否を固定する。一方、`_Atomic(int) values[1]`はpointer-to-atomic-elementへ調整されるためplain elementとは別型として選択し、consumer再宣言とfunction pointer初期化を拒否する |
| **array parameter調整後pointer objectのmodifiable lvalue制約** `int values[static const restrict 2]` | 🧪 | should_reject 5件 / compile_fail: {const,const_atomic,static_const_restrict,const_volatile}_array_parameter_reassignment, old_style_const_array_parameter_reassignment | array parameter declaratorの`[]`内にある`const`/`volatile`/`restrict`/`_Atomic`は、関数本体で使うadjusted pointer parameter objectへ適用される。関数型互換性では最外層qualifierを除去する一方、definition内のobject型には保持し、`const`付きparameterへの再代入をE3077で拒否する。bound省略、`static`最小bound、複合qualifier、pointer側Atomic、旧形式definitionの5件をClang strictと照合し、should_reject正本からNative/Wasm object compile-failへ登録する |
| **nested callback function pointerの式制約** `int (*consumer)(int (*callback)(void))` | 🧪 | nested_callback_pointer_constraint_boundaries / compile_fail: assign_incompatible_nested_callback_pointer, compare_incompatible_nested_callback_pointers, conditional_incompatible_nested_callback_pointers, {argument,return}_incompatible_nested_callback_pointer | array→pointer調整後に互換となるcallbackを持つconsumer function pointerについて、初期化・代入・等価比較・null比較・条件演算子・実引数・return・間接呼出しをClang strictおよびNative/WAT/objectで維持する。callback戻り型のtop-level修飾が異なるconsumerは再帰的に非互換とし、代入E3099、比較E3122、条件演算子E3101、実引数E3120、return E3108の各制約経路で拒否する |
| **nested callbackのprototype有無とvariadicness** `int consume(int callback())` ↔ `int consume(int callback(int))` | 🔧 | nested_callback_prototype_variadic_boundaries / compile_fail: generic_duplicate_nested_callback_unprototyped_int, nested_callback_{unprototyped_narrow,variadic}_{redeclaration,pointer}_mismatch | consumer関数のparameter内でも、parameter未指定callbackとprototype付きcallbackはdefault argument promotion後の各parameterが変化しない場合だけ互換とする。`int` prototypeとの再宣言・保存済みfunction pointer・条件演算子・間接呼出しを維持し、両型の`_Generic`重複をE3105で拒否する。Wasm object emitterはparameter未指定からprototypeへのsignature refinementを最外層だけで判定し、合法なnested callback宣言・定義をE4008で拒否していた。canonical signature内の対応するparameter listを再帰的に見つけ、default argument promotionで不変な場合だけ具体化する。`signed char`はpromotionで`int`へ変化するため非互換であり、固定引数callbackとellipsisを持つvariadic callbackも別function typeとして区別し、再宣言とfunction pointer初期化を拒否する |
| **nested callback戻りfunction pointerのprototype互換性** `int (*factory(void))()` ↔ `int (*factory(void))(int)` | 🔧 | nested_callback_return_function_prototype_boundaries / compile_fail: generic_duplicate_nested_callback_return_function_unprototyped_int, nested_callback_return_function_narrow_{redeclaration,pointer}_mismatch | consumer parameterのcallbackがさらにfunction pointerを返す三層型でも、parameter未指定のtarget functionとdefault argument promotionで不変な`int` prototypeを再帰的にcompatibleとする。consumer再宣言、保存済みfunction pointer、条件演算子、factory→targetの二段間接呼出しをNative/WAT/objectで固定する。Wasm object canonical signature refinementもfactory resultの内側まで再帰し、合法なempty parameter listから`int` prototypeへの具体化をE4008で誤拒否しない。`_Generic`の互換重複はE3105で拒否し、target parameterが`signed char`ならpromotionで型が変わるためconsumer再宣言とfunction pointer初期化を拒否する |
| **const pointer変換の修飾階層と文脈** `const int *` → `int *`, `int **` → `const int **`, `int **` → `void **` | 🧪 | compile_fail: add_const_through_double_pointer, discard_const_through_double_pointer, object_double_pointer_to_void_double_pointer, assign_discards_const_pointer, assignment_discards_const_pointer, argument_discards_const_pointer, return_discards_const_pointer | 単段pointerのpointee `const`破棄を初期化・代入・引数・return変換で拒否し、多段pointerの内側へ`const`を追加／破棄する変換も拒否する。object pointerと`void *`の互換性は一段だけであり、`int **`から`void **`へ再帰適用しない。7件をNative/Wasm object compile-failへ登録し、E3078/E3099/E3109/E3121の診断ID parityまで固定する |
| **volatile破棄とatomic pointeeのpointer変換制約** `volatile int *` → `int *`, `_Atomic int *` ↔ `int *` | 🧪 | atomic_pointer_conversion_constraints / compile_fail: assign_discards_volatile_pointer, assign_{atomic_to_plain,plain_to_atomic}_pointer, conditional_atomic_plain_pointers | pointeeの`volatile`を暗黙に破棄する代入を拒否する。atomic-qualified pointeeとplain pointeeは暗黙互換にせず、両方向の代入と条件演算子での型合成を拒否する。`_Atomic int *`同士、明示cast、単段`void *`roundtripは維持する。4件をNative/Wasm object compile-failへ登録し、E3078/E3099/E3101の診断ID parityまで固定する |
| **`_Static_assert` の構文・条件・宣言位置制約** `_Static_assert(1.0, "...")`, `for (_Static_assert(1, "..."); ; )` | 🔧 | static_assert_constraints, static_assert_in_struct, static_assert_for_initializer_boundaries / compile_fail: static_assert_{missing_message,identifier_message,non_string_message,floating_condition,comma_condition,variable_condition,floating_expression_cast,signed_overflow,sizeof_vla,false_condition,for_initializer_false,for_initializer_nonconstant}, logical_{unselected_call,selected_comma}_static_assert | C11のconditionには整数定数式、messageには文字列literalを要求する。top-level・block・file/local struct/union member位置に加え、N1570 6.7の`declaration`を取る`for`初期節のstatic assertionを維持する。direct resolverが`for`初期節をlocal object declarationだけと仮定してND_STATIC_ASSERTを式解決へ渡していた問題は宣言scope内のstatement preflightで解消する。さらにlocal recordのdeclaration specifier検査が`PSX_PARSED_AGGREGATE_STATIC_ASSERT`を未対応構文として無診断拒否しE0006にしていたため、aggregate bodyのdirect value resolutionで共通のstatic-assert定数評価・E3011/E3012診断を適用する。local enum/typedef、struct/union、nested local recordをClang strict・Native・Wasm objectで固定する。浮動・comma・object値・VLA `sizeof`・signed overflow・関数callを含む非ICE、文字列以外のmessage、偽conditionを分離し、14件のshould_reject正本をNative/Wasm object compile-failでE2006/E3010/E3011/E3012まで固定する。Clang strictとは`for`初期節の受理可否が異なるため、この境界ではN1570文法を正本とする |
| **`for`初期宣言のobject/storage class制約** `for (static int value; ... )` | 🔧 | for_initializer_storage_boundaries / compile_fail: for_initializer_{typedef_declaration,static_object,extern_object,function_declaration,standalone_tag} | C11 6.8.5p3に従い、`for`初期節のdeclarationが宣言できる識別子を`auto`または`register`記憶域のobjectに限定する。通常block宣言と同じcontextで解決していたため、typedef名、static/extern object、function、standalone tagを受理していた。専用の`PSX_DECLARATION_CONTEXT_FOR_INITIALIZER`を導入し、decl-specifierと完成したdeclarator型の両方を検証する。明示`auto`/`register` objectと省略時のautomatic objectをNative・WAT・Wasm objectで維持し、5つの不正宣言をClang strict同様にE3064で拒否する。識別子を宣言しない`_Static_assert`の既存対応は維持する |
| **compound literalの型制約** `(void){0}`, `(int(void)){0}`, `(struct incomplete){0}`, `(int[n]){1}` | 🧪 | compound_literal_scalar_lvalue, nested_compound_literal, compound_literal_array_size_and_decay / compile_fail: compound_literal_{void_type,function_type,incomplete_record,vla} | compound literalのtype nameは完全なobject型またはサイズ未指定配列に限定し、`void`・関数・未完成record・VLA型を拒否する。scalar・record・固定長/推論配列とfile/block scopeの合法形は維持する。4件はshould_reject正本をcompile-fail registryから参照し、E3064/E3114のNative/Wasm診断ID parityまで固定する |
| **`_Alignas`の値・適用対象・再宣言制約** `_Alignas(3)`, typedef/function/parameter/bit-field/register, 宣言間alignment不一致 | 🧪 | alignas_redeclaration_consistency, dynamic_alignas_specifiers / should_reject: alignas_comma_constant / compile_fail: alignas_{weaker_than_natural,non_power_of_two,signed_overflow,typedef,function_declaration,function_definition,parameter,bitfield,register_object,redeclaration_missing_definition,redeclaration_conflict,zero_missing_definition,zero_conflicts_strict,aligned_then_plain_tentative,definition_then_aligned_extern} | alignment値は有効な整数定数式かつ対象型の自然alignmentを弱めず、specifierは完全objectの適切な宣言だけへ適用する。同一objectの全宣言・定義では要求alignmentを一貫して反復し、0指定も別の明示指定と混在させない。comma式の拒否を含む16件をshould_reject正本とNative/Wasm object compile-failでE3064まで固定する。合法なover-alignment、同一宣言上の複数specifier、同値の型名指定は維持する |
| **block-scope compound literalのobject identity** `again: p=(int[2]){...}; goto again` | 🧪 | compound_literal_block_identity | 同一block実行中に同じsource occurrenceへ再到達したcompound literalは、同じautomatic objectを再初期化する。一方、別々のsource occurrenceは寿命が重なる別objectになる。address identity、再初期化された配列値、別literal間の非同一性をClang strictとの比較およびnative/WAT/objectで固定する |
| **compound literalの再帰frame identity** 同一source occurrenceを含むblockの同時再帰実行 | 🧪 | compound_literal_recursive_frame_identity, union_compound_literal_recursive_identity, compound_literal_scalar_array_recursive_identity, integer_compound_literal_recursive_identity, floating_compound_literal_recursive_identity, pointer_compound_literal_recursive_identity, typedef_compound_literal_recursive_identity, restrict_compound_literal_recursive_identity, complex_compound_literal_recursive_identity, qualified_compound_literal_recursive_identity | block-scope compound literalは同一frame内の後方`goto`では同じautomatic objectを再初期化する一方、再帰で同じblockが同時に実行される各frameには独立objectが必要になる。struct・active memberを切り替えるunion・scalar・array、`_Bool`/sub-int/64-bit整数/enum、実数浮動3幅、object/function/pointer-to-array、scalar/record/多次元arrayのtypedef chain、direct/typedef/pointer-to-arrayのrestrict pointer、complex 3幅、const/volatile/const volatile scalarの別lowering経路を16〜24段の再帰で重ね、全ancestorとのaddress非同一性と内容保持、各frame内の再初期化後も同一addressであること、initializer副作用回数をClang strictおよびnative/WAT/objectで固定する |
| **file-scope aggregate subobjectのcompound literal初期化** `struct S a[2]={(struct S){...}, ...}` | 🔧 | file_scope_aggregate_compound_literal_subobjects | file-scope compound literal用の隠しglobalは正しく初期化されていたが、外側static aggregate initializerがそのwhole-aggregate値をscalar定数として評価しE3064になっていた。resolved HIRの同型aggregate targetとoffsetをroot範囲内で検証し、隠しglobalのinitializer slotsを対象subobjectへ複製する。ordinary/atomic struct配列、nested member、string/pointer relocation、floating slot、union active memberとnested union activationをClang strictおよびnative/WAT/objectで固定する |
| **qualified file-scope aggregate compound literalの値変換** `T a[]={(const T){...},(volatile T){...}}` | 🔧 | file_scope_qualified_bitfield_compound_literal_subobjects, file_scope_qualified_union_compound_literal_subobjects | top-level const/volatileを持つcompound literalはlvalue conversionでunqualified値になるためHIRが`CAST(GLOBAL)`となるが、static aggregate copierが直下の`GLOBAL`だけを認識してE3116としていた。同一TypeIdでunqualified結果へ変換するwrapperを透過する。structではoverlapping bit-field allocation unit、const aggregate member、anonymous union経由のobject/function pointerと文字列relocationを、unionではdirect active member、const/volatile/atomic destination、`_Generic`選択、object/function/string relocationをClang strictおよびnative/WAT/objectで固定する |
| **qualified aggregateの実行時value context** `dst = choose ? volatile_record : const_record` | 🧪 | qualified_aggregate_value_context_boundaries | 非atomicのtop-level `const`・`volatile`・`const volatile`を持つstruct/union lvalueは、初期化・代入式とその結果値・関数引数・return・条件式・comma式で対応するunqualified aggregate値へ変換される。register相当のsmall objectと間接受渡しになるlarge object、qualified仮引数、条件式の選択branchだけの単一評価をClang strictおよびnative/WAT/objectで固定する |
| **register aggregateの実行時value context** `register struct S value; consume(value)` | 🧪 | register_aggregate_value_context_boundaries | address-ofを明示的に適用できないregister storageのstruct/unionも、member accessだけでなくaggregate値としての初期化・代入式とその結果値・関数引数・register仮引数・return・条件式・comma式には利用できる。3-byte partial-register objectとlarge indirect object、top-level cv修飾を組み合わせ、内部copy loweringが不要なaddress制約を誤発火しないことをClang strictおよびnative/WAT/objectで固定する |
| **anonymous memberを含むaggregateの値渡し** `consume((struct Outer){.promoted=...})` | 🧪 | anonymous_member_aggregate_value_boundaries | anonymous struct/unionから昇格されたfieldを持つ外側containerも、通常のaggregateと同じlayoutで初期化・代入式と結果値・引数・return・条件式・comma式を通る。partial-registerとlarge indirectのstruct/union、direct/indirect call、object/function/string pointerを含むlarge containerをClang strictおよびnative/WAT/objectで固定する |
| **bit-field aggregateの値渡しABI** `va_arg(ap, struct Bits)` | 🧪 | bitfield_aggregate_value_abi_boundaries | signed/unsigned bit-field、通常memberと共有するallocation unit、複数unitを跨ぐfield、zero-width区切りを含むsmall/large struct・unionをdirect/indirect call、return、variadic `va_arg`、代入結果、条件式、comma式へ流す。signed抽出、large return前後canary、全field値をClang strictおよびnative/WAT/objectで固定する |
| **over-aligned aggregateの値渡しABI** `struct S{_Alignas(64) long x;}` | 🧪 | overaligned_aggregate_value_abi_boundaries, overaligned_callback_value_abi_xtu, overaligned_variadic_value_abi_xtu, overaligned_named_parameter_variadic_abi_xtu, overaligned_va_list_forwarding_xtu, overaligned_variadic_return_abi_xtu | memberのalignment指定により型自体が16/32/64-byte alignmentを持つstruct/unionについて、global/local配置だけでなくdirect/indirect callのparameter object、return temporary、variadic `va_arg` local、代入・条件式・comma式の各destinationを実アドレスで検査する。さらに64-byte整列structを値で受け取り返すcallback型を別TU間で共有し、direct call、別TU dispatcherから呼ぶmain TU callback、別TUから返されたfunction pointer、dispatcher自体の間接callを通して、各callee parameter・return destinationのalignment、値、前後canaryを固定する。cross-TU variadic関数にはinteger・float complex・32-byte aggregate・double・double complex・64-byte aggregate・pointer・32-byte aggregateの混在列を渡し、開始位置とcomplexを含むprefix消費後の途中位置で`va_copy`したlistをoriginalから独立に最後まで走査して、`va_arg`取得先の実アドレスとargument-areaのslot進行をdirect callとvariadic function pointer経由のindirect callで検査する。開始位置と部分走査後に`va_copy`したlistを別TUのhelperへby-valueで転送する経路でも同じ混在列または残りのtailを読み、元のlistを独立に再走査する。さらに32/64-byte aggregateを名前付き引数に置き、最後の名前付き64-byte aggregateを`va_start`の基準にして、その直後のinteger・double・32-byte aggregate・pointer・64-byte aggregate・long longをdirect/indirect callで読み取る。32-byte aggregateを`va_arg`で取得しながら64-byte aggregateをhidden return areaへ返す関数もdirect/indirectで呼び、結果destinationのalignment・値・canaryを固定する。Clang strict、Nativeの真の2-object link、WAT結合、Wasm 2-object linkで照合する |
| **over-aligned compound literalのstorage・一時領域・再帰frame identity** `&(struct S){...}`, `(struct S[2]){...}` | 🧪 | overaligned_compound_literal_{storage_boundaries,recursive_identity} | 32/64-byte alignmentを型から継承するstruct・union・array・nested recordのcompound literalについて、file-scope hidden globalとblock-scope automatic objectの実アドレス、array element stride、nested member alignment、値と前後canaryを固定する。同一blockで同じsource occurrenceへ`goto`再到達したときは同一addressで再初期化し、同じoccurrenceを同時実行する16段の再帰frameでは各frameが整列済みの別objectを持ち、子frameの実行後も全ancestorの値を保持することを検査する。direct/indirect callへcompound literal値を渡す一時領域もClang strict・Native・WAT・Wasm objectで照合する |
| **over-aligned Atomic storageとaggregate member layout** `_Alignas(32) _Atomic(unsigned long long) value` | 🧪 | overaligned_atomic_storage_boundaries | 64-byte整列Atomic scalarと32-byte整列Atomic memberをglobal/automatic/static localへ配置し、実アドレス整列と初期値、8-byte atomic storage幅、前後member offset、64/128-byte record size、aligned record配列strideとnested record offsetを固定する。`atomic_load/store/exchange/fetch_add`とexplicit memory orderをdirect/member/nested objectへ適用し、前後canaryを保持することをClang strict・Native・WAT・Wasm objectで照合する |
| **complex memberを含むaggregateの値渡しABI** `va_arg(ap, struct ComplexPair)` | 🧪 | complex_member_aggregate_value_abi_boundaries | 2個のfloat/double complexからなるhomogeneous struct、float/double/long double complexと整数を持つmixed large struct、complex-active unionをdirect/indirect call、return、variadic `va_arg`、代入結果、条件式、comma式へ流す。全real/imaginary component、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **多次元array memberを含むaggregateの値渡しABI** `va_arg(ap, struct Grid)` | 🧪 | nested_array_member_aggregate_value_abi_boundaries | 2次元のbyte arrayを持つsmall struct、nested float arrayで構成するHFA相当struct、整数array・function pointer array・文字列pointer arrayを含むlarge indirect struct、array-active unionをdirect/indirect call、return、variadic `va_arg`、代入結果、条件式、comma式へ流す。全subobject、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **sub-integer・enum memberを含むaggregateの値渡しABI** `va_arg(ap, struct IntegerMembers)` | 🧪 | integer_member_aggregate_value_abi_boundaries | `_Bool`、signed/unsigned char・short、非負enumと負値を含むenumを同じsmall/large structおよびunion active struct memberへ配置し、direct/indirect call、return、variadic `va_arg`、代入結果、条件式、comma式へ流す。`_Bool`の0/1正規化、sub-integerの符号拡張・zero拡張、enum値、全array element、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **const subobjectを含むaggregateの合法な値渡しABI** `consume(choose ? left : right)` | 🧪 | const_subobject_aggregate_value_abi_boundaries | direct const memberだけでなく、nested record、const配列、typedef const member、const pointer object、const文字配列、const memberを持つactive union memberを含むaggregateはmodifiable lvalueではないためwhole-object代入できない一方、初期化・引数・return・条件式・comma式・variadic `va_arg`では値として利用できる。small/large structとunion、direct/indirect call、全subobject、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **nested real-floating aggregateの値渡しABI** `struct HFA{struct Pair top,bottom;}` | 🧪 | nested_floating_aggregate_value_abi_boundaries | named nested recordからなる4-floatおよび3-double HFA相当struct、float・double・long doubleとdouble配列・整数を含むmixed large struct、double-pair active unionをdirect/indirect call、return、variadic `va_arg`、代入結果、条件式、comma式へ流す。recursive flattening後の全real component、3幅の保持、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **pointer memberを含むaggregateのtarget別値渡しABI** `struct Pair{int *object; int (*callback)(int);}` | 🧪 | pointer_member_aggregate_value_abi_boundaries | Nativeの8-byte pointerとWasm32の4-byte pointerによりsize・register/indirect分類が変わるsmall pair、object/function pointer配列、多段object/function pointer、pointer-to-arrayとその配列、const文字列・void pointerを持つlarge struct、pointer-pair active unionをdirect/indirect call、return、variadic `va_arg`、代入結果、条件式、comma式へ流す。全pointer identityとdereference/call/subscript結果、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **atomic memberを含む通常aggregateの値渡しABI** `struct Outer{_Atomic(int) count; _Atomic(struct B) packet;}` | 🧪 | atomic_member_aggregate_value_abi_boundaries | top-level atomic aggregateではなく、atomic integer・unsigned short・double、`_Atomic(struct bytes3)`とその配列をsubobjectに持つ通常small/large structおよびactive union memberをdirect/indirect call、return、variadic `va_arg`、whole-object代入結果、条件式、comma式へ流す。atomic memberごとのload/store、promoted storageの全byte、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **volatile memberを含む通常aggregateの値渡しABI** `struct Outer{volatile int count; volatile int samples[3];}` | 🧪 | volatile_member_aggregate_value_abi_boundaries | top-level volatile aggregateではなく、volatile integer・sub-integer・double、volatile scalar array、nested record、volatile pointer objectをsubobjectに持つ通常small/large structおよびactive union memberをdirect/indirect call、return、variadic `va_arg`、whole-object代入結果、条件式、comma式へ流す。全volatile memberのload/store、pointer identity、選択branchの単一評価、large return前後canaryをClang strictおよびnative/WAT/objectで固定する |
| **aggregate値渡しABIのcross-TU一致** `extern struct Packet transform(struct Packet)` | 🧪 | aggregate_value_abi_xtu_boundaries | nested 4-float HFA相当struct、Native/Wasm32で分類が変わるobject/function pointer pair、多次元整数array・double・object/function pointer array・負のlongを含むlarge struct、active unionを共有headerで宣言し、caller TUとdefinition TUを独立compileする。direct/indirect parameter・return、calleeからのcallback、pointer identityと副作用、4種類を連続取得するvariadic `va_arg`をClang strict・Native 2-object link・Wasm object linkで固定し、単一module WAT統合経路も検証する |
| **別TUのnamed record C型互換性** `struct packet { int value; }` | 🔧 | named_record_signature_xtu_{main,other} / record_member_signature_mismatch_{main,other} / tentative_incomplete_record_address | Wasm objectのcanonical署名がnamed struct/unionをタグ名だけで表していたため、同じタグ名でもmember型が`int`対`unsigned int`の非互換定義を、同じWasm `i32` ABIとして誤ってlinkしていた。タグatomへ完成状態・member名・bit-field属性・再帰canonical member型を追加し、両側がcompleteなら構造を再帰比較する。一方、片側がincompleteのrecord pointerは同じタグのcomplete定義と互換のまま維持する。同一object内では先に記録されたincomplete function-reference署名を、タグ定義後のcomplete構造署名へ一方向に精緻化する。完全recordの値parameterとincomplete→complete pointerをNative/WAT/object link/runで固定し、member型不一致はobject linkerで拒否する |
| **別TUのunion member順序互換性** `union U{int i; unsigned u;}` | 🔧 | union_member_order_signature_xtu_{main,other} / struct_member_order_signature_mismatch_{main,other} / union_member_type_signature_mismatch_{main,other} | C11の別翻訳単位にあるcompatible unionは、対応memberの宣言順が異なってもよいが、旧Wasm object linkerはrecord bodyを列順で比較して誤拒否していた。complete unionだけはmember名・bit-field幅/signedness・canonical型による一対一対応を順序非依存で照合し、pointer qualifierとbit-fieldを含む並べ替えをClang strict・Native・WAT・objectで固定する。structは対応memberの宣言順も一致が必要なため同じ名前・型の逆順定義を拒否し、unionも並べ替え後の`const int *`対`volatile int *`のmember型差は同じWasm ABIでも拒否する |
| **別TUのrecord member alignment互換性** `_Alignas(4) int value` | 🔧 | record_member_alignment_signature_xtu_{main,other} / record_member_alignment_presence_mismatch_{main,other}, variadic_parameter_member_alignment_presence_mismatch_{main,other}, callback_parameter_member_alignment_presence_mismatch_{main,other}, global_callback_member_alignment_presence_mismatch_{main,other}, global_callback_factory_member_alignment_presence_mismatch_{main,other} / record_member_alignment_value_mismatch_{main,other}, callback_return_member_alignment_value_mismatch_{main,other}, global_callback_return_member_alignment_value_mismatch_{main,other} | 対応memberの型と実レイアウトが同じでも、alignment specifierの指定有無または等価な指定値が異なるrecord定義はC11上compatibleではない。旧canonical署名はmemberの`_Alignas`を捨てて誤受理していたため、RecordDeclに指定有無と解決値を保持してobject署名へ含める。同一指定の別TUはNative・WAT・objectで受理する。`_Alignas(4)`対指定なし、および同じ自然alignmentになる`_Alignas(0)`対`_Alignas(4)`を、direct関数の通常値引数とellipsis直前の固定値引数、consumer function type内のcallback parameter/return、外部global dataが保持するcallback parameter/return、さらにglobal factoryのreturn callback parameterまで再帰的に比較し、function/data signature mismatchとしてobject linkerで拒否する |
| **別TUのaggregate ABI layout一致** `#pragma pack(1) struct P{char tag; int values[4];}` | 🔧 | packed_{indirect,pointer,callback}_record_signature_xtu_{main,other} / incomplete_callback_record_signature_xtu_{main,other} / {indirect,nested,pointer,nested_pointer,callback_pointer,callback_return}_record_layout_signature_mismatch_{main,other} / incomplete_{pointer,callback}_sibling_layout_mismatch_{main,other} | 大きなaggregateはWasm ABIで間接pointerへlowerされ、aggregate pointerとcallback pointerは呼出し上は同じWasm pointerになるため、同じcanonical C型と同じ低水準Wasm signatureでも、TU間のpack差を旧linkerは誤受理していた。C型署名と分離したversion 3 target ABI layout fingerprintを`agc.abi_layout` object sectionへ記録し、size・alignment・member offset・bit offset・nested member layoutに加えてpointerから到達可能なcomplete aggregate layoutとcallbackのresult/parameter layoutを再帰照合する。自己参照recordはcycle markerで有限化し、incomplete record pointerは型単位wildcardとして他の既知parameter layoutを比較したまま従来のcompatible complete定義を維持する。同一packの値渡し・再帰pointer・callback、およびcallback内のincomplete→complete record正例をNative・WAT・objectで固定し、外側size/alignmentが同じnested union、pointer memberの先、callbackの引数先と戻り値、および外側関数・callback内のincomplete pointerと同居する別parameterのpack不一致もobject linkerで拒否する |
| **別TUの外部data symbol型・layout・storage一致** `extern struct P shared;` | 🔧 | {packed_global_record,packed_global_callback,incomplete_global_array,incomplete_global_record_{pointer,object},anonymous_global_{record,union},anonymous_flexible_{global,function,callback},nested_anonymous_global_union}_signature_xtu_{main,other}, aligned_global_{definition,data_reloc}_xtu_{main,other}, {function_{return_pointee_qualifier,parameter_{qualifier_adjustment,multidimensional_qualifier,function_adjustment}},global_{callback_parameter_qualifier,enum_integer_compatible}}_xtu_{main,other} / global_{record,pointer_record,callback_record}_layout_signature_mismatch_{main,other} / anonymous_global_{record,union}_member_{type,name}_mismatch_{main,other} / anonymous_{global_union_bitfield_width,flexible_{global,function,callback}_member_type}_mismatch_{main,other} / nested_anonymous_global_union_member_type_mismatch_{main,other} / function_{return_{const,pointer_const,pointee_const},parameter_{pointee_const,array_element_const,callback_signedness}}_qualifier_mismatch_{main,other} / global_{scalar_type,array_bound,void_pointer_type,pointer_array_shape,function_object_pointer_kind,distinct_enum_type,anonymous_tagged_union}_mismatch_{main,other} / global_{thread_local_storage,alignment_requirement,alignment_value,alignment_data_reloc,callback_pointee_const_qualifier,{const,volatile,atomic,pointee_const,restrict_pointer}_qualifier}_mismatch_{main,other} | Wasm objectのdata relocationはsymbol名だけでreferenceとdefinitionを結び、同じstorage幅でも`int`対`unsigned int`やdistinct enum同士の非互換C型、`void *`対object pointer、pointer-to-array対element pointer、function pointer対object pointerのshape・address-space差、top-level `const`・`volatile`・`_Atomic`修飾差、pointer階層内の`const`やpointer objectの`restrict`修飾差、callback仮引数のpointee修飾差、packed宣言対plain定義のmember offset差、`_Thread_local`宣言対通常storage定義、強い`_Alignas`参照対指定なし/異なるalignment定義を誤受理していた。IR data objectからcanonical C型・target layout・thread-local属性・明示alignment要求を保持し、version 2 `agc.data_signature` sectionへ外部data symbolごとに記録する。linkerはコード本体と静的初期化子の両方のmemory relocation適用前に、既存の再帰C型・layout互換規則とobject属性で照合する。同一packのrecordとcallback pointer、未完成配列宣言→完成配列定義、未完成recordのpointer/object宣言→完成定義、flexible array memberを含む無名record、enumとそのsigned/unsigned compatible integer型、両TUが`_Thread_local`の宣言・定義、alignment指定なし参照→強alignment定義、および直接関数とcallback仮引数で無視されるtop-level `const`・`restrict`、`static`最小bound付きarray、prototype-scope VLA `[*]`のpointer adjustment、function parameterのfunction-to-pointer調整をNative・WAT・objectで受理する。多次元array parameterは外側arrayをpointerへ調整しつつ内側boundとelement `const`を保持する。対応memberが一致する無名record/unionと、無名struct内で対応する無名unionは同一TUへ統合できないため、Nativeの真の2-object linkとWasm object linkで受理し、unionの対応member順序は再帰的に問わない。flexible array memberを含む無名recordも固定prefixを直接の関数引数・戻り値、およびcallback pointer内の引数・戻り値として別TU間で渡せることを確認する。function returnのpointer先にある`const volatile`も別TU間で保持する。scalar・distinct enum型差、無名record/unionおよび入れ子無名unionの対応member型差、flexible array memberのelement型差、無名record/unionの対応member名差、無名unionのbit-field幅差、無名union対同形のtag付きunion、pointer shape・kind差、top-levelおよびpointer階層内の修飾差、直接関数とcallback仮引数のpointee修飾差、array parameter調整後のelement修飾差、function-to-pointer調整後のcallback signature差、関数戻り値と戻りpointer自体のtop-level修飾差、関数戻りpointerのpointee修飾差、異なる既知配列bound、record本体のpack差、global pointer先とglobal callback parameter先のpack差、thread-local/通常storage差、強alignment参照→指定なし/異なる定義を拒否する。flexible array memberのelement型差はdata symbol、直接の関数引数・戻り値、callback function typeのcanonical C型で拒否する。静的ポインタ初期化子だけが外部dataを参照する正負例でデータ再配置経路も固定する。version 1はobject属性不明として受理し、metadataがない旧objectも後方互換のため従来どおりlink可能とする |
| **別TUのcallback戻り値修飾互換性** `int invoke(const volatile int *callback(void))` | 🧪 | function_parameter_callback_return_qualifier_xtu_{main,other} / function_parameter_callback_return_pointee_const_qualifier_mismatch_{main,other} | function parameterのfunction-to-pointer調整後も、入れ子のcallback戻りpointerにある`const volatile`をcanonical C型へ保持する。同一修飾の宣言・定義はNative・WAT・object link/runで受理し、外側Wasm署名が同じでもcallback戻り型が`const int *`対`int *`ならobject linkerで拒否する |
| **別TUのcallback戻りpointer-to-array互換性** `int invoke(const volatile int (*callback(void))[3])` | 🧪 | function_parameter_callback_return_array_qualifier_xtu_{main,other} / function_parameter_callback_return_array_{bound,element_const_qualifier}_mismatch_{main,other} | callbackが返すpointerの先にあるarray型もcanonical署名で再帰比較する。同一boundと`const volatile` element型はNative・WAT・object link/runで受理し、外側とcallbackのWasm署名が同じ`i32`でも、内側arrayの`[2]`対`[3]`およびelementの`const int`対`int`をobject linkerで拒否する |
| **別TUのcallback factory戻りfunction pointer互換性** `int invoke(int (*factory(void))(int), int)` | 🧪 | function_parameter_callback_return_function_pointer_xtu_{main,other} / function_parameter_callback_return_function_{parameter,result}_signedness_mismatch_{main,other} | callback factoryが返すfunction pointerの署名まで再帰比較する。同じ`int(int)` callbackを返すfactoryはNative・WAT・object link/runで受理し、すべての低水準Wasm型が`i32`でも、返される関数のparameterまたはresultが`int`対`unsigned int`ならobject linkerで拒否する |
| **別TUの多段pointer parameter修飾互換性** `int read(const int *const *const values)` | 🧪 | function_parameter_nested_pointer_qualifier_xtu_{main,other} / function_parameter_nested_pointer_const_qualifier_mismatch_{main,other} | function parameter自身の最外層`const`は調整後の関数型から除去する一方、pointer階層内の`const`はcanonical署名へ保持する。`const int *const *const`宣言と`const int *const *`定義はNative・WAT・object link/runで受理し、中間pointerの`const`を落とした`const int **`定義は同じWasm `i32`でもobject linkerで拒否する |
| **別TUのatomic scalar関数署名互換性** `_Atomic(int) transform(const volatile _Atomic(int))` | 🧪 | atomic_function_signature_xtu_{main,other} / function_{parameter,return}_atomic_type_mismatch_{main,other} | `_Atomic(int)`は通常`int`とは異なる型であり、function parameterの最外層`const volatile`だけを互換判定時に除去してatomic型自体は保持する。修飾付きatomic parameter宣言と無修飾atomic parameter定義、およびatomic resultが一致する組合せはClang strict・Native・WAT・object link/runで受理し、Wasm ABIが同じ`i32`でもparameterまたはresultの一方を通常`int`へ変えた定義はobject linkerで拒否する。atomic function call resultはatomic objectへ初期化後、atomic lvalue conversionで通常`int`として検査する |
| **別TUのpointer内atomic関数署名互換性** `_Atomic(int) *` / `_Atomic(int *)` | 🧪 | atomic_pointer_function_signature_xtu_{main,other} / function_parameter_atomic_{pointee,pointer}_type_mismatch_{main,other} | atomic-qualified objectへのpointerと、pointer自体を値とするatomic型をcanonical C署名の異なる再帰位置へ保持する。両形をparameterに持つ一致宣言・定義はClang strict・Native・WAT・object link/runで受理し、いずれもWasm ABIでは`i32`になる一方、`_Atomic(int) *`対`int *`または`_Atomic(int *)`対`int *`へ変えた別TU定義はobject linkerで拒否する |
| **別TUのatomic pointer関数戻り署名互換性** `_Atomic(int *) make(void)` / `_Atomic(int) *get(void)` | 🧪 | atomic_pointer_return_signature_xtu_{main,other} / function_return_atomic_{pointer,pointee}_type_mismatch_{main,other} | parameterだけでなくfunction resultの再帰型でも、pointer自体を値とするatomic型とatomic objectへのpointerを区別する。一致宣言・定義はClang strict・Native・WAT・object link/runで受理し、戻り値をatomic objectへ初期化してから読み出す。いずれもWasm resultは`i32`だが、atomic型またはatomic pointeeを通常pointerへ変えた別TU定義はobject linkerで拒否する |
| **別TUのatomic pointer-to-array署名互換性** `_Atomic(int (*)[2])` / `_Atomic(int) (*)[2]` | 🧪 | atomic_pointer_to_array_signature_xtu_{main,other} / function_parameter_{atomic_pointer_to_array,pointer_to_atomic_array}_type_mismatch_{main,other} | pointer自体を値とするatomic型の内側にあるarray boundと、pointer先arrayのatomic elementを異なる再帰位置へ保持する。一致宣言・定義はClang strict・Native・WAT・object link/runで各rowを読み出し、同じWasm `i32` parameterでもAtomic wrapperまたはatomic elementを通常型へ変えた定義はobject linkerで拒否する |
| **別TUのatomic pointer-to-record data互換性** `_Atomic(struct P *)` / `_Atomic(struct P) *` | 🧪 | atomic_pointer_to_record_signature_xtu_{main,other} / global_atomic_{pointer_to_record,record_pointee}_type_mismatch_{main,other} | complete recordへのpointer自体を値とするatomic型と、atomic record objectへのpointerを区別する。一致宣言・定義はClang strict・Native・WAT・object link/runでatomic pointer loadとatomic aggregate loadを実行し、record layoutとpointer幅が同じでもAtomic wrapperまたはrecord pointeeのatomic型を落とした別TU定義はobject linkerで拒否する |
| **別TUのatomic pointer内incomplete record互換性** `_Atomic(struct P *)` | 🧪 | atomic_incomplete_record_{pointer,wrapper}_signature_xtu_{main,other} | pointerの内側にあるrecordが宣言側でincompleteでも、定義側の同tag complete recordと互換にする。直接global/function parameterと、atomic pointer memberを持つholderのglobal/by-value parameterをClang strict・Native・WAT・object link/runで受理し、complete側で値42を読み出す |
| **別TUのatomic pointer先complete record不一致** `_Atomic(struct P *)` | 🧪 | function_parameter_atomic_pointer_record_{member_type,layout}_mismatch_{main,other} / global_atomic_pointer_record_{member_type,layout}_mismatch_{main,other} | 両側がcompleteならAtomic wrapper越しでもrecord bodyとABI layoutを比較する。同じtag・pointer幅でもmemberが`int`対`unsigned int`ならcanonical function/data署名で、member型が同じでもpacked対natural layoutならABI fingerprintでobject linkerが拒否する |
| **別TUのAtomic自己参照record署名** `struct N{int value; _Atomic(struct N *) next;}` | 🧪 | atomic_self_referential_record_signature_xtu_{main,other} / function_parameter_self_record_atomic_{pointer_qualifier,member_type}_mismatch_{main,other} | atomic pointer edgeで自身へ戻るrecord bodyとlayout fingerprintをcycle markerで有限化する。一致するglobalとfunction parameterはClang strict・Native・WAT・object link/runで2 nodeを辿って42を返し、cycle内のAtomic wrapper欠落または`int`対`unsigned int` member差はobject linkerで拒否する |
| **別TUのAtomic相互参照record署名** `struct L{_Atomic(struct R *) next;}` | 🧪 | atomic_mutual_record_signature_xtu_{main,other} / global_mutual_atomic_record_{member_type,layout}_mismatch_{main,other} | 二つのrecordがatomic pointerで相互参照するcycleも有限にcanonical化し、一致するglobal/function経路はClang strict・Native・WAT・object link/runで42を返す。cycle先recordのmember型差と、両recordのpacked対natural layout差は同じ外側global幅でもobject linkerで拒否する |
| **別TUのcallback内atomic scalar署名互換性** `_Atomic(int) callback(_Atomic(int))` | 🧪 | atomic_callback_function_signature_xtu_{main,other} / function_parameter_callback_atomic_{parameter,result}_type_mismatch_{main,other} | function pointer parameterの内側でも、atomic parameter/resultを通常`int`と区別してcanonical C署名へ再帰保持する。callback parameter最外層の`const volatile`だけが調整された一致宣言・定義はClang strict・Native・WAT・object link/runで受理し、外側関数とcallbackの低水準Wasm署名がすべて`i32`でも、callback parameterまたはresultのatomic型を通常`int`へ変えた別TU定義はobject linkerで拒否する。atomic callback resultはatomic objectへ初期化してから通常`int`へ変換する |
| **別TUのpadded Atomic aggregate callback parameter** `consume(callback(_Atomic(struct Words3)))` | 🧪 | nested_atomic_aggregate_callback_parameter_xtu_{main,other} / function_parameter_callback_atomic_aggregate_parameter_mismatch_{main,other} | 12-byte recordが16-byte Atomic storageになるcallback parameterを、別TU定義のconsumer function parameter内へ再帰保持する。consumerからcallbackへの間接呼出し、callback parameter object上の16-byte atomic load・store、by-value copy、呼出し元の前後canaryをClang strict・Native/WAT/object link/runで固定する。同じWasm hidden-pointer ABIになるplain aggregate callbackへ別TU定義だけを変えた場合は、外側`consume_atomic_words`のcanonical C署名不一致としてobject linkerで拒否する |
| **別TUのAtomic union/complex callback parameter** `consume(callback(_Atomic(union T)))` / `consume(callback(_Atomic(T _Complex)))` | 🧪 | nested_atomic_union_complex_callback_parameter_xtu_{main,other} / function_parameter_callback_atomic_{small_union,union,float_complex,complex}_parameter_mismatch_{main,other} | record以外のaggregateである4-byte unionと、12-byte unionから16-byte Atomic storageへのpadding、および8-byte float complex・16-byte double complexのAtomic callback parameterを別TUのconsumer function parameter内へ保持する。header側の`const volatile _Atomic(T)` parameter宣言と無修飾`_Atomic(T)`定義を互換にし、最外層cvだけを除去してAtomic wrapperは維持する。間接呼出し、parameter objectのatomic load・store、by-value copy、呼出し元の前後canaryをClang strict・Native/WAT/object link/runで固定する。同じ`i32`・`i64`またはhidden-pointer ABIになるplain union/complex callbackへ定義側だけを変えた場合はcanonical C署名不一致としてobject linkerで拒否する。object fixtureは`complex.h`の未使用math runtimeを取り込まず`--nostdlib`で自己完結させる |
| **別TUのAtomic union/complex callback result** `consume(_Atomic(union T) callback(void))` / `consume(_Atomic(T _Complex) callback(void))` | 🧪 | nested_atomic_union_complex_callback_result_xtu_{main,other} / function_parameter_callback_atomic_{small_union,union,float_complex,complex}_result_mismatch_{main,other} / function_parameter_callback_atomic_{union_result_const,complex_result_volatile}_qualifier_mismatch_{main,other} | nested callbackの戻り型でも4-byte／12-byte unionと8-byte float complex／16-byte double complexのAtomic wrapperを再帰保持する。別TUのcallbackから返った値をconsumer側のguarded Atomic objectへ代入し、load・storeと前後canaryをClang strict・Native/WAT/object link/runで固定する。同じ`i32`・`i64`またはhidden result-pointer ABIになるplain union/complex callbackへ定義側だけを変えた場合、およびAtomic wrapperを保ったまま戻り型の最上位`const`／`volatile`だけを定義側から落とした場合は、外側consumerのcanonical C署名不一致としてobject linkerで拒否する |
| **別TUのcallback factory内Atomic union/complex署名** `invoke(factory(void) -> target(_Atomic(union T)))` / `invoke(factory(void) -> _Atomic(double _Complex) target(void))` | 🧪 | nested_atomic_union_complex_callback_factory_xtu_{main,other} / function_parameter_callback_factory_atomic_{union_parameter,complex_result}_mismatch_{main,other} | consumer parameterからfactory戻りfunction pointer、そのtarget parameter/resultまで三層に再帰したcanonical C署名でAtomic wrapperを保持する。12-byte unionの16-byte parameter storageと16-byte Atomic complex resultをfactory経由で間接呼出しし、load・store・canaryをClang strict・Native/WAT/object link/runで固定する。target parameterの最上位`const volatile`宣言と無修飾定義は互換にする一方、外側・factory・targetの低水準Wasm型がすべて同じでもtarget内だけをplain union parameterまたはplain complex resultへ変えた別TU定義は外側`invoke_*_factory`署名でobject linkerが拒否する |
| **別TUのAtomic callback factory外部data署名** `_Atomic(factory_t *) shared_factory` | 🧪 | atomic_union_complex_callback_factory_data_xtu_{main,other} / global_callback_factory_atomic_{union_parameter,complex_result}_mismatch_{main,other} | Atomic function pointerである外部data symbolの内側へ、factory戻りfunction pointerとtargetのAtomic union parameter／Atomic complex resultを三層に保持する。別TU定義からfactory pointerをatomic loadし、factory→targetを間接呼出しして12-byte union parameterと16-byte complex resultの値・canaryをClang strict・Native/WAT/object link/runで固定する。global自体のAtomic wrapperと低水準data幅が一致していても、target内だけをplain union parameterまたはplain complex resultへ変えた定義は`agc.data_signature`不一致としてdata relocation前にobject linkerが拒否する |
| **別TUのrecord/array内Atomic callback factory署名** `struct H{_Atomic(factory_t *) member;}` / `_Atomic(factory_t *) slots[2]` | 🧪 | atomic_union_complex_callback_factory_container_xtu_{main,other} / record_member_callback_factory_atomic_union_parameter_mismatch_{main,other} / global_array_callback_factory_atomic_complex_result_mismatch_{main,other} | 外部data署名のrecord memberまたはarray elementへ一段入った位置でも、Atomic factory pointerからfactory戻りfunction pointer、targetのAtomic union parameter／Atomic complex resultまで再帰保持する。memberと配列の両方からfactoryをatomic loadして三段間接呼出しをClang strict・Native/WAT/object link/runで固定し、同一record layout・array bound・pointer幅でもtarget内だけをplain型へ変えた定義はcontainer globalのdata署名不一致としてobject linkerが拒否する |
| **別TUのatomic function pointer関数署名互換性** `_Atomic(int (*)(int)) roundtrip(...)` | 🧪 | atomic_function_pointer_signature_xtu_{main,other} / function_{parameter,return}_atomic_function_pointer_type_mismatch_{main,other} | function pointer自体を値とするatomic型をparameterとresultのcanonical署名へ保持する。`const volatile`付きparameter宣言と無修飾定義はClang strict・Native・WAT・object link/runで受理し、atomic load後のindirect callを実行する。Wasm上は通常function pointerと同じ`i32`だが、Atomic wrapperを落とした別TU定義はobject linkerで拒否する |
| **別TUのatomic function pointer data互換性** `_Atomic(callback_t *) callback` | 🧪 | atomic_function_pointer_data_signature_xtu_{main,other} / global_atomic_function_pointer_{type,callback_signature}_mismatch_{main,other} | atomic function pointerを直接global、配列element、record memberへ入れたdata署名を固定する。一致宣言・定義はClang strict・Native・WAT・object link/runで3経路のatomic loadとindirect callを実行する。Atomic wrapperを通常function pointerへ変えた定義と、wrapperを保ったまま内側callback parameterを`int`対`unsigned int`へ変えた定義は、いずれも同じdata幅でもobject linkerで拒否する |
| **別TUのglobal atomic pointer data互換性** `_Atomic(int *)` / `_Atomic(int) *` | 🧪 | atomic_pointer_global_signature_xtu_{main,other} / global_atomic_{pointer,pointee}_type_mismatch_{main,other} | 外部data symbolについても、pointer自体を値とするatomic型とatomic objectへのpointerを`agc.data_signature`の異なる再帰位置へ保持する。一致する宣言・定義はClang strict・Native・WAT・object link/runで受理し、いずれもpointer幅のdata objectになる一方、`_Atomic(int *)`対`int *`または`_Atomic(int) *`対`int *`の別TU定義はdata relocation適用前にobject linkerで拒否する |
| **別TUのglobal配列内atomic pointer互換性** `_Atomic(int *) slots[2]` / `_Atomic(int) *slots[2]` | 🧪 | atomic_pointer_array_signature_xtu_{main,other} / global_array_atomic_{pointer,pointee}_type_mismatch_{main,other} | 外部data署名のarray elementへ一段入った位置でもatomic pointerとpointer-to-atomicをcanonical型へ保持する。一致宣言・定義はClang strict・Native・WAT・object link/runで各elementを読み出し、同じ配列bound・element幅でもatomicを通常pointerへ変えた別TU定義はdata relocation適用前にobject linkerで拒否する |
| **別TUの多次元配列内atomic pointer互換性** `_Atomic(int *) matrix[1][2]` / `_Atomic(int) *matrix[1][2]` | 🧪 | atomic_pointer_multidimensional_array_signature_xtu_{main,other} / global_multidimensional_array_atomic_{pointer,pointee}_type_mismatch_{main,other} | 二段のarray型を再帰したelement位置でもatomic pointerとpointer-to-atomicを保持する。一致宣言・定義はClang strict・Native・WAT・object link/runで受理し、同一bound・layoutの通常pointer matrixへ変えた別TU定義はobject linkerで拒否する |
| **別TUのrecord member内atomic pointer互換性** `struct R{_Atomic(int *) pointer; _Atomic(int) *pointee;}` | 🧪 | atomic_record_member_signature_xtu_{main,other} / record_member_atomic_{pointer,pointee}_type_mismatch_{main,other} | named recordのcanonical member型でもatomic pointerとpointer-to-atomicを区別する。同一tag・member名・layoutの一致定義はClang strict・Native・WAT・object link/runで受理し、member幅とrecord layoutが同じでもいずれかのatomic型を通常pointerへ変えた別TU定義はobject linkerで拒否する |
| **別TUのarray parameter atomic調整** `int values[const _Atomic 1]` / `_Atomic(int) values[1]` | 🔧 | function_parameter_atomic_array_adjustment_xtu_{main,other} / function_parameter_atomic_array_element_type_mismatch_{main,other} | `[]`内のqualifierはparameter objectのadjusted pointerへ適用されるが、C11 6.7.6.3p15の関数型互換判定ではその最外層qualifierを除去する。旧canonical型はarray由来の`_Atomic`だけを残し、標準上互換な`int values[const _Atomic 1]`宣言と`int *values`定義をobject linkerで誤拒否していた。parameter object側のatomic load/storeは維持したままfunction parameter型だけをunqualified pointerへ正規化する。prototypeと旧形式definition、およびarray parameter由来のfunction pointerと通常pointer parameterのcallback互換性も同じfixtureで固定する。一方、`_Atomic(int) values[1]`はpointer to atomic elementへ調整されるため`int *values`とは非互換であり、同じWasm `i32`でもobject linkerで拒否する |
| **別TUの旧形式global callback data互換性** `extern int (*callback)();` と `int (*callback)(int)` | 🔧 | unprototyped_global_callback_signature_xtu_{main,other} / global_unprototyped_callback_promotion_mismatch_{main,other} | function symbol用の旧形式互換判定は存在したが、外部data symbolのcanonical型比較はfunction pointer内部の空parameter listをprototypeとの差として扱い、default argument promotionで不変な`int` prototypeまで誤拒否していた。`agc.data_signature`のcanonical C型とABI layoutの`?` markerを組み合わせ、pointer・pointer-to-pointer・array wrapperを再帰して旧形式側を特定する。指定側parameterがpromotion後も不変なら受理し、`char`等のnarrow parameterは拒否する |
| **union内structの共通先頭列** `view.extended={...}; view.compact.tag` | 🧪 | union_common_initial_sequence | unionのactive memberと別のstruct memberが互換型・同一alignmentの先頭member列を共有する場合、その共通部分は別viewからも検査できる。通常memberと同じ幅のbit-fieldについて、global/local初期化、inactive viewからの読出し、別viewへの書込み後の逆向き読出しをClang strictとの比較およびnative/WAT/objectで固定する |
| **character type経由のobject representationコピー** `(unsigned char *)&object` | 🧪 | character_object_representation_copy | 任意objectの表現はcharacter typeのlvalueから検査・複製できる。padding・`double`・sub-byte整数配列を含むstruct全体を`unsigned char`・plain `char`・`signed char`の各pointerでbyte単位にコピーし、通常のmemberアクセスで元の値が復元されることをClang strictとの比較およびnative/WAT/objectで固定する |
| **動的確保領域へのobject representationコピーとeffective type** `memcpy(malloc(...), &typed, sizeof typed)` | 🧪 | allocated_object_effective_type_copy | 宣言型を持たない割当て済みobjectへ、型付きsourceの表現を`memcpy`・`memmove`またはcharacter配列としてコピーすると、後続の非変更accessではsourceのeffective typeを持つ。paddingを含むnested struct・`double`・sub-int・配列memberを3経路で複製し、typed pointerから全memberを読めることをClang strictとの比較およびnative/WAT/objectで固定する |
| **aggregateと先頭memberのpointer相互変換** `(struct S *)&s.first`, `(T *)&s` | 🧪 | aggregate_initial_member_pointer_interconversion | struct object pointerは適切に変換すると先頭memberを指し、そのmember pointerから元structへ戻せる。先頭がscalarの場合と配列の場合をglobal/local objectで固定し、union object pointerは全memberと同じaddressで各member pointerから元unionへ戻れることもClang strictとの比較およびnative/WAT/objectで固定する |
| **void object宣言の型制約** `void value;` | 🔧 | compile_fail: void_variable, array_of_void_rejected | `void`はcomplete object型ではないためobjectを宣言できず、配列要素型にもできない。direct Syntax local declarationがvoid型を汎用失敗として返して`E0006`内部不変条件違反になっていたため、宣言名を保持したE3087へ変換し、Native/Wasm object双方で固定する |
| **initializer designatorの対象・添字制約** `[-1]`, `[N]`非ICE, sized arrayのone-past, unknown member | 🔧 | designator_then_positional, duplicate_designator_override, nested_array_designator / compile_fail: array_designator_{negative,nonconstant,out_of_bounds}, struct_designator_unknown_member, scalar_array_designator | array designatorは非負の整数定数式かつ既知bound内、member designatorは対象aggregateの実在memberを要求する。flat initializer plannerがこれらを拒否しても失敗理由とdesignator tokenを呼出側へ返さず、direct Syntax診断が`E0006`内部不変条件違反になっていたため、array index不正をE3085、unknown memberをE3084へ変換する。scalarにarray designatorを適用する形も拒否し、順不同・重複後勝ち・designator後のpositional継続など合法な初期化規則は維持する。5件はshould_reject正本をcompile-fail registryから参照し、E3064/E3084/E3085のNative/Wasm診断ID parityまで固定する |
| **character arrayの文字列長境界** `char exact[3]="abc"`, `char short[2]="abc"` | 🔧 | static_character_array_string_boundaries, ordinary_character_array_exact_boundaries / compile_fail 9件: static_character_array_{string,embedded_null,concatenated_string}_too_long, static_local_character_array_string_too_long, automatic_character_array_{string,braced_string}_too_long, character_array_{member,compound_literal}_string_too_long, multidimensional_character_array_string_too_long | 非bracedなグローバル/static local文字列初期化だけがaggregate planを通らず、配列への文字列代入を無条件で許可して長い内容を切り詰めていた。共通character-array plannerで対象要素幅と文字数を検証し、「文字数＝配列長」の終端NUL省略は許可したまま、通常・連結・埋め込みNULの超過をE3027で拒否する。automaticの直接・braced初期化、struct member、多次元row、array compound literalにも同じ境界を適用し、9件すべてをNative/Wasm object compile-failへ登録する。exact-boundのglobal/local、braced、member、row、compound literalはClang strictおよびNative/WAT/Wasm objectで固定する |
| **const subobjectを含むaggregateのmodifiable lvalue制約** `struct S{const int x;} a,b; a=b` | 🔧 | aggregate_const_subobject_conversion / compile_fail: struct_with_const_member_assignment, nested_struct_with_const_member_assignment, struct_with_const_array_member_assignment, union_with_const_member_assignment, typedef_const_member_assignment, anonymous_union_const_member_assignment, const_pointer_member_assignment | aggregate本体のtop-level qualifierだけを見ていたため、const memberを直接・入れ子record・配列member・union・typedef・anonymous union・const pointer member経由で含むstruct/union全体への代入を許していた。canonical RecordDeclを配列要素も含め再帰してconst subobjectを検出し、source assignmentだけをE3077にする。初期化、値渡し、returnはmodifiable lvalueを要求しないassignment conversionとして別APIへ分離し、const pointeeへのpointer memberやconst aggregateへのpointer memberを含む通常のaggregate代入は維持する。Apple Clang 17が受理する「const memberを持つrecordの配列member」再帰は、C11 6.3.2.1p1の`element of all contained aggregates`に従いparser unitで固定 |
| **top-level `const` aggregateのmember lvalue伝播** `const struct S a[1]; a[0].x=1` | 🧪 | should_reject 10件 / compile_fail: assign_const_scalar, assign_member_of_{const_struct,const_struct_array,global_const_struct_array}, assign_member_through_const_struct_{array_pointer,pointer,pointer_return,array_pointer_return,pointer_callback,array_pointer_callback} | `.`・`->`で得るmember lvalueはrecord objectのtop-level `const`を継承する。local/file-scope配列、pointer-to-array、record pointer、direct function return、function pointer経由のindirect returnを組み合わせてもconstnessを失わないことをClang strictと照合し、scalarを含む10件をshould_reject正本からNative/Wasm object compile-failへ登録してE3077 parityを固定する |
| **jump・label・switch statement制約** loop外`break`/`continue`, switch外`case`/`default`, label/default重複, 未定義goto | 🧪 | switch_fallthrough, goto_vla_scope_constraints / should_reject: declaration_immediately_after_{label,case,default}, switch_complex_control / compile_fail: break_outside_loop_or_switch, continue_outside_loop, {case,default}_outside_switch, duplicate_{label,default,case_simple}, goto_undefined_label, case_nonconstant_expression | `break`/`continue`の包含context、case/defaultのswitch所属、関数内labelの一意性とgoto解決、caseの整数定数式要件、switch制御式の整数型要件をClang strictと照合する。C11の通常label・case・defaultはstatementを要求するため、直後のdeclaration 3件をshould_reject正本からNative/Wasm object compile-failへ登録する。complex制御式も同じ正本経路でE3107として拒否する。合法な入れ子loop/switch、forward/backward goto、fallthroughとVM scope制約は維持する |
| **selection/iteration statementのbody制約** `if (1) int value;`, `for (;;) int value;` | 🧪 | should_reject: declaration_as_{if,else,while,do,for,switch}_body | C11のif/else/while/do/for/switch bodyはstatementを要求し、declarationを直接置けない。6構文をClang strictと照合し、E3064のNative/Wasm診断ID parityを固定する |
| **return statementのvoid/non-void制約** `void f(){return 1;}`, `int f(){return;}` | 🔧 | function_argument_return_constraints, tag_redef_and_return, nonvoid_aggregate_fallthrough_boundaries, noreturn_cfg_termination_boundaries, noreturn_fallthrough_warning_boundaries, constant_condition_cfg_termination_boundaries / compile_fail: return_value_from_void_function, bare_return_from_nonvoid_function | expression付きreturnはnon-void関数、expression無しreturnはvoid関数だけに許可する。非void関数末尾への到達は別途warning、`main`末尾は暗黙0、void関数末尾は合法として維持する。暗黙return生成がdirect scalarしか扱わず、aggregate/complex戻り関数では実行されない`_Noreturn`呼出し後の経路でもE4007になっていたため、未定義値用のtarget-layout準拠temporaryを生成してNative/WAT/Wasm objectおよびself-host compileを固定する。`_Noreturn`はfunction typeへ混ぜず宣言symbol metadataとしてdefinition・prototype・block-scope宣言からHIR/IRのdirect callへ伝える。IR CFGの到達可能性はnoreturn call以後のedgeを切って評価し、単一直線だけでなくif/else・switch・条件演算子の全経路がnoreturnならW3005を抑止する。一方で片側だけのnoreturn、短絡式、間接callではclosing braceが到達可能なためW3005を維持する。Typed HIRのinteger/floating定数条件はTarget DataLayout込みで評価し、literal、cast、単項・二項式、比較、ternary、short-circuitの到達不能edgeを除外する。`switch`や内側loopの`break`は外側の無限loopを終端させず、外側loopを抜ける`break`・`goto`・switchの有限pathだけがW3005を維持することを正負fixtureで固定する。HIRのcase終端判定も同じmetadataを使い、全経路noreturnではW3017の誤警告を防ぎつつ部分経路では警告を維持する |
| **二項演算子のscalar・category制約** aggregate equality/logical・complex integer-only operators・pointer/scalar comparison | 🧪 | should_reject: binary_{struct_equality,pointer_nonzero_integer_equality,pointer_float_equality,complex_relational,complex_modulo,complex_bitwise,complex_shift,struct_relational,pointer_integer_relational,struct_logical_and,struct_logical_or} | equalityと論理演算はscalar、順序比較はreal型または適切なobject pointer、剰余・bitwise・shiftは整数operandを要求する。structのequality/relational/logical、complexのrelational/modulo/bitwise/shift、pointerと非zero整数・floating値のequality、pointerとintegerのrelationalを独立fixtureでClang strictと照合し、E3122のNative/Wasm object診断parityを固定する。incompatible pointer、function/void equality、void/object・function・incompatible pointer relationalは既存fixtureの登録を再利用し、同内容だった古いインライン5件を削除する |
| **pointer算術・比較のoperand制約とスケーリング** `p+q`, `void_ptr+1`, `function+1`, `incomplete_ptr+1` | 🔧 | pointer_arithmetic_comparison_boundaries, binary_operator_constraints, array_pointer_qualification_constraints / compile_fail: add_two_pointers, multiply_pointer, {void,function,incomplete}_pointer_arithmetic, subtract_{incompatible,atomic_plain}_pointers, relational_{incompatible,void_object,function}_pointers, equality_incompatible_pointers | 加算はpointerとintegerの一方ずつ、減算はpointerとintegerまたはcompatible complete object pointer同士に限定する。complete struct、pointer-to-array、VLA、enum/_Bool/_Atomic integer index、qualified pointee、atomic pointeeの実行時スケーリングを固定。relationalはcompatible object pointerのみ、equalityはobject/void pointerとcompatible function pointerも許可する。`void`・関数・未完成object pointerは要素サイズを持たないため算術を拒否し、異なるpointee型やatomic/plain pointee同士の減算も拒否する。11拒否fixtureはNative/Wasm object compile-failでE3122 parityまで固定する |
| **pointer算術offsetとshiftのoperand category制約** `pointer + 1.0`, `pointer - 1.0`, `1 << pointer` | 🧪 | should_reject: binary_{pointer_add_floating,pointer_sub_floating,integer_shift_pointer}, decrement_incomplete_pointer | pointer加減算のoffsetとshift両operandには整数型を要求し、pointer更新にはcomplete object pointeeを要求する。floating offset、pointer shift count、incomplete record pointerのdecrementをClang strictと照合し、E3122/E3123のNative/Wasm object診断parityを固定する。incompatible pointer減算とincomplete pointer加算は既存fixture登録を再利用し、同内容だった古いインライン2件を削除する |
| **pointer比較の互換性制約** `int *` vs `double *` の`==`/`<` | 🧪 | binary_operator_constraints, conditional_operator_constraints / compile_fail: {equality,relational}_incompatible_pointers | pointerの等価・順序比較はcompatible object pointer、適切な`void *`、compatible function pointer、null pointer constantの組合せに限定する。互換でないobject pointee型同士の比較をClang strict同様に拒否する |
| **整数・単項演算子のoperand制約** `double % double`, `double << int`, `double & int`, `~pointer`, `~complex`, `+pointer`, `!struct`, `&rvalue` | 🧪 | unary_semantic_resolution, binary_operator_constraints / should_reject: bitwise_not_{pointer,complex} / compile_fail: modulo_floating_operands, shift_floating_operand, bitwise_floating_operand, binary_complex_{modulo,bitwise,shift}, unary_plus_pointer, logical_not_struct, address_of_rvalue | `%`・shift・bitwiseと単項`~`は整数operand、単項`+`は算術型、`!`はscalar、`&`はfunction designator・lvalue・bit-field以外の適切なoperandを要求する。complexも算術型だが整数型ではないため、整数専用演算をClang strict同様に拒否する。pointer/complexへの`~`を含むshould_reject正本をNative/Wasm object compile-failへ登録し、E3064/E3122の診断ID parityを固定する |
| **address-ofの配列lvalue型とアドレス可能性境界** `&"abc"`, `&(a=b)`, `&(a,b)`, `&(cond?a:b)`, `&(bits.value)`, `&register_object.member` | 🔧 | address_of_string_literal_array / compile_fail: address_of_{rvalue,assignment_result,comma_result,conditional_result,parenthesized_bitfield,register_parameter,register_array,register_struct_member,register_atomic_parameter,register_atomic_aggregate_parameter}, address_generic_selected_{bitfield,register_object,register_member,register_array}, bitfield_addr | 文字列リテラルは通常式では先頭要素へのpointerへdecayする一方、`&` operandでは `char[N]` lvalueのまま扱う必要がある。direct Typed HIRのlvalue判定が`ND_STRING`を含まず、合法な`char (*)[N] p=&"..."`をE3112で拒否していた。address専用にcanonical array `QualType`を構成し、HIR stringのlvalue addressをIRへlowerする。static/local pointer、`_Generic`でのpointer-to-array型、`sizeof *p`、native/Wasm実行を固定する。非lvalue・bit-field・registerおよび`_Generic`で選択した各派生lvalueの15件は既存fixtureをNative/Wasm object compile-failへ登録し、E3112/E3113/E3119の診断ID parityも固定する |
| **encoded/wide文字列literalのdecay・address・`_Generic`選択境界** `&u"ab"`, `_Generic(0, int: U"abc", default: U"x")` | 🧪 | generic_encoded_string_literal_boundaries | `u8/u/U/L` literalが通常value contextとconditionalではそれぞれ`char *`・`char16_t *`・`char32_t *`・`wchar_t *`へdecayし、`sizeof`とaddress-ofでは終端を含むarray型とpointer-to-array型を保つことを固定する。generic-selected literalも周囲の`sizeof`・`&`適用前には選択associationのarray型とvalue categoryを維持し、file-scope pointer/pointer-to-array初期化、関数引数、添字と終端code unitをClang strict・Native・WAT・Wasm objectで照合する |
| **更新演算子とcompound assignmentのscalar・pointer境界** `++const_value`, `++void_pointer`, `array+=1`, `pointer*=2` | 🧪 | update_expression_boundaries / compile_fail: increment_{array,const_scalar,const_bitfield,complex,void_pointer,incomplete_pointer,function_pointer}, decrement_function, compound_assign_{const_scalar,array,struct,pointer_multiply,pointer_bitwise}, compound_complex_{modulo,bitwise,shift} | `++`/`--`はmodifiableなreal/整数またはcomplete object pointer、compound assignmentは対応する二項演算が成立するmodifiable lvalueに限定される。complexの`+=`/`-=`/`*=`/`/=`は維持し、整数専用の`%=`/bitwise/shiftは拒否する。float、complete object pointer、bit-field、atomic、整数のshift/bitwise/modulo compound assignmentをnative/Wasmで固定する。配列・関数・const・complex・void/incomplete/function pointerの8更新演算fixtureと、const/array/struct/不正pointer演算の5 compound-assignment fixtureはNative/Wasm object compile-failへ登録し、E3064/E3077/E3098/E3099/E3123の診断ID parityも固定する |
| **compound assignmentのcomplete object pointer・整数operand制約** `void_ptr += 1`, `function_ptr += 1`, `complex %= 2` | 🧪 | should_reject: compound_{void_pointer_add,incomplete_pointer_add,function_pointer_add,incomplete_array_pointer_add,complex_modulo,complex_bitwise,complex_shift} | pointer `+=`/`-=`はcomplete object pointerと整数offsetの組合せに限定し、`void`・未完成record/array・function pointeeを拒否する。`%=`・bitwise・shift compound assignmentは整数operandを要求するためcomplex objectへ適用できない。7件をClang strictと照合し、E3099のNative/Wasm object診断parityを固定する |
| **間接参照・添字の完全オブジェクト型境界** `*incomplete_pointer;`, `void_pointer[0]`, `function_pointer[0]`, `&*incomplete_pointer` | 🔧 | indirection_subscript_boundaries / compile_fail: deref_int, void_ptr_deref, subscript_int, incomplete_lvalue_conversion{,_void_cast,_comma_left}, subscript_{void,incomplete,function}_pointer | 単項`*`は不完全オブジェクトへのポインタにも適用でき、`&*p`のような非変換文脈は正当だが、式文・void cast・comma左辺ではlvalue-to-rvalue変換に完全型が必要。direct Typed HIRのvalue conversion入口でのみ完全性を検査する。また`[]`は整数との組合せだけでなくcomplete object pointerを要求し、従来E4007まで落ちていた不完全型・関数型と、後続演算で偶然拒否していた`void *`をsemantic E3064で早期拒否する。reverse subscript、enum/bool/atomic index、function deref、pointer-to-array、VLA、unknown-bound extern arrayをnative/Wasmで固定し、9拒否fixtureはNative/Wasm object compile-failでE3064 parityまで検査する |
| **明示castのscalar・aggregate category制約** pointer↔floating/complex, aggregate→integer, distinct aggregate間, void expression→integer | 🔧 | explicit_cast_category_boundaries, constant_cast_sizeof_character, pointer_constant_cast_wrapper / should_reject / compile_fail: cast_{pointer_to_double,double_to_pointer,pointer_to_complex,complex_to_pointer,struct_to_integer,struct_from_nonscalar,void_expression_to_integer} | arithmetic型同士、pointer↔integer、object/function/void pointer間の明示cast、pointer→`_Bool`、完全aggregate→`void`を維持し、変換規則を持たないpointer↔floating/complexをsource-cast type resolutionでE3064として早期拒否する。従来この4分類はTyped HIRを通過してIR内部エラーE4007まで落ちていた。aggregate→scalar、互換でないunion→struct、void値→scalarもsemantic拒否を固定し、`(struct S)scalar`等は既存の設定可能なaggregate cast拡張として別管理する。7件をNative/Wasm object compile-failへ接続して診断ID parityを固定する |
| **静的aggregateの整数→pointer address constant** `handler_t a[]={SIG_DFL,SIG_IGN,SIG_ERR}` | 🔧 | integer_pointer_cast_aggregate_boundaries, signal_macro_type_boundaries | scalarの明示整数→pointer castは静的初期化できたが、array/struct/static localのpointer leafは0またはsymbol relocationだけに限定され、非zeroの`(pointer)integer-constant`をE3116拒否していた。明示castかつ整数定数に限定してsymbolなしのpointer値slotへ保持する。さらにApple ARM64のIR整数変換がtarget依存`IR_TY_PTR`を固定size 0として扱い、32-bit signed sourceもzero-extendして`SIG_ERR`の-1を`0x00000000ffffffff`へ壊していた。backendの型幅をDataLayoutから取得し、signedはsign extension、unsignedはzero extensionとして、data/function pointer、global array/struct、static local、32-bit high-bit境界をClang strict・Native/WAT/Wasm objectで固定する |
| **function declarator・parameter list・旧形式definition制約** variadic-only、`void` marker、parameter名重複、identifier-list、未完成record、array/function戻り | 🔧 | incomplete_return_prototype, incomplete_record_parameter_prototype, old_style_function_definition_boundaries / compile_fail: {named,nested_named,qualified}_void_parameter, {void,typedef_void}_variadic_parameter, variadic_only_parameter_list, parser_variadic_parameter_not_last, duplicate_parameter_names_{prototype,definition}, incomplete_record_{parameter,return}_definition, call_incomplete_return, function_returning_{array,function}, old_style_{parameter_*,identifier_list_declaration,nested_identifier_list,incompatible_float_prototype}_rejected | `...`には先行するnamed parameterが必要で、ellipsisはparameter list末尾に置き、同一prototype scope内のparameter名は一意にする。`void` markerは唯一のunnamed parameterに限り、修飾・名前・ellipsisとの併用を拒否する。functionはarray型やfunction型を返せない。未完成recordの値parameter/return prototypeは宣言時には許可し、定義時または値を生成する呼出し時に拒否し、record完成後の定義・呼出しをnative/Wasmで固定する。C11でobsoleteだが合法なidentifier-list形式のfunction definitionを通常のprototype parameter listと区別し、後続宣言をidentifier順へ対応付ける。関数型・ABIには`char`/`short`→`int`と`float`→`double`のdefault argument promotionを反映する一方、関数内parameter objectは宣言型のサイズ・値域を維持する。配列・関数parameter調整、typedef、`register`、aggregate値渡し、先行するcompatible prototypeをNative・WAT・Wasm objectで固定し、宣言欠落・余分/重複宣言・initializer・不正storage class・definition以外のidentifier-list・非互換prototypeを両compile-fail経路で拒否する |
| prototype-scope tag・enumerator・parameter identifierのidentity・可視性境界 | 🔧 | incomplete_record_parameter_prototype, prototype_scope_{tag_parameter_visibility,type_query_visibility,repeated_enumerator_names}, prototype_parameter_declaration_point, nested_prototype_parameter_scope / compile_fail: prototype_scope_{tag_file_definition,enumerator_file_use}, repeated_prototype_scope_tag_declarations, prototype_parameter_{self_reference,file_scope_use}, nested_prototype_parameter_scope_leak | function prototypeのparameter list内で初めて導入されたrecord tagとenumeratorは、同じparameter listの後続parameterから参照できる一方、prototype宣言の終了までしか有効でなく、同名の後続file-scope tagや別prototype内の同名tagとは別identityになる。parameter identifierのscopeはその宣言子の完了直後から始まるため、自身のpointer-to-array boundでは外側の同名enumeratorを参照し、後続parameterのboundでは先行parameterを参照する。callback型の内側にあるparameter identifierは内側prototypeだけに属し、外側functionの後続parameterからは見えない。遅延解決するarray boundのtype queryにも先行parameter適用後のprototype-scope lookup pointを使い、`sizeof`・`_Alignof`・`offsetof`、cast・`_Generic`・compound literal内の型名、およびenumerator boundをNative・WAT・Wasm objectで固定する。外側に同名宣言がない自己宣言子内の参照、prototype終了後のparameter名・enumerator参照、内側callback prototype終了後のparameter名参照はE3066で拒否する。別々のprototype内では同じordinary identifierのenumerator名を再利用できる。明示的なfile-scope forward宣言を先行させる合法形は維持し、prototype内だけのtagを使った先行宣言と後続file-scope tagを使うdefinition、および同名tagを各parameter listで新規導入する繰り返しprototype宣言をClang strict同様にE3064で拒否する。NativeとWasm objectのcompile-fail parityへ同じ正本fixtureを登録する |
| **enumeratorの宣言位置とinitializer診断** `enum { value = value + 1 }` | 🔧 | enumerator_declaration_point / compile_fail: enumerator_self_reference | enumerator名のscopeはそのdefining enumeratorの直後から始まるため、同名enumerator自身のinitializerでは外側scopeの名前を参照し、後続enumeratorから新しい値を参照する。block-scope shadowで7→8→9となる定数評価をNative・WAT・Wasm objectで固定する。外側に同名宣言がない自己参照ではdirect Typed HIRが保持したundefined-identifier rejectionをenum専用callerが汎用の非整数定数式E3064へ潰していたため、共通semantic rejection診断へ渡してE3066を発行する。should_reject正本をNative/Wasm object compile-failへ登録する |
| **typedef-name classifierの宣言位置とordinary identifier shadow** `T (*T)[sizeof(T)]`, `enum { T=sizeof(T) }` | 🔧 | typedef_{object,enumerator}_declaration_point, typedef_for_scope_shadow / compile_fail: typedef_hidden_by_{object,parameter,enumerator}, typedef_hidden_in_for_scope | block内で外側typedefと同名のobjectを宣言するとき、宣言子の完了まではtypedef-nameとして分類し、pointer-to-array boundの`sizeof(T)`を元の`char`型で評価する。宣言子完了後はobject名がtypedefを隠すため、initializerと後続式の`sizeof(T)`はpointer objectを参照する。enumeratorもdefining enumeratorの完了後からtypedefを隠すため、自身のinitializerでは`sizeof(T)`を外側typedefの型問い合わせとして解析し、後続enumeratorでは新しいenum定数の式問い合わせとして解析する。parserがenumerator名をinitializer解析前にNameClassifierへ登録して前者を未定義式へ誤分類していたため、登録をinitializer解析後へ移す。`for`初期宣言で導入した同名objectは条件・反復式・bodyでtypedefを隠し、loop終了時には外側typedefを再び可視にする。通常のlocal object、prototypeの先行parameter、enumerator、`for`初期宣言によるshadow後は、各有効scope内で同名tokenを後続宣言の型指定子として再利用できない。正例をNative・WAT・Wasm objectで固定し、4つの不正な再利用をClang strict同様に拒否してNative/Wasm object compile-fail parityへ登録する |
| **Atomic型指定子・qualifierの型形成制約** qualified/Atomic/void/array/function/incomplete型、typedef経由の派生宣言 | 🧪 | should_reject 19件 / compile_fail: atomic_{const,volatile,void,array,function,incomplete_record,const_pointer}_type, nested_atomic_{type,pointer_type}, atomic_typedef_{function_qualifier,function_local,function_member,function_parameter,function_typedef,function_pointer_outer,array_qualifier,array_pointer_outer,array_type_name}, atomic_incomplete_record_pointer_outer | `_Atomic(type-name)`のtype-nameはarray/function/Atomic/qualified型ではない完全なobject型に限定する。qualifier形式もtypedefが隠すarray/function型や未完成recordへ適用できず、後続のpointer declaratorを付けても不正なAtomic基底型は有効にならない。file/local/member/parameter/typedef/type-nameの全宣言文脈をshould_reject正本からNative/Wasm object compile-failへ登録し、E3064/E3117診断parityを固定する |
| **`restrict` qualifierの適用対象・宣言文脈制約** scalar、function pointer、typedef、type-name | 🧪 | restrict_object_pointer_types / should_reject 12件 / compile_fail: restrict_{nonpointer_prefix,nonpointer_parameter,function_pointer,typedef_function_pointer,typedef_function_pointer_prefix,function_pointer_type_name,typedef_function_pointer_type_name,typedef_scalar_type_name,function_pointer_global,function_pointer_parameter,function_pointer_member,function_pointer_typedef_declaration} | `restrict`で修飾できるのはobject型または未完成型へのpointerから派生した型に限定する。object/void/incomplete-record pointerのfile/local/member/parameter/typedef/type-nameはpositive fixtureで維持し、scalarとfunction pointerは直接宣言、prefix、typedef経由、type-name、file/local/member/parameter/typedefの各文脈で拒否する。12件をshould_reject正本からNative/Wasm object compile-failへ登録し、E3064/E3117診断parityを固定する |
| **静的初期化の論理演算・条件演算子の選択評価** `0 && (1 / 0)`, `1 || (1 / 0)`, `1 ? 7 : (1 / 0)` | 🔧 | static_initializer_short_circuit / should_reject 6件 / compile_fail: static_{initializer,array_initializer,local_initializer}_evaluated_* | Syntax定数評価は短絡していたが、静的HIR評価が常に左右両辺を評価し、未評価であるべき右辺のゼロ除算を理由にE3116としていた。論理演算子と条件演算子は選択されたoperandだけを評価し、未選択側のゼロ除算はscalar、配列、struct、static local、enum、`_Static_assert`で合法として維持する。逆に`1 && E`、`0 || E`および条件演算子のtrue/false各選択branchでゼロ除算が評価される6件は、global scalar・array・block-scope staticをshould_reject正本からNative/Wasm object compile-failへ登録してE3116 parityを固定する |
| **空initializer・静的記憶域initializerの定数制約** `int value={}`, `static int copy=source`, `static int *p=&automatic` | 🧪 | should_reject: empty_{object,compound_literal}_initializer, file_scope_compound_literal_{nonconstant_initializer,qualifier_discard}, static_{scalar,aggregate,floating}_object_value_initializer, static_complex_function_value_initializer, block_static_{scalar,aggregate}_object_value_initializer, block_static_{compound_literal,automatic}_address_initializer, block_static_complex_function_value_initializer | C11で空のbrace initializerを拒否し、file-scopeおよびblock-scope static initializerを算術定数式・null pointer定数・address constant・address constant±整数定数式へ限定する。object値、automatic objectのaddress、block-scope compound literalのaddress、function callによるcomplex値をE3116で拒否し、compound literalの`const`をmutable pointer初期化で破棄する経路はE3078とする。13件をClang strictと照合し、should_reject正本からNative/Wasm object compile-failへ登録する |
| **静的初期化の浮動条件・比較・scalar変換** `1.0 ? 3 : 4`, `1.0 || E`, `2.0 > 1.0`, `(_Bool)-0.5`, `(double)(1ULL << 63)`, `(unsigned long long)9223372036854777856.0` | 🔧 | static_initializer_floating_conditions | 静的HIR評価が条件と比較の被演算子を常に整数値として読み、浮動条件の三項選択を誤り、浮動短絡では未評価右辺まで評価していた。さらに比較・論理・整数三項の結果を`double`へ変換する初期化では変換元の整数HIRまで浮動式として読んで0を生成し、浮動値から`_Bool`への変換では先に整数へ切り捨て、64-bit符号なし整数と浮動値の相互変換では同じビット列を符号付き値として読んでいた。canonical HIR型から整数/浮動評価を選び、`_Bool`は浮動値を直接0.0と比較し、整数と浮動値の変換は変換元・変換先のcanonical型とDataLayoutを使う。scalar、double結果、mixed比較、配列、struct、static localを網羅 |
| **静的アドレス定数式の条件演算子** `static int *p = 1 ? &a[1] : &a[4]` | 🔧 | static_address_constant_expressions / compile_fail: static_address_selected_{function_call,assignment} | C11 strictで合法な条件演算子を含むアドレス定数式を、静的HIRのアドレス解決が扱わずE3116としていた。条件を型付き定数評価して選択された枝だけを再帰的にアドレス解決する。配列・record member・文字列・関数・null pointer、整数/浮動条件、ネスト、未選択枝の関数呼出し・代入・ゼロ除算の非評価、global aggregate、static localを網羅し、選択された非定数枝は引き続き拒否する |
| **静的初期化のポインタ比較定数** `static int same = &a[0] == &a[0]` | 🔧 | static_pointer_comparison_constants | C11 strictで合法な同一配列・record内アドレス、同一関数、null pointerの比較を、静的HIR整数評価がアドレス被演算子を扱わずE3116としていた。両辺のsymbolとbyte offsetを解決して同一symbol内の`== != < <= > >=`を評価し、アドレス対nullとnull同士の等価比較も処理する。三項条件、配列初期化、static localを網羅 |
| **異なるsymbolの静的ポインタ等価比較** `static int different = &a != &b` | 🔧 | static_distinct_symbol_comparisons / compile_fail: static_cross_symbol_{one_past,order}_comparison | 異なるobject内部または異なる関数の`==/!=`はC11 strictで定数化できるが、ag_cは同一symbolだけを扱いE3116としていた。global objectのcanonical型をDataLayoutでsizeofし、offsetが`[0,size)`内のときだけ異なるsymbolを不等と確定する。異なる関数もoffset 0だけを確定し、配置依存のcross-symbol one-pastと無関係object間の順序比較はClang同様に拒否する |
| **静的pointer-to-integer symbol変換** `static uintptr_t address = (uintptr_t)&object` | 🔧 | static_pointer_to_integer_constants / compile_fail: static_pointer_to_narrow_integer, static_string_pointer_comparisons | 明示的なpointer-to-integer castはpointer幅以上の整数ならClang C11 strictでsymbol定数として受理されるが、ag_cは整数値評価だけを試してE3116としていた。明示castとcanonical型を確認し、DataLayout上の整数幅がpointer幅以上の場合だけ既存のsymbol relocationへ流す。object・member・array element・string・function、条件演算子、配列・struct・static localを網羅。narrow整数はコンパイル段階で拒否し、string pointer比較もClang同様に非定数として拒否する |
| **enumと実装選択整数型の互換性** `enum E *` ↔ `unsigned int *` / `int *` | 🔧 | enum_compatible_integer_types, enum_compatible_function_signature_xtu_{main,other} / compile_fail: enum_incompatible_{integer,nested_integer}_pointer, enum_negative_incompatible_nested_unsigned_pointer, distinct_enum_pointer_compatibility, generic_duplicate_{positive,negative}_enum_compatible_type, conditional_incompatible_{positive,negative}_enum_function_pointer, enum_{incompatible,distinct}_return_signature_mismatch_{main,other} / c-testsuite 00170 | 非負列挙だけのenumはunsigned int、負の列挙子を含むenumはintを互換型としてcanonical shapeに保持する。型互換判定をenum identityだけでなく選択整数型にも拡張し、object再宣言、関数戻り値・仮引数、qualified pointer・多重pointer・conditional operator・array pointer・function pointer conditional・`_Generic`選択と重複関連型制約へ再帰的に反映。Wasm objectのcanonical C signatureにもenum tag identityとcompatible integerの符号・幅を併記し、別TUのprototype/definition間で戻り型、値parameter、pointer、nested function pointerに現れるenum↔整数互換を再帰比較する。同じ整数型を選ぶ別enumのidentityは統合せず、反対符号の整数pointer・function pointer・戻り型も同じWasm `i32`へlowerされても引き続き拒否する |
| **Atomic wrapper内のenum互換性** `_Atomic(enum E *)` ↔ `_Atomic(unsigned int *)` | 🧪 | atomic_enum_{function,container,callback_signature,callback_data_signature,pointer_to_array_signature,array_data_signature,record_function_signature,record_data_signature,self_record_signature,mutual_record_signature,union_function_signature,anonymous_union_data_signature,flexible_record_signature,incomplete_record_signature}_xtu_{main,other} / function_parameter_atomic_{pointer,pointee}_distinct_enum_mismatch_{main,other}, function_return_atomic_enum_integer_mismatch_{main,other}, global_record_atomic_enum_integer_mismatch_{main,other}, function_{parameter_atomic_callback_enum_parameter,return_atomic_callback_enum_result}_mismatch_{main,other}, {global_atomic_callback_enum_parameter,record_member_atomic_callback_enum_result}_mismatch_{main,other}, function_parameter_{atomic_pointer_to_enum_array_distinct,pointer_to_atomic_enum_array_integer}_mismatch_{main,other}, {global_multidimensional_atomic_enum_integer,record_member_atomic_pointer_to_enum_array_distinct}_mismatch_{main,other}, function_{parameter_atomic_pointer_record_distinct_enum_member,return_atomic_pointer_record_enum_integer}_mismatch_{main,other}, {global_atomic_pointer_record_distinct_enum_member,global_atomic_record_pointee_enum_integer}_mismatch_{main,other}, function_parameter_atomic_{self_record_distinct_enum,mutual_record_enum_integer}_mismatch_{main,other}, global_atomic_{self_record_enum_integer,mutual_record_distinct_enum}_mismatch_{main,other}, function_{parameter_atomic_pointer_union_distinct_enum_member,return_atomic_union_pointee_enum_integer}_mismatch_{main,other}, global_atomic_{pointer_anonymous_union_distinct_enum,anonymous_union_pointee_enum_integer}_mismatch_{main,other}, function_{parameter_atomic_pointer_flexible_distinct_enum,return_atomic_pointer_flexible_enum_integer}_mismatch_{main,other}, global_atomic_pointer_{flexible_distinct_enum,anonymous_flexible_enum_integer}_mismatch_{main,other} | enumと実装選択整数型の互換性をAtomic qualifierの内側へ再帰させる。atomic pointer、pointer-to-atomic enum、atomic function result、atomic array element、record member、atomic function pointerのcallback parameter/result、atomic pointer-to-array、pointer-to-array-of-atomic-enum、Atomic pointer先のnamed record body、Atomic pointerで形成される自己・相互再帰record cycle、member順不同のnamed union、structuralに比較するanonymous union、atomic enum flexible array member、およびincomplete→complete record refinementを含む一致例はClang strict・Native・WAT・object link/runで42を返す。同じWasm `i32`またはpointer幅でも、Atomic wrapper内のdistinct enum tagと反対符号整数、record member内の非互換enum/integer、atomic callback内部のdistinct enumと反対符号integer、多次元arrayとpointer-to-array内部の非互換enum、Atomic pointer先record member、再帰cycle内member、named/anonymous union member、およびnamed/anonymous flexible array elementのdistinct enumと反対符号integerをobject linkerで拒否する |
| **符号付き整数定数式の表現可能範囲** `INT_MAX + 1`, `INT_MIN / -1`, `2 << 31` | 🔧 | signed_integer_constant_boundaries / compile_fail: signed_constant_{add,sub,mul,negate,div,mod,shift}_overflow, signed_constant_{add_underflow,sub_negative_overflow,mul_negative_rhs_overflow,mul_both_negative_overflow,negative_shift}, signed_long_long_{add,div,shift}_overflow, {static_assert,alignas,bitfield_width,array_bound}_signed_overflow | typed整数定数評価がhostのunsigned演算で安全にwrapした後、signed結果までそのまま受理していた。対象型とDataLayoutからsigned最小・最大を求め、32/64-bitの加減乗除・剰余・単項minusと左shiftの未定義overflowを全operand符号方向で検出し、enum・`_Static_assert`・`_Alignas`・bit-field幅・配列境界で拒否する。C11で有効な`1 << 31 == INT_MIN`と`1LL << 63 == LLONG_MIN`、未選択の短絡operandを含む表現可能な境界値はnative/Wasmで維持する。32-bit 12件と64-bit 3件のshould_reject正本をNative/Wasm object compile-failへ登録し、E3064診断parityまで固定する |
| 符号付き除算・剰余（負値, long） | ✅ | integer_runtime_arithmetic_boundaries | 正負operand全組合せの0方向丸めと剰余符号、long/long long境界、`/=`・`%=`をnative/Wasmで固定 |
| **Atomic enum recordのpack・alignment互換性** `_Atomic(struct P *)` | 🧪 | atomic_enum_{packed,aligned}_record_signature_xtu_{main,other} / function_parameter_atomic_pointer_{packed,aligned}_enum_layout_mismatch_{main,other}, global_atomic_pointer_{packed_record_distinct_enum,aligned_record_enum_integer}_mismatch_{main,other} | enumとcompatible integerをmemberに持つpacked/aligned recordは、同一layoutならClang strict・Native・WAT・object link/runで受理する。packまたはmember alignment差はABI layoutとして、同一layout内のdistinct enumまたは反対符号integerはcanonical C型としてobject linkerが拒否する |

### 浮動小数（float / double）
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| 配列メンバ access/init | 🔧 | float_array_member, 10b9748 | |
| 多次元 subscript の fp load | 🔧 | multidim_float_array_subscript, 10d291d | |
| ブール条件分岐 / `&&` `||` | 🔧 | ec96e30, 87a24a1 | |
| static ローカル float init | 🔧 | f873777 | |
| fp ポインタ仮引数 `double *a` の deref/subscript | 🔧 | fp_pointer_parameter | |
| 匿名 struct/union メンバの fp 昇格 | 🔧 | anon_member_fp_unsigned_promote | |
| 可変長引数の double | ✅ | function_default_argument_promotions, variadic_via_func_pointer | floatからdoubleへのdefault promotionと、直接・関数ポインタ経由のdouble `va_arg`をnative/Wasmで固定 |
| double 比較・三項 | ✅ | floating_runtime_boundaries | 全比較演算、double/float混在三項、負のゼロの条件・選択をnative/Wasmで固定 |
| NaN / Inf の比較（`nan!=nan`, `1/inf`, denormal） | ✅ | floating_runtime_boundaries | NaNのunordered比較とscalar条件、正負Inf、double/float最小subnormalの生成・underflowをnative/Wasmで固定 |
| 単項マイナスの -0.0（`-0.0`, `-z`, `1.0/-0.0`=-inf） | 🔧 | fp_unary_minus_neg_zero | ND_FNEG/IR_FNEG。従来は `0.0-x` で符号消失 |
| 負値の浮動小数点グローバル初期化子 `float g=-1.0f` | 🔧 | negative_fp_global_init | psx_eval_const_fp が ND_FNEG 未対応で畳み込み失敗→スカラ .comm(0)/配列要素 0。ND_FNEG 畳み込み＋fp配列要素の非ND_NUM評価を追加 |
| Wasm object の global FP scalar/array data | 🔧 | test_wasm32_object: global_fp_scalar, global_fp_array | file-scope `float`/`double` scalar と配列初期化を object data segment の IEEE754 bytes として固定。scalar は `gv->init_count` を使わない別分岐なので専用 fixture で確認 |
| 同梱 `<complex.h>` (I/creal/cimag/conj/cabs/carg、算術) | 🔧 | stdheader/complex_ops | _Complex brace初期化・複素数compound literal、I={0,1}、component/value関数、初等関数とネイティブ複素算術を提供する。値渡し・値返しと関数pointer ABIは後続の専用fixtureで固定 |
| GNU `__real__` / `__imag__` 演算子 | 🔧 | real_imag_operators | 複素数式を temp slot に materialize し re/im を fp load。実数は x/0。rvalue 可 |
| `double _Complex` ↔ `float _Complex` 変換 | 🔧 | complex_float_double_convert | build_complex_to が成分の fp 種別を無視して memcpy し虚部が壊れていた。源 fp で materialize→各成分 F2F 変換 |
| `_Complex` の値渡し / 値返し ABI | 🔧 | complex_by_value_abi | HFA として re→d0/s0, im→d1/s1 で受渡し。param 受取(2 IR_PARAM)・return・呼出引数(2 FP 展開)・戻り値受取(d0/d1→slot) を実装。double complex はポインタ渡し、float complex は 1 レジスタで虚部喪失していた |
| 複素数初等関数 cexp/clog/csqrt/cpow/csin/ccos/ctan 等 | 🔧 | stdheader/complex_ops | 値渡し/値返しが効くので static 関数で実装。実部/虚部は <math.h> 経由。libm 同名を static で隠し ag_c/clang 同一実装で一致 |
| 本物の `<stdatomic.h>` (LSE アトミック命令) | 🔧 | stdheader/stdatomic_ops | 単一スレッド退化版から、Apple ARM64 LSE 命令 (ldaddal/ldsetal/ldclral/ldeoral/swpal/casal/ldar/stlr/dmb ish) による真のアトミックへ。fetch 系は規格通り旧値を返す。__ag_atomic_* 組込み + IR_ATOMIC + 1/2/4/8 バイト codegen。全 seq_cst 強度 |
| **fp 実引数 → 整数仮引数** の暗黙変換 `ei(7.9)` (直接呼出) | 🔧 | fp_arg_to_int_param | C11 6.5.2.2p7。ag_c は double/float 実引数を fp レジスタ(d0)に置いたまま callee を呼び、callee が整数レジスタ(x0/w0)から読みゴミ値に。callee の整数スカラ仮引数幅 (4/8) を semantic_ctx に記録 (param_int_sizes、既存 param_fp_kinds と排他) し、呼び出し側 build_node_funcall で実引数が fp なら F2I(fcvtzs) を挿入。long 仮引数は i64 幅で変換 (i32 だと大値が wrap)。同一 TU 定義関数で有効。**符号性は fcvtzs 固定** (fp→unsigned の大値は既存 F2I の制約) |

### 関数ポインタの FP 戻り値（`d0` で読む）
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| funcptr 変数 / 仮引数 / typedef / `(*p)()` | 🔧 | funcptr_fp_return, 0b980b0 | |
| 関数ポインタ配列の要素 `ops[i]()` | 🔧 | funcptr_array_fp_return, 45bd478 | |
| struct メンバ funcptr `s.f()` / `sp->f()` | 🔧 | funcptr_member_fp_return, 20c4b17 | nested struct / struct-array member (`h.ops[i].f`, `h.p->f`) も追加確認 |
| struct の先頭 aggregate funcptr メンバと `{0}` 初期化 | 🔧 | struct_funcptr_zero_init, struct_funcptr_designated_zero_init, nested_struct_funcptr_designated_zero_init | `struct Holder h = {0};` で先頭メンバが `struct Ops ops[2]` のとき、明示 `0` が 16B aggregate 代入として IR に渡り E4007。struct 全体の pre-zero-fill 後、terminal `0` は no-op として消費し、synthetic lvar の `tag_kind` 既定値も `TK_EOF` に初期化。designator 経由の `.ops={0}`、nested struct/union designator、global/static local も Wasm object scan 対象 |
| グローバル funcptr `gops()` | 🔧 | funcptr_global_fp_return, ada7696 | |
| グローバル funcptr **配列** `gops[i]()` の fp 戻り (N>=2) | 🔧 | funcptr_global_array_fp_return, e62862e | |
| 要素数 1 の括弧内配列グローバル `(*g[1])()` / `(*g[1])` | 🔧 | global_size1_funcptr_array | paren 内 `[1]` の有無で配列判定。funcptr/ポインタ両方 |
| 間接呼び出しの int→fp 引数昇格 (直書き funcptr) `fp(3)` | 🔧 | funcptr_int_to_fp_arg | 宣言時に skip_func_params で各仮引数の fp 種別を funcptr_param_fp_mask に記録し、parse_call_postfix で fp 仮引数の実引数を wrap_to_fp(ND_INT_TO_FP) でラップ。float/double/混在/(*fp)() 対応 |
| **typedef 関数ポインタ経由の int→fp 引数昇格** `typedef double (*Op)(double); Op op; op(3)` | 🔧 | typedef_funcptr_int_to_fp_arg (続き107) | 直書き funcptr と違い typedef に仮引数 fp マスクを保存せず、整数実引数を x0/w0 に置いたまま間接呼び出ししていた。`psx_typedef_info_t` と local/global funcptr 変数に mask を伝播し、`ND_LVAR` / `ND_GVAR` 呼び出しで wrap_to_fp。複数 typedef が続いた後に直近 typedef の stale mask が漏れないよう、直書き宣言子だけ `psx_last...` を優先 |
| **struct メンバ関数ポインタ経由の int→fp 引数昇格** `ops.f(3)` / `ops.arr[i](3)` | 🔧 | funcptr_member_int_to_fp_arg (続き108) | callee が `ND_DEREF` になるため local/global funcptr の mask 参照に乗らず、整数実引数を x0/w0 に置いたままだった。tag member と `node_mem_t` に funcptr_param_fp_mask を伝播し、`ND_DEREF` callee でも wrap_to_fp。併せて brace 初期化で funcptr メンバの戻り fp_kind をメンバ自身の FP 型と誤解し、関数アドレスを double 化して格納していた問題を修正。直書き/typedef/配列メンバ・`.`/`->` を網羅 |
| **関数ポインタ経由の fp 実引数→整数仮引数変換** `fp(7.9)` / `ops.f(7.9)` | 🔧 | funcptr_fp_to_int_arg (続き109) | 直接呼び出しは `param_int_sizes` で F2I していたが、間接呼び出し用の funcptr 型には整数仮引数幅を保存しておらず、FP 実引数を d0/s0 に置いたまま callee が x0/w0 を読んでいた。`funcptr_param_int_mask` を typedef/local/global/tag member/`node_mem_t` に伝播し、`ND_LVAR` / `ND_GVAR` / `ND_DEREF` callee で FP 実引数を `ND_FP_TO_INT` にラップ。直書き/typedef/struct メンバ/配列要素、int/long、FP 戻りとの混在を網羅 |
| 可変長プロトタイプの無名固定引数 `int printf(const char*,...)` | 🔧 | variadic_unnamed_proto_fixed_args | 固定引数数 0 誤算→crash。定義なし外部関数で顕在化 |
| **`void *` 戻り型を void 関数と誤判定** | 🔧 | void_ptr_return | stmt.c の return チェックが ret_token_kind==TK_VOID のみ見て is_pointer 無視→`void *f(){return p;}` が E3005 で弾かれ malloc 風関数が書けず |

### ポインタ / 配列 / subscript
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| 多段ポインタ subscript / pql 減算 | 🔧 | d0ff96c, 8e937e0 | |
| struct/union ポインタ算術スケール | 🔧 | b6a42ec | |
| struct ポインタ配列メンバ `h.arr[i]->v` | 🔧 | struct_ptr_array_member_access, db98d34 | |
| 多次元配列の行 decay `m[0]`, `**m`, `*(*(m+1)+2)` | 🔧 | a2a8328, 47975d4 | 3D まで |
| 局所「2D 配列へのポインタ」mid_stride | 🔧 | 5a47279 | |
| 配列へのポインタ（要素 >4B struct） | 🔧 | 10072cc | |
| インラインのポインタキャスト deref `*(int*)(cp+4)` | 🔧 | d44947f | |
| **グローバル data pointer の fp deref** `*dp`/`dp[i]` | 🔧 | global_fp_data_pointer_deref, 426ff01 | |
| **グローバル pointer-to-array** `(*pa)[N]` subscript | 🔧 | global_ptr_to_array_subscript, 9547728 | int/double |
| `(*dp)[j]` 単項 deref + subscript の fp load | 🔧 | ptr_to_array_deref_fp, 8cf0749 | local/global |
| **配列へのポインタ経由の struct メンバ** `struct S (*ap)[N]; (*ap)[i].m` | 🔧 | ptr_to_array_struct_member | `(*ap)[i].m` の `.` が E3005 で弾かれていた。ap (lvar) は tag_kind=STRUCT を持つが is_tag_pointer=0 (ポインタ-to-配列であって tag ポインタでない) のため、build_unary_deref_node 冒頭の `is_tag_ptr` ガードで tag が deref ノードに carry されない。subscript 段で psx_node_get_tag_type が TK_EOF を返し member access が E3005。outer_stride+!is_array 経路で tag を carry するよう修正 (is_tag_pointer=0: 結果は配列、要素が struct)。struct 値コピー `s=(*ap)[1]` (元から動作)、メンバ read/write・仮引数経由・2D `(*ap2)[i][j].m`・union 要素 `(*up)[i].s.a`・グローバル `gap` を網羅 |
| **struct メンバ `int (*p)[N]` (配列へのポインタ)** `struct H { int (*p)[3]; }` | 🔧 | struct_ptr_to_array_member (続き70) | parser が struct メンバ宣言子 `int (*p)[N]` を `int *p[N]` (配列要素はポインタ) と区別できず両方 paren_array_mul=1, is_ptr=1, trailing-array=N として解析。`sizeof(struct H)` が 8 ではなく 24 (3 ポインタ分の誤レイアウト)、`(*g_h.p)[i]` が SIGSEGV (`*g_h.p` を int 1 個 load → スカラ + i deref)。修正: member_decl_head_t に ptr_in_paren を追加し parse_member_decl_name_recursive で `(` 内 `*` 消費を持ち回す。ptr_in_paren=1 && paren_array_mul=1 のとき trailing `[N]` は pointee dim としてメンバ自身は単一ポインタ (8B)、pointee 全バイト数 (N*elem) を tag_member_info.outer_stride に保存。build_member_deref_node に pointer-to-array 分岐 (is_tag_pointer && outer_stride>0 && array_len==0) を追加しローカル `int (*p)[N]` lvar と同じ表現 (deref_size=outer_stride、inner_deref_size=elem) で ND_DEREF を組む。build_unary_deref_node に ND_DEREF operand 用の対応分岐 (is_tag_pointer && inner_deref_size>0 && deref_size>inner_deref_size) を追加し新 deref の deref_size を elem に再設定 → subscript_base_address_of が lhs を返し `(*s.p)[i]` がポインタ load+elem ストライド add で解決。グローバル/ローカル struct、read/write、`->` 経由を網羅 |
| **struct メンバ `int (*p)[M][N]` (2D pointee の pointer-to-array)** `struct H { int (*p)[2][3]; }` | 🔧 | struct_ptr_to_2d_array_member (続き72) | 続き70 の延長。sizeof は正しかったが pointee の dims 情報 (M, N) が outer_stride に「全バイト数」しか保存されておらず、`(*g_h.p)[i][j]` の 1 段目 subscript で elem 段の stride になり SIGSEGV/誤値。修正: struct_layout.c で pointee_arr_dim_count / pointee_arr_first_dim を保存し、2D pointee のとき psx_ctx_set_tag_member_mid_stride で mid_stride (= N*elem) も保存。build_member_deref_node の pointer-to-array 分岐で mem_info->mid_stride>0 のとき deref->inner_deref_size に mid_stride、deref->next_deref_size に elem を置きローカル `int (*p)[M][N]` lvar 表現と整合。build_unary_deref_node の続き70 分岐 (ND_DEREF operand) で probe->next_deref_size>0 を見て結果 deref の inner_deref_size に carry。グローバル/ローカル struct、read/write、`->` 経由を網羅 |
| **struct メンバ `int (*p[M])[N]` (array-of-pointer-to-array)** `struct H { int (*p[2])[3]; }` | 🔧 | struct_array_of_ptr_to_array_member (続き71) | 続き70 の延長。parser が `int (*p[M])[N]` を `int *p[M*N]` と区別できず arr_size=M*N として解析、sizeof が 8*M*N (M=2,N=3 で 48) と誤レイアウト、`(*g_h.p[i])[j]` も SIGSEGV/誤値。修正: struct_layout.c の ptr_in_paren && paren_array_mul>1 分岐を追加し arr_size=M, pointee 全バイト数 (N*elem) を新フィールド `ptr_array_pointee_bytes` に保存 (tag_member_info_t / tag_member_t / node_mem_t に追加、setter / fill / 匿名 struct 昇格伝播を追加)。build_member_deref_node の array_len>0 && is_tag_pointer 分岐で deref に carry。build_subscript_deref に新分岐: base ND_DEREF.ptr_array_pointee_bytes>0 のとき結果 deref を single pointer-to-array 形 (is_tag_pointer=1、deref_size=ptr_array_pointee_bytes、inner_deref_size=elem、pql=0) に組み直し、続き70 の build_unary_deref_node の pointer-to-array 分岐に乗せる。グローバル/ローカル struct、read/write、`->` 経由、`int *q[M]` との区別保持を網羅 |
| **ローカル `int (*p[M])[N]` (array-of-pointer-to-array)** | 🔧 | local_array_of_ptr_to_array | struct メンバ版の `ptr_array_pointee_bytes` が lvar 側になく、`int (*p[2])[3] = {a,b}; p[0][0][0]` が配列スロット内のポインタ値ではなくスロット自身を基点にして誤値。lvar_t に ptr_array_pointee_bytes を追加し、識別子参照/配列 decay へ伝播。`build_subscript_deref` は中間行では ptr_array_pointee_bytes/base_deref_size を carry、最終次元では pointer-to-array 値として組み直す。1D/2D 配列、direct `p[i][j][k]`、explicit `(*p[i])[j]`、書き込みを網羅 |
| **typedef chain で基底が配列の場合の dims 合成** `typedef int Row[3]; typedef Row Matrix[2]` | 🔧 | typedef_array_chain | Matrix が int[2] として登録され sizeof(Matrix)=24 のはずが 8、`Matrix m={{1,2,3},{4,5,6}}` も E3064。トップレベル/関数内の両方で base typedef の array_dims (= [3]) と declarator の dims (= [2]) を [declarator..., base...] の順で結合するよう修正 (parser.c の define_toplevel_typedef_from_declarator と stmt.c の parse_typedef_decl)。**ついでに**: stmt.c parse_typedef_decl が通常の配列 typedef `typedef int Row[3]` でも is_array=1 を立てておらず (トップレベル parser.c とは非対称)、関数内 `Row r={1,2,3}` が E3064 で弾かれていた回帰も同時に修正 (is_plain_array 分岐を追加)。3 段 chain (A→B→C)・基底が多次元 (`typedef int M23[2][3]; typedef M23 M4[4]`)・declarator が多次元 (`typedef Row Cube[2][5]`)・グローバル変数・flat init・関数内 chain を網羅 |
| **グローバル多次元 pointee** `(*pa)[N][M]` | 🔧 | global_ptr_to_multidim_array, eb74293 | int/double, 4D まで |
| 変則形 `*(t+N)[K]`（pointer-to-array に subscript+deref 混在） | 🔧 | ptr_array_arith_subscript_deref | `*((t+N)[K])`。make_subscript_scaled_offset に ND_ADD/ND_SUB 分岐を追加し、ポインタ算術 base からポインタ被演算子の inner_deref_size 等を引き継ぐ。これがないと `(t+N)[K]` が配列へ decay せずスカラ load→外側 `*` が値をアドレス deref して SIGBUS。スカラポインタは inner=0 で無影響 |
| typedef 配列へのポインタの stride `typedef int R[3]; R *p` (1D/2D) | 🔧 | typedef_array_pointer_stride, typedef_pointer_element_array_sizeof | is_pointer+td_array 分岐で outer_stride/mid_stride を設定。pointer_qual_levels を立てない (= 多段ストライド連鎖を使う) ことで 2D `m23 *q; q[i][j][k]` の深い添字も正しく動く。`typedef BinOp OpArr3[3]; OpArr3 *pa; (*pa)[i](...)` は typedef の要素関数ポインタ分まで pointer level に数えて分岐から外れ、関数コードを int として読んで SIGBUS していたため、配列 typedef 自体に宣言子 `*` を足した形も stride 分岐に入れる |
| typedef 自体が配列へのポインタ `typedef int (*PA)[3]; PA p` (局所使用) | 🔧 | typedef_ptr_to_array | 上の `R *p` と別経路。typedef 定義 (toplevel: parser.c / 関数内: stmt.c) が is_ptr のとき括弧の後ろの `[3]` (ポインティ extent) を捨てていて、`p+1`/`p[i]` が 1 行でなく要素 1 個 (4B) しか進まなかった。`*` が括弧内 (ptr_in_paren) のとき `[3]` をポインティ dims として記録し、resolve_typedef_array_dims の is_array ゲートを外して宣言側の `is_pointer && td_array_dim_count>0` 分岐に乗せる。多次元ポインティ `(*PB)[2][3]` も対応 |
| ポインタ typedef を基底にした**グローバル変数** `typedef int *PI; PI gp` | 🔧 | global_pointer_typedef | gp が int スカラ登録 (sizeof=4, `gp[i]` で E3064)。parse_toplevel_decl_after_type のオブジェクト経路が base_is_ptr を 0 固定で宣言子へ渡し、typedef 基底のポインタ性を捨てていた (typedef 経路は渡していた)。`g_toplevel_decl_base_is_ptr` を渡すよう修正。int/char/long/unsigned/double*・struct ポインタ・pointer-to-array (PA/PB) を網羅。double は pointee fp_kind を実効段数で判定して伝播、pointer-to-array typedef グローバルは typedef のポインティ dims から outer_stride を設定 (直書き `int *gp`/`int(*gp)[3]` は宣言子の `*` で立つので不変) |
| 多段ポインタ typedef `typedef int **PP; PP p` (局所/仮引数) | 🔧 | multilevel_pointer_typedef | typedef がポインタ段数を bool でしか持たず `**p` が誤 deref→SIGSEGV。typedef_name_t に pointer_levels を追加し、定義時 (toplevel: parser.c / 関数内: stmt.c・decl.c) に「基底段数+宣言子 prefix `*` 数」を 2 段以上だけ記録、宣言時 (decl.c) に getter で取得して total_pointer_levels/pql に反映。直書き `int **p` と同一ノード属性になる。2/3 段・合成 `typedef PI *PP2`・仮引数を網羅 |
| グローバル変数の多段ポインタ `int **gp; **gp` (直書き/typedef経由) | 🔧 | global_multilevel_pointer | 第1 deref `*gp` が int* (8B) でなく int (4B, ldrsw) ロードされ、続く deref が壊れた値を deref→SIGSEGV。register_toplevel_global_decl がポインタ deref_size を常に要素サイズにし、global_var_t が段数を持たなかった。global_var_t に pointer_qual_levels を追加 (宣言子 `*` 数 + 基底ポインタ typedef 段数)、try_build_global_var_node が pql>=2 のとき参照ノードに deref_size=8/base_deref_size=要素/pql を立てローカル `int **lp` と同一表現に。int/char/struct・3段・`(*gp)[i]` 添字・`*gp=` 代入を網羅 |
| 多段ポインタの fp pointee `double **p; **p` / `(*pp)[i]` 配列 decay 添字 | 🔧 | multilevel_pointer_fp_pointee, global_multilevel_pointer | fp_kind が多段ポインタへ伝播せず float がゴミ・double 書き込みが落ちていた。(1) 宣言時に多段ポインタへも最内 pointee_fp_kind を設定 (旧: total_pointer_levels==1 のみ)、(2) build_unary_deref_node / build_subscript_deref が pql を 1 段消費するとき pointee_fp_kind を結果へ引き継ぐ。続き223で global `double **gdp` / typedef `DPP gtdp` も修正: top-level のデータポインタ pointee_fp_kind 保存が実効段数 1 に限定されていたため、global 多段だけ最内 double 印を落としていた。double/float の read/write・3段・`*pp[i]`・`(*pp)[i]`・global 直書き/typedef を網羅 |
| struct 配列の部分/0/designator 初期化の 0 補完 | 🔧 | struct_array_partial_init | 部分初期化された struct 要素の残メンバが 0 補完されず garbage / ネスト struct 配列が ir_build 失敗 / `[i].field=` が E2006。配列全体を先に 0 埋め+明示初期化子で上書き、`[i].member` designator もパース対応 |
| **pointer先頭memberと後続配列の部分初期化** `struct S{const char *p; char c[3]; int n[2];}` | 🧪 | struct_pointer_array_partial_initializer_boundaries | string literalで先頭pointer memberを初期化した後も、後続するchar/int配列memberの省略要素と省略memberを型ごとの0で補完する。global、automatic、static local、compound literal、struct配列の入れ子、明示的な部分初期化と末尾member全省略をClang strict・Native・WAT・Wasm objectで固定する |
| 匿名 struct wrapper 越しの配列メンバ brace 初期化 `struct H{struct{int a[2];};} h={{{1,2}}}` | 🔧 | nested_struct_brace_elision, file_scope_ptr_from_array_compound (続き263) | 匿名 struct メンバは外側 tag へ昇格されるが、初期化子には匿名 aggregate 用 brace が残るため、`parse_member_initializer` が内側 `{1,2}` / `{a,b}` を「配列要素 1 個の scalar brace」として E3064。スカラ配列メンバで内側 brace が top-level comma/designator を持つ場合は、配列全体の wrapper として平坦化して読む。`[i]=` designator も網羅 |
| 2 段匿名 aggregate wrapper 越しの配列メンバ brace 初期化 `struct H{struct{struct{int a[2];};};} h={{{{1,2}}}}` | 🔧 | nested_struct_brace_elision (続き264) | 続き263 の wrapper 判定は内側 comma が top-level にあるケースだけを拾っていたため、もう 1 段匿名 wrapper が重なると `{1,2}` を scalar 要素 1 個の brace として読み E3064。配列メンバ body 側で配列全体 wrapper を再帰 unwrap。二重匿名 struct、匿名 union、struct 配列要素内の匿名 wrapper を網羅 |
| **グローバル struct 配列の flat brace elision + 未完了 `[]` 次元推論** `struct PT{long c[4];long b,e,k;} cases[]={1,2,...}` | 🔧 | global_struct_array_flat_elision | c-testsuite 00205。psx_gbrace_flat が配列 level の positional 初期化で scalar 1 個ごとに struct 要素境界へ揃えていたため、`c[1]` 以降が 0 になっていた。境界揃えは braced subinitializer の直後だけに限定。さらに `T a[]` の type_size 推論が flat scalar slot 数を外側要素数として扱い、`sizeof(cases)/sizeof(cases[0])` が過大になって余分な 0 要素を出力していたため、struct 要素は `global_flat_slot_count` で割り上げて外側次元を推論し末尾 slot を 0 補完。 |
| sized array 複合リテラルのアドレス `&(int[N]){...}` | 🔧 | addr_of_array_compound_literal | build_unary_addr_node の COMMA 分岐が rhs(既に ADDR)を二重ラップ→ir_build失敗。rhs に & ロジックを再帰適用 |
| 多次元/typedef 配列複合リテラルのアドレス `&(int[N][M]){...}` / `&(Row){...}` | 🔧 | addr_of_array_compound_literal | cast parser が raw `[N][M]` の 2 次元目を読まず、typedef 配列 (`typedef int Row[3]`) は配列情報を compound literal へ渡していなかった。さらに `&` 後に pointer-to-array の stride が 1 段ずれず、内側 subscript の幅が落ちる穴があった。cast type から dims を渡し、匿名 lvar/global に outer/mid/extra stride を設定、`&` で deref/inner/next をシフトするよう修正。raw 2D、typedef 1D/2D、struct 配列 typedef を網羅 |
| ファイルスコープのスカラ複合リテラルのアドレス `int *p=&(int){5};` | 🔧 | file_scope_addr_of_compound_literal | B6。ファイルスコープのスカラ複合リテラルが値 (ND_NUM) に短絡され `&` がアドレスを解決できず `.comm`(0)→`*p` が NULL deref で SIGSEGV。C11 6.5.2.5 で静的記憶域なので、`&` 配下のとき (g_addr_of_compound_pending) は gvar 実体を生成しアドレス初期化。int/long/char/unsigned/double/float・ポインタ配列要素を網羅。**根因の副次バグも修正**: 関数プロトタイプ/定義後に g_current_funcname を NULL に戻しておらず、`<assert.h>` の `__assert_rtn` 宣言後にファイルスコープ複合リテラルが「関数内」と誤判定されローカル lvar 経路に乗っていた |
| ファイルスコープの **struct/配列** 複合リテラルのアドレス `&(struct S){...}` | 🔧 | file_scope_aggregate_compound_literal_addr | ファイルスコープ分岐が単一スカラ (psx_expr_assign 1 個) しか扱えず `,` で E2006。集約 (struct/union/配列) のとき gvar 実体を作り、グローバル struct/配列と同じ psx_parse_global_brace_init_flat で brace 初期化を展開してアドレス可能なノードを返す分岐を追加。init_fvalues も確保して fp 配列に対応。struct/designator/ネスト/union/char配列/int・double 配列を網羅 |
| Wasm object の file-scope compound literal address data relocation | 🔧 | test_wasm32_object: global_compound_literal_addr_data_reloc | `&(int){7}` / `(int[]){...}+1` / `&(struct S){...}` の隠し `__compound_lit_*` local data symbol と `reloc.DATA` addend (`<__compound_lit_1>+0x4`) を固定 |
| ポインタ要素配列複合リテラル `(int *[]){&g,...}` / `(struct S *[]){...}` / typedef 経由 `(IP[]){...}` | 🔧 | file_scope_ptr_from_array_compound, test_wasm32_object: global_compound_literal_inner_ptr_data_reloc, typedef_compound_literal_inner_ptr_data_reloc | cast parser がポインタ型後続の `[]` を非ポインタ型だけに限定し、`(int *[]){...}` を compound literal と認識できず E3064。hidden global/lvar に「要素がポインタ」の metadata を載せ、`**ptrs` / `node_ptrs[i]->value` と Wasm object の data initializer 内 `&g` / struct pointer relocation を固定。続き259でローカル `struct Node *[]` compound literal が struct 値 brace 省略扱いになり、ARM64 が struct 内容を 32bit store して SIGSEGV する穴を修正。続き260で `int (*[N])[M]` / `typedef int (*RowPtr)[M]; RowPtr[]` の pointer-to-array 要素を追加し、cast type / local / global で pointer slot 8B と pointee array stride を分離。続き261で `struct H { RowPtr *rows; }` と `int (*rows[2])[M]` / `int (*(*rows)[N])[M]` 仮引数にも同じ metadata を伝播。続き262で `struct H { RowPtr rows[2]; }` の typedef pointer-to-array 配列メンバも array-of-pointer-to-array として登録。続き263で匿名 struct wrapper 越しの `struct H { struct { RowPtr rows[2]; }; } h={{{a,b}}};` も固定 |
| 大きいスタックフレーム（>4095B）の `sub sp`/`add x,x29,#off` | 🔧 | large_stack_frame | imm12 上限超を 4096 倍数部(lsl#12)+端数に分割。続き94で長大 00200 により vreg spill の `ldr/str [x29,#off]` が 32KB 超でも失敗することが露出したため、frame load/store helper で範囲外は `add x16,x29,#off; ldr/str [x16]` にフォールバック |
| ポインタ減算・比較 | 🔧 | pointer_arithmetic_comparison_boundaries | complete struct、pointer-to-array、VLA、qualified/atomic pointeeと、void/function/incomplete pointer比較境界をnative/Wasmで固定 |
| **ポインタ戻り関数の subscript/算術** `g()[i]` / `*(g()+i)` | 🔧 | func_pointer_return_subscript | parser がポインタ戻り値の pointee 型を覚えず ND_FUNCALL の deref_size=0。subscript/算術がスケールせず 1 バイト加算で miscompile (`int* g(); *(g()+3)` が 5120)、`double* g()` は戻り値を d0 から誤読し SIGSEGV、`unsigned char* g()` は符号拡張。修正: (1) semantic ctx の戻り値型 (tag/token_kind) から pointee サイズ/fp 種別を導出し ND_FUNCALL の deref_size/type_size/pointee_fp_kind アクセサに反映、(2) ポインタ返しの fp_kind を funcall ノードに立てない (戻り値は x0)、(3) 基底型 unsigned を ctx に保存し subscript の zero-extend に伝播 (戻り値そのものの符号化は is_pointer でガード)。int/long/char/unsigned char/short・double/float・struct(.member/->)・void* cast・ポインタ算術 (+/-)・変数添字を網羅 |
| **配列へのポインタを返す関数** `int (*f())[N]` の `f()[i][j]` | 🔧 | func_return_pointer_to_array | parse_func_declarator が pointee 配列次元 `[N]` を読み飛ばし、call ノードに行ストライド (N*elem) が無く base 要素 (4) で誤スケール→SIGSEGV (`int(*p)[N]=f()` 経由は動作)。semantic ctx に先頭次元 N を記録 (ret_pointee_array_first_dim)、ps_node_deref_size(ND_FUNCALL)=N*elem、subscript inner_ds=elem、build_unary_deref_node が `*f()` を要素サイズで解決。f()[i][j]・(*f())[j]・(*(f()+k))[j]・write・引数あり・double を網羅 |
| **配列へのポインタを返す関数の 2D pointee** `int (*f())[N][M]` の `f()[i][j][k]` | 🔧 | func_return_pointer_to_2d_array (続き111) | 続き19 の直接関数版を多次元 pointee へ拡張。先頭次元 N だけでは `f()[i]` の stride が N*elem になり、実際に必要な N*M*elem / M*elem / elem を carry できず SIGSEGV。parse_func_declarator が第2次元 M も ctx に記録し、ps_node_deref_size(ND_FUNCALL)=N*M*elem、subscript 結果へ M*elem と elem、`*f()` へ inner_deref_size を伝播。int/double、read/write、`(*f())[j][k]`、`(*(f()+i))[j][k]`、引数ありを網羅 |
| **配列へのポインタを返す関数ポインタ** `int (*(*fp)())[N]` の `fp()[i][j]` | 🔧 | funcptr_return_pointer_to_array (続き110) | 続き19 の間接呼び出し版。`ND_FUNCALL.callee != NULL` では関数名 ctx を引けず、callee 側に戻り値の pointee 配列次元/要素サイズが無いため `fp()[i][j]` が E3064 または誤スケール。さらに `double (*(*fp)())[N]` では callee の pointee_fp_kind を「戻り値が double」と扱い、実際はポインタ戻り値なのに d0 から読んで SIGSEGV。修正: function pointer 型に funcptr_ret_pointee_array_first_dim/elem_size を保存し、local/global/typedef/struct member から ND_FUNCALL indirect・subscript・`*fp()` へ伝播。pointer-to-array 戻りでは fp_kind を funcall 戻り値に立てず、要素 fp 種別として subscript に渡す。直書き・typedef・global・struct member、int/double、write、`(*fp())[j]`、`(*(fp()+k))[j]` を網羅 |
| **配列へのポインタを返す関数ポインタの 2D pointee** `int (*(*fp)())[N][M]` の `fp()[i][j][k]` | 🔧 | funcptr_return_pointer_to_2d_array (続き112) | 続き110 の間接呼び出し版を多次元 pointee へ拡張。function pointer 型が first_dim/elem_size しか保持せず `fp()[i]` の stride が N*elem になって SIGSEGV。funcptr_ret_pointee_array_second_dim を typedef/local/global/tag member/node_mem_t へ伝播し、ND_FUNCALL(callee) の deref_size/subscript/`*fp()` で N*M*elem / M*elem / elem を carry。直書き global/struct member では trailing `[N][M]` をオブジェクト配列ではなく戻り pointee 次元として登録。int/double、read/write、`(*fp())[j][k]`、`(*(fp()+i))[j][k]`、引数あり、typedef/global/struct member を網羅 |

### 集約初期化（C11 6.7.9）
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| 多次元配列メンバ / ネスト designator | 🔧 | 4b92768, aadf3b7 | |
| 重複 designator 後勝ち / 位置継続 | 🔧 | 4a5942d, df23e17 | C11 準拠 |
| struct 配列メンバ brace init | 🔧 | 1684a8c | |
| グローバル designator `.member[idx]`/`.member.sub` | 🔧 | global_designator_member_index, 1e843b4 | |
| ローカル designator の struct/union leaf brace init | 🔧 | local_designator_aggregate_leaf, 7e39081 | union leaf (`.arr[1] = {.n=7}`) は旧コメントで除外されていたが、union 配列要素 brace init 側が修正済みになったため同じ nested designator leaf 経路で `parse_union_initializer` へ委譲するよう拡張 |
| `_Bool` 初期化子の 0/1 正規化（全経路） | 🔧 | 5b3d592 | |
| グローバルポインタ配列 `&data[n]`/`data+n` | 🔧 | global_ptr_array_addr_init, 138cd70 | |
| Wasm object の global array address addend data relocation | 🔧 | test_wasm32_object: global_array_addr_addend_data_reloc | file-scope `int *p=arr+2` と struct pointer メンバ `arr+1` が `reloc.DATA` の `R_WASM_MEMORY_ADDR_I32` addend (`<arr>+0x8` / `<arr>+0x4`) 付きになることを固定 |
| Wasm object の global string offset pointer data relocation | 🔧 | test_wasm32_object: global_string_offset_data_reloc, global_struct_string_offset_data_reloc | file-scope `const char *p="abc"+1`、pointer 配列要素 `"de"+1`、struct pointer メンバ `"abc"+2` が `reloc.DATA` の `R_WASM_MEMORY_ADDR_I32` addend (`<.LC*>+0xN`) 付きになることを固定 |
| >8B struct の複合リテラル代入 `s=(struct S){...}` | 🔧 | compound_literal_struct_assign | ir_build_module failed。build_assign_struct が src に ND_COMMA(init,temp) 形を扱わず fail。init 評価後 temp を struct ソースに |
| >8B struct を返す関数呼び出しを分岐に持つ ternary 初期化/代入 `struct B x = c ? f() : g()` | 🔧 | struct_init_from_ternary_funccall | <=8B は既に ND_ASSIGN(var, ternary) で動いていたが、>8B は parser が E3064 で拒否していた。ternary の型サイズが代入先 struct サイズと一致する場合は ND_ASSIGN にし、IR の materialize_aggregate_expr_to が各分岐を dst へ materialize する経路に乗せる。funccall/funccall、funccall/lvar、初期化、既存変数への代入を網羅 |
| 2D char 配列の文字列リスト初期化 `char a[2][6]={"hello","world"}` | 🔧 | char_2d_array_string_init | 各文字列がスカラ/ポインタ扱いで行を埋めず壊れる。ローカル/グローバル両経路で文字列を行 (row 幅) のバイト列に展開し残り 0 埋め |
| **prefix付き多次元文字配列の文字列初期化と診断** `unsigned short a[2][4]={u"ab",u"c"}` | 🔧 | encoded_multidimensional_string_array_boundaries, encoded_string_row_designator_boundaries / should_reject 7件: encoded_{multidimensional_string,string_row_designator}_* | ordinary/u8/u/U/Lを2D/3D配列とglobal/local struct memberの各行へ展開し、外側推論・各行zero-fill・exact-boundをNative/WAT/Wasm objectで固定する。行・ネストmember designator、designator後のpositional継続、配列compound literalも同じ規則に従う。flat initializer plannerが過長文字列とprefix/element型不一致を一律NOT_SUPPORTEDへ潰していたため、global、local member、compound literalでは診断なしのdirect Syntax rejectionからE0006内部不変条件違反になっていた。失敗理由をTOO_LONG/INCOMPATIBLEとして保持し、E3027/E3099へ変換する。7件をClang strictと照合し、should_reject正本からNative/Wasm object compile-failへ登録する |
| union 配列要素の brace init `arr[2]={[1]={.n=5}}` / struct メンバ union 配列 `.u={[1]={.n=7}}` | 🔧 | union_array_brace_init | 旧 ⚠️(E3064)。E3064 は解消済みだが今度は値が格納されず 0 に化けていた (miscompile)。parse_array_elem_struct_brace_init が要素を常に parse_struct_initializer へ投げ、union 要素の `.n=5` を struct レイアウトで誤解決していた。続き216で struct メンバ配列版も同根と判明: parse_member_initializer が union 配列要素を parse_struct_initializer へ投げ、`.n=7` 後に未指定 `.l=0` が同じ offset を上書きしていた。要素が union のとき parse_union_initializer へ委譲。続き217で global struct メンバ union 配列の ARM64 data emitter padding も修正: active union メンバ出力後に union 要素サイズまで 0 padding せず、後続要素/tail が前へ詰まっていた。designator/positional/混在メンバ/部分初期化/local+global struct メンバ union 配列を網羅 |
| Wasm object の global `_Bool` scalar/array data | 🔧 | test_wasm32_object: global_bool_data | file-scope `_Bool` scalar と配列初期化が object data segment で 0/1 正規化されることを固定。`_Bool b=5` は `01`、`_Bool bs[3]={5,0,9}` は `0100 01` |
| **ローカル struct メンバ多次元 struct タグ配列の designator init** `struct Grid g = {.rows={[2]={[1]={.val=99}}}};` | 🔧 | local_struct_member_multidim_nested_designator (続き76/218) | 続き75 のローカル経路。関数内で designator (`[N]=`, `[M]=`, `.member=`) が「E3064: [primary] 数値が必要です」で拒否されていた。parse_member_initializer の outer_stride 経路が designator を全く扱わず、`[` を見ると parse_scalar_brace_initializer に投げて失敗。修正: 外側 brace 開始時に `[N]=` 検出で flat=N*inner_len、内側 brace で `[M]=` 検出で k=M、要素 tag が struct/union のとき nested lvar (offset/size/tag を 1 slot に絞る) で parse_struct_initializer を呼んで `.member=` も解決。続き218で 3D 以上も修正: 中間 brace を struct 要素として早く解釈せず、次元ごとに再帰して最下層だけ struct/union initializer に委譲。単一フィールド struct の positional は既存挙動を維持 |
| **グローバル struct メンバ多次元 struct タグ配列の内側 brace の designator** `g = {.rows={[2]={[1]={.val=99}}}}` / `g = {.rows={[2]={{.val=99}}}}` | 🔧 | global_struct_member_multidim_nested_designator (続き75) | 続き74 の延長。内側 brace で `.val=` や `[1]=` を使うと E3064。原因: parser.c の gbrace_child_at が sub_dims を 1 段消費する処理を `ctx.tag_kind == TK_EOF` 限定としていたため struct タグ多次元配列の内側 brace ctx が「単一 struct」になり、内側 designator が解決できなかった。修正: gbrace_child_at の sub_dims 処理を tag_kind 非依存にし、struct タグ配列でも中間段/最内 1 段で「内側次元の配列 (タグ継承)」ctx を返す。2D/3D struct タグ配列・`.member=` designator・`[N]=` designator・部分初期化を網羅。ローカル版も local_struct_member_multidim_nested_designator で対応済み |
| **グローバル struct メンバ 2D struct タグ配列の外側 `[N]=` designator** `struct S { struct C rows[3][2]; } g = {.rows={[2]={{99},{100}}}}` | 🔧 | global_struct_member_multidim_struct_array_designator (続き74) | 99/100 が rows[2] でなく rows[1] に書かれていた。3 経路の合わせ技: (1) struct_layout.c の arr_dims 保存条件 `member_tag_kind == TK_EOF` がタグ配列メンバを除外。修正: `!member_is_ptr` に緩和し struct タグ配列も保存。(2) parser.c gbrace_ctx_from_member の `mi->tag_kind == TK_EOF` 条件を `mi->arr_ndim >= 2` に緩和、struct タグ多次元配列も sub_dims を carry。(3) psx_gbrace_flat の `[N]=` elem_slots 計算で tag_kind==TK_STRUCT 経路でも sub_dims の積を掛けて内側次元を考慮。3D struct 配列も網羅。内側 designator `[2]={[1]=...}` と内側 brace 内 `.member=` は global_struct_member_multidim_nested_designator で対応済み |
| **グローバル plain 多次元配列の `[N]={[M]=V}` designator** `int g[3][2]={[2]={[1]=99}}` | 🔧 | global_multidim_array_nested_designator_plain (続き73) | 続き13 (struct メンバ多次元 designator) と類似だが、struct メンバではなくトップレベル plain 多次元グローバル配列で発生する別経路。psx_gbrace_flat の外側 `[N]=` の elem_slots 計算が ctx.sub_dims を見るが、トップレベル ctx 初期化 (psx_parse_global_brace_init_flat) が sub_dims を埋めておらず、`[2]=` が単一スカラ scale で誤ジャンプ、99 が g[1][1] に書かれていた。修正: gv->outer_stride / mid_stride / extra_strides / deref_size の隣接ペアから sub_dims を算出して ctx に埋める。2D/3D/4D 配列・positional 混在・designator なし完全 positional の不変を網羅 |
| グローバルのネスト brace 配列添字 `{.items={[2]={.a=7}}}` | 🔧 | global_nested_brace_designator | flat 初期化パーサがネスト brace の再帰に「その level の集約型」コンテキストを渡さず、`.member` designator を常に最外 gv の tag で解決して E3064。型コンテキスト gbrace_ctx_t を再帰へ渡し ctx に対して解決するよう変更。併せて (a) ネスト配列 `[N]=` の絶対 slot に level 起点を加算、(b) 配列レベル positional 要素の境界整列 (部分初期化 `{.a=1}` 後のズレ) も修正。ネスト struct `.s={.a}`・配列添字・positional 混在・fp メンバ網羅 |

### struct / union ABI・値渡し
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| 小さい struct（3/5/6/7B）の値渡し/返し | 🔧 | 050e1bf | |
| <=8B struct を ternary / funccall から初期化 | 🔧 | 96b5510, 7697cf8 | |
| **ARM64 aggregate 値渡し/戻り/可変長引数** (c-testsuite 00204) | 🔧 | arm64_aggregate_varargs | 3/7/9B と HFA 系 aggregate の global 値引数・global struct return source・variadic aggregate を網羅。IR return/funcall の ND_GVAR aggregate アドレス化、`va_arg(ap, struct S)` の `*(struct S*)...` を同型 struct copy として扱う deref 初期化、`(struct S*)` cast の deref_size、長さ 1 配列メンバの array_len 保持と式中 decay、variadic aggregate の 8B stack slot 展開、`va_arg` の rounded sizeof advance を修正。副作用として length-1 配列 decay は scalar pointer member / funcptr array / pointer-to-array と区別する条件を追加 |
| static struct/union 局所の永続化 | 🔧 | static_local_struct_persist, 8167e8e | 続き228で inline anonymous tag の struct/union/array も file-scope tag へ昇格して global lowering 対象化 |
| long bitfield（>32bit / ユニット跨ぎ） | 🔧 | 040da11 | |
| bitfield の符号（signed sign-extend / unsigned）・幅切り詰め | ✅ | bitfield_signedness_boundaries | 明示signed/unsignedの1/4/12/20-bit load、compound演算、unsigned切り詰め、nested storageをnative/Wasmで固定 |
| 非 bitfield メンバ直後の bitfield パッキング（`char c; int x:20`） | 🔧 | bitfield_pack_after_member | AAPCS: 収まれば同一 storage ユニットへ |
| pragma pack(1) と通常の混在 sizeof | ✅ | packed_bitfield_tail_layout, pragma_pack/{pack1_sizeof,pop_restores} | pack(1)/(2)のbit-field・tail offset・alignと、pop後の通常layout復帰をnative/Wasmで固定 |
| ゼロ幅 bitfield `:0` のユニット分離 | ✅ | zero_width_unnamed_bitfield_initializer, mixed_base_bitfield_tail_layout, packed_bitfield_tail_layout | 通常・mixed base・pack(1)での新規allocation unit、unnamed fieldと各storage durationの初期化を固定 |
| **over-aligned通常member前後のbit-field allocation unit** `_Alignas(16) unsigned anchor` | 🧪 | aligned_bitfield_record_layout_boundaries | bit-field列の直後に置いた通常memberの明示alignmentが先行allocation unitを終了しrecord全体のalignmentを引き上げること、その後のzero-width bit-fieldが次のnamed fieldを新しいunitへ送ることを固定する。`_Alignof`・`sizeof`・非bit-field memberの`offsetof`、global/automatic/static local、whole-record copy、nested record、配列stride、実アドレス整列をClang strict・Native・WAT・Wasm objectで照合する |
| enum 型 bitfield `enum E e:2` / `enum Byte e:8` の符号拡張境界 | 🔧 | bitfield_enum_and_static_init, enum_compatible_integer_types, enum_bitfield_extension_boundaries | tag-keyword分岐の一律signed扱いを解消した後もenumを一律unsignedにする暫定分岐が残っていた。compatible integer typeの選択結果を使い、非負列挙はunsigned、負の列挙子を含むenumはsignedとして抽出する。8-bit fieldの127/128/150/255と-128/-1をglobal・automatic・static local、配列、nested union、aggregate引数/戻り、switch、再代入で検査し、bit 7を持つ非負値のzero extensionと負値のsign extensionをClang strict・Native・WAT・Wasm objectで固定する |
| グローバル/静的 struct の bitfield 初期化子 | 🔧 | bitfield_enum_and_static_init | 同一 storage ユニットの先頭フィールドしか出力せず残り 0 (`{3,5}`→`.long 3/.long 5` で 0x53 でなく)。同一ユニットの bitfield を 1 整数に詰めて出力 |

| 整数リテラルの sizeof `sizeof(0)` | 🔧 | sizeof_int_literal | psx_node_type_size が ND_NUM に 0 を返し sizeof 既定 8 に落ちて `sizeof(0)`=8。ND_NUM を fp_kind/long サフィックスで判定 (int=4) |
| 文字列リテラルの sizeof がエスケープを生バイト数で数える / `sizeof(&array)` | 🔧 | sizeof_string_and_addr_of_array | `sizeof("\t")`=3 (raw 2+1)→デコード後長で。併せてグローバル `char g[]="a\tb"` の未デコード格納も修正。`sizeof(&arr)` は要素サイズ→`int(*)[N]` なので 8 |

### プリプロセッサ
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| `#if` 定数式のビット/シフト/剰余/三項 (`& | ^ << >> % ?:`) | 🔧 | pp_if_operators | 定数式パーサにレベル欠落で `#if (3&5)==1` 等が E1006。conditional/bitor/bitxor/bitand/shift と mul の % を追加 |
| `#if` 定数式の `&&`/`||`/`?:` 短絡評価 (未選択側のゼロ除算 skip) | 🔧 | pp_if_short_circuit | `#if 0 != (0 && (0/0))` 等で右辺が評価され E1006。未選択側も構文解析しつつ評価だけ抑止し、`(0 && (0/0)) || 1` の後続低優先演算子を過剰消費しないよう各右辺を正しい優先順位レベルで解析する。c-testsuite 00145 |
| **`#if` 整数tokenの `intmax_t` / `uintmax_t` 型保存** | 🔧 | pp_if_integer_type_boundaries | 整数tokenの`is_unsigned`/`uval`を捨てて全式をhost `long`で評価し、`-1 < 1U`、`~0U >> 63`、unsigned減算・除算、高bit ULL、三項演算子の共通型がC11 6.10.1と乖離していた。64-bit bit patternとsignednessを保持し、通常算術変換・論理/算術shift・比較・三項の共通型を適用する |
| `#if`の通常・escape・wide/UTF文字定数 | ✅ | conditional_directive_boundaries | `'A'`、`'\n'`、`L'Z'`、`u'B'`、`U'C'`が制御定数式の整数値としてClang strictと一致することをNative/WAT/object共通fixtureで固定 |
| `#if`の非整数literal・C式専用構文・不完全な短絡式・ゼロ除算 | 🔧 | should_reject 10件: preprocess_if_{floating_literal,string_literal,cast,comma_expression,sizeof,incomplete_logical_and,incomplete_logical_or,incomplete_conditional,division_by_zero,modulo_by_zero} | 浮動小数・文字列・cast・comma・`sizeof`はpreprocessingの整数制御定数式では使えず、`0 &&`、`1 ||`、`1 ? 1 :`も未評価側を含め構文上は完結が必要。10件をClang strictと照合したshould_reject正本へ移し、内部診断だったE1007/E1008/E1009/E1012を実際の不正tokenのfilename／line付きでNative/Wasm object双方から固定する |
| `defined`演算子の形式とoperand非展開 | 🔧 | conditional_directive_boundaries / should_reject 6件: preprocess_defined_{missing_operand,empty_operand,numeric_operand,nested_parentheses,missing_rparen,extra_tokens} | `defined name`と`defined(name)`、未定義名、macro置換される識別子自体の定義判定を共通fixtureで固定。operand欠落・空・数値・二重括弧をE1010、閉じ括弧欠落をE1011、余分tokenをE1012として拒否する6件をClang strictと照合したshould_reject正本へ移し、実際のoperandまたは指令終端のfilename／lineをNative/Wasm object双方で固定する |
| `#line` 指令の引数マクロ展開 | 🔧 | pp_line_macro_arg | `#line line` (`#define line 1000`) が raw TK_IDENT のまま無視され __LINE__ が更新されない。c-testsuite 00152 |
| **`#include`引数のmacro展開とfilename token境界** `#include HEADER` / `#include MAKE_HEADER(x)` | 🔧 | include_macro_expansion_boundaries / compile_fail: include_macro_invalid_rejected, include_{missing_filename,empty_quoted_filename,empty_angle_filename,angle_missing_gt,extra_tokens,concatenated_filename,macro_extra_tokens,macro_concatenated_filename,wide_string_filename,utf16_string_filename,utf32_string_filename,utf8_string_filename,macro_wide_string_filename,macro_utf8_string_filename}_rejected | streaming includeが指令行のraw tokenを直接filename parserへ渡し、C11 6.10.2p4で有効なobject macro、二段alias、function macroのstringize、macro生成`<stddef.h>`を空pathとしてE1001拒否していた。直接の`"..."`/`<...>`形でないpp-token形式を完全macro展開してからfilenameとして再解釈し、結果が単一の非空header形式であることを検証する。欠落・空・複数filename・余分tokenをE1001、閉じ`>`欠落をE1017で拒否する。prefix付き`L/u/U/u8"..."`も直接・macro生成ともheader filenameではないためE1001で拒否する。相対header・標準header・comment終端・型/値利用をNative/WAT/Wasm objectで固定する |
| **`#include`構文・path拒否の診断位置** E1001-E1004/E1017/E1032/E1034/E1036 | 🔧 | should_reject: preprocess_include_*.c / compile_fail: include_*_location_rejected | filename欠落・prefix不正・`>`不足・余分tokenだけでなく、absolute/parent path、allowlist外realpath、include深度・循環、file size、not-foundも内部診断経路を使いfilename／lineを失っていた。filename parserから実ファイル解決・include stackまでinclude directive tokenを渡し、余分tokenとmacro展開operandはその実token、行末不足はdirective tokenへ結び付ける。既存15構文形とnot-found・parent・absolute pathの18固定fixtureをNative/Wasm objectで共有し、直接形は`:1`、macro形は呼出し行`:2`まで検査する |
| **標準 `_Pragma` 演算子とmacro展開境界** `_Pragma(P)` / `#define DO(x) _Pragma(#x)` | 🔧 | pragma_operator_macro_boundaries / should_reject: pragma_operator_invalid | C99/C11 6.10.9 の演算子を通常識別子としてparserへ渡し、有効な標準CをE3005で拒否していた。括弧内を完全macro展開して単一文字列を要求し、`\"`/`\\`をdestringizeしたpragma token列を既存の`#pragma`処理へ接続する。直接・object macro operand・function macro replacement、encoding-prefix、`pack(push/pop)`のlayout復帰をNative/WAT/Wasm objectで固定し、不正形はE1043でNative/Wasm object双方から診断する |
| 定義済みマクロ `__LP64__` (ARM64 LP64) | 🔧 | pp_predefined_lp64 | Apple ARM64 ターゲットなのに未定義で c-testsuite 00212 が失敗 |
| C11実行環境・条件付き機能・Unicode macro `__STDC_HOSTED__` / `__STDC_NO_THREADS__` / `__STDC_UTF_16__` / `__STDC_UTF_32__` | 🔧 | predefined_environment_feature_macros | C11必須のhosted実装識別macro、`threads.h`を提供しない実装が示す条件付き機能macro、実装済みの`char16_t`/`char32_t`符号化を表すmacroが未定義だった。native/Wasmともhosted環境・thread未対応・UTF-16/UTF-32を`1`として公開し、UTF-16 surrogate pairとUTF-32 code pointの実値、提供済みのatomic・complex・VLAについて対応不可macroを誤定義しないことも固定する |
| 整数値predefined macroの型・完全綴り `__STDC__` / hosted・feature・target macro | ✅ | predefined_integer_macro_spelling_boundaries | `__STDC__`、`__STDC_HOSTED__`、`__STDC_NO_THREADS__`、`__STDC_UTF_16__`、`__STDC_UTF_32__`とtarget固有`__LP64__`/`__wasm32__`が、値1だけでなく`int`型のpp-number token `1`として展開されることを固定する。`_Generic`、二段stringize、token pasteによる`1U`の`unsigned int`型をClang strict・Native/WAT/Wasm objectで照合する |
| **`__STDC_VERSION__`の型・完全綴り** `201112L` | 🔧 | predefined_stdc_version_type_boundaries | 定義済みmacro生成が値だけを受け取り、C11 6.10.8.1で要求される`201112L`を`201112`という`int` tokenへ変えていた。整数macro生成で元綴りと`tk_int_size_t`を保持し、値、`sizeof`、`_Generic`による`long`型、二段stringize結果をClang strict・Native/WAT/Wasm objectで固定する |
| `__DATE__` / `__TIME__`の文字列契約と不変性 | ✅ | predefined_date_time_boundaries / should_reject: preprocess_predefined_macro_{define,undef}_{date,time} | 定義存在、`Mmm dd yyyy`の11文字、`hh:mm:ss`の8文字と時分秒範囲、同一翻訳単位内の一致、隣接文字列連結をNative/WAT/objectで固定する。通常式では`char *`へ配列変換し、address-ofでは`char (*)[12]` / `char (*)[9]`として元の文字配列型を保持することも`_Generic`で照合し、標準predefined macroのdefine/undefはE1049で拒否する |
| トップレベル宣言の関数プロトタイプと変数の混在 | 🔧 | mixed_decl_func_proto_and_var | `int f(int), g(int), a;` が funcdef 経路になり E2006。c-testsuite 00121 |
| pointer-to-function (戻り funcptr) の呼び出し / 連鎖呼び出し | 🔧 | func_returning_funcptr_call, func_returning_funcptr_chain | `(*p)(a,b)` と `(*(*p)(a,b))(c,d)` が二重ロードで SIGBUS (00124)。`go()()->zerofunc()` は 2 段目 funcall の戻り tag 未伝播で E3005 (00089)。typedef/関数の funcptr 戻り型と指し示す関数の戻り tag を記録し、間接 funcall の callee が funcptr 返し funcall のとき導出する |
| グローバル多次元配列の最外 `[]` 次元推論 + 部分行初期化 | 🔧 | global_incomplete_outer_array_dim | `int arr[][3][5]={...}` で行境界ずれ + 外側次元誤推論。c-testsuite 00151 |
| 可変長マクロの空 __VA_ARGS__ `F(a,...)`→`F(42)` | 🔧 | variadic_macro_empty_va | コール側引数チェック `parsed_args <= num_named` が空 va を E1024 で拒否。`< num_named` に緩め名前付き不足時のみエラー (clang/gcc 互換) |
| 文字列リテラルと stringize 結果の連結 `"a" S(b)` | 🔧 | string_concat_stringize | stringize 結果は char_width=0。parse_string_literal_sequence の幅比較が 0 を正規化せず 2番目以降で E3002。比較側も 0→CHAR 正規化 |
| `#` stringize が文字列リテラル引数のクォート/エスケープを落とす | 🔧 | stringize_string_literal | `STR("hi")` が `"hi"` でなく hi に。C11 6.10.3.2 通り囲み `"` を保持し内部の `"`/`\` の前に `\` を挿入 (token_text は引用符なし内容を返すため再構築) |
| `#` stringizeの空白・comment・単一空引数正規化 | 🔧 | macro_stringize_whitespace_boundaries | parameterを持つmacroの`F()`は1個の空実引数だが、引数収集が即時`)`を常に0引数として数えて`STRINGIZE_RAW()`をE1024で拒否していた。0-parameter macroだけは0引数のまま、parameterありではNULL placemarker 1個として数える。通常置換・`##`の左右・stringize、先頭末尾空白除去、token間の空白列/commentの単一space化、括弧内comma、直接stringizeと二段展開をpreprocessor unitおよびClang strict・native/WAT/objectで固定する |
| **`#` stringizeの文字定数・encoding prefix・再stringize** | 🔧 | macro_stringize_character_constant_boundaries | 文字定数tokenの元綴りにある`\`を生成文字列内で再escapeせず、`STR('\n')`が改行を含む文字列へ変化していた。文字定数と文字列リテラルの完全なpp-token綴りを再構築して`\`/`"`をescapeし、通常・escape・hex・wide/UTF文字定数、`L/u/U/u8`文字列prefix、macro引数の事前展開有無、生成済みstringize tokenの再stringizeをClang strict・Native/WAT/objectで固定する |
| **macro再定義の同一性制約** `#define VALUE (1 + 2)` の再掲 | 🔧 | macro_redefinition_identity_boundaries / should_reject 4件: preprocess_macro_redefinition{,_form,_parameter,_whitespace} | 既存macroを新しい`#define`で無条件に置換し、C11 6.10.3が要求するobject/function形式、function parameterの個数・綴り、replacement pp-tokenの個数・順序・綴り・token間空白配置の同一性を検査していなかった。同一定義、commentによる同じ空白分離、先頭末尾空白、空replacement、`#undef`後の別定義は維持し、不一致4件をClang strictと照合したshould_reject正本からE1044でNative/Wasm object双方へ登録する |
| **function-like macro parameter list制約** 重複名・comma・`__VA_ARGS__` | 🔧 | macro_parameter_list_boundaries / should_reject 7件: preprocess_macro_{duplicate_parameter,parameter_missing_comma,parameter_trailing_comma,parameter_list_unclosed,va_args_parameter,va_args_nonvariadic,va_args_name} | parameter parserがidentifier後のcommaを任意扱いし、`F(x x)`、`F(x,)`、重複parameter、行末まで閉じないlistを受理していた。さらに`__VA_ARGS__`を通常parameterや非variadic replacementとして定義時に制約検査していなかった。空・named・comment区切り・inline容量超え・標準variadicの正例を維持し、不正listをE1045/E1046、`__VA_ARGS__`誤用をE1047で拒否する。7件をshould_reject正本からNative/Wasm object compile-failへ登録する |
| **macro replacement operatorの定義時制約** `#` / `##` | 🔧 | macro_replacement_operator_definition_boundaries / compile_fail: macro_{stringize_{nonparameter,missing_parameter},function_paste_{leading,trailing},object_paste_{leading,trailing}}_unused_rejected | `#` operandと`##`位置をfunction-like macro展開時だけ検査していたため、未使用macroの`# 1`、末尾`#`、object/function replacement list先頭末尾の`##`を受理していた。body構築直後にfunction-like `#`の次tokenがparameterか、全macroの`##`が先頭末尾でないか検査し、前者をE1048、後者をE1031でNative/Wasm object双方から定義時拒否する。object-like macroの単独`#`は通常pp-tokenとして維持する |
| **不正なtoken paste結果の展開位置診断** `PASTE(+, *)` | 🔧 | should_reject 4件: preprocess_macro_{invalid_paste_result,object_invalid_paste_definition,mapped_invalid_paste_invocation,header_invalid_paste_definition} | paste結果が単一pp-tokenにならないE1030だけが内部診断経路を使い、同じmacro生成の宣言・designator制約と異なってfilename／lineを失っていた。paste左operandの展開tokenを診断位置として保持する。function-like macroの実引数は呼出し行`:2`、object-like replacementは定義行`:1`、`#line`後の実引数は論理位置`mapped_invocation.c:70`、include headerのreplacementはheader名と`:1`を報告する。4件をClang strictと照合したshould_reject正本からNative/Wasm objectへ登録する |
| **プリプロセッサresource上限の診断位置** E1029/E1037/E1038 | 🔧 | compile_fail: preprocess_if_{token,eval}_limit_location_rejected, macro_{arg_,}expansion_limit_location_rejected | macro展開回数、`#if` token数、`#if`評価step数の上限診断が内部診断経路を使い、filename／lineを失っていた。macro展開counterへ現在のmacro token、定数式評価counterへ現在の式tokenを渡し、`#line`由来の論理位置を保持する。倍化object macro、function-like macro引数内の倍化展開、4096 token超、深い760重括弧を固定fixtureとして共有し、Native/preprocessor unit/Wasm objectの全経路で位置付き拒否を検証する |
| **`#undef` directive境界と標準predefined macroの不変性** | 🔧 | macro_undef_directive_boundaries / should_reject 23件: preprocess_predefined_macro_*.c, preprocess_macro_undef_{va_args,extra_tokens,extra_parens}.c | `#undef`が識別子後のtokenを黙って捨て、実際に提供中の標準predefined macroと`__VA_ARGS__`も解除可能だった。特別展開の`__LINE__`/`__FILE__`はmacro表外なので`#define`も素通りしていた。`__DATE__`、`__FILE__`、`__LINE__`、`__STDC__`、`__STDC_HOSTED__`、`__STDC_NO_THREADS__`、`__STDC_UTF_16__`、`__STDC_UTF_32__`、`__STDC_VERSION__`、`__TIME__`のdefine/undefを個別にE1049、`__VA_ARGS__`をE1047、余分tokenをE1050で拒否する一方、通常macro名は展開せず指定名だけ解除し、未定義名・comment終端・implementation macro `__LP64__` の解除をNative/WAT/objectで維持する |
| **macro定義・解除制約の診断位置** E1018/E1031/E1044-E1050 | 🔧 | should_reject 46件: preprocess_macro_*.c, preprocess_predefined_macro_*.c | macro再定義、parameter list、`__VA_ARGS__`、`#`/`##`、predefined macro変更、`#undef`余分tokenの拒否が内部診断経路を使いfilename／lineを失っていた。定義名、重複parameter、不正operand/operator、余分tokenを直接診断位置へ渡し、行末で不足する名前・`)`はdirective tokenへ戻す。Clang strictと照合した46件の固定fixtureをNative/Wasm objectで共有し、定義行・再定義行・`#undef`行まで検査する |
| **条件付きpreprocessing directiveの行末・ファイル境界** | 🔧 | conditional_directive_boundaries / should_reject 12件: preprocess_unterminated_{true,false}_conditional, preprocess_{ifdef,ifndef,else,endif}_extra_tokens, preprocess_{unknown,nonidentifier}_directive, preprocess_header_{unterminated_conditional,cross_file_{endif,else,elif}} | `#ifdef/#ifndef/#else/#endif`の余分tokenと未知指令を黙って破棄し、未閉鎖条件groupおよびinclude元/先をまたぐ`#else/#elif/#endif`も受理していた。条件指令行末をE1051、activeな未知指令をE1052、各preprocessing file終端の未閉鎖groupをE1053で拒否し、include frameごとに条件stack境界を固定する。12件をClang strictと照合したshould_reject正本へ移す。E1051は余分token、E1052は未知identifierまたは非identifier directive token、E1053は未閉鎖groupの開始directiveをfilename／line付きで指し、include header内の開始位置も保持する。null directive、comment終端、選択済みbranch後の未評価`#elif`式、skipped group内の未知指令はClang strict同様に維持する |
| **`#error` directiveの診断位置** E1033 | 🔧 | should_reject 5件: preprocess_error_{location,empty,token_spelling,macro_name_not_expanded,comment_whitespace} | `#error`がmessage spellingだけを内部診断として出し、filename／lineを失っていた。directive tokenを診断位置として保持し、空messageとtoken列messageの両方で1行目を報告する。message内のmacro名を展開しないこと、token spelling、comment空白の既存契約も維持する |
| **`#if/#elif`の空の制御定数式** | 🔧 | conditional_directive_boundaries / should_reject 4件: preprocess_{if_empty,if_comment_only,if_macro_empty,elif_empty}_expression | 制御定数式が元から空、commentのみ、またはmacro展開後に空になると偽として受理していた。4件をClang strictと照合したshould_reject正本へ移し、実際に評価される空式をE1008で拒否して指令tokenから合成EOFへfilename／lineを継承する。先行branchが選択済みで未評価となる空・不正`#elif`はClang strict同様に許容する |
| 条件groupの孤立・重複指令と`#ifdef/#ifndef` operand | 🔧 | should_reject 9件: preprocess_{stray_else,stray_elif,stray_endif,duplicate_else,elif_after_else,ifdef_missing_name,ifdef_nonidentifier_name,ifndef_missing_name,ifndef_nonidentifier_name} | 孤立した`#else/#elif/#endif`、重複`#else`、`#else`後の`#elif`をE1019〜E1023、`#ifdef/#ifndef`のmacro名欠落・非識別子をE1018として拒否する9件をClang strictと照合したshould_reject正本へ移す。batch/streamingで内部診断だった各経路をdirective名または不正operand tokenへ接続し、通常ファイルとinclude headerのfilename／lineをNative/Wasm object双方で固定する |
| **`#error`のpp-token綴り・空白・macro非展開** | 🔧 | should_reject 4件: preprocess_error_{empty,token_spelling,macro_name_not_expanded,comment_whitespace} | 診断構築時に数値tokenを値へ変換し、文字列の引用符/prefixを落とし、`has_space`をtoken後方へ付けていたため、`alpha+beta 0x2a L"wide" '\n'`が`alpha +beta42 wide 10`へ変形していた。元pp-token綴りを保持し、空白はtoken間だけへ正規化し、macro名は展開しないC11契約をpreprocessor unitおよびClang strict・Native/Wasm object compile-failで固定する |
| **`#line`のdigit sequence・filename・行末構文境界** | 🔧 | line_directive_syntax_boundaries, line_directive_maximum_value / compile_fail: line_directive_{missing,zero,above_maximum,identifier,hex,suffixed,signed,character,floating}_number_rejected, line_directive_{identifier,wide,u8}_filename_rejected, line_directive_{extra_tokens,concatenated_filename,macro_extra_tokens}_rejected / should_reject: preprocess_line_*.c | 不正な`#line`引数を診断せず無視し、filename後のtokenも黙って受理していた。さらにC11の行番号は10進digit sequenceなのに、tokenizerのC整数値を使って`010`を8としていた。macro展開後の元spellingをASCII digitだけの十進列として1..INT_MAXへ検証しE1027、通常prefixなし文字列以外をE1028、filename後の余分tokenをE1054で拒否する。空filenameは有効なので`__FILE__ == ""`として扱い、filename再設定、leading zero、最大値、macro生成の番号+filename、comment終端、filename省略時の既存mapping維持をNative/WAT/objectで固定する。最大値から生成される`__LINE__`についても`int`型、十進綴り`2147483647`、二段stringize、`U` suffix token pasteを照合する。不正番号・filename・余分tokenの15形は固定fixtureをNative/Wasm objectで共有し、E1027/E1028/E1054が該当行（macro展開形は呼出し行）を報告することまで検査する |
| **tokenizer拒否境界のNative/Wasm parity** 整数・comment・literal・escape | ✅ | should_reject: tokenizer_*.c / compile_fail: tokenizer_*_rejected | tokenizer unitだけで検査していた不正hex/octal/binary/suffix/pp-number、Unicode identifier継続を連結した数値suffix、未閉鎖comment/string/character、tokenize不能文字、不正general/hex/UCN escape、空character、`ULLONG_MAX`超過を固定fixtureに分離。decimal fast pathもASCIIだけでなく生UTF-8/UCN identifier継続をpp-number終端として検出し、host strict/native/Wasm objectで拒否してE2009/E2011-E2013/E2015-E2018/E2022-E2029の診断ID parityを固定する。hostがextensionとして受理する`q` suffixとbidi UCNはshould_rejectに混在させずunit coverageに残す |
| `#line`論理filenameのescape/UCNとinclude frame復元 | ✅ | line_directive_include_location_boundaries | backslashとUCNを含む親の論理filename・line deltaを保存してincludeへ入り、include内の別`#line`を適用後に親の`__FILE__`/`__LINE__`へ正確に復帰する。include内で定義した`__FILE__`/`__LINE__` macroも親での展開時には定義位置でなく呼出位置を使うことをClang strict・Native/WAT/Wasm objectで固定する |
| 空のマクロ実引数 `F(7,)` / `F(,8)` / `F(a,,c)` | 🔧 | empty_macro_argument | C99 6.10.3p4 で空引数は合法 (placemarker) なのに has_empty_arg で E1024 拒否。引数個数チェックは残し空引数自体ではエラーにしない |
| **function-like macro呼び出しの引数個数・括弧境界と診断位置** | 🔧 | macro_invocation_argument_boundaries / should_reject 8件: macro_invocation_{zero_parameter_extra_argument,zero_parameter_comma_argument,single_parameter_extra_argument,two_parameter_missing_argument,two_parameter_empty_call,missing_rparen,nested_missing_rparen,variadic_named_argument_missing} | bareなmacro名は展開せず、commentを挟む0-parameter呼び出し、1-parameterの空実引数、nested comma、先頭空引数をNative/WAT/objectで固定する。0/1/2/variadic named parameterの過不足をE1024、単純・nested未閉鎖をE1026としてClang strict同様にNative/Wasm object双方から拒否する。両診断が内部診断経路でfilename／lineを失っていたため、batch／streaming共通の引数収集へmacro名tokenを渡し、8件すべてで呼び出し行`:2`も固定する |
| function-like macro実引数の事前展開と`#`/`##`抑止 | ✅ | macro_argument_prescan_boundaries | 通常置換では実引数を完全展開し、同じparameterでも`#`/`##`隣接時は元pp-token列を使い、paste結果は再走査する規則を固定する。直接/二段paste、同一replacement内の通常置換+paste、classic `AFTERX/XAFTERX`、3-token pasteと各位置の空placemarker、直接/二段stringize、paste後のmacro再展開をClang strict・Native/WAT/objectで照合する |
| **object-like macro置換列と周辺演算子のtoken境界** `#define N 0xe` / `N+1` / `1 PLUS +2` | 🧪 | object_macro_operator_token_boundaries | replacement listを周辺sourceと再走査しても、pp-tokenを再字句化して暗黙の`++`・`--`・comment delimiterへ融合してはならない。数値token直後/直前の演算子、二段alias、括弧なし置換列の通常の演算子優先順位、macro生成括弧・function designator、`+ +`・`- -`・`/ *`のtoken分離を、array bound・enum・`_Static_assert`・実行時式で検査し、c-testsuite 00211相当をClang strict・Native・WAT・Wasm objectで固定する |
| macro生成宣言子・初期化子の合成境界 | ✅ | macro_generated_{declarator,initializer}_boundaries | macro展開結果を単なる式で終わらせず、factory戻りfunction pointer、function pointer配列へのpointer、array parameter調整、VLA多次元parameter、nested Atomic callback、Atomic function pointer、token pasteで作るtypedefを`_Generic`と間接呼出しまで接続する。初期化側はmacro生成の匿名member／配列designator、nested designator後のpositional cursor、variadic多次元initializer、function pointerを含むcompound literal、flexible array prefix、paste生成した`+=`／`*=`／`<<=`を同一fixtureで実行する。独立した正例14件と制約違反10件をClang strictと差分探索し、新規差分がないことを確認した上で、重複の少ない正例境界をNative/WAT/Wasm objectへ恒久登録する |
| **token pasteで生成するencoding-prefix付きliteral** `u8 ## "\u03A9"` | 🔧 | macro_paste_literal_prefix_boundaries | `paste_tokens`が文字列operandへ`token_text`の内容部分だけを使い、引用符と既存prefixを落としたため、`u8 ## "\u03A9"`を`u8Ω`という識別子へ再tokenizeしていた。文字列pp-tokenのprefix・引用符・escapeを含む完全綴りを左右operandで再構築し、L/u/U/u8文字列、L/u/U文字定数、macro prefixの二段展開、paste結果の再stringizeをClang strict・Native/WAT/Wasm objectで固定する |
| **二段 paste + 直後呼び出し** `CAT(A,B)(x)` (`#define CAT(a,b) CAT2(a,b)` / `AB(x) CAT(x,y)`) | 🔧 | macro_nested_paste_call | ストリーム経路で `)(` 後続が置換列と分離し `AB (x)` 化。さらに hideset を親鎖ごと伝播すると paste 済み AB からの `CAT(x,y)` が外側 CAT でブロック。`)(` サフィックスを pull コピーして preprocess_ctx で縮約 + hideset は展開マクロ名のみ付与。c-testsuite 00201 |
| **stream macro 展開下の cast 先読み窓** `PTYPE((X) << (unsigned long)1)` | 🔧 | shift_left_operand_type | c-testsuite 00200 の長大マクロで `(int) sizeof(...)` の `sizeof` が未 materialize のまま parse_cast_type が `(int)` だけを cast と誤認し SIGSEGV。cast 型先読み前に tk_ensure_lookahead() を呼び、`)` 後 NULL は成功扱いしない。併せて `CAT(A,B)(x)` 用の `)(` splice は単一関数風呼び出し置換列 (`CAT2(A,B)`) に限定し、PTYPE のような任意式マクロで外側 stream を余分に覗かない |
| **翻訳フェーズ2の全域backslash-newline除去** `iden\\\ntifier`, `12\\\n34`, `// comment \\\ncontinued` | 🔧 | line_splicing_translation_boundaries, test_translation_phase_two_line_splicing | scannerがtoken間の行継続だけを読み飛ばしていたため、identifier・pp-number・文字列・macro名・directiveを跨ぐspliceと、`//` commentが次の物理行まで継続する規則を処理できなかった。trigraph置換後かつtokenization/comment除去前にLF/CRLF spliceを全域削除し、削除offsetから後続tokenの物理行番号を復元する。trigraph由来backslashも含めtokenizer unitおよびClang strict・native/WAT/objectで固定する |
| **stream tokenizerの正規化入力寿命** | 🔧 | test_stream_normalized_input_lifetime, test-wasm-selfhost-source/wasm32_ir.c | line spliceを含む大規模sourceでは翻訳フェーズ2の正規化入力全体がrecyclable arenaへ確保されていた。parser cursorが4 chunkのlook-behindを越えると、pull lexerの`p`がまだ指す入力bufferまで解放され、同じsourceの自己ホストcompileが成功・E1052・E2006・Typed HIR内部失敗へ非決定的に分岐した。正規化入力とsplice offsetを翻訳単位寿命のarenaへ置き、生成tokenだけをrecyclableへ戻す。12,000宣言をchunk回収しながら末尾まで読むtokenizer fixture、ASan反復compile、実際に発火した`wasm32_ir.c`の自己ホスト回帰で固定する |
| **trigraph置換sourceと永続文字列literalの所有** | 🔧 | tokenizer/trigraph_{or,xor}, test_global_registry_checkpoint_boundary | ASanで、`pp_stream_close`がrecyclableなtrigraph置換sourceを解放した後、translation-unit data loweringが登録済み文字列literalを参照するuse-after-freeを検出。永続global registryへ登録するときraw contentsを複製し、registryがlabel・contents・literal nodeを所有してrollback/reset/destroyで解放する。trigraph経由のassert macro展開をASanで固定 |
| digraphのdirective・macro operator・C構文 | ✅ | digraph_preprocessing_boundaries | `%:`によるinclude/define/if/error/endifとstringize、`%:%:`によるtoken paste、`<:`/`:>`の配列添字、`<%`/`%>`の集約初期化・関数/struct本体を一つのtranslation unitで組み合わせる。直接/二段macro展開を含めClang strict・Native/WAT/Wasm objectで固定する |
| **空引数と `##` の placemarker** `P(jim,) => A##B ; bob` / `Q(+,)3` | 🔧 | macro_paste_empty_operand | 空引数を何も挿入せず `jim ## ;` と並べ E1030 になっていた。C11 6.10.3.2p3 通り placemarker 側の `##` を削除し非空側のみ残す。c-testsuite 00202 |
| **未完了タグのポインタ / 関数型仮引数** `enum E *e` / `enum E const *e2` / `int f(int (int x), int)` | 🔧 | incomplete_tag_and_nested_func_param | 未完了 enum/struct を前方宣言せず E3066。タグ直後の const を読まず E3016。`(int x)` を nested param list として解釈せず E2006。c-testsuite 00209 |
| **GNU `__attribute__` の読み飛ばし** `} __attribute__((packed)) T` / `int ATTR f()` / cast 内 `ATTR` | 🔧 | gnu_attribute_parse | `__attribute__((...))` を未対応で E2006。宣言・関数・キャスト各所で括弧グループごと skip するのみ (属性意味は未実装)。c-testsuite 00210 |
| **GNU statement expression** `({ ...; expr })` / 三項偽側 | 🔧 | gnu_statement_expression | `({` を primary で compound literal と区別せず E3064。ND_STMT_EXPR でブロック実行後に最終式の値を返す。c-testsuite 00213 |
| **`__builtin_expect(exp, c)`** 分岐ヒント builtin | 🔧 | builtin_expect_fold | 未宣言関数として ND_FUNCALL 化し `_builtin_expect` 未定義シンボルで link fail。GCC 同様第1引数 exp のみ評価して返す (hint 無視)。c-testsuite 00214 |
| `#if 0` 内の非 C トークン (`@` `$`・未終端リテラル・不正数値) の skip | 🔧 | if0_skip_non_c_tokens | 全体 tokenize 後に preprocess する構造のため、偽分岐内の未知文字・未終端リテラル (`don't` の `'`)・不正数値が tokenize 時に E2028/E2018 で先に落ちていた。寛容モードを導入: プリプロセッサが pps_skip_cond_incl と pps_materialize_line の行末先読みの間だけ tk_set_tolerate_untokenizable(true) を立て、tk_stream_next が setjmp を張る。scanner の各種 TK_DIAG_* は寛容モード中のみ tk_tolerate_longjmp_if_active 経由で longjmp し、当該トークンを 1 文字の TK_UNKNOWN にして進める (skip が捨てる)。over-pull で active に漏れた TK_UNKNOWN は pps_step が E2028。active コードの不正は従来どおり即エラー (位置も正確) |

### 型機能 / その他
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| **parser/semantic制約のNative/Wasm拒否parity** return・member・`_Generic`・cast・declarator | ✅ | should_reject: parser_*.c, bare_return_from_nonvoid_function.c, return_value_from_void_function.c, goto_undefined_label.c, cast_struct_to_integer.c, zero_array_bound.c / compile_fail: parser_*_rejectedほか | parser unit/direct HIR経路だけに残っていた非voidの空return、voidの値return、scalarの`.`/`->`、未定義member/label、`_Generic`の不一致・default重複、aggregate制御式、arrayへのcast、aggregateからscalarへのcast、function/incomplete member、zero bound、variadic位置、`_Alignof`/`_Atomic`/`_Alignas`構文、不正型指定を19件の固定fixtureで検査。14件を新設し、同等sourceが既にあった5件は重複させずE2E診断parityへ昇格した。Clang C11 strict・Native・Wasm objectの拒否と現行診断IDを固定する。scalar/pointerからstruct/unionへのcastは明示的なag_c拡張のため対象外 |
| **member access operatorの左operand category制約** `aggregate.member`, `pointer->member` | 🧪 | should_reject: parser_dot_on_scalar, parser_arrow_on_scalar_pointer, arrow_nonpointer_operand | `.`はstruct/union値、`->`はstruct/unionへのpointerを要求する。scalarへの`.`、scalar pointerへの`->`、非pointer aggregate値への`->`を分離してClang strictと照合し、E3005のNative/Wasm object診断parityを固定する。重複していた旧`dot_nonaggregate_operand`インライン登録は既存`parser_dot_on_scalar`正本へ統合する |
| **`sizeof` / `_Alignof` の完全object型制約** incomplete record/array・flexible array member・function | 🧪 | should_reject: sizeof_{incomplete_type,incomplete_expression,incomplete_array_type,incomplete_array_expression,flexible_array_member,function_expression,function_type}, alignof_{incomplete_type,incomplete_array_type,function_type} | 両演算子のoperandとなる型は、bit-field以外の完全なobject型でなければならない。未完成recordを型名・式で与える場合、未指定長arrayを型名・extern式で与える場合、flexible array member、function designator・function型を独立fixtureでClang strictと照合し、E3117のNative/Wasm object診断parityを固定する |
| **`register` objectのアドレス取得制約** direct object・parameter・array・aggregate member | 🧪 | should_reject: address_of_register_{object,parameter,array,struct_member}, address_generic_selected_register_{object,member,array} | `register` storage-classを持つobjectと、そのarray変換前の配列・aggregate memberはアドレス取得できない。直接object、parameter、array、memberおよびgeneric selectionで選択した各lvalueをClang strictと照合し、E3119のNative/Wasm object診断parityを固定する。memberは既存の`address_of_register_struct_member`正本と登録を再利用し、同内容だった古いインライン登録を削除する |
| `_Alignof` の集約アラインメント | 🔧 | 3e8a4d1 | |
| `sizeof`/`_Alignof` の複数語整数型 `(long long)` 等 | 🔧 | sizeof_multiword_int | parse_parenthesized_type_size が先頭1語のみ消費し `sizeof(long long)` が E2006。整数型指定子列を一括解釈 |
| `_Generic` の文字列/long リテラル/ポインタ種別 | 🔧 | e0b5190 ほか | |
| `_Generic` のスカラ整数キャスト制御式 `(char)x` | 🔧 | generic_scalar_cast_control | `(T)0` idiom。ポインタ/関数/タグは infer 維持 |
| `_Generic` の修飾子除去 / ポインタ段数 | 🔧 | generic_selection_constraints, generic_qualified_aggregate_conversion_boundaries, const_function_pointer_typedef_boundaries, generic_qualified_scalar_cast_boundaries, generic_restrict_atomic_pointer_boundaries, type_decl/generic_ptr_{const_pointee,level_const,level_volatile}_match | scalarに加えてconst/volatile/atomicのstruct・union本体とqualified aggregate memberへlvalue conversionを適用し、対応するunqualified値型で選択する。address-ofではpointee修飾を、qualified/atomic aggregate配列のdecayではelement修飾を保持し、selector関数やatomic loadを含む制御式を評価しない。function designator変換、pointee qualifierと多段pointerの区別もClang strictおよびnative/WAT/objectで固定する。`typedef int (*const CF)(int); _Generic((CF)f, int (*)(int): ...)`ではcast targetのtop-level `const`を制御式に残してdefaultへ誤選択していたため、association照合直前に制御式のtop-level cv/restrict qualifierだけを除去する。`_Atomic(T)` castはatomic型のidentityを、pointerのpointee・nested qualifierは各段の修飾を保持する。整数・浮動・complex・pointer・function pointer castと制御式の非評価を広いfixtureで固定する。さらにrestrict pointer object・castの最外修飾だけが値変換で消え、address型と多段pointerの内側restrict、Atomic pointerとその内側identityが保持されることをglobal/local/struct memberと未評価副作用で検査する。const function-pointer object自身とその配列・struct member・address型には修飾を保持してglobal/local/static、parameter、間接call、conditionalも検査する |
| `_Generic` 深いネスト型 (`(int(*(*)(void))[3])`) の同一型マッチ | 🔧 | generic_complex_derived_type{,_global} | 構造的フィールドでは区別不能だったため、型を正規化トークン文字列にして照合 (control が局所変数 / キャスト / グローバル変数のとき有効)。control/assoc 双方に sig があれば strcmp、片方のみは従来照合 (加算的) |
| `_Generic` が `long` と `long long` を同一視 | 🔧 | generic_long_vs_longlong | リテラル `0LL`/`0ULL` の LL サフィックス (token int_size) を node→generic_type へ伝播し、同サイズ整数でも long long rank を照合 |
| `_Generic` が `char`/`signed char`、`long`/`long long` を**変数**で同一視 | 🔧 | generic_char_and_longlong_identity | 制御式が変数だとサイズと符号しか持たず誤マッチ。integer_kind / is_plain_char の型識別を宣言 (psx_consume_type_kind の side-channel)→lvar→参照ノード→infer_generic_control_type へ伝播し generic_type_matches で照合。assoc 側は parse_integer_cast_spec_sequence が is_plain_char を出力 |
| `_Generic` が int 制御式を単一 int メンバの struct に誤マッチ | 🔧 | generic_struct_vs_scalar | generic_type_matches が control 側しか tag を見ず、`_Generic((int), Anon:.., int:..)` がサイズ一致でスカラ経路で Anon に化けた。どちらか一方でも tag を持てば tag 一致を要求 |
| **`_Generic` の配列 association と関数 designator** `int[4]` / `typedef int (*F)(int); _Generic(foo, F:...)` | 🔧 | generic_array_assoc_and_func_designator | c-testsuite 00219。関連型 `int[4]` の配列 suffix を読み飛ばすだけで配列型の印を残さず、scalar `int` とサイズ一致で誤マッチしていたため `is_array` を追加。関数名制御式 `foo` は ND_FUNCREF のまま戻り値 `int` のように扱われ、function pointer typedef に一致せず `int:` 側へ落ちていたため、ND_FUNCREF を function pointer 型として `_Generic` に渡す。直接書いた複雑 function pointer association は既存の type_sig 照合を優先し、関数名側に sig が無い場合は broad match しない。 |
| `_Generic` が引数違いの関数ポインタを同一視 | 🔧 | generic_complex_derived_type{,_global} | `int(*)(int)` と `int(*)(int,int)`、`int(*)(int)` と `double(*)(int)` を区別。型を正規化トークン文字列化して比較するので引数の個数・型・戻り型まで照合する。制御式が局所変数 / キャスト `(T)0` / グローバル変数のいずれでも有効 |
| `sizeof`/`_Alignof(enum E)` | 🔧 | sizeof_enum_type | parse_parenthesized_type_size が struct/union/typedef のみ対応し enum 型名を E3064 で拒否。enum は int 相当 4B として TK_ENUM 分岐を追加 (sizeof(enum 変数) は元から動作) |
| **`static` ローカル配列の `sizeof`** `static int a[10]` | 🔧 | static_local_array_sizeof | sizeof が要素サイズ(4)を返していた。static ローカル配列は try_lower_static_local_array でグローバルへ lowering され alias lvar が is_array=0/size=0 で登録されるため、parse_sizeof_operand の `arr_var->is_array` 特別処理を素通りし一般経路 ND_ADDR(ND_GVAR) の type_size=要素ストライド(4) になっていた。lvar_is_static_local_array のとき lowering 先グローバルの type_size (=全体サイズ) を返す分岐を追加。int/char/double・`sizeof(a)/sizeof(a[0])` イディオム |
| **多次元 `static` ローカル配列の永続化** `static int a[2][3]` / `static double d[2][2]` / `static int *p[2][2]` / `typedef int M[2][3]; static M m` / `typedef int *P[2][2]; static P p` | 🔧 | static_local_multidim_array, static_local_pointer_array_init, static_local_typedef_multidim_array | 1D static local 配列は lowering されるが、多次元 suffix は `try_lower_static_local_array` が `[` の連続を見て対象外にし、通常 auto 多次元配列として frame に置かれていた。呼び出し間で値が保持されず、Wasm IR でも alloca になっていた。多次元定数 suffix を peek/consume して consumed lowering へ渡し、global/alias に outer/mid/extra stride を保存、`ND_ADDR(ND_GVAR)` へ伝播。続き229で static local alias の unsigned pointee 伝播漏れと FP 配列の `fp_kind` / `init_fvalues` 漏れを修正し、unsigned char/short/long/double も網羅。続き230で typedef-array local 登録より前に static typedef-array lowering を追加し、`static M m` も stack local に落ちないようにした。続き231で pointer-element typedef 多次元配列の leaf 要素サイズを row size から復元し、static alias の `pointer_qual_levels/base_deref_size` を `ND_ADDR(ND_GVAR)` へ伝播するよう修正。続き232で直書き `static int *p[2][2]` の `[` suffix 経路も pointer-element static lowering に入れた |
| `_Alignas(>16)` のローカル変数 | 🔧 | alignas_overaligned_local | x29 が 16 整列のみで固定オフセットでは過剰整列不可。align_bytes>16 のローカルだけ予備領域+実行時丸め (IR_ALIGN_PTR=add #A-1;and #-A)。併せて `_Alignas(N) struct ...` 局所の (N) を読み飛ばせず E3015 だったパースも修正 |
| **過剰整列VLAの動的storage・多次元stride** `_Alignas(64) struct Cell cells[n]` | 🔧 | overaligned_vla_storage_boundaries, overaligned_vla_recursive_frames, overaligned_vla_scope_exit_boundaries, overaligned_vla_typedef_boundaries, overaligned_pointer_to_vla_typedef_boundaries, overaligned_vla_parameter_qualifier_xtu, overaligned_vla_global_callback_xtu, overaligned_vla_callback_factory_xtu / object: global_vla_callback_factory_element_const_mismatch_{main,other} | aggregate要素を持つ多次元VLA parameterがruntime内側boundを持つ場合にVLA loweringから除外され、通常arrayとして解決してE4007になっていた。また動的allocationがbyte sizeを16-byte単位へ丸めるだけで、自然32-byte・明示64/128-byte alignmentをstack pointerへ反映していなかった。実効alignmentをruntime planからTyped HIR・IR・Wasm Machine IRへ伝播し、両backendで`(sp - size) & -alignment`によりbase addressを整列する。1次元・多次元VLAの全element/subarray alignment、captured boundによる`sizeof`、32/64-byte element stride、VLA parameter、nested scope、異なるruntime bound、前後canaryとscope退出後の再確保を固定する。runtime boundを捕捉する1D/2D array typedefから生成したobjectでも、bound評価が各typedef宣言で1回だけ行われ、元変数変更後もsize・64-byte element stride・自然alignment・追加の128-byte object alignmentを保持する。同じruntime-bound typedefから作るpointer-to-VLAでも、捕捉後に元変数を変更してなお`sizeof *p`、64-byte element alignment、128-byte matrix alignment、行pointerの加減算・増減・byte距離とVLA仮引数への受け渡しが捕捉済みstrideを使うことを固定する。さらに別TU間で`const Cell input[static restrict rows][columns]`／mutable outputのarray parameter宣言と`const Cell (*restrict input)[columns]`形式の定義を互換として接続し、prototype-scope `[*]` function typeによる間接呼出し、runtime stride、64-byte element alignment、128-byte caller object alignmentを固定する。同じfunction typeを外部global function-pointer objectへ格納する経路でも、別TUのdata signatureとrelocation、global直接呼出しとlocal copy経由呼出し、複数runtime shapeを固定する。さらにcallback factoryの戻りfunction pointerへ同型を一段再帰させ、直接factory symbol、外部global factory data、そのlocal copyから取得したcallbackの三経路でruntime VLAを実行する。factoryを保持する外部global dataの負例では、factory戻りcallbackのVLA要素`const`差を再帰的なcanonical C署名で検出し、同一の低水準Wasm function typeでもlinkを拒否する。12段の同時再帰frameでは自然64-byte整列aggregate VLAと明示128-byte整列byte VLAを生存させ、全ancestorとのstorage非重複、各要素のalignmentと内容保持、復帰後のframe解放状態を検査する。さらに両VLAを各18,000回確保する`continue`・`break`・外側labelへの`goto`経路でalignment paddingを含むstack checkpointが毎回復元され、canaryと後続allocationが壊れないことをClang strict・Native・WAT・Wasm objectで固定する |
| **factoryが返すVLA callbackのcross-TU修飾互換性** `const Cell input[static restrict 1][*]` | 🧪 | overaligned_vla_callback_factory_xtu / object: function_return_vla_callback_element_const_mismatch_{main,other}, global_vla_callback_factory_element_const_mismatch_{main,other} | factoryが返すcallback型のprototype-scope VLA parameterは、array-to-pointer調整後も内側要素の`const`をcanonical C署名へ保持する。直接factory関数の戻り型と、factory pointerを格納する外部global dataの両経路について、各TUはClang strictおよびag_cで単独コンパイル可能だが、別TUでmutable elementへ変えた定義は低水準Wasm function typeが同じでもそれぞれ`function signature mismatch`／`data signature mismatch`としてobject linkを拒否する。 |
| **factory生成VLA callbackの過剰整列aggregate return ABI** `AlignedResult callback(int n, int m, const Cell input[static 1][*])` | 🧪 | overaligned_vla_callback_aggregate_return_xtu | 64-byte要素のruntime VLA parameterと、64-byte alignment・sizeを持つaggregateのhidden return destinationを同じ間接callへ載せる。別TUの直接factory関数、外部global factory pointer、そのlocal copyからcallbackを取得し、複数shapeの128-byte整列caller VLAについてruntime row stride・値・canaryを検査する。各callback結果のdestinationも64-byte整列を要求し、Clang strict、Nativeの真の2-object link、WAT結合、Wasm 2-object link/runで固定する。 |
| **factory生成VLA callback戻りrecordのmember alignment互換性** `_Alignas(64) ull sum; ull count` | 🧪 | function_return_vla_callback_result_alignment_presence_mismatch_{main,other} | factory関数の戻りcallbackがさらに返す64-byte aggregateについて、`count`の指定なしと`_Alignas(8)`指定を区別する。両recordはsize・alignment・member offsetが同一で、callbackの低水準ABIも同じhidden pointerだが、対応memberのalignment specifier指定有無が異なるためC型は非互換になる。各TUはClang strictおよびag_cで単独コンパイル可能なまま、Wasm object linkではfactory関数の再帰的canonical署名により`function signature mismatch`として拒否する。 |
| **Wasm objectの過剰整列global/static配置** `_Alignas(64/128)` | 🔧 | alignas_global_static_storage, alignas_redeclaration_consistency / object: overaligned_data_segments | object emitterがlinking sectionのsegment alignmentを最大`p2align=3`へ丸め、link後のglobal/static/static-localが要求alignmentを満たさなかった。メモリアクセス命令の自然alignment上限とdata segment配置を分離し、要求値を`p2align=6/7`として保持。object link実行scanで両fixtureの`main() => i32:0`を固定 |
| **Apple ARM64のthread-local data/BSS配置alignment** | 🔧 | thread_local_alignment_initializer_boundaries | `_Thread_local` objectのIR dataには正しいalignmentがある一方、Apple emitterがinitialized TLSを通常の`__thread_data` sectionへalignment無しで出し、uninitialized TLSも通常labelと`.space`で作っていたため、linkerがatom alignment 1を警告し、直前の1-byte TLS object次第でnatural/`_Alignas(16)`配置を破った。initialized dataを`thread_local_regular` section + `.align`、BSSをalignment付き`.tbss`へ変更。file/block-static TLS、明示初期化・zero-fill、pointer/string relocation、complex・wide配列をnative/WAT/objectで固定 |
| **同一宣言の多数alignment specifier** 17個の`_Alignas` | 🔧 | dynamic_alignas_specifiers | C11では同一宣言に複数のalignment specifierを置けるが、Syntax specifierが8要素固定配列を持ち、9個目を`E3064 declaration alignas limit exceeded`で拒否していた。expression形・type-name形を動的配列へ移し、宣言phase/function definitionへのmove ownershipとdisposeを維持する。旧上限と初期動的容量16を超える17個をglobal/local、native/WAT/objectで固定し、設計invariantで固定配列の再導入を拒否する |
| **大規模syntax listの動的容量** 1025 declarator/member/enumerator、4097 initializer | 🔧 | dynamic_syntax_list_capacities | 関数parameterは動的化済みだったが、top-level/local宣言子・aggregate宣言/item・enum memberは`PS_MAX_DECLARATOR_COUNT=1024`、initializer listは`PS_MAX_INITIALIZER_ELEMENTS=4096`で、Clang strictが受理する標準CをE3064で拒否していた。全listをallocation可能な範囲まで動的に伸長し、特に毎要素realloc/copyしていたtop-level/local宣言を明示capacityによる倍増へ修正。標準macro展開で旧上限を1件超えるtypedef・record・enum・静的配列をcompactに生成し、native/WAT/objectと設計invariantで固定する |
| enum 値・算術 | ✅ | control_flow_enum_compound_boundaries | 加算・shift・単項minus・剰余からなる列挙定数と実行時利用をnative/Wasmで固定 |
| switch / fallthrough / default | ✅ | control_flow_enum_compound_boundaries, switch_edge/nested_switch | enum case、fallthrough、break、default、nested switchを正式fixture化 |
| **switch case 値の制御型変換後の重複判定** `switch(unsigned x)` で `case -1` / `case 4294967295u` | 🔧 | switch_case_promoted_values / should_reject 9件 / compile_fail: switch_duplicate_case_* | C11 6.8.4.2p3。case 定数を生の `long long` 値で比較していたため、整数昇格後の制御式型へ変換すると同値になる case を見逃していた。switch scope に昇格後の整数型を保持し、各 case 値をその型で正規化してから重複判定する。signed/unsigned int、unsigned char/short のint昇格、正負enumのcompatible integer型、32-bit unsigned定数式のwrap/shift/comparison、64-bit unsigned long変換を網羅し、9件をshould_reject正本からNative/Wasm object compile-failへ登録してE3060 parityを固定する。変換後も異なる合法caseはpositive fixtureで維持する |
| 複合代入演算子 `<<=` `>>=` `&=` `|=` `^=` `%=`（型幅・符号） | ✅ | integer_runtime_arithmetic_boundaries | sub-int/メンバ/ポインタ deref/配列要素を正式fixture化 |
| do-while / goto / 多重代入 / 前後置インクリメント | ✅ | control_flow_enum_compound_boundaries | do-while反復、前方・後方goto、連鎖代入、pre/post incrementをnative/Wasmで固定 |
| **goto / switch label による variably modified identifier のスコープ進入拒否** | 🔧 | goto_vla_scope_constraints / should_reject 15件 / compile_fail: goto_into_* / switch_*_into_* | C11 6.8.6.1p1 / 6.8.4.2p2。関数内のVM宣言ごとに永続的なscope markerを作り、goto元とlabel先の祖先鎖を比較する。switchは制御式位置のmarkerとcase/default位置を比較する。VLA object、pointer-to-VLA、VLA typedef、pointer-to-VLA typedef、for-init、同一・入れ子・兄弟block、case/defaultからfor-init VLA scopeへの進入を網羅し、VM宣言前のlabelやスコープからの退出は維持する。15件をshould_reject正本からNative/Wasm object compile-failへ登録し、E3064診断parityを固定する |
| **整数定数式arrayのscopeへのgoto** `int a[1 && 1]; goto label;` | 🧪 | goto_ice_array_scope_boundaries | logical `&&`/`||`、条件演算子、非VLA `sizeof`、floating constantの直接cast、列挙定数からなるboundはすべて整数定数式であり、variably modified identifierではない。これら6種類の固定長array宣言を飛び越えてblock内labelへ入る合法形をClang strict・Native・WAT・Wasm objectで固定し、VLA進入拒否のscope markerがICE arrayまで過剰適用されないことを検査する |
| **typedef 名と同名ラベル** `goto s;` … `s:` (typedef `s` と shadow) | 🔧 | typedef_label_shadow | `s:` が is_decl_like_start_stmt (typedef 名) より先に宣言として解釈され E3064。`ident:` ラベル形を宣言判定より優先。c-testsuite 00129 |
| **明示 `extern` 付き関数内 prototype** `extern int f(int); f(1)` | 🔧 | local_function_prototype | 続き77 で `int f(char *);` は暗黙 extern の関数 prototype としてローカル変数登録を避けるよう修正済みだったが、`extern` 付きは parse_local_extern_declarator_list の別経路で `global_var_t` の extern 変数として登録され、呼び出しが直接 `bl _f` ではなく GOT load した値への間接 call になって SIGBUS。extern 宣言子ループでも g_decl_trailing_func_suffix をリセット/検出し、non-pointer の関数 declarator は extern 変数登録せず読み飛ばす。`extern int f(int), value;` の混在も網羅 |
| **関数内 `extern struct/union T obj;`** `extern struct S gs; gs.x` | 🔧 | local_extern_tag_decl | stmt.c の tag-keyword fast path が `extern` を消費しても storage class を復元せず、`struct S gs;` と同じ auto 変数として登録して未初期化スタックを読んでいた。tag 経路で extern を保存/復元し、after_type_ex 側でも extern なら local extern 登録へ回す。登録時に tag/fp/unsigned 情報を保持し、先行定義・後方定義・union・struct pointer を網羅 |
| compound literal（引数・式中） | ✅ | control_flow_enum_compound_boundaries, compound_literal_{scalar_lvalue,struct_arg}, nested_compound_literal | scalar/array/record、address取得、引数、nested、file/block scopeを正式fixture化 |
| ファイルスコープ複合リテラル初期化子 `T g=(T){...};` | 🔧 | file_scope_compound_literal_init | struct で `,` で E2006、scalar で先行関数宣言があると x=0。`T g=(T){...}` は `T g={...}` と等価 (C11 6.5.2.5) なので先頭の `(型)` を読み飛ばし既存 brace 初期化へ。scalar/array/struct 対応 |
| ファイルスコープ `static struct/union/enum var` | 🔧 | static_tag_global | parse_toplevel_decl_spec が tag 判定前に storage class を skip せず、`static struct S g` を E3016 で拒否。tag 前に修飾子先読み skip を追加 |
| **storage class 付きタグ戻り関数** `static struct S *f(){...}` | 🔧 | static_tag_return_function | is_toplevel_function_signature と parse_func_decl_spec がどちらも「タグキーワードの前の storage class (static/extern)」を飛ばさず、`static struct S *g()` がオブジェクト宣言と誤判定 (E2006 `;` 期待) / 戻り型が implicit int に化け E3064。`static int *g()`(builtin) や非 static `struct S *g()` は動作。両関数で storage class の直後がタグキーワードなら storage class を消費してタグ経路へ。struct/union/enum・ポインタ/値返し・引数あり・戻り値 subscript を網羅 |
| **ファイルスコープ `static <typedef名> 変数`** `static Point p;` | 🔧 | static_typedef_name_global | parse_toplevel_decl_spec が storage class (static/extern) を tag キーワードの前でしか skip せず、typedef 名の前では skip しなかったため `static Point p` で `static` が残り Point が型と認識されず E2006 (`;` 期待)。非 static や builtin (`static int`) や tag (`static struct S`) は動作。typedef 名の前にも修飾子先読み skip を効かせる (toplevel_prefix_precedes_typedef_name)。const 修飾・ポインタ・配列・named/anon typedef・関数戻りを網羅 |
| **const/volatile 付きポインタ戻り型** `int *const f()` | 🔧 | qualified_pointer_return | is_toplevel_function_signature の先読みと parse_pointer_suffix_flags が `*` の後の const/volatile を飛ばさず、関数と認識されずオブジェクト宣言と誤判定して E2006 (`;` 期待)。`const int *f()` (pointer to const) は動作。lookahead (3 関数の `*` skip ループ) と suffix 解析の両方で `*` の後の const/volatile を読み飛ばす。int/double/struct・const/volatile・subscript 併用を網羅 |
| **const pointee の関数戻り値経由メンバ代入** `const struct S *f(); f()->x=...` / `const struct S (*f())[N]; (*f())[i].x=...` | 🔧 | const_func_ret_pointee_member_assign (続き105) | `const struct S *get(void)` が parse_func_decl_spec の tag fast path に入らず implicit int に落ちていた。さらに関数戻り値の pointee const/volatile を semantic ctx に保存していなかったため、直接呼び出し結果の `->` / `*` / `[]` 経由の lvalue に const が伝播しなかった。tag 前の const/volatile を consume し、関数定義/プロトタイプ登録で ret_pointee qualifier を保存、build_member_deref_node / build_unary_deref_node / build_subscript_deref が ND_FUNCALL から qualifier を復元する。通常 pointer 戻りと pointer-to-array 戻り、読み取り OK / 書き込み E3077 を網羅 |
| **const pointee の関数ポインタ戻り値経由メンバ代入** `const struct S *(*fp)(); fp()->x=...` / `const struct S (*(*fp)())[N]; (*fp())[i].x=...` | 🔧 | funcptr_return_const_pointee / compile_fail (続き106) | 続き105 の間接呼び出し版。`ND_FUNCALL.callee != NULL` では関数名表を引けず、callee の関数ポインタ型にある pointee const/volatile を見ていなかったため `fp()->x=...` が通っていた。また pointer-to-array 戻りでは `*fp()` の結果に戻り tag/要素サイズが伝播せず、合法な `(*fp())[0].x` 読み取りも E3005。修正: 間接呼び出しは callee の `node_mem_t` から pointee qualifier を復元し、`build_unary_deref_node` の ND_FUNCALL indirect 経路で callee の戻り tag と要素サイズを carry。読み取り・mutable 書き込み OK、const 書き込み E3077 を網羅 |
| **多段ポインタを返す関数の直接 deref** `int **g(); **g()` | 🔧 | multilevel_pointer_return | semantic ctx の ret_is_pointer が bool で段数を持たず `int **` を単段 `int *`(pointee 4B) 扱い→`*g()` が int になり `**g()` が int 値をアドレス参照で SIGSEGV。型付き変数経由 `int **q=g(); **q` は元から動作。戻り型の `*` 段数を ret_pointer_levels に記録し、node_utils の funcall 経路 (pointer_qual_levels / base_deref_size / ps_node_deref_size) が段数>=2 のとき `*g()` を「1 段減らしたポインタ」(8B 値・最内基底型 deref) として組む。build_subscript_deref も funcall base を 1 段消費 (`g()[i]`→int*)。int**/char**/int***・prefix deref・deref+subscript 混在 `(*rg())[1]`・直接 subscript `rg()[0][i]` を網羅。単段ポインタ戻りは不変 (段数>=2 ゲート)。`int *const *f()` 等の qualified 多段もこれで解禁 |
| **タグ戻り + `(*...)` 宣言子** `struct P (*f())[3]` / `struct R (*f())(int)` | 🔧 | tag_return_complex_declarator | is_tag_return_function_signature が `(*...)` 宣言子を扱わず (`(` 後に IDENT を期待し `*` で return 0)、変数宣言と誤判定して E2006。builtin `int (*f())[3]` は is_toplevel_function_signature が処理できていた。両関数の宣言子判定を共有ヘルパ is_function_declarator_sig に抽出し、タグ戻りでも配列へのポインタ戻り・関数ポインタ戻りを検出 |
| **struct を返す関数ポインタの間接呼び出しメンバアクセス** `op(41).v` / `op(41)->v` | 🔧 | funcptr_return_struct_member | 間接呼び出し (callee != NULL) の funcall ノードに戻り tag 型が伝播せず psx_node_get_tag_type が TK_EOF を返し E3005 (`.`/`->` の左辺が構造体でない)。直接呼び出し `mk(41).v` は ret_tag 表から引けて動作。callee の funcptr 変数は tag フィールドに戻り tag を保持するので導出し、戻り値がポインタか否かは pointer_qual_levels で判定 (値戻り `struct R (*op)()`=pql1→ptr0 / ポインタ戻り `struct R *(*op)()`=pql2→ptr1)。値戻り/ポインタ戻り・deref形 `(*op)(x).v`・global funcptr・union 戻り・8B ネストメンバ連鎖を網羅。1/2/4/8B のレジスタ返し限定 (非 pow2 サイズは下行で対応) |
| **複数registerまたはhidden ret_areaで struct を返す関数ポインタの間接呼び出し** `struct Big (*ob)(int); ob(100).a` | 🔧 | funcptr_return_large_struct, aggregate_return_register_width_boundaries | aggregate返り値の間接呼び出しは以前 IR build 失敗 ("ir build/emit failed")。3 箇所を修正: (1) parse_call_postfix が間接 funcall ノードにret sizeを未設定 → callee funcptrのcanonical戻り型から導出。(2) build_assign_struct が間接struct戻りを明示fail → 汎用funcall経路へ委譲。(3) result storage確保をdirect/indirect call共通へ変更。さらにtarget ABIを分離し、Apple ARM64は1〜16Bを`x0/x1`（端数registerは有効byteだけ保存）、16B超を`x8`、Wasmは既存のhidden return pointer契約でloweringする。3/5/6/7/9〜16Bと20B、struct/union、変数代入、直接メンバ、global/local funcptr、canaryをnative/WAT/objectで網羅 |
| **Wasm 制御フロー越し関数ポインタ呼び出し** `if(1) g=set7; g(&x);` / `ops.f(&x)` | 🔧 | wasm32_*_funcptr_control_flow_store, wasm_nonvoid_indirect_unused_result (続き116/239/240) | global/struct member の関数ポインタが制御フロー越しに上書きされると、Wasm emitter が callee 名を逆引きできず、void call と非 void unused-result call を区別できず E4008。関数ポインタ型に `funcptr_ret_is_void` を保存し、typedef/local/global/tag member/`node_mem_t` から `ND_FUNCALL.is_void_call` と `IR_CALL.is_void_call` へ伝播。続き239で非 void unknown indirect の未使用結果も `IR_CALL.dst.type` から typeuse を組み、WAT は `drop (call_indirect ... (result T))` として出せるようにした。続き240で Wasm object も objdump と link/run で確認 |
| **Wasm E2E subset / mixed int+fp 直接関数引数** `int f(int,double,int)` | 🔧 | test_wasm32_e2e / double_param_int_param_mix (続き117-121/241-247) | native `test_e2e` は Wasm backend を通らないため、既存 assert fixture を Wasm 用に変換し `ag_c_wasm -> wat2wasm -> wasm-validate -> wasm-interp` で `main() => i32:0` を確認する `test/test_wasm32_e2e.c` を追加し `make test` に組み込み。現時点で 1110 件を実行。続き118で arithmetic/switch/array/type_decl/inline/flex array/pragma pack/evil/func_name/string/stdheader 定数系/struct by-value/struct return まで拡張し、続き119で preflight 済み 447 件を `test/wasm32_e2e_extra_cases.txt` として追加。続き120で pointer operand を含む整数 binop を Wasm `i32` 演算に正規化し、比較結果型を `i32` として扱い、`i32.const` を 32bit 表現にすることで `pointer/array_decay_diff.c` など追加 19 件を回収。さらに byref struct 仮引数の Wasm signature を `i32` に合わせ、indirect callee vreg を table index (`i32`) として型付けして large struct by-value/direct member funcptr 系 4 件を追加回収。続き121で関数ポインタ型の戻り pointer/整数幅を indirect call の `IR_CALL.dst.type` へ伝播し、long 戻りと関数ポインタ返しチェーン 2 件を追加。導入時に `IR_PARAM` の `src1.imm` が integer/fp 別 ABI index なのに、Wasm signature 生成が単一 param list index として扱い、`int,double,int` が `(param i64 i64)` に潰れて WAT type mismatch。Wasm signature と entry `local.get $pN` は IR_PARAM 出現順で並べる。続き241で output-only minimal libc stub に `puts`、続き242で raw `assert.h` 用の `__assert_rtn` stub を追加。続き243で WAT c-testsuite preflight を 206/218 から 214/218 へ改善し、`strlen` 旧宣言、empty indirect table、string/calloc/sprintf minimal stub を backend test で網羅。続き244で forward 関数 table refs、`fprintf` 関数ポインタ stub、`getc` 宣言、file I/O minimal stub を追加し、WAT c-testsuite preflight は 218/218 compile + validate green。続き245で `make wasm32-wat-c-testsuite-scan`、続き246で `make wasm32-wat-fixture-scan` を恒久 target 化し、続き247で object/WAT fixture/c-testsuite scan をまとめる `make wasm32-scans` を追加。standalone 可能な全 fixture 1110 件を WAT compile + wat2wasm + validate、object fixture 1111 件を compile + validate、c-testsuite 218 件を WAT/object compile + validate で確認 |
| **Wasm positive E2E parityと拒否fixtureの分離** | 🔧 | test_wasm32_e2e, wasm32_e2e_extra_cases.txt, check-should-reject | Native E2E registryのfixture pathをWasm positive registryと照合する検査が、`should_reject`と`compiler_limits`のcompile-fail fixtureまで実行成功対象として要求し、inline sourceのfixture化後に大量の偽missingを報告していた。両directoryは専用Native/Wasm拒否gateの対象なのでpositive parityから明示除外し、通常のpositive fixtureだけは引き続きWasm static/link2/extra registryへの登録を必須にする。`goto_ice_array_scope_boundaries`を両registryへ追加し、Wasm E2E 1751 compile・1745 executeの全通過で分離を固定する |
| **Native/Wasm正例fixture集合の双方向parity** | 🧪 | test_design_invariants | Nativeのsingle-TU/multi-TU正例fixtureとWasmのstatic/link2/extra registryを集合として双方向照合する。Native専用の匿名aggregate ABI fixture、Wasm専用のruntime fixture、Wasmで意図的に拒否するstrict-C fixtureだけを明示差分として許可し、それ以外の未収載・余剰登録とNative側の重複を自動検出する。これによりNative E2E、Wasm E2E、object fixture scanの件数が異なっても、inline case、multi-TUの束ね方、target専用caseという根拠のない乖離はgreenにならない |
| **Wasm target固有multi-TU mismatch fixtureの完全収載** | 🧪 | test_design_invariants, test_wasm32_object, wasm32 object/WAT fixture scan | `test/fixtures/wasm32`の`*_main.c` / `*_other.c`をfilesystemから列挙し、companionの片側欠落を拒否する。全fixtureがobject linkerの拒否テストに含まれ、positive object link scanとstandalone WAT scanでは意図的なmismatchとして除外されていることを双方向に照合するため、新しい署名・layout mismatch fixtureが手書き表から漏れたままgreenにならない |
| **全C fixtureのtest-role partition** | 🧪 | test_design_invariants, test_compile_link_pipeline | `test/fixtures`配下の全Cファイルをportable positive、should-reject、compiler-limit、strict-C reject、Wasm target固有、continuation integrationへ重複なく分類し、未分類・多重分類・stale分類を拒否する。未実行だったrootの`wasm_continuation_basic.c`はJS pipelineのインライン複製を置き換えて直接実行し、fixture追加がどのgateにも入らない状態を防ぐ |
| **fixture補助headerの参照完全性** | 🧪 | test_design_invariants | `test/fixtures`配下の全headerをfilesystemから列挙し、basenameの衝突と、どのC fixtureからも参照されないorphan headerを拒否する。multi-TU共有宣言、macro include、source-location用header、compile-fail用conditional headerがfixture本体から切り離されたまま残る状態を防ぐ |
| **ポインタ typedef 仮引数の subscript** `typedef char* Str; len(Str s){ s[i] }` | 🔧 | pointer_typedef_param_subscript | param_decl_spec_t が typedef のポインタ性 (_ti.is_pointer) を捕捉せず、宣言子に `*` が無い (param_is_ptr=0) ため register_param_lvar のポインタ分岐に入らずスカラ登録され `s[i]` が E3064。deref `*s` は build_unary_deref が 8B 値を寛容に許し動作、直書き `const char* s` も動作。typedef 基底のポインタ段数を捕捉し宣言子の `*` と合成して実効ポインタ性を決める。char*/int*/const char*/struct*・多段 typedef・typedef+宣言子 `*`・ポインタ算術・添字代入を網羅 |
| **unsigned char/short ポインタ経由の zero-extend** `unsigned char* p; p[i]` | 🔧 | unsigned_char_pointer_zero_extend | pointee が符号拡張 (ldrsb/ldrsh) され 200→-56 等に化けた。unsigned スカラ・unsigned 配列要素は元から正常。3 経路を修正: (1) local subscript の最終要素判定 `pql==0 && inner_ds==0` が単段ポインタ (pql=1/inner_ds=elem) を最終要素と認識できず → fp の中間行判定と対称な `!is_pointer && !(inner_ds>0 && es>inner_ds)` に。(2) 仮引数 `unsigned char* p` の pointee unsigned を param_decl_spec_t に捕捉し var->is_unsigned へ伝播。(3) `*(p+i)` の ND_ADD/SUB operand を辿る node_pointee_is_unsigned ヘルパ追加。signed char*/short* は符号拡張維持。local/param・deref/subscript/arith・unsigned short・多段/2D を網羅 |
| **グローバルの 2 次元以上のポインタ配列** `int *t[2][2]` / `char *names[2][2]` / `int(*t[2][2])(void)` | 🔧 | global_2d_pointer_array | `t[i][j]` が SIGSEGV (非ポインタ `int t[2][2]` は動作)。3 修正: (1) apply_global_multidim_strides の `!head.is_ptr` ゲートでポインタ要素配列を除外し stride が立たず `t[i]` を「ポインタ値 load→[j] で deref」と誤計算→ゲートを外し elem_size=8 で stride。(2) build_subscript_deref の pointee_is_scalar_ptr が中間次元でも load していた→最終次元 (inner_ds==0) のみ load し中間は伝播 + 要素 pointee サイズを base_deref_size で carry (最終 base は中間 ND_DEREF で gv を引けないため)。(3) 括弧内配列 `(*t[2][2])` の paren_array_mul は積(4)のみで dims を捨てていた→psx_parse_array_suffixes_capture_dims で {2,2} を捕捉。2D/3D データ・char* 文字列・要素代入・2D funcptr (call/値) を網羅 |
| **ローカルの 2 次元以上のデータポインタ配列** `int *t[2][2]` | 🔧 | local_2d_pointer_array | `*t[i][j]` が SIGSEGV (グローバル版は別行、非ポインタ `int t[2][2]` は動作)。register_multidim_array_lvar が outer_stride を立てるが登録後に pql=1/base_deref_size=4 を立てるため、build_subscript_deref の「要素はポインタ」分岐 (pql>=1 && bds>0) が **1 段目** `t[i]` で発火し deref_size を inner_ds(8) から bds(4) に上書き→2 段目が +4/ldrsw (4B) に化けた。fp/unsigned と同じ中間行判定 (inner_ds>0 && es>inner_ds) で 1 段目を中間行と認識し pointer-element 化を最終次元まで遅延・pql/bds を carry。2D/3D・char*・代入を網羅。1D `int *arr[N]`・genuine `int **pp` は不変 |
| **ローカルの 2 次元以上の関数ポインタ配列** `int(*t[2][2])(void)` | 🔧 | local_2d_funcptr_array | ネスト brace init `{{a,b},{c,d}}` が E3064、個別代入でも `t[i][j]()` が SIGSEGV (1D `int(*ops[N])(int)` は動作)。funcptr 配列の局所登録 (decl.c:3185) が括弧内 `[N][M]` を inner_array_mul の積に潰し flat 1D 登録で多次元 stride 未設定。括弧内個別次元 (g_inner_array_dims) を捕捉し outer_stride/mid_stride (要素 8B funcptr) を設定。stride が立つことで 2D 配列と正しく認識され brace init も通る (E3064 も解消)。2D/3D・brace init・個別代入・値→呼び出し・引数つきを網羅。1D は不変。これで 2D ポインタ配列 (global/local × data/funcptr) を全て解消 |
| **UTF-16/UTF-32/wide 文字列リテラルの配列初期化** `unsigned short s[]=u"hi"` | 🔧 | wide_string_literal_init | 文字定数 `u'A'` は動くのに配列初期化子で壊れた: 明示サイズは値が 0、`[]` は E3064、global は .comm 0。ローカル init / `[]` 推論 / global init の 3 経路が要素幅 1 (char) 決め打ち。要素幅が char_width (char/u8=1,u=2,U/L=4) と一致するとき各コード単位を要素幅で格納し type_size=count*elem。ASCII 内容のみ |
| **非 ASCII の UTF-16/UTF-32 文字列リテラル** `U"aあb"` | 🔧 | wide_string_literal_init, string_array_typedef_unicode_inference_boundaries, encoded_string_unicode_exact_boundaries / should_reject 6件: encoded_multidimensional_string_overlong_global, encoded_string_*_unicode_too_long_* | 非 ASCII をUTF-8 byteのままcode unit化せず、char/u8はUTF-8 byte、uはUTF-16 BMP 1 unit・補助面サロゲート対2 units、U/Lは1 code pointとして数える。typedef経由の要素数不明配列は直接/単一brace初期化、global/automatic/static local/compound literal、BMP・補助面・終端NULを固定する。明示boundではu8補助面4 bytes、UTF-16補助面2 units、UTF-32/wide補助面1 unitのexact-boundだけ終端NUL省略を許可し、1要素短いglobal/local/member/multidimensional/compound literalをE3027で拒否する。6件をClang strictと照合し、should_reject正本からNative/Wasm object compile-failへ登録する |
| **混在Unicode wide stringのpointer反復** `for (p=L"..."; *p; ++p)` | 🧪 | wide_string_pointer_iteration_boundaries | UTF-8 source spellingのbyte数ではなく、targetの4-byte `wchar_t` code point単位で配列サイズ・subscript・pointer加算・pointer差を計算する。ASCII、CJK、cent/euro、BMP Greek/Japanese、補助面emojiを混在させ、global/local array、string literal pointer、compound literal、終端pointer、code point合計を検査し、c-testsuite 00220相当をClang strict・Native・WAT・Wasm objectで固定する |
| **encoded string内の明示NULと暗黙終端NUL** `char16_t s[3]=u"\U0001F600\0"` | 🔧 | encoded_string_embedded_null_boundaries / should_reject 5件: encoded_string_*_embedded_null_too_long_* | 明示`\0`は文字列内容のcode unitとして必ず配列へ格納し、省略可能なのは末尾の暗黙終端NULだけとする。u8補助面4 bytes+明示NUL、UTF-16サロゲート対2 units+明示NUL、UTF-32/wide補助面1 unit+明示NULのexact-boundをglobal/local/static local/member/multidimensional/compound literalで固定し、明示NULが収まらない1要素不足はE3027で拒否する。5件をClang strictと照合し、should_reject正本からNative/Wasm object compile-failへ登録する |
| **union active memberのencoded文字配列** `union U{long raw; char16_t text[4];} u={.text=u"..."}` | 🧪 | encoded_string_union_member_boundaries / should_reject 6件: encoded_union_* | unionで選択したu8/UTF-16/UTF-32/wide文字配列について、補助面code unit展開、暗黙終端NUL、残余要素のzero padding、exact-boundをglobal/static local/automatic、named・anonymous・nested union、外側struct、union配列designator、compound literalでClang strictと比較する。active member以外は読み出さず、選択memberと外側の前後memberを検証する。overlongはglobal/nested local/array designator/compound literal/明示NULをE3027、同幅の非互換element型をE3099で拒否し、6件をshould_reject正本からNative/Wasm object compile-failへ登録する |
| **ordinary/u8文字列内UCNのUTF-8 code unit展開** `u8"\u03A9"` | 🔧 | narrow_ucn_string_literal_boundaries | narrow文字列のcode unit計数・配列初期化がUCN値を1要素へ切り詰め、`\u03A9`を`0xA9` 1バイトとしてサイズ2の配列へ格納していた。UCN escapeだけをUTF-8の1～4 code unitへ変換し、raw UTF-8とhex/octal escapeのbyte保持を分離する。さらにaggregate初期化で合成した各byteをsource整数literalと誤認していたW3011を抑止する。BMP・補助面、ordinary/u8、raw/hex、global/local、structのchar配列メンバ、2次元char配列、静的`sizeof`、pointer literalをClang strict・Native/WAT/Wasm objectで固定する |
| **文字列prefixと配列element型の互換性** `short s[]=u"hi"` | 🔧 | string_array_element_type_boundaries, string_array_typedef_element_boundaries, string_array_compound_literal_type_boundaries / should_reject 16件: string_array_{utf16_signed_short,utf32_signed_int,wide_unsigned_int,typedef_enum,typedef_bool,compound_literal}_* | 文字列初期化の互換性をcode unit幅だけで判定していたため、同じ2-byteのsigned shortへ`u`、同じ4-byteのsigned intへ`U`、unsigned intへ`L`を誤受理していた。通常/u8はchar系、`u`はunsigned short、`U`はunsigned int、`L`はsigned intへ限定し、cv qualifierとtypedef aliasは許可、atomic qualifier・enum・`_Bool`は拒否する。明示長・推論長・global/automatic local/static local・aggregateに加えてfile/block scopeの配列compound literalを通る全経路と、終端NULだけを省略できるexact-bound配列をNative/WAT/Wasm objectで固定する。16件の型不整合をClang strictと照合し、should_reject正本からNative/Wasm object compile-failへ登録する。要素数不明配列で非互換文字列を要素数推論失敗へ潰していたE3064/E3114も、型不一致のE3099へ統一した |
| **encoding prefix付き・無prefix隣接文字列の連結** `u"A" "B"` / `"A" U"B"` | 🔧 | adjacent_encoded_string_literal_boundaries / should_reject: different_encoded_string_prefixes | parserが各tokenの文字幅を直接比較し、C11 6.4.5p5で接頭辞付きtokenと同じ接頭辞として扱う無接頭辞tokenをE3002拒否していた。sequence内の唯一の接頭辞を前後どちらからでも採用し、空の先頭tokenでも`u/U/L/u8`の幅を失わないようtoken有無と内容長を分離する。macro展開後の連結、global/local配列、`sizeof`、終端code unit、UTF-16 BMPとUTF-32補助面UCNをNative/WAT/Wasm objectで固定し、異なる非空接頭辞同士はClang strictと照合したshould_reject正本からE3058で拒否する |
| **接頭辞付き文字定数の型とUnicode値** `u'\u3042'` / `U'\U0001F600'` / `L'あ'` | 🔧 | encoded_character_constant_boundaries / should_reject: char16_{supplementary_character,hex_escape_out_of_range} | tokenizerがUCN・16進escapeの値を常に下位8 bitへ切り詰め、raw UTF-8をcode pointでなく複数byte文字として畳み、semantic型も全て`int`にしていた。u/U/Lではescape値を幅どおり保持し、raw UTF-8を1 code pointへdecodeする。型は同梱`char16_t`=`unsigned short`、`char32_t`=`unsigned int`、`wchar_t`=`int`へ一致させ、16 bitに収まらないu文字定数はE2025で拒否する。通常文字定数と既存の実装定義マルチ文字定数方針は維持し、UCN/hex/octal/raw、enum・static assert・global/local初期化をNative/WAT/Wasm objectで固定する |
| **接頭辞付き文字定数の整数昇格・variadic ABI** `u'\xFFFF'`, `U'\x80000000'`, `L'\u3042'` | 🧪 | encoded_character_promotion_varargs_boundaries | `char16_t`=`unsigned short`はunary・算術・default argument promotionで`int`へ昇格し、`char32_t`=`unsigned int`はunsignedを保持、`wchar_t`=`int`はintのまま渡る。`_Generic`でのunary plus・加算・mixed conditional結果型、literalと通常/volatile objectをdirect variadic関数およびvariadic関数pointerから`va_arg(int/unsigned int)`で回収し、16-bit最大値・Unicode scalar・bit31値が符号や上位bitを失わないことをClang strict・Native・WAT・Wasm objectで固定する |
| **UCNと生UTF-8による識別子の同一性・backend symbol** `caf\u00E9` / `café` | 🔧 | universal_character_identifier_boundaries | tokenizerが識別子内のUCNだけを受理し、同じ文字を生UTF-8で記述するとE2028で拒否していた。妥当なUTF-8 scalarをUCNと同じUTF-8列へ正規化し、bidi・zero-width・surrogate・overlong・範囲外は拒否する。さらにMach-O assembler symbolは必要時に全体をquoteし、WAT内部function IDは予約prefix付きbyte hexへ衝突なく符号化する。macro、ordinary/tag/member/label namespace、typedef、enum、token paste、global/function symbolをClang strict・Native/WAT/Wasm objectで固定する |
| **`__func__` のconst配列型・object identity・宣言名制約** `&__func__` / `__func__[0]='x'` / `int __func__` | 🔧 | predefined_function_name_const_boundaries / should_reject 6件: predefined_function_name_{element_assignment,mutable_pointer,*_redeclaration} | `__func__` を通常の変更可能な文字列pointerとして解決し、要素代入と`char *`初期化を許可、`&__func__`は未定義識別子扱い、複数回の参照には別々の文字列labelを登録していた。さらに暗黙のfunction-local宣言をscope graphへ登録しない実装のため、local object・typedef・parameter・function名としての再宣言も受理していた。C11 6.4.2.2どおり関数ごとの`static const char __func__[]`として、通常式ではconst char pointer、lvalue保持時はconst char配列型を返し、同一関数内の全参照で一つのstatic object labelを共有する。宣言入口では共通のpredefined identifier判定によりE3067で衝突を拒否し、`sizeof`、`_Generic`、要素・配列アドレス型、pointer identity、const違反と宣言名制約をNative/WAT/Wasm objectで固定する。6件をClang strictと照合し、should_reject正本から両compile-fail経路へ登録する |
| **`__func__` のstatic local定数初期化** `static const char *p=__func__` / `static const char (*a)[N]=&__func__` | 🔧 | predefined_function_name_static_initializer_boundaries | 関数本体のdirect resolverが持つ関数名と共有object labelをstatic initializer用の入れ子resolverへ渡さず、有効な`__func__`をE3066未定義識別子として拒否していた。関数コンテキストを明示的に渡し、scalar・aggregate initializer、配列全体のアドレス、address constant offsetを解決するとともに、初期化子と通常式の全参照が同じfunction-local static objectを指すことをClang strict・Native/WAT/Wasm objectで固定する |
| `__func__` のmacro展開位置・非macro性・関数外制約・Unicode関数名 | ✅ | predefined_function_name_macro_unicode_boundaries / inline compile_fail: predefined_function_name_file_scope_use_rejected | `__func__`自体はpreprocessor macroではなく、file-scopeで定義したmacro中の`__func__`は定義位置ではなく展開先関数を参照し、関数外の式ではag_cがE3066未定義識別子として拒否することを固定する。system Clangは関数外の直接使用をextension警告付きで受理するため、この1件はshould_reject正本へ入れずinlineに保つ。UCN表記・生UTF-8表記の関数識別子は正規化済みUTF-8関数名として格納し、UTF-8 byte数を反映した配列型、function-local static pointer初期化、同一object identity、Unicode backend symbolをClang strict・Native/WAT/Wasm objectで交差検証する |
| **グローバル struct の char 配列メンバの文字列初期化** `struct S{char name[8];} g={"main"}` | 🔧 | global_struct_char_array_member | char[] メンバを char* と取り違え、文字列ラベルのアドレス (.quad .LC0) を 8 バイトに格納し name 全体がポインタ値に化けていた。global brace flat パーサ (psx_gbrace_flat) が char 配列メンバ (tag 無し・要素 1 バイト・array_len>0) の文字列をポインタでなく array_len バイトへ展開するよう修正 (多次元 char 配列の行展開と同じ機構)。要素サイズは tag_member_info.type_size から (char 配列メンバは deref_size=0)。scalar/配列メンバ併存・escape・短い文字列 (0 埋め)・ローカル struct を網羅。char[] メンバの後に char* メンバが続く形 (旧「未対応 (a)」) は別バグ (下記 fp_kind 汚染) が真因で global_struct_member_after_fp_decl で修正済み。2D/3D 以上と brace elision は global_struct_2d_char_array_member / local_struct_2d_char_array_member / local_struct_3d_char_array_member / multidim_char_member_brace_elision で対応済み。struct 配列内の char メンバ (c) は global_struct_array_char_member で修正済み。**大きめプログラムの差分探索で発見** |
| **グローバル struct 配列要素の char 配列メンバ** `struct{char tag[4]; int n;} g[2]={{"aa",1},{"bb",2}}` | 🔧 | global_struct_array_char_member | emit_global_struct_array_init がメンバごとにフラット slot を 1 個だけ消費する単純ループで、配列メンバ (`char tag[4]`)・char 配列の文字列展開・入れ子 struct メンバ・bitfield を扱えず、tag が 1 バイトしか出ず (`.byte 97; .space 3`) 後続メンバ n に 2 文字目 'a'(97) が入り総崩れ。emit_global_struct_array_init を各要素について emit_global_struct_members_rec を呼ぶ形に書き換え、非配列 struct (emit_global_struct_init) と同じメンバ展開機構を要素ごとに適用して統一 (配列メンバ/char 配列展開/入れ子 struct/bitfield/部分初期化ゼロ埋め共通処理)。parser 側は元から各要素の char 配列をバイト展開できており emit 側のみの不具合。char配列先頭+スカラ・スカラ先頭+char配列・部分初期化・入れ子struct要素・char配列2本を網羅。**HANDOFF サブケース (c)。差分テストで発見** |
| **グローバル struct の 2 次元 char 配列メンバの文字列初期化** `struct{char rows[2][4];} g={{"ab","cd"}}` | 🔧 | global_struct_2d_char_array_member | 2 次元 char メンバの行幅 (outer_stride=4) が global brace flat パーサの gbrace_ctx_t に伝わらず、ネスト brace `{"ab","cd"}` の各行文字列が要素 (char) 扱いされ array_len=0 でポインタ (.LC ラベル) 出力 (`.quad .LC0; .quad .LC1`) になり行データがポインタ値に化けていた。gbrace_ctx_t に row_width を追加し多次元 char メンバ (tag 無し・elem 1B・outer_stride>0・array_len>outer_stride) で outer_stride を行幅に持たせ、gbrace_child_at が各要素を内側 1 次元 char 配列 (char[row_width]) として返し既存 char 配列展開分岐 (8c8ce2a) に乗せる。2D 基本/後続スカラ/先行スカラ/短い文字列 0 埋めを網羅。3D 以上は local_struct_3d_char_array_member、brace elision は multidim_char_member_brace_elision、ローカル 2D は local_struct_2d_char_array_member で対応済み。**HANDOFF サブケース (b)。差分テストで発見** |
| **fp 宣言の直後に来る tag グローバルの decl-spec fp_kind 汚染** `typedef long double T; struct{char b[4];char*p;} g={"x","y"}` で p が `.quad 0` | 🔧 | global_struct_member_after_fp_decl | トップレベル dispatcher (ps_next_function) が tag キーワード始まりの宣言を parse_toplevel_tag_decl へ直接回す経路で reset_toplevel_decl_spec_state を呼ばず、g_toplevel_decl_fp_kind が前宣言 (例: stddef.h の `typedef long double max_align_t;`→string.h 等が間接 include) の DOUBLE のまま残り、struct/union object の fp_kind が DOUBLE になっていた。すると global brace init の fp-fold 経路 (gv->fp_kind != NONE) が文字列リテラル/関数参照/アドレス初期化子を fp 定数(0)として食べ後続メンバが NULL/0 に化けた。parse_toplevel_tag_decl 冒頭の手動 extern/static リセットを reset_toplevel_decl_spec_state() に置換し宣言ごとに全クリア (tag 情報は install_toplevel_tag_decl_globals が再設定)。char[]+char*・funcref 初期化子・アドレス初期化子・文字列ポインタを網羅。**HANDOFF サブケース (a) の真因。先行 fp 宣言が無ければ char[]+char* 自体は元から動作。差分テストで発見** |
| **_Generic で long double を double と区別** `_Generic((long double)x, long double:.., double:..)` | 🔧 | generic_long_double | ag_c は long double→double lowering (fp_kind=DOUBLE) のため long double 制御式が `double:`/`default:` に当たっていた。long long/plain char と同じ side-channel ビット (node_mem_t.is_long_double) を宣言時に立てノードへ伝播し infer_generic_control_type が読む。fp_kind は不変で値は double と同一 (Apple ARM64)、_Generic 選択のみ変わる。ローカル変数・cast に加え、global/param と `typedef long double LD;` / `typedef LD LD2;` 経由の local/global/param へも typedef/global/param 型メタデータから伝播する。マクロ経由で制御式が `((T)x)` になる cast も parse_generic_selection で cast 型を採用する。 |
| **別TUの浮動rank署名互換性** `long double f(long double)` 対 `double f(double)` | 🔧 | floating_rank_signature_xtu / object: function_{,complex_}floating_rank_signature_mismatch_{main,other}, global_callback_{,complex_}floating_rank_signature_mismatch_{main,other} | canonical C署名が実数・complex型を物理bit幅だけの`f64`/`x128`として記録していたため、Apple ARM64およびWasm32で同じ表現を持つ`double`と`long double`、および対応するcomplex型を別TU間で誤って互換とした。rank suffix `F`/`D`/`L`を幅とは別に保持し、同じ`long double`／`long double _Complex`型の直接関数parameter/resultと外部global callback dataがNative・WAT・Wasm objectの真の2-TU経路で接続・実行できる正例を固定する。負例では低水準Wasm ABIが同じでも`double`／`double _Complex`との差を再帰的に保持し、それぞれ`function signature mismatch`／`data signature mismatch`として拒否する。旧metadataとの混在はversion差によりABI/layout検査へ安全にフォールバックする。 |
| **別TUの整数rank署名互換性** `short f(short)` 対 `int f(int)` | 🔧 | integer_rank_signature_xtu / object: function_short_int_rank_signature_mismatch_{main,other}, global_callback_char_short_rank_signature_mismatch_{main,other} | canonical C署名がsigned/unsigned char・short・intを符号と物理bit幅だけの`iN`/`uN`として記録していたため、複数rankが同じ幅になる合法なtargetで異なるC型を誤って互換とし、旧式関数prototypeのdefault argument promotion判定も幅だけで誤判定し得た。rank suffix `C`/`S`/`I`を幅とは別に保持し、同じ型の直接関数parameter/resultと外部global callback dataをNative・WAT・Wasm objectの真の2-TU経路で接続・実行する正例、および全rankを同じ32-bitへしたDataLayout単体境界を固定する。負例では同じ低水準Wasm `i32` ABIを持つshort対intの直接関数とsigned char対shortのglobal callback dataを、それぞれ`function signature mismatch`／`data signature mismatch`として拒否する。enumのcompatible intにも`I`を保持し、旧metadataとの混在はversion差によりABI/layout検査へ安全にフォールバックする。 |
| `float.h`の`FLT_*`/`DBL_*` macro型・表現境界 | 🧪 | float_binary_macro_boundaries | 両targetのbinary32/binary64契約について、radix・評価方式、仮数桁、10進精度、指数範囲、subnormal対応の整数macroをpreprocessorと`_Generic`で固定する。最大値・正規化最小値・epsilon・true-minのfloat/double型、静的配列初期化、volatile runtime演算、最小subnormalの半分がzeroになる境界をsystem Clang strict・同梱header使用時のClang・Native/WAT/Wasm objectで照合する。`FLT_ROUNDS`は実装によりruntime式になり得るため`#if`には置かず、int型とround-to-nearest値を実行時に検査する |
| `FLT_ROUNDS`の現在rounding direction追従 | 🔧 | float_rounds_fenv_state | `float.h`が`FLT_ROUNDS`を常に1へ固定していたため、`fesetround(FE_TOWARDZERO/FE_UPWARD/FE_DOWNWARD)`後もround-to-nearestと報告した。`fegetround()`を1回だけ読む内部inline helperでApple ARM64/Wasm共通のFPCR mode fieldをCの0=toward-zero、1=nearest、2=upward、3=downwardへ写像し、取得不能時は-1を返す。`float.h`→`fenv.h`のinclude順、元環境の保存・復元、4 modeの`fegetround()`/`FLT_ROUNDS`一致をsystem Clang strict・Native/WAT/Wasm objectで固定する |
| `float.h`の`LDBL_*` macro完全性・型境界 | 🔧 | float_long_double_macro_boundaries | Apple ARM64とag_c Wasmでは`long double`を8-byte IEEE binary64として扱うため、欠落していた`LDBL_MANT_DIG`、指数・10進桁、最大・最小・epsilon・true-min・subnormal macroを同じ表現に合わせて公開する。整数macro値、浮動macroが`double`ではなく`long double`型であること、正規化最小値・subnormal・epsilon・最大値の関係をnative/WAT/objectで固定する |
| `stdint.h`のleast/fast型・limit/constant macro型境界 | 🔧 | stdint_limit_constant_type_boundaries, inttypes_max_format_macro_boundaries | exact-width型だけだった同梱headerへleast/fast型、greatest/pointer/other limit、全`INTn_C`/`UINTn_C`と`INTMAX_C`/`UINTMAX_C`を追加する。小幅unsigned maxはinteger promotion後の`int`、`INT32_MIN`は`int`、`SIZE_MAX`は`unsigned long`になるよう値だけでなくmacro式のC型を固定する。nativeの`intmax_t`/`uintmax_t`はApple SDKの`long`/`unsigned long`、Wasmは既存runtime契約の`long long`/`unsigned long long`へ分離し、`inttypes.h`の全MAX/PTR/64-bit printf・scanf書式macroと`imaxdiv_t` member型も同期する |
| `inttypes.h`のleast/fast幅printf・scanf書式macro完全性 | 🔧 | inttypes_least_fast_format_macro_boundaries / wide_format_scan_boundaries | `stdint.h`が全`int_leastN_t`/`int_fastN_t`を公開している一方、対応するC11必須`PRI*LEASTN`/`PRI*FASTN`と`SCN*LEASTN`/`SCN*FASTN`計88 macroが欠落していた。各型が同幅exact型のaliasである契約に合わせて既存書式macroへ正本化し、全展開文字列と、signed・octal・hex・unsignedを含む全幅値の`snprintf`/`sscanf`往復をsystem/bundled Clang strictとnative/WAT/objectで固定する。standalone WATの`sscanf`/`swscanf` wrapperが完全な可変引数領域を無視して先頭2ポインタだけを渡していたため、3個目以降の出力先が壊れる問題も、呼出し側`__ag_va_arg_area`を優先する共通経路へ修正し、narrow 8出力・wide 4出力で固定する |
| `inttypes.h`のwide greatest-width変換・overflow境界 | 🔧 | inttypes_wide_conversion_function_pointer_boundaries | C11必須の`wcstoimax`/`wcstoumax`宣言とruntime symbolが欠落していた。`wchar_t`宣言可視性、targetごとに異なる`intmax_t`の基底型を含む関数ポインタ互換性、base 0/10/36、負のunsigned、変換不能時と成功時の`endptr`をnative/WAT/objectで固定する。standalone WATの共通wide整数変換は64-bit境界を越えるとwrapしていたため、signed/unsigned別のcutoff/cutlim判定、最大・最小値への飽和、`ERANGE`設定、overflow後も入力末尾まで走査する動作を追加する |
| `limits.h`の必須整数macro・式型・multibyte上限境界 | 🔧 | limits_integer_macro_boundaries | 欠落していた`MB_LEN_MAX`を追加し、native DarwinはSDK ABIの6、Wasmは同梱UTF-8変換が扱う最大4 byteとして分離する。全signed/unsigned character・short・int・long・long long limit macroの値、integer promotion後の式型、`#if`での利用、静的初期化をnative/WAT/objectで固定する |
| `stdlib.h`の`RAND_MAX`・`MB_CUR_MAX`とruntime整合境界 | 🔧 | stdlib_macro_runtime_boundaries | 欠落していた必須macroを追加する。`RAND_MAX`はnative Darwinの`0x7fffffff`と同梱Wasm LCGの32767へ分け、全`rand()`結果が範囲内で同じseedを再現することを固定する。`MB_CUR_MAX`はnative libcの動的`__mb_cur_max`とWasm UTF-8の4を公開し、常に1以上`MB_LEN_MAX`以下であること、各macroのC型と定数式利用をnative/WAT/objectで検証する |
| `stdio.h`の必須容量macro・同時stream境界 | 🔧 | stdio_macro_constant_boundaries | 欠落していた`FOPEN_MAX`と`FILENAME_MAX`を追加する。nativeはDarwin libcの`BUFSIZ=1024`、`FOPEN_MAX=20`、`FILENAME_MAX=L_tmpnam=1024`、`TMP_MAX=308915776`へ同期し、Wasmは標準3 streamと8個の動的FILE slotを合わせた`FOPEN_MAX=11`、64-byte path、32-byte一時名と既存buffer契約を公開する。全必須整数macroの値・式型・`#if`利用・配列定数式と、native/objectで標準3本に加えて`FOPEN_MAX-3`本を同時openできることを固定する。standalone WATのunavailable-file stubはmacro境界のみ検証する |
| `assert.h`再include時の`NDEBUG`切替・`static_assert`境界 | 🔧 | assert_reinclude_ndebug_boundaries | include guard内に`assert`定義があったため、最初のinclude後に`NDEBUG`を変更して再includeしても有効/無効が切り替わらなかった。宣言とC11必須`static_assert`だけをguardし、`assert`は毎includeでundef・再定義する。1 TU内で無効→有効→無効→有効を切り替え、無効時はoperand副作用なし、有効時は1回評価されることをnative/WAT/objectで固定する。WAT E2Eが`<assert.h>`を削除して`return 100` macroへ置換していた旧回避も廃止し、全WAT fixtureが実際の同梱headerと`__assert_rtn` stubを通るようにする |
| **C11 標準ヘッダ 10 個の同梱** iso646/stdalign/stdnoreturn/uchar/inttypes/fenv/locale/wctype/wchar/tgmath | 🔧 | c11_standard_headers | 欠落していた C11 ヘッダを追加 (関数実体はシステム libc)。tgmath 対応で 4 件のコンパイラバグも修正: (1) `long double` が _Generic 関連型でパース不可 (整数 cast-spec が `long` だけ食べる)→次が `double` なら整数列でないと判断。(2) `double` 制御式が `long double:` に誤マッチ (共に kind=TK_DOUBLE)→is_long_double で区別。(3) 外部関数アドレス `fp=sqrt` が adrp @PAGE 直参照でリンク失敗→GOT 経由 (@GOTPAGE)。(4) `_Generic(...)(args)`/`(name)(args)` の bare funcref 呼び出しが間接化しシグネチャを失い fp 戻り値を x0 で読む→funcref callee を直接呼び出しに変換しプロトタイプ ABI を適用。ASCII のみ |
| `iso646.h`の全11代替tokenと評価境界 | 🧪 | iso646_operator_macro_boundaries | `and/or/not`だけでなく`bitand/bitor/xor/compl/not_eq`と3 compound assignment macroをすべて固定する。preprocessor整数式、macro引数、論理演算の短絡とoperand 1回評価、bit演算・比較の組合せをsystem Clang strict・同梱header使用時のClang・Native/WAT/Wasm objectで照合する。headerの重複includeも許容する |
| `stdalign.h`のmacro表面とobject/member配置境界 | 🧪 | stdalign_header_macro_boundaries | `alignas`の整数式形・type-name形・zero指定、`alignof`のscalar/array/over-aligned record、`__alignas_is_defined`/`__alignof_is_defined`を固定する。global/local/static objectとover-aligned struct memberの実アドレス、前後memberの値、headerの重複includeをsystem Clang strict・同梱header使用時のClang・Native/WAT/Wasm objectで照合する |
| **`tgmath.h` 複数総称引数の共通実数型と単一評価** `pow(float,double)` | 🔧 | tgmath_mixed_dispatch_boundaries | `pow`/`fma`/`remquo`等を先頭引数だけでf/無印/l版へ振り分けていたため、後続の`double`/`long double`や整数引数を含むC11の共通実数型規則を反映できなかった。各総称引数をfloat/long double/整数をdoubleへ写してから通常算術変換でまとめる。`frexp`/`ldexp`/`scalbn`/`scalbln`/`modf`の指数・出力pointerは非総称引数として分離し、`_Generic`制御式が副作用を評価せず実呼出しで各引数を1回だけ評価することをnative/WAT/objectで固定する |
| **`tgmath.h` complex domain dispatchと共通幅変換** `sqrt(float _Complex)` / `pow(float _Complex,int)` / `fabs(complex)` | 🔧 | tgmath_complex_dispatch_boundaries | complex対応familyを実数関数へ渡して実部だけに変換し、戻り型も実数へ落としていた。`acos`/`asin`/`atan`、双曲線版、三角・双曲線、`exp`/`log`/`sqrt`を3幅の`c*`関数へ振り分け、`pow`は全総称引数の幅とdomainを合成して各引数を選択complex型へ1回だけ変換する。`fabs(complex)`は`cabs*`、`carg`/`creal`/`cimag`/`conj`/`cproj`は実数引数も対応するcomplex関数へ振り分け、型・値・副作用回数をnative/WAT/objectで固定する |
| **`math.h`/`tgmath.h`のgamma・隣接表現family** | 🔧 | tgmath_extended_real_family_boundaries | C11必須の`nextafter*`、`nexttoward*`、`tgamma*`、`lgamma*`計12 entry pointと4個の型総称macroが欠落していた。nativeはsystem libc ABIを宣言し、Wasmはfloat/double bit表現による隣接値移動と、整数factorial・Lanczos/reflectionによるgammaをheader内の自己完結実装として提供する。全幅function pointer、共通型dispatch、`nexttowardf`のlong double方向精度、NaN・無限・符号付きzero・pole、既知値、単一評価をsystem/bundled Clang strictとnative/WAT/objectで固定する |
| `void *` 戻り型を void 関数と誤判定 | 🔧 | void_ptr_return | (整数型の節参照) return チェックが is_pointer 無視 |
| 間接呼び出しの int→fp 引数昇格 (funcptr / typedef funcptr / struct member funcptr) | 🔧 | funcptr_int_to_fp_arg, typedef_funcptr_int_to_fp_arg, funcptr_member_int_to_fp_arg | (関数ポインタ節参照) 直書き・typedef 経由・struct メンバ経由とも対応済み |
| 間接呼び出しの fp 実引数→整数仮引数変換 (funcptr / typedef funcptr / struct member funcptr) | 🔧 | funcptr_fp_to_int_arg | (関数ポインタ節参照) 直書き・typedef 経由・struct メンバ・配列要素、int/long、FP 戻り混在まで対応済み |
| 可変長引数（int） | ✅ | call_conversion_qualifier_boundaries, variadic_via_func_pointer | 直接・関数ポインタ経由のint `va_arg`と複数slotをnative/Wasmで固定 |
| **可変長／未プロトタイプ呼び出しのデフォルト引数昇格境界** | 🔧 | function_default_argument_promotions, variadic_complex_arguments | `float`→`double`、整数昇格、`long double`/pointer/aggregate 不変に加え、`float _Complex` は昇格せず8Bにpackedされることを固定。呼び出しIRが可変長／未プロトタイプのcomplexを未対応としてE4007にし、対応後もApple ARM64で実部・虚部を別々の8B slotへ置いていたため、真の`...` complexだけtargetのaggregate slot policyで梱包するよう修正。未プロトタイプcomplexは従来どおり2つのFP pieceとして保持 |
| **プロトタイプなし関数ポインタの昇格・function address ABI** `int (*callback)()` | 🔧 | unprototyped_function_pointer_promotion_boundaries, unprototyped_funcptr_xtu_{main,other}, unprototyped_funcptr_return_xtu_{main,other}, unprototyped_{enum,atomic}_funcptr_xtu_{main,other}, unprototyped_parameter_categories_xtu_{main,other}, unprototyped_{promotion,narrow,bool,variadic,return}_signature_mismatch_{main,other}, test_typed_hir_unprototyped_callback_relocation_without_ast | bit-fieldの幅依存整数昇格、`float`→`double`、array/function designatorのdecay、complex/aggregate不変を、global・配列・member・selector由来の関数ポインタからold-style定義へ渡す。Wasm objectではプロトタイプなしfunction address relocationを0引数の確定signatureとして関数表へ登録し、後続定義の実ABIと衝突していた。IR/Machine IRで「parameter未指定」を保持し、同一TUではaddress placeholderを定義signatureでrefineする。cross-TUでは`agc.function_flags` custom sectionに未指定情報を保存し、リンカが同じ戻り値ABIかつdefault argument promotion後も不変な定義parameterだけを許可する。signed char・float・small/large structを別objectのold-style定義へ渡す経路に加え、compatible prototype定義からsmall structの直接返却、large structのhidden return領域、`double _Complex`を戻す経路を固定する。prototype definitionのpointer・`float _Complex`・aggregate・入れ子のfunction pointer parameterもcross-TU callで固定する。Wasmでは両側が同じ`i32`でも非互換な`signed char`・`_Bool` prototypeを拒否し、ellipsisを持つvariadic prototypeも拒否する一方、compatible integer typeと同じrankのsigned/unsigned enum prototypeは許可する。atomic typeは基底のchar/floatとは別categoryでdefault promotionにより変化しないため、`_Atomic(signed char)`・`_Atomic(float)` parameterを持つ未呼出しfunction addressの互換リンクを許可する一方、個々のprototypeなし呼出しの実引数互換性とは分離する。`float` prototypeと、Wasm上では同じhidden-pointer形になる別large struct戻り値も個別の負例で拒否し、Native/WAT/object link/runとselector単一評価を検証する |
| 関数ポインタ経由の可変長呼び出し `int(*f)(int,...); f(...)` | 🔧 | variadic_via_func_pointer | 可変長引数がレジスタ渡しされ Apple ARM64 ABI (stack 渡し) 違反で va_arg がゴミ。直接呼び出しは正常。funcptr lvar に is_variadic_funcptr+固定引数数を記録し経由呼び出しでも variadic ABI を選択 |
| **グローバル可変長関数ポインタ経由の呼び出し** `int (*fp)(FILE*, const char*, ...)=&f; fp(...)` | 🔧 | global_variadic_funcptr_call | ローカル funcptr は上記で修正済みだが、トップレベル declarator の関数サフィックスが skip_balanced_group のみで `...` を解析せず global_var に is_variadic_funcptr が立たなかった。skip_func_params 経路に統一し IR build で ND_GVAR callee も variadic ABI を選択。c-testsuite 00189 |
| volatile スカラ/ポインタの値（`volatile int`, `*p+=`） | ✅ | call_conversion_qualifier_boundaries | volatile scalarをpointer経由でload/store・compound assignment・incrementする値を固定。順序保証は別 |
| **破棄されるvolatileスカラaccess** `(void)global_value`, `(void)*pointer` | 🔧 | volatile_discarded_access_boundaries, test_typed_hir_volatile_load_lowering_without_ast, test_volatile_load_dce | HIRが通常の`IR_LOAD`だけを生成し、DCEが結果未使用のloadとそのaddress materializationを`nop`へ変えていた。`IR_LOAD.is_volatile`をglobal/local、dereference、volatile pointer object、subscript、member、bit-field、floating lvalueから伝播し、結果にuseがなくてもDCEで保持する。IR dumpにも`volatile` markerを出し、非volatile unused loadだけは従来どおり削除することを単体テストで固定する。address選択関数の単一評価と値をClang strictおよびnative/WAT/objectで照合する |
| **破棄されるvolatile complex access** `(void)volatile_complex` | 🔧 | volatile_complex_discarded_access_boundaries, test_typed_hir_volatile_complex_load_lowering_without_ast | complex lvalueは値表現として元objectのaddressだけを返していたため、discarded-value contextでは実部・虚部を一度も読まず、最適化後IRが`nop`だけになっていた。volatile complex lvalue conversion時に両componentを各1回volatile loadして非volatile一時値へmaterializeし、後続の値利用によるsource再読出しも防ぐ。float/double/long double complexのglobal array、local、pointer・top-level volatile pointer、member、qualified compound literal、address選択の単一評価をClang strictおよびnative/WAT/objectで固定する |
| **破棄されるvolatile aggregate access** `(void)volatile_record` | 🔧 | volatile_aggregate_discarded_access_boundaries, test_typed_hir_volatile_aggregate_load_lowering_without_ast | struct/union lvalueの値表現が元objectのaddressだけだったため、discarded-value contextではcomplete objectを一度も読まず、最適化後IRが`nop`になっていた。volatile aggregate lvalue conversion時にtarget layoutのsize/alignmentを持つ非volatile一時値へ`IR_MEMCPY`で1回materializeする。`.`の左operandはlvalue conversionの例外なのでTyped HIR構築時にqualifier除去castを挟まず、nested volatile aggregate memberへの書込みが一時コピーへ逸れないことも固定する。small/large struct、union、global array、local、pointer・top-level volatile pointer、nested member、qualified compound literal、comma、address選択の単一評価をClang strictおよびnative/WAT/objectで照合する |
| **volatile complex代入式のresult snapshot** `copy = (volatile_target = source)` | 🔧 | volatile_complex_assignment_result_snapshots, test_typed_hir_volatile_complex_assignment_result_without_ast | 通常代入とcompound assignmentがstore後のdestination addressを結果表現として返したため、結果を初期化・引数などへ使うとvolatile targetの実部・虚部を余分に再読出ししていた。complex source、scalar→complex変換、`+=`/`-=`/`*=`/`/=`は、storeに使った非volatile sourceまたは計算結果snapshotをそのまま返す。float/double/long double complex、global array、local、member、pointer・top-level volatile pointer、target/source/scalar選択の単一評価をClang strictおよびnative/WAT/objectで固定する |
| 文字/エスケープリテラル（`\n` `\0` `\x41` `\101`） | ✅ | literal_vla_boundaries | character/文字列中のnewline・NUL・hex・octal・quote・backslash・tabと終端をnative/Wasmで固定 |
| char/short 実引数の int 昇格（可変長経由） | ✅ | call_conversion_qualifier_boundaries, function_default_argument_promotions | signed/unsigned char・shortのdefault argument promotionと`va_arg(int)`を固定 |
| 非可変長の char/short/float 実引数昇格 | ✅ | call_conversion_qualifier_boundaries, function_argument_return_constraints | prototype parameterへのsigned/unsigned整数変換とfloat→double変換を固定 |
| `restrict` 修飾ポインタ（値の正しさ） | ✅ | call_conversion_qualifier_boundaries, restrict_compound_literal_recursive_identity | `restrict`付きdestination/source parameterによる配列更新に加え、direct・typedef・pointer-to-arrayのrestrict型名を持つcompound literalのstorage identityと値利用をnative/WAT/objectで固定 |
| `_Atomic` キーワード（`_Atomic int`/`long` の値） | ✅ | call_conversion_qualifier_boundaries, stdheader/stdatomic_ops | `_Atomic int`/`long`のload/store・compound assignment・incrementと標準atomic APIを固定 |
| 8-byte `_Atomic(float complex)`の値load/store・compound境界 | 🔧 | atomic_float_complex_value_boundaries, test_typed_hir_atomic_complex_lowering_without_ast | atomic-qualified complex lvalueを通常complexと同じaddressとして返していたため、実部・虚部を別々の`ldr/str`で読み書きし、8-byte objectとしてのatomic性を失っていた。float complex全体をI64 bit列としてIR_ATOMIC load/storeし、一時領域との変換後に通常のcomplex loweringへ渡す。compound assignmentは右辺を1回だけ評価し、旧値全体のloadから複素演算、I64 CASまでをretry loopにする。global/local、atomic同士の代入、代入式と`+=`/`*=`の値をnative/WAT/objectで実行し、IR unit testで8-byte atomic load/store/CAS命令数を固定する。16-byte double complexは単一word経路へ混入させない |
| 16-byte `_Atomic(double/long double complex)`の値load/store・compound境界 | 🔧 | atomic_wide_complex_value_boundaries, atomic_wide_complex_compound_boundaries, test_typed_hir_atomic_wide_complex_lowering_without_ast | double/long double complexのatomic lvalueも通常の2成分load/storeになっていた。Apple ARM64では16-byte aggregateと同じpair atomic storage経路へ統合し、aligned `LDP/STP` + `dmb ish`で値全体を読み書きする。compound assignmentは右辺を1回だけ評価し、16-byte旧値のloadから複素演算、`CASPAL`までをretry loopにする。Wasm32では単一thread backend契約に従い通常の複素演算へloweringする。global/local、pointer・配列、atomic同士の代入、代入式と`+=`/`-=`/`*=`/`/=`の値、関数return、lhs/rhs単一評価をnative/WAT/objectで固定する |
| 1/2/4/8/16-byte `_Atomic(struct/union)`の値load/store境界 | 🔧 | atomic_aggregate_value_boundaries, test_typed_hir_atomic_aggregate_lowering_without_ast | atomic-qualified aggregate lvalueを通常aggregateのaddressとして返し、代入も`IR_MEMCPY`だったため、値load/storeが分割された通常memory accessになっていた。qualified `DataLayout`からatomic storage幅を求め、1/2/4/8-byte object全体を単一`IR_ATOMIC` load/storeで一時領域と往復する。Apple ARM64の16-byte objectはClangのi128 seq_cst loweringと同じaligned `LDP/STP` + `dmb ish`を用い、12-byte recordも16-byte一時領域へpaddingする。Wasm32は128-bit atomic命令を持たない単一thread backendのため既存copy contractを維持する。struct/union、global/local、member・配列・pointer dereference、代入式の値とlhs/rhs単一評価をnative/WAT/objectで固定する |
| **破棄されるatomic aggregate/complex access** `(void)atomic_object` | 🧪 | atomic_discarded_object_access_boundaries, test_typed_hir_atomic_discarded_object_loads_without_ast, test_atomic_load_dce | atomic objectのlvalue conversionは結果が未使用でもobservableなatomic readを行う。4/8/16-byteのstructおよびfloat/double complexについて、global/local、pointer、top-level volatile pointer、member、array、address selectorの各経路をClang strictおよびnative/WAT/objectで固定する。直接HIR testは幅別の`IR_ATOMIC` load数を検査し、IR DCE testは未使用結果のatomic loadを削除しないことを保証する |
| **破棄されるscalar atomic access** `(void)atomic_scalar` | 🧪 | atomic_discarded_scalar_access_boundaries, atomic_enum_discarded_type_query_boundaries, test_typed_hir_atomic_discarded_scalar_loads_without_ast, test_atomic_load_dce | 結果未使用でも必要な1/2/4/8-byte atomic readを、integer、enum、float/double、data/function pointerについて固定する。global/local、pointer、top-level volatile pointer、member、array、address selectorをClang strictおよびnative/WAT/objectで照合し、直接HIR testでは各幅の`IR_ATOMIC` load数を検査する。Atomic enumの`sizeof` operandは逆に未評価であり、selectorを呼ばず型のsize/alignmentだけを返す |
| **破棄されるconst/volatile atomic access** `(void)cv_atomic_object` | 🧪 | atomic_cv_qualified_discarded_access_boundaries, test_typed_hir_cv_atomic_discarded_loads_without_ast | `const`または`volatile`をatomic object自体へ追加してもlvalue conversionはcomplete atomic readを行う。integer、floating、data pointer、4/16-byte struct、8/16-byte complexのglobal/localとqualified pointer selectorをClang strictおよびnative/WAT/objectで固定し、volatile atomicを通常の分割volatile loadへ誤loweringしないことも直接HIR testで検査する |
| **atomic compound literal拡張の値・address・読み捨て境界** `(_Atomic(T)){...}` | 🧪 | atomic_brace_initializer_extension, atomic_compound_literal_value_boundaries, test_typed_hir_atomic_compound_literal_loads_without_ast | system ClangがC11 strictで拒否するatomic compound literalを、ag_cは既存の非strict拡張として維持する。integer、floating、data pointer、4/16-byte struct、union、8/16-byte complexについてinitializer単一評価、discarded complete atomic read、address取得後のload/store、代入式resultをNative/WAT/objectで固定する |
| **atomic値の可変長・プロトタイプなし実引数変換とABI境界** `sink(0, atomic_value)` | 🔧 | atomic_variadic_value_promotion_boundaries, atomic_unprototyped_value_promotion_boundaries, test_typed_hir_atomic_variadic_value_promotion_without_ast, test_typed_hir_unprototyped_indirect_call_without_ast | variadic/unprototyped実引数のdefault argument promotionをHIRへ記録せず、IR call loweringが狭整数をI32、floatをF64へ遅延変換していたため、Typed HIRとcall argumentのcanonical型・source sizeが昇格前のままだった。atomic lvalue conversion後の`_Bool`/char/shortへ`int` cast、`float`へ`double` castをHIRで生成し、double・data pointer・float/double complex・4/16-byte struct・unionは対応する非atomic型のまま渡す。またプロトタイプなし呼び出しまでellipsis実引数用の8-byte aggregate分割を使い、old-style定義の通常parameter ABIと不一致になっていたため、ellipsisだけにvariadic policyを限定した。各atomic objectのcomplete load、selector単一評価、`va_arg`またはold-style parameterの型・値をNative/WAT/objectで固定する。system Clang 17は直接の`_Atomic float`だけAtomicToNonAtomic後の`float`→`double`昇格を落として実行結果が不一致になるため、この点はC11 6.3.2.1p2と6.5.2.2p7に従うag_cの挙動を正本とする |
| 同梱 `<stdatomic.h>` | 🔧 | stdheader/stdatomic_ops | **本物の LSE アトミックに更新済** (浮動小数の節「本物の <stdatomic.h>」行を参照)。旧・単一スレッド退化版から置き換え、fetch 系は規格通り旧値返却 |
| **Atomic enumのcompatible integer符号と更新式** `_Atomic(enum U)` | 🧪 | atomic_enum_{signedness_expression,integer_operator,qualified_storage_update}_boundaries, stdatomic_enum_{generic,compatible_expected}_boundaries, test_typed_hir_{qualified_atomic_enum_compound_lowering,atomic_enum_compatible_expected}_without_ast | 非負enumのatomic loadがcompatible `unsigned int`、負の列挙子を持つenumが`int`として式へ入ることを、負数との除算・剰余・比較、unary符号/bit反転、shift/bitwise、mixed conditional、subscript、switch、enum外表現値の代入、整数拡張・浮動小数変換、post-increment、pre-decrementで固定する。`%=`, `<<=`, `>>=`, `&=`, `^=`, `|=`はatomic CAS read-modify-writeとして結果値とobject値を一致させ、lhs selectorを各1回だけ評価する。`volatile`/`const volatile` atomic、local、struct member、array element、pointer dereference、top-level volatile pointerでも同じ値型と更新snapshotを維持し、直接IRテストでqualified enumの3更新が通常のvolatile load/storeではなく4-byte CASになることを検査する。`stdatomic.h`のload/store/exchange/CASでも対応する非atomic enum型を保持し、CASの`expected`にはenum自身だけでなく対応するcompatible integer型のpointerを、typedef、固定長配列/VLA decay、top-level `restrict`を経ても受理する。一方、別enum型、非互換整数型、`const`/`volatile`/`_Atomic` expected objectは拒否する。CAS失敗時のexpected更新、unsigned enum範囲外表現値、object/expected/value/order引数の単一評価を保証する。同じWasm `i32` storageでも通常算術変換の符号、`UINT_MAX` wrap、4294967295へのzero extension、atomic更新結果をClang C11・Native・WAT・object link/runで照合する |
| **プロトタイプなしfunction addressとdirect callの順序別ABI** `int (*p)()=f; f(atomic_enum);` | 🔧 | atomic_enum_value_promotion_boundaries, unprototyped_{address_then_direct_call,direct_call_then_address,repeated_direct_call,void_parameter,void_zero}_xtu_{main,other}, unprototyped_void_{zero,parameter}_signature_refinement / unprototyped_{address_direct_promotion,direct_address_bool,void_promotion}_mismatch_{main,other} | Wasm object生成でaddress参照が作った0引数wildcard署名を後続direct callの実引数ABIへrefineせず、定義前のcallを`conflicting Wasm object function signature`として拒否していた。unprototyped属性はlinker metadata用に保持したまま、最初のcall署名をWasm ABIの正本にし、後続callと定義を照合する。`_Atomic(enum)`の通常・条件・comma式からのlvalue変換、signed/unsigned compatible integerへのdefault argument promotion、variadic・direct/indirect old-style call、address→callとcall→address両順序のcross-TU metadata保持をNative・WAT・object link/runで固定する。複数callでは`signed char`/`int`、`float`/`double`、aggregateを同一昇格後ABIとして再利用する。逆にfloat prototypeと`_Bool` prototypeはdefault promotionで不変でないため、addressとdirect callのどちらが先でもobject linkerが拒否する。`void ()`の0引数Wasm署名は未初期化slotと同形でも同一TUの定義時にwildcard metadataを必ず消去し、cross-TU caller objectでは逆にmetadataを保持して`void (void)`定義へlinkする。void戻りかつ非空の`(i32, f64, aggregate)`署名も同一TUおよびcross-TUのaddress、direct call、indirect call、default promotion後に互換なprototype定義を通して同じABIへrefineする。void戻りでも`double`へ昇格したcallと`float` prototype定義はobject linkerが拒否する |
| `stdatomic.h` typedef identity・generic引数評価 | 🔧 | stdatomic_typedef_order_boundaries | `atomic_char16_t`/`atomic_char32_t`を誤ってsigned `int`へ、Wasmの`atomic_intmax_t`/`atomic_uintmax_t`を`long`へ固定していた定義を、それぞれ`uint_least16_t`/`uint_least32_t`/target別`intmax_t`へ同期する。全`_explicit`操作・flag・fenceの`memory_order`式、`kill_dependency`、定数展開で引数を捨てていた`atomic_is_lock_free`を各1回評価するgeneric関数契約をnative/WAT/objectで固定する |
| `stdatomic.h` generic load/storeのaggregate・complex型境界 | 🔧 | stdatomic_generic_object_load_store, stdatomic_enum_generic_boundaries, should_reject: stdatomic_{load_result_assignment,plain_object_argument} | 公開macroが戻り値・valueを`long`に固定したcompiler intrinsicを呼ぶため、標準Cで有効な`atomic_load(_Atomic(struct)*)`がE3120になり、aggregate/complexの対応する非atomic型を返せなかった。load/storeをatomic lvalueの直接読書きとしてloweringし、既存の型別atomic IR経路へ統合する。load結果はcomma式で非lvalue化し、代入対象にはできないgeneric function契約を維持する。1/2/4/8/16-byte struct/union、float/double complex、signed/unsigned enum、global/local/volatile、`atomic_init`、明示orderとobj/value/orderの単一評価、戻り型をnative/WAT/objectで固定する。非atomic object引数とload結果への代入はshould_reject正本から両compile-fail経路へ登録する |
| `stdatomic.h` generic exchange/compare-exchangeのobject型境界 | 🔧 | stdatomic_generic_object_exchange, stdatomic_enum_{generic,compatible_expected}_boundaries, test_typed_hir_{atomic_object_exchange_lowering,atomic_enum_compatible_expected}_without_ast, should_reject: stdatomic_const_expected_argument, stdatomic_compare_exchange_{distinct_enum,incompatible_integer,volatile,atomic}_expected | compiler intrinsicがinteger/pointerだけを受理していたため、標準Cで有効なfloating・complex・struct/unionの`atomic_exchange`と`atomic_compare_exchange_*`がE3120になっていた。対応する非atomic値を1回だけ評価してatomic storage表現へ変換し、1/2/4/8-byteはsingle-word exchange/CAS、Apple ARM64の16-byteは`CASPAL` retry、Wasm32の16-byteは単一thread backend契約の比較・copyへloweringする。enum CASのexpected型は同一TypeIdだけでなくCのcompatible integer型もtypedefや配列decayを経て受理し、同じWasm `i32`表現を持つだけの別enum・非互換整数型と、non-atomic unqualified型ではないexpected objectはE3120で拒否する。3→4、5→8、12→16のpadding、成功・失敗時のexpected更新、weak/strong・explicit order、float/double・complex・signed/unsigned enum、global/local/volatile、戻り型をnative/WAT/objectで固定し、expected型の5つの拒否境界はshould_reject正本から登録する |
| `stdatomic.h` generic関数のconst/volatile object境界 | 🔧 | stdatomic_const_volatile_object_boundaries / should_reject 9件 / compile_fail: stdatomic_const_object_{store,exchange,compare_exchange,fetch,init}, stdatomic_const_{aggregate_store,flag_clear,pointer_object_store,enum_object_exchange} | atomic objectの`const`を除去してvalueの代入変換だけを検査していたため、`atomic_store`等が`const _Atomic T *`を誤受理し、実際に書込み命令を生成していた。`atomic_load(_explicit)`だけはconst/const volatile objectを受理し、store/exchange/CAS/fetch/init/flag clearはconst objectをE3120で拒否する。整数、aggregate、atomic_flag、data pointer、enumの9件をshould_reject正本からNative/Wasm object compile-failへ登録し、compatible integerやpointer storageへloweringする場合もconstnessを保持する。mutable volatile objectへの全分類の操作、aggregate load、戻り型と引数の単一評価はpositive fixtureで固定する |
| `stdatomic.h` generic関数のoperand変換 | 🔧 | stdatomic_generic_operand_conversions / should_reject / compile_fail: stdatomic_fetch_aggregate_operand, stdatomic_pointer_{bitwise,incomplete_fetch} / inline compile_fail: stdatomic_fetch_floating_object_rejected | compiler intrinsicがfetch operandを整数型だけに限定していたため、generic関数の対応する非atomic整数型またはpointer用`ptrdiff_t`へ通常変換できるfloating/complex値をE3120で過剰拒否していた。またstore/exchange/CASではsemanticが変換を受理してもHIRへ反映せずE4007になり、pointerへのnull pointer constantもloweringできなかった。全scalar value引数を仮引数型へ1回だけ変換し、integerの全RMW、floating/complex object、complete object pointerのnull・加減算をnative/WAT/objectで固定する。aggregate operand、pointer bitwise fetch、`void *`のような不完全pointeeへの加減算はshould_reject正本からNative/Wasm object compile-failへ登録する。system Clangが受理するfloating object fetchはag_c固有のinline compile-failとして維持する |
| `stdatomic.h` explicit操作のmemory_order引数制約 | 🔧 | stdatomic_memory_order_argument_constraints, should_reject: stdatomic_{store_struct,load_pointer,compare_exchange_struct_success,compare_exchange_pointer_failure,thread_fence_struct,flag_pointer}_order | explicit操作とfenceがorder式を`void`化するだけだったため、通常の`memory_order`仮引数なら不適合なstructやpointerまで誤受理していた。scalar compound literal初期化でenumへの代入変換を必ず適用し、real/complex算術値は受理、非算術値は拒否する。全16個の正常系order引数の単一評価をnative/WAT/objectで固定し、不適合な6件はshould_reject正本から両compile-fail経路へ登録する |
| `stdatomic.h` kill_dependencyの型制約・値カテゴリ | 🔧 | stdatomic_kill_dependency_constraints, compile_fail: stdatomic_kill_dependency_{struct,union,void,result_assignment}_rejected | 単なる`(y)` macroだったため、規格外のaggregate/void引数を受理し、scalar結果もlvalueのまま代入可能だった。unevaluatedな条件式でscalar制約を課し、comma式で引数を1回だけ評価した非lvalue結果を返す。整数・atomic・const・complex・pointer・array decay・function designatorの型保持をnative/WAT/objectで固定する。system Clangの公開macroも単なる`(y)`でこの4件を受理するため、ag_c固有のinline compile-failとして維持する |
| `stdatomic.h` atomic_is_lock_freeの型・幅・戻り型 | 🔧 | stdatomic_lock_free_query_boundaries, should_reject: stdatomic_lock_free_{void_pointer,nonpointer}, compile_fail: stdatomic_lock_free_plain_object_rejected | 引数を評価して常に`int`の1を返していたため、atomic型以外も誤受理し、backendがatomic命令を生成できない16-byte超のobjectまでlock-freeと報告していた。unevaluatedなgeneric loadで`const volatile atomic A *`制約を検査し、target両方で対応する1/2/4/8/16-byte storageだけをtrue、超過幅をfalseとする`_Bool`結果へ修正する。任意aggregate・単一評価・型をnative/WAT/objectで固定する。void pointerとnonpointerはshould_reject正本から両compile-fail経路へ登録し、system Clangのmacroが受理するplain object pointerだけはinlineで維持する |
| `stdatomic.h`の全`ATOMIC_*_LOCK_FREE` macro境界 | 🧪 | stdatomic_lock_free_macro_boundaries | bool、char、char16/32、wchar、short、int、long、long long、pointerの10 macroをint整数定数式かつtargetで常時lock-freeを表す2として固定する。対応するbasic atomic objectに加え、data pointerとfunction pointerへ`atomic_is_lock_free`を適用し、公開macroと実queryがnative/WAT/objectで一致することを検証する |
| `stdatomic.h` generic関数の_Bool/void戻り型 | 🔧 | stdatomic_generic_return_type_boundaries, should_reject: stdatomic_{thread,signal}_fence_value | internal intrinsicの`int`を公開macroがそのまま返したため、compare-exchange 4種とflag test-and-set 2種が`int`、thread/signal fenceが値を返す式になっていた。公開境界で成否を`_Bool`へ正規化し、fence結果を`void`化する。成功・失敗時の値更新、全variantの型をnative/WAT/objectで固定し、fenceの値利用拒否はshould_reject正本から両compile-fail経路へ登録する |
| `stdarg.h` macroのvoid結果・va_copy独立性 | 🔧 | stdarg_macro_contract_boundaries, should_reject: stdarg_va_{start,copy}_value | `va_start`と`va_copy`が内部代入式の`va_list`値をそのまま返していたため、規格上voidのmacro結果を値として利用できた。両macroを明示的にvoid化し、`va_start`のlast仮引数は値評価せず型だけを検査する。va_start/va_copy/va_endの結果型、int/double/long longの独立した2本のva_list走査、va_argの指定型保持をnative/WAT/objectで固定し、値としての利用拒否はshould_reject正本から両compile-fail経路へ登録する |
| `stdarg.h` のtarget別`va_list` ABI・標準v関数転送境界 | 🔧 | stdarg_target_va_list_abi_boundaries | 同梱headerが`va_list`を両targetとも`long`へ固定し、Apple arm64 libcが公開する`char *`型と不一致だった。nativeはsystem `__builtin_va_list`と同じcharacter pointer、Wasmは既存runtimeの64-bit cursorへ分離する。型同一性・size/alignment、`va_list *`経由の`va_arg`、`va_copy`の独立走査、`vsnprintf`/`vsscanf`/`vswprintf`/`vswscanf`へのby-value転送をsystem/bundled Clang strictとnative/WAT/objectで固定する |
| `stddef.h` offsetofの整数定数式・designator契約 | 🔧 | stddef_stdbool_macro_contract_boundaries, should_reject: stddef_offsetof_{nonaggregate,missing_member,bitfield}, compile_fail: stddef_offsetof_nonconstant_index_rejected | 旧macroはnull pointerからmember addressを作る式で、実行時offsetは得られてもC11の整数定数式ではなく、列挙子や`_Static_assert`でClang strictにもag_cにも拒否された。`__builtin_offsetof(type, member-designator)` syntaxを追加し、record layoutからmember・nested member・constant array index・union・flexible arrayのoffsetをsize_t定数として解決する。非aggregate、存在しないmember、bitfieldはshould_reject正本からNative/Wasm object compile-failへ登録する。system Clangが拡張として受理する非定数indexはag_c固有のinline compile-failで維持し、併せてstdboolのbool/true/false型とNULL型をnative/WAT/objectで固定する |
| ワイド文字/文字列リテラル `L'A'` `L"hi"[i]` | ✅ | literal_vla_boundaries, wide_string_literal_init | wide characterとwide string subscript・終端、UTF-16/32配列初期化を正式fixture化 |
| stddef.h の wchar_t / max_align_t（C11 7.19） | 🔧 | stddef_wchar_t, stddef_max_align_t | wchar_t=int(4B), max_align_t=long double(8/8) |
| 戻り値の暗黙変換（int↔double, char/short/unsigned 切り詰め） | 🔧 | call_conversion_qualifier_boundaries, narrow_integer_return_boundaries, function_argument_return_constraints | double→intの0方向変換、int→double、`char`/signed・unsigned char、signed・unsigned short、`_Bool`の戻り値変換をdirect/関数ポインタ経由で固定。狭い整数戻り値の上位bitが呼出側へ漏れるARM64/Wasm不具合を、HIR→IRでのABI用I32正規化とtarget固有のzero/sign extensionで修正 |
| 整数から浮動小数への全幅・全変換文脈境界 | 🔧 | integer_to_floating_conversion_boundaries | ARM64の`IR_I2F`が32-bit以下のIR値にも64-bit `scvtf`/`ucvtf` operandを使い、直前の32-bit書込みで上位bitがzeroになった負値を巨大な正値として変換していた。IR source幅に応じてAArch64のW/X operandを選び、`_Bool`、signed/unsigned char・short・int・long long、runtime縮小castからfloat/doubleへの初期化・代入・aggregate、算術・compound/conditional/comma、return、direct/間接call、bit-field・enum、単一評価をClang strict C11およびnative/WAT/objectで固定する |
| 浮動小数から整数への全幅・全変換文脈境界 | 🧪 | floating_to_integer_conversion_boundaries | 表現可能範囲内のfloat/double/long doubleから`_Bool`、signed/unsigned char・short・int・long longへの変換を0方向切捨てとして横断固定する。明示castと代表的な暗黙初期化・代入・aggregate、算術・compound/conditional/comma、return、direct/間接call、bit-field、単一評価をClang strict C11およびnative/WAT/objectで照合し、F2Iのtarget幅・符号性とABI経路を一つのfixtureで検証する |
| formatted inputのfield width・代入抑制・length modifier | 🔧 | formatted_scan_boundaries, wasm32/stdio_sscanf_ops | `sscanf`/`vsscanf`の整数・狭/wide文字列・複数文字`%c`へfield widthを適用し、`*`変換は入力だけを消費してva_list・後続出力・assignment countを進めない。`%ld/%llu`の64-bit格納、`%u`での任意符号と符号を含む幅制限、`%ls/%lc`のwide出力をnative/WAT/objectで固定 |
| formatted inputのinput failure・matching failure | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | `sscanf`/`vsscanf`/`swscanf`は最初の代入前に空入力へ到達したinput failureで`EOF`を返し、非数字など入力はあるが変換できないmatching failureでは0を返す。`*`抑制変換だけが成功した後に入力終端へ達した場合も代入数0のinput failureとして`EOF`を返し、出力先を変更しないことを3経路で固定 |
| formatted inputの基数・pointer変換・消費文字数 | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | narrow/wideの`%i/%x/%X/%o`で10/8/16進を選択し、`%i`の`0`/`0x`自動判定、任意符号、prefixを含むfield width、`%li`の64-bit格納を固定。`%p`は`0x`/`0X` prefixとfield widthを含む16進pointer入力をWAT/object runtimeへ格納する。`%n/%ln`はnarrowではbyte数、wideではwide character数を格納し、入力もassignment countも増やさず、代入抑制後の位置も正しく記録する |
| formatted inputのscanset | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | `sscanf`/`vsscanf`/`swscanf`の`%[...]`へfield width、`^`反転、文字範囲、先頭`]`とliteral `-`、代入抑制、`l`付きwide出力を実装し、後続変換の入力・出力引数・assignment countを維持する。macOSのwide scanfは範囲表記を実装しないため、wide fixtureは明示文字集合で同じ境界を固定する。空入力のinput failureと先頭不一致のmatching failureを区別し、未代入bufferを変更しない |
| formatted inputのdecimal浮動変換 | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | WAT `sscanf`/`vsscanf`/`swscanf`へ`f/F/e/E/g/G`の符号・小数部・10進指数、field width、代入抑制を実装する。既定`float *`、`l`の`double *`、target上でdouble表現となる`L`の`long double *`を分けて格納し、指数部が不完全なら直前までへ戻す。空入力とmatching failureでは出力先を変更しない |
| formatted inputの特殊浮動値 | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | narrow/wideの浮動変換で大文字小文字を区別せず`inf`/`infinity`と`nan(payload)`を受理する。符号付きinfinity、`nan` payload後の入力位置、`%3lf`で`infinity`を`inf`までに制限するfield widthと後続文字をnative/WAT/objectで固定 |
| formatted inputの16進浮動変換 | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | `a/A`指定をobject/WAT dispatcherへ追加し、narrow/wideで`0x`/`0X` prefix、16進小数部、`p/P`の符号付き2進指数、既定`float *`と`l`の`double *`、prefixと指数を含むfield widthを固定する |
| formatted inputのsub-int length modifier | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | narrow/wideの`%hhd/%hhu/%hd/%hu`と`%hhn/%hn`で格納幅を1/2 byteへ縮め、隣接canaryを壊さないことをnative/WAT/objectで固定する。WAT runtimeの整数・消費文字数格納はlength modifierを1/2/4/8 byteのstore sizeへ正規化する |
| formatted inputの最大幅・size・difference length modifier | 🔧 | formatted_scan_boundaries, wide_format_scan_boundaries | narrow/wideの`%jd/%ju/%zu/%td`と`%jn/%zn/%tn`を対象ABIの8-byte格納へ正規化する。object runtimeは`long`と`long long`の出力pointer型を区別し、32-bitを越える値と抑制変換後の消費文字数をnative/WAT/objectで固定する |
| formatted outputの整数・count length modifier | 🔧 | formatted_output_boundaries | `snprintf`/`vsnprintf`の`hh/h/j/z/t`を解析し、`%hhd/%hhu/%hd/%hu`のdefault-promotion後の縮小、`%jd/%ju/%zu/%td`の8-byte値、`%hhn/%hn/%jn/%zn/%tn`の1/2/8-byte count格納をnative/WAT/objectで固定する。WATの固定引数`snprintf`も共通va-list formatterへ接続し、3個以上のdirect可変引数ではコンパイラが構築した完全なva_arg領域を選択する。object runtimeは`long`と`long long`のva_arg型を区別する |
| formatted outputの整数基数・代替形式 | 🔧 | formatted_output_integer_base_boundaries | `snprintf`/`vsnprintf`の`%x/%X/%o/%p`、`#`、幅、左寄せ、zero padding、整数精度をnative/WAT/objectで固定する。`hh/h/j/z`による縮小と64-bit値、精度指定時の`0` flag無効化、pointerの`0x`表現、精度0のzero-value抑制、複数の動的width/precision/value引数によるva_list cursorを検証する |
| formatted outputの文字列・文字境界 | 🔧 | formatted_output_string_character_boundaries, formatted_output_wide_character_conversion_boundaries | `snprintf`/`vsnprintf`の`%s/%c`と`%ls/%lc`を区別し、固定・可変幅、左寄せ、文字列精度、精度0を適用する。複数のwidth/precision/value引数によるva_list cursor、切り詰め時にも完全な必要長を返して終端する境界、wide文字列・文字から実行時multibyte文字列への基本実行文字変換をnative/WAT/objectで固定する |
| formatted outputの浮動小数点境界 | 🔧 | formatted_output_floating_boundaries, formatted_output_narrow_wide_composition | `snprintf`/`vsnprintf`の`f/F/e/E/g/G/a/A`へ精度、幅、左寄せ、zero padding、`+`/space sign、`#`、`L`を実装する。10進・16進の丸め繰上がり、10進tieのnearest-even、`g`の固定小数/指数表記切替、負のゼロ、`inf`/`nan`、負の動的精度による既定精度復帰をnative/WAT/objectで固定する。WATではnarrow/wide formatterが同じ浮動小数点中核とscratchを一度だけ生成し、同一translation unitで順に使用できることも検証する |
| formatted outputの64-bit `size_t`境界 | 🔧 | formatted_output_large_size_boundaries | object runtimeのnarrow/wide formatterが出力上限を比較するたびに`long`へcastし、`SIZE_MAX`を負数として扱って短い結果さえ書き込めず、終端位置も負のindexへしていた。出力・終端判定を`size_t`のunsigned比較へ揃え、`snprintf`/`vsnprintf`/`swprintf`/`vswprintf`の短い整数・文字列出力、終端NUL、結果直後のbyte保持をsystem Clang strictおよびnative/WAT/objectで固定する |
| WAT variadic標準関数のfunction pointer ABI境界 | 🔧 | variadic_function_pointer_wat_boundaries | standalone WATの直接呼出し用`printf`/`fprintf`/`snprintf`/`sscanf`/`swprintf`/`swscanf` stubは便宜上2個の可変引数parameterを持つ一方、関数tableへ同じstubを登録していたため、宣言上の固定引数だけを持つ`call_indirect`と型が一致せずtrapしていた。address取得時だけ固定引数signatureのadapterをtableへ登録し、compilerが準備する`__ag_va_arg_area`を既存formatter/scannerへ渡す。空のstream出力、narrow/wide整形、2個の整数scan、global function pointer初期化をsystem Clang strictおよびnative/WAT/objectで固定する |
| `sprintf`共通formatter経路 | 🔧 | sprintf_common_formatter_boundaries | WATの旧`d/u/s/c`専用parserを廃止して`sprintf`/`vsprintf`を`snprintf`系と同じva-list formatterへ接続する。3個以上の可変引数、`#`/zero padding、浮動小数点、`%ls/%lc`、`%n`を組み合わせ、direct可変引数領域のcursorと完全な戻り長をnative/WAT/objectで固定する |
| 整数文字列変換の基数自動判定 | 🔧 | strto_integer_base_detection_boundaries | `strtol`/`strtoul`/`strtoll`/`strtoull`/`strtoimax`/`strtoumax`とwide版について、`base == 0`の10進・8進・16進自動判定、符号、終端位置、最大基数の数字をnative/WAT/objectで固定する |
| 標準文字列変換APIのconst入力 | 🔧 | stdlib_const_input_qualifier_boundaries | `atoi`/`atof`/`atol`/`atoll`、全`strto*`、`strtoimax`/`strtoumax`、wide版へ`const`配列を渡せる標準宣言と、変換値・終端pointerをnative/WAT/objectで固定する |
| 標準multibyte変換APIの型と変換長境界 | 🔧 | stdlib_multibyte_const_boundaries | `mblen`/`mbtowc`/`wctomb`/`mbstowcs`/`wcstombs`の`const`・`wchar_t`・`size_t`を含む標準宣言、出力NULLでの変換長照会、出力上限での切り詰めと終端NULをnative/WAT/objectで固定する |
| multibyte変換APIの64-bit `size_t`境界 | 🔧 | multibyte_large_count_boundaries | object runtimeの`mbstowcs`/`wcstombs`/`mbsrtowcs`/`wcsrtombs`が64-bit unsigned上限をsigned `long`で保持し、`SIZE_MAX`を負数として短いASCII入力も0件にしていた。全8関数のlimitと、`size_t`を返す変換群の戻り値・内部counterをunsigned longへ揃え、2文字変換、終端NUL、restartable版のsource pointer NULL更新、標準signatureの関数pointerをsystem/bundled Clang strictおよびnative/WAT/objectで固定する |
| 標準メモリ管理・環境・終了callback APIの型 | 🔧 | stdlib_management_signature_boundaries | `malloc`/`calloc`/`realloc`/`aligned_alloc`の`size_t`、`getenv`の`const`入力、`atexit`/`at_quick_exit`の関数pointer型と、確保・ゼロ初期化・再確保・alignment・callback登録をnative/WAT/objectで固定する |
| `stdlib.h`終了API・assert失敗経路の`_Noreturn`境界 | 🔧 | stdlib_noreturn_cfg_termination_boundaries / stdlib_noreturn_cfg_warning_boundaries | C11で呼出し元へ戻らない`exit`/`abort`/`_Exit`/`quick_exit`と、assert失敗実装`__assert_rtn`の同梱header宣言に`_Noreturn`がなく、direct call後も非void関数の終端が到達可能としてW3005を出していた。宣言metadataをdirect HIR/IR callへ伝えて通常・parenthesized callと`assert(0)`のCFGを終端する。一方、function typeには`_Noreturn`を混ぜないため関数pointer経由の終了API、成功可能な`assert(value)`、`assert(1)`はW3005を維持する。Clang strict C11と照合し、正負fixtureをNative/WAT/objectで固定する |
| allocation失敗・64-bit `size_t`境界 | 🔧 | allocation_failure_size_boundaries | standalone WATのbump allocatorが64-bit要求量をi32へ無条件に切り詰め、`malloc(SIZE_MAX)`などの割り当て不能要求を小さな成功として返していた。heap位置・alignment・要求量をi64で計算し、現在のstack境界を越える場合と`calloc`の積overflowを非破壊で拒否する。`malloc`/`calloc`/`realloc`/`aligned_alloc`の巨大要求失敗、失敗した`realloc`後の元領域保持、標準signatureの関数pointerをsystem Clang strictおよびnative/WAT/objectで固定する |
| `string.h` APIの関数pointer呼出し境界 | ✅ | string_function_pointer_boundaries | copy・連結・比較・collation・検索・tokenize・初期化・長さ・error文字列APIの標準signatureと、function table越しの`size_t`引数・戻りpointer・`strtok`状態をnative/WAT/objectで固定する |
| `strchr`/`strrchr` の検索文字`char`変換境界 | 🔧 | string_search_char_conversion_boundaries | standalone WATが文字列byteをunsigned loadしながら、負のplain `char`からpromoteされた検索値を8-bit表現へ戻さず直接比較していたため、同じbit patternの文字を見つけられなかった。検索値を`unsigned char`表現へ正規化し、先頭・末尾の高bit byte、終端NUL、未検出、標準signatureの関数pointer呼出しをsystem/bundled Clang strictおよびnative/WAT/objectで固定する |
| narrow/wide `*n*`文字列APIの64-bit `size_t`境界 | 🔧 | string_large_count_boundaries | standalone WATの`strncmp`/`strncat`が64-bit countを32-bit pointer差/countへ切り詰め、`2^32`で0件として扱っていた。object runtimeの`strncmp`/`strncat`/`wcsncmp`/`wcsncat`はunsigned `size_t`をsigned `long`で比較し、`strxfrm`/`wcsxfrm`もsigned limitへ変換して`SIZE_MAX`を負数扱いしていた。短いNUL終端入力なら巨大上限でもアクセス範囲は短いままなので、WATをi64 unsigned counter、objectをunsigned longの上限・indexへ揃え、両極値、変換結果とnarrow/wide関数pointerをsystem/bundled Clang strictおよびnative/WAT/objectで固定する |
| `ctype.h` / `wctype.h` APIの関数pointer・入力domain境界 | ✅ | character_classification_function_pointer_boundaries | narrow/wideの全分類・大小文字変換、`wctype`/`wctrans` descriptor APIの標準signatureと間接呼出しを固定し、`EOF`・`WEOF`・unsigned-char上限で分類0・変換恒等となるC locale動作をnative/WAT/objectで検証する |
| `wctype.h` のtarget別descriptor基礎型ABI境界 | 🔧 | wctype_descriptor_target_abi_boundaries | 既存の関数pointer試験は宣言と同じtypedefを両側に使うため、同梱`wctype_t`のsigned `int`固定がApple arm64 libcの`unsigned int` ABI型と異なることを検出できなかった。nativeの`wctype_t`だけをunsigned int、Wasm runtime契約はintに分離し、`wint_t`/`wctrans_t`は両targetでintを維持する。基礎型同一性・符号・size/alignment、基礎型を直接書いた`wctype`/`iswctype` function pointer、descriptor検索・分類・大小変換をsystem/bundled Clang strictとnative/WAT/objectで固定する |
| `time.h` / `locale.h` APIの関数pointer・失敗境界 | ✅ | time_locale_function_pointer_boundaries | 時刻取得・差分・分解・正規化・文字列化・format・timespecおよびlocale APIのconst/sizeを含む標準signatureを間接呼出しで固定する。`setlocale`はC localeの照会・選択を受理し、未対応locale名にはNULLを返す契約をnative/WAT/objectで検証する |
| `strftime` / `wcsftime` の64-bit `size_t`境界 | 🔧 | time_format_large_count_boundaries | standalone WATの`strftime` formatterが64-bitの出力上限を比較時にi32へ切り詰め、`2^32`を0件として短い結果も拒否していた。出力文字と終端NULの上限判定をi64 unsigned比較へ揃え、標準signatureの関数pointer経由で日時文字列、戻り長、終端NUL、結果直後の保持をsystem Clang strictおよびnative/WAT/objectで固定する |
| `locale.h`の全category macro・選択境界 | 🧪 | locale_category_macro_boundaries | `LC_ALL/LC_COLLATE/LC_CTYPE/LC_MONETARY/LC_NUMERIC/LC_TIME`をtargetのint整数定数式として固定し、headerの重複include、enum・静的配列での利用、各categoryのC locale query/setを関数pointer経由で検証する。負値と十分大きい不正categoryはNULLを返し、native libc・standalone WAT・object runtimeで同じ公開境界を維持する |
| `timespec_get` の`TIME_UTC`成功・失敗境界 | 🧪 | time_timespec_get_utc_boundaries | `TIME_UTC`が正のbase識別子であり、直接呼出しと関数pointer経由の呼出しが成功時に`TIME_UTC`、calendar clockを提供できない環境では0を返すことを固定する。成功時だけ`tv_nsec`が0以上10億未満へ正規化されることを確認し、nativeの時計取得とWAT/object runtimeの許容された失敗経路を同じfixtureで検証する |
| `time.h` の`CLOCKS_PER_SEC`・`TIME_UTC`型境界 | 🔧 | time_macro_type_boundaries | `CLOCKS_PER_SEC`を裸の`int`定数として公開していたため、nativeの`unsigned long`とWasmの`long`である`clock_t`型契約を満たしていなかった。target別`clock_t`へ明示castし、1000000という値、整数定数式、静的配列初期化、差分・除算での型保持を固定する。`TIME_UTC`は正の`int`整数定数式としてenumと`_Static_assert`で検証する |
| `time.h` のtarget別`clock_t`型・符号境界 | 🔧 | time_target_type_abi_boundaries | Apple SDKの`clock_t`は`unsigned long`だが、同梱headerはWasm runtime用のsigned `long`をnativeにも公開していた。`time_t`は両targetで`long`を維持しつつ、`clock_t`の型同一性・符号・size/alignmentを`_Generic`と比較で固定し、`time`/`clock`の標準signature、間接呼出し、`time_t *`書込みの前後canaryをnative/WAT/objectで検証する |
| `time.h` の`struct timespec` ABI・書込み幅境界 | 🧪 | time_timespec_target_abi_boundaries | `tv_sec`が`time_t`、`tv_nsec`が`long`である型identityと、0/8のmember offset、16-byte size、8-byte alignmentを両targetで固定する。`timespec_get`を直接・標準signatureの関数pointer経由で呼び、未対応baseおよびcalendar clockを提供できない失敗経路では構造体を保持し、成功時のnanosecond正規化と全経路の前後canary保持をsystem/bundled Clang strictとnative/WAT/objectで検証する |
| `time.h` のtarget別`struct tm` ABI・書込み幅境界 | 🧪 | time_struct_tm_target_abi_boundaries | `wchar.h`が`struct tm`を先行宣言した後でも`time.h`が同じtagを完成させることを固定する。標準9個のint member offsetに加え、nativeはApple libc ABIの`tm_gmtoff`/`tm_zone`を含む56-byte・8-byte alignment、Wasmはruntime契約の36-byte・4-byte alignmentを検証する。`gmtime`/`localtime`/`mktime`/`strftime`/`wcsftime`を標準signatureの関数pointer経由で呼び、`mktime`の正規化書込みが前後canaryを保持することをsystem/bundled Clang strictとnative/WAT/objectで固定する |
| `localeconv`の完全`lconv` layout・C locale値境界 | 🔧 | localeconv_full_layout_boundaries | 既存fixtureは先頭の`decimal_point`だけを読み、object runtimeも1 pointerだけの短い擬似構造を返していたため、後続23 fieldが別globalを誤読していた。10個の文字列pointer、14個の数値field、target別size/offsetを完全な構造へ揃え、C localeの`.`、空文字列、`CHAR_MAX`を関数pointer経由でnative/WAT/objectに固定する。standalone WATのstatic dataも全fieldを明示初期化する |
| `sys/resource.h` の関数pointer・target別`rusage` ABI境界 | 🔧 | resource_usage_function_pointer_abi_boundaries | `getrusage`の標準signatureと`RUSAGE_SELF/CHILDREN`を固定する。同梱headerの8-byte Wasm shapeをnativeでも使用していたため、Apple `_getrusage`の144-byte書込みがcaller stackを破壊する境界をtarget別layoutへ分離し、timeval・全counterのoffsetと前後canaryをnativeで検証する。sandboxed WAT/object runtimeは`ENOSYS`で失敗し、callerの`rusage`とcanaryを変更しない契約を同じ間接呼出しで固定する |
| `setjmp.h` のtarget別`jmp_buf` ABI・非局所制御境界 | 🔧 | setjmp_native_control_flow_abi_boundaries, setjmp_noreturn_cfg_termination_boundaries / setjmp_noreturn_cfg_warning_boundaries | 同梱headerがWasm用`long[48]`（384 bytes）をnativeにも公開していたため、Apple arm64 SDK/libSystemの`int[48]`（192 bytes）とtarget別に分離する。nativeでは`setjmp`/`longjmp`を標準signatureの関数pointer経由で呼び、非zero値の復帰と`longjmp(..., 0)`の1への変換、`volatile`状態保持を検証する。C11の`longjmp`宣言には`_Noreturn`を付け、direct/parenthesized call後のCFGを終端してW3005を抑止する一方、function typeには非復帰属性を混ぜず間接callではW3005を維持する。NativeとWasm objectの診断をClang strict C11と照合する。Wasm WATは両fixtureを理由付きunsupported caseとして登録し、非局所制御を誤って実行可能扱いしないことも固定する。standalone stub用opaque storageを維持し、object linkerの明示的なunsupported-control-flow方針とは分離する |
| `wchar.h`の`WEOF`・`WCHAR_MIN/MAX`型・include順境界 | 🔧 | wchar_macro_type_boundaries | `wchar.h`→`stdint.h`の順では、前者の`(-2147483647-1)`と後者の`(-WCHAR_MAX-1)`が同値でも異なるmacro token列としてE1044になっていた。両headerで`WCHAR_MAX`を先に定義し、個別guard下の同一`WCHAR_MIN`式へ正本化する。`WEOF`をtargetの`wint_t`型かつ-1、両limitをsigned 32-bit `wchar_t`表現のint整数定数式として、両include順・再include・enum・静的wide配列・wide文字との非同一性をsystem Clang strict・native/WAT/objectで固定する |
| `wchar.h` wide文字列・メモリAPIの関数pointer境界 | ✅ | wide_string_function_pointer_boundaries | wide文字列のcopy・連結・比較・変換・検索・tokenizeとwide memory APIのconst/sizeを含む全標準signatureを関数pointer代入で固定し、間接呼出し時の戻りpointer、終端NUL検索、padding、重複move、count=0をnative/WAT/objectで検証する |
| `wchar.h` wide数値・restartable変換APIの関数pointer境界 | ✅ | wide_conversion_function_pointer_boundaries | `wcsto*`、`btowc`/`wctob`、`mbrtowc`/`mbrlen`/`mbsinit`/`wcrtomb`/`mbsrtowcs`/`wcsrtombs`の標準signatureと間接呼出しを固定する。出力NULLの必要長照会では`*src`を保持し、出力上限で停止した場合は次の入力位置へ進め、終端まで変換した場合だけNULLへ更新する契約をnative/WAT/objectで検証する |
| `wchar.h` wide浮動変換の10進指数・`endptr`境界 | 🔧 | wide_floating_exponent_conversion_boundaries | standalone WATの`wcstof`/`wcstod`/`wcstold`共通parserが整数部と小数部だけを読み、`1.25e2`を`1.25`で止めていた。wide文字の4-byte strideで`e`/`E`、任意符号、指数digitを走査して10の累乗を適用し、指数digitが1個もなければ指数marker位置へ巻き戻す。先頭小数点、正負指数、空白・符号、変換不能入力、元wide文字列基準の`endptr`と3幅の関数pointer呼出しをsystem/bundled Clang strictおよびnative/WAT/objectで固定する |
| `stdlib.h`/`wchar.h` 浮動文字列の特殊値・16進・range境界 | 🔧 | floating_string_special_range_boundaries | standalone WATのnarrow/wide浮動parserが`inf`/`infinity`/`nan`、16進仮数と2進指数、double overflow/underflow時の`ERANGE`を扱えなかった。1-byte/4-byte文字幅だけを引数にする共通parserへ統合し、大文字小文字を問わない特殊値、`nan(...)` payload、`0x`仮数と明示`p`指数、過大指数の上限化、非zero入力が無限大またはzeroへrange outした場合のerrno、符号付きzero、元文字列基準の`endptr`を固定する。6関数の標準signatureを関数pointerで保持し、system/bundled Clang strictおよびnative/WAT/objectで検証する |
| `strtof`/`wcstof` のfloat固有overflow・underflow境界 | 🔧 | floating_string_float_range_boundaries | standalone WATとobject runtimeが`strtof`/`wcstof`を「double parserの結果を単にfloatへcast/demote」していたため、doubleでは有限な`3.5e38`がfloatで無限大になる場合や、`1e-40`がsubnormal、`1e-100`が符号付きzeroになる場合に`ERANGE`を設定しなかった。doubleとして有限・非zeroの値をfloatへ縮小した後、無限大または`FLT_MIN`未満になった結果を検出してerrnoを設定する。double変換側では同じ入力がrange errorにならないこと、正負符号、subnormal/zero、元文字列基準の`endptr`、narrow/wide関数pointerをsystem/bundled Clang strictおよびnative/WAT/objectで固定する |
| `strto*`/`wcsto*` のNaN payload完結・巻き戻し境界 | ✅ | floating_string_nan_payload_boundaries | `nan(_A19)`の英数字・underscore payloadと空の`nan()`は閉じ括弧まで消費し、未閉鎖の`nan(payload`は`nan`直後の開き括弧で停止することを固定する。大文字小文字、narrow/wide、float/double/long doubleの6標準signatureを関数pointer経由で呼び、元文字列基準の`endptr`をsystem/bundled Clang strictおよびnative/WAT/objectで検証する |
| `strtod`/`wcstod` のhex significand・任意`p`指数境界 | 🔧 | floating_string_hex_subject_boundaries | standalone WATとobject runtimeが16進浮動subjectに`p`指数を必須としていたため、`0x1.8z`を1.5ではなく先頭`0`だけとして扱い、不完全な`0x1p+x`もvalid significandの1.0と指数marker直前へ巻き戻せなかった。16進digitがあれば指数なしでもsubjectを確定し、`p`/`P`後にdigitがなければmarkerへ戻す。hex digit自体がない`0x.p1`は先頭`0`の10進変換として`x`で停止し、object wide end scannerも実値parserと揃える。大文字小文字、narrow/wide、double/long doubleの関数pointerと`endptr`をsystem/bundled Clang strictおよびnative/WAT/objectで固定する |
| `wchar.h` wide stream APIの関数pointer・状態境界 | ✅ | wchar_stream_function_pointer_state_boundaries | `f/get/put/unget` wide文字、wide文字列、`fwide`の全固定引数signatureを関数pointer代入で固定する。native/objectではwide orientationを維持したまま間接出力、rewind、pushback、改行単位入力、EOF/error clearを検証し、standalone WATではfile store非対応時の入出力stub契約を同じ間接呼出し経路で明示する |
| `uchar.h` restartable変換APIの関数pointer・状態境界 | ✅ | uchar_function_pointer_state_boundaries | `mbrtoc16`/`c16rtomb`/`mbrtoc32`/`c32rtomb`の`char16_t`・`char32_t`・const・`size_t`・`mbstate_t`を含む標準signatureを関数pointer代入で固定する。symbolを持たないDarwin libc向けには同梱headerのUTF-8 state実装を使い、fixture内の代替定義を廃止する。ASCII/NUL、入力長0、NULL出力、state reset、最大`size_t`、4-byte scalarの分割入力、UTF-16 surrogate、invalid sequenceの`EILSEQ`、出力幅と前後canaryをnative/objectで検証し、standalone WATのASCII-only stub境界も明示する |
| `wchar.h` / `uchar.h` のtarget別`mbstate_t` ABI境界 | 🔧 | mbstate_target_abi_boundaries | Apple SDK/libcの`mbstate_t`は128-byte・8-byte alignmentのopaque unionだが、同梱headerはWasm runtime用32-byte・4-byte stateをnativeにも公開していた。両headerを同時includeした型のsize/alignmentをtarget別に固定し、`mbrtowc`・`mbrtoc32`・`mbsinit`を同じguarded stateへ関数pointer経由で適用して前後canaryとreset状態をnative/WAT/objectで検証する |
| 標準header間の共有定義・逆include順境界 | 🧪 | standard_header_shared_definition_boundaries | 既存coverageと逆の`stdio→wchar→uchar→wctype→stdlib→stddef→stdint`順と全headerの再includeを固定し、`FILE`、`mbstate_t`、`wint_t/WEOF`、`NULL`、`WCHAR_MIN/MAX`、`char16_t/char32_t`が単一の互換定義へ収束することを検証する。target別`mbstate_t` size/alignment、公開関数pointer signature、整数limit値をnative/WAT/objectで同時に保持する |
| 全同梱public headerの合成・再include境界 | 🧪 | public_header_composition_boundaries | 同梱する30 headerを逆順で初回includeし、通常順で再includeする。共有typedef、target依存の公開型、include guardが単一translation unitで衝突せず、Native/WAT/Wasm objectで同じ基本幅・alignment・zero initializationを保つことを固定する |
| `inttypes.h` APIの関数pointer・構造体戻り値ABI境界 | ✅ | inttypes_function_pointer_abi_boundaries | `imaxabs`/`imaxdiv`/`strtoimax`/`strtoumax`の`intmax_t`・`uintmax_t`・constを含む標準signatureを固定し、64-bit値と`imaxdiv_t`のtarget固有aggregate returnを伴う間接呼出し、負数の商・剰余、end pointer更新をnative/WAT/objectで検証する。Apple ARM64は16-byte値を`x0/x1`で受け、Wasmはhidden return pointer契約を維持する |
| Apple ARM64 aggregate戻り値のregister幅・端数境界 | ✅ | aggregate_return_register_width_boundaries | 1〜16 byteのinteger-class aggregateを最大2個の結果registerへ分類し、3/5/6/7/9〜16 byteの部分registerを有効byte数だけ保存・復元する。関数pointer呼出し、全byte、前後canaryをnativeで検証し、WAT/objectでは既存のtarget固有hidden return契約との分離を同じfixtureで固定する |
| `fenv.h` APIの関数pointer・状態遷移境界 | ✅ | fenv_function_pointer_boundaries | 全11 APIのconstを含む標準signatureを関数pointer代入で固定する。例外flagのraise/clear/get/setとmask保持、`feholdexcept`の保存・clear、`feupdateenv`のmerge、4 rounding mode、`FE_DFL_ENV`、元環境の復元をnative/WAT/objectで検証する。Apple ARM64ではSDK ABIに合わせて`fenv_t`をFPSR/FPCR順、`fexcept_t`を16-bit、`FE_DFL_ENV`を`&_FE_DFL_ENV`とし、Wasmの16-byte環境と`-1` sentinelはtarget branchへ隔離する |
| `signal.h` の入れ子関数pointer・handler状態境界 | ✅ | signal_function_pointer_boundaries | 標準必須の`sig_atomic_t`を公開する。`signal`を「handler関数pointerを受け取りhandler関数pointerを返す」生の入れ子宣言へ代入し、間接呼出しで旧handler返却、handler実行、`SIG_IGN`、元disposition復元をnative/WAT/objectで検証する |
| `signal.h`の必須macro・型・sentinel境界 | 🧪 | signal_macro_type_boundaries | `SIGABRT/SIGFPE/SIGILL/SIGINT/SIGSEGV/SIGTERM`のtarget値とint整数定数式、signed intの`sig_atomic_t`、`SIG_DFL/SIG_IGN/SIG_ERR`のfunction-pointer型と相互非同一性、`signal`/`raise` signatureをsystem Clang strict・同梱header使用時のClang・Native/WAT/Wasm objectで固定する。process終了signalは配送せず、既存signal_function_pointer_boundariesの安全なSIGINT状態遷移と分離する |
| `errno.h` の必須macro・modifiable lvalue境界 | 🔧 | errno_macro_contract_boundaries | `EDOM`/`EILSEQ`/`ERANGE`が正の整数定数式として`#if`・enum・配列boundで使えること、`errno`が変更可能な`int` lvalueであり`&errno`と間接代入が成立することをnative/WAT/objectで固定する。Typed HIRのW3013自己比較判定は同じ変数参照だけに限定し、`_Generic`や`sizeof`から得た同値の定数同士を自己比較と誤診断しない |
| aggregate配列member代入のlocal usage診断境界 | 🔧 | aggregate_array_member_usage_boundaries | `s.values[i][j]`や`s.ops[i]`への代入は直接member代入と同様に基底local aggregateを初期化するが、AST上は`ND_SUBSCRIPT`を挟むためW3004が残っていた。subscript基底の宣言型が配列の場合だけ代入先を遡り、多次元配列・関数pointer配列の全要素書込み後の読出しを無警告でnative/WAT/object実行する。未初期化member読出しと未初期化pointerの`p[i]`代入は引き続きW3004にする |
| out-parameter経由のlocal初期化診断境界 | 🔧 | out_parameter_initialization_usage_boundaries | `set(&local)`の固定引数が非const pointeeへのpointerならcalleeがobjectを書き込めるため、呼出し後の読出しをW3004にしていたfalse-positiveを解消する。canonical function parameter型からpointee qualifierを確認し、直接`&local`（明示castで包まれた形を含む）だけを初期化可能として記録する。`const T *`への引渡し、未初期化pointer値の使用、単なるアドレス保存は引き続き警告対象にする |
| read-modify-writeのlocal初期化順序診断境界 | 🔧 | read_modify_write_usage_boundaries | `int x; x += 1;`や`++x`が左辺を単純代入と同じ初期化済みにしてW3004を落としていた。identifier usageをsource順に記録し、値を要求するreadとwrite-only lvalueを分離して、compound assignment・increment/decrementと「read後に単純代入」のprior-value不足を警告する。宣言初期化・先行する単純代入・parameter・static localのread-modify-writeは無警告のまま、scalar・aggregate member・配列要素・pointer dereferenceの実行結果をnative/WAT/objectで固定する。function-wide解析より前のVLA bound解決は順序診断eventを記録せず、初期化済みboundへの誤警告も防ぐ |
| 宣言初期化・単純代入のusage event順序境界 | 🔧 | initialization_event_order_boundaries | 宣言初期化と単純代入のwrite eventが右辺readより前に記録され、`int x=x;`と未初期化の`x=x;`でW3004を落としていた。初期化markerをpreflight中の右辺identifier bindingより後へ追加し、HIR build時の先行eventを廃止する。自己初期化・未初期化自己代入・comma左辺の先行readと、out-parameter呼出しの別argumentによるcall前readは警告する。初期化済み自己代入、複数declarator間のread、aggregate/list initializer、out-parameter後のreadは無警告にする。さらに`do`は本体→条件、`for`は本体→incrementの実行順でpreflightし、本体で初期化したlocalへの後続readを誤警告せずnative/WAT/object実行する |
| 分岐合流のdefinite-initialization境界 | 🔧 | definite_initialization_branch_boundaries | source順の単一event列では、`if`のthen側writeがelse側や合流後の未初期化readを隠し、三項演算子と`&&`/`||`にも同じW3004欠落があった。preflightがlocalごとのdefinite-initialization snapshotを分岐入口で保存し、各経路を独立に解析して論理積で合流する。片側だけのwrite後のreadと他方branch内のreadは警告し、if/else・三項演算子・入れ子分岐の全経路writeと、初期化後の短絡評価は無警告でnative/WAT/object実行する |
| `switch`合流のdefinite-initialization境界 | 🔧 | definite_initialization_switch_boundaries | switch本体をsource順に一度だけ走査すると、先行caseのwriteが後続caseへの直接流入やdefaultなしの非一致経路を隠してW3004を落としていた。switch入口、caseへの直接流入、fallthrough、break出口、return/continueの非到達経路をsnapshotで分離し、最内側のloop/switchをbreak targetとして追跡する。定数controlは事前にcase定数式を走査して選択labelだけを直接流入させる。一部case/defaultだけのwrite、未選択の定数case、未初期化のfallthrough readは警告し、全case/default、選択済み定数case、if内break、再初期化するfallthrough、入れ子switch・loop break・switch内continueを無警告でnative/WAT/object実行する |
| `goto`合流と後方edgeのdefinite-initialization境界 | 🔧 | definite_initialization_goto_boundaries | goto後もsource順の状態を継続すると、初期化文を飛び越えて共通labelへ入る経路や、source順ではlabel通過後に到着する後方edgeをW3004が見落としていた。事前収集済みlabelごとにgoto入口snapshotを蓄積し、通常fallthroughと論理積で合流してgoto後を非到達にする。さらにlabel入口が変化しなくなるまで初期化フローだけを再解析し、多段の後方gotoでも固定点へ収束させ、定数条件で非到達なgotoはlabel入口へ混ぜない。初期化を飛び越える前方/後方goto、local initializer・compound literal・call引数・generic選択・VLA boundからの未初期化readを警告し、全goto経路が初期化する共通label、複数経路、後方cycle、out parameter初期化、定数非到達gotoを無警告でnative/WAT/object実行する |
| `stdlib.h` algorithm APIの入れ子callback関数pointer境界 | ✅ | stdlib_algorithm_function_pointer_boundaries | `qsort`/`bsearch`を「比較callback関数pointerを受け取る関数pointer」へ代入し、APIとcallbackの二重間接呼出し、重複要素、検索hit/miss、要素数0で比較を行わない境界をnative/WAT/objectで検証する |
| `stdlib.h` 整数APIの関数pointer・aggregate戻りABI境界 | ✅ | stdlib_integer_function_pointer_abi_boundaries | `abs`/`labs`/`llabs`と`div`/`ldiv`/`lldiv`の標準signatureを関数pointer代入で固定し、負数の0方向丸め・剰余符号、8-byteと16-byte aggregate戻りをnative/WAT/objectで検証する。Apple ARM64のregister戻りとWasmのhidden return pointerを独立した標準API群から回帰確認し、native統合E2Eのassembly名前空間化でも6 symbolを外部libcとして保持する |
| `stdlib.h` 文字列変換APIの関数pointer・範囲境界 | ✅ | stdlib_conversion_function_pointer_boundaries | `atoi`/`atol`/`atoll`/`atof`と整数・浮動小数の全`strto*`を標準signatureの関数pointer越しに呼び、符号・基数・指数・`endptr`・変換不能入力を固定する。64-bit signed/unsigned overflowでは全桁を消費し、上限/下限へ飽和して`errno == ERANGE`となることをnative/WAT/objectで検証する。WAT standaloneの共通変換helperはsigned/unsigned別のlimit・cutoff・cutlimをunsigned演算で求め、wrap前にoverflowを記録する |
| `math.h` 出力pointer APIの関数pointer・商符号境界 | ✅ | math_output_pointer_function_boundaries | `frexp*`/`modf*`/`remquo*`のfloat/double/long double全9 signatureを関数pointer代入で固定し、指数・整数部・剰余商bitの出力pointer書込みをnative/WAT/objectで検証する。`remquo*`は負の商でも規定された商bitの符号が負になることを確認し、WATは絶対値の下位3bitを求めてから商の符号を復元する |
| `math.h` unary値返しAPIの関数pointer・三幅境界 | ✅ | math_unary_function_pointer_boundaries | 三角・双曲線・指数・対数・誤差・root・丸め・絶対値の30 familyをfloat/double/long doubleの標準signatureを持つglobal tableへ格納し、全90関数を間接呼出しする。domain端点、負数丸め、整数結果をnative/WAT/objectで固定し、FP register幅と関数table relocationを横断検証する |
| `math.h` mixed signature APIの関数pointer境界 | ✅ | math_mixed_function_pointer_boundaries | binary値返し9 familyの三幅、`fma*`、int/long指数の`ldexp*`/`scalbn*`/`scalbln*`、整数戻りの`ilogb*`/`lrint*`/`lround*`、`logb*`、const文字列入力の`nan*`を標準signatureで間接呼出しする。符号付き剰余、copy-sign、hypot、丸め規則とWasm i64引数・戻りをnative/WAT/objectで固定する |
| `math.h` 分類・順序比較macroの型幅・単一評価境界 | 🔧 | math_classification_macro_boundaries | `fpclassify`/`isfinite`/`isinf`/`isnan`/`isnormal`/`signbit`と順序比較6種を外部関数宣言だけにしていたため、Darwin nativeで対応symbolが未解決だった。同梱header内の型総称macroへ移し、float/doubleのzero・subnormal・normal・infinity・NaN、負zero、mixed幅比較、unorderedと各引数1回評価をnative/WAT/objectで固定する。double NaNのfraction上位bitだけが立つことで、Wasmの64-bit条件分岐が下位32-bit truncateで偽になる不具合も発見し、IR branch前にfull-width `!= 0`へ正規化する |
| `math.h` 特殊浮動定数・error handling macro境界 | 🔧 | math_special_macro_boundaries | 欠落していた`HUGE_VAL`/`HUGE_VALF`/`HUGE_VALL`/`INFINITY`/`NAN`を追加する。ag_cではpreprocessor所有のfloat/double/long double infinity・quiet NaN tokenとして公開し、関数呼出しやunion punningではなく静的初期化可能な浮動定数を保つ。各macroの型・分類・符号と、native Darwinの動的`__math_errhandling()`、Wasm fenv契約の`MATH_ERREXCEPT`をnative/WAT/objectで固定する |
| `math.h` の評価型`float_t`・`double_t`境界 | 🔧 | math_evaluation_type_boundaries | C11必須の2 typedefが同梱headerから欠落し、system headerでは通る宣言がag_cと同梱header使用時のClangで未宣言になっていた。両targetの`FLT_EVAL_METHOD == 0`に対応して`float_t`を`float`、`double_t`を`double`として公開し、型identity・size/alignment、重複include、`sqrt*`/`modf*`のtypedef表記function pointerと出力pointer書込みをsystem/bundled Clang strictとnative/WAT/objectで固定する |
| **64-bit整数条件の上位bit真偽判定** `if (1ULL << 63)` | 🔧 | wide_integer_control_flow_truth_boundaries | Wasmの条件分岐がi64をそのままbranchへ渡し、backendで`i32.wrap_i64`されるため下位32bitが0の非zero値を偽と判定していた。IR branch生成時にi64をfull-widthの`!= 0`でi32真偽値へ正規化し、signed/unsignedの`if`・三項・`while`・`for`と副作用付き条件の単一評価をnative/WAT/objectで固定する |
| `complex.h` 初等関数の関数pointer・複素ABI境界 | ✅ | complex_function_pointer_abi_boundaries | 同梱実装済みのdouble complex初等関数10個とfloat complex版5個を標準signatureのglobal関数pointer tableへ格納する。Apple ARM64のHFA引数・戻り値とWasmのhidden return pointerを間接呼出しで通し、実部・虚部がともに非zeroの既知値、unary/binary signature、float/double幅をnative/WAT/objectで固定する。偏角計算は`0.5 < |x| <= 2`をπ/4近傍へrange reductionし、45度付近で遅い交代級数を直接使う精度劣化も防ぐ |
| `complex.h` C11構築macroの型・単一評価境界 | 🔧 | complex_construction_macro_boundaries | 欠落していた`CMPLX`/`CMPLXF`/`CMPLXL`を実部・虚部のcompound literal初期化で追加する。各macroがdouble/float/long double complexを正しく保持し、実部・虚部を独立変換して各引数を1回だけ評価することをnative/WAT/objectで固定する |
| `complex.h` 値関数のaddress・単一評価・射影境界 | 🔧 | complex_value_function_boundaries | `conj*`/`cabs*`/`carg*`/`cproj*`を関数形式macroだけで公開していたため、標準関数のaddressを取得できず、`conj*`/`cabs*`/`carg*`は引数を複数回評価していた。3幅すべてを実関数へ移し、標準signatureの関数pointer経由呼出し、direct callの引数1回評価、有限値、および実部・虚部のいずれかが無限の`cproj*`が正の無限実部と虚部の符号を持つzeroへ射影することをnative/WAT/objectで固定する |
| `complex.h` 初等関数のfloat/long double variant境界 | 🔧 | complex_width_variant_function_boundaries | double版の`cexp`/`clog`/`csqrt`/`cpow`/`csin`/`ccos`/`csinh`/`ccosh`/`ctan`/`ctanh`に対し、float版は5関数だけ、long double版は全て欠落していた。残るfloat 5関数とlong double 10関数を標準signatureで追加し、global関数pointerからのunary/binary呼出し、float complex ABI、long double complex ABI、既知の実部・虚部をnative/WAT/objectで固定する |
| `complex.h` 逆三角・逆双曲線関数の3幅ABI境界 | 🔧 | complex_inverse_function_boundaries | 欠落していた`cacos*`/`casin*`/`catan*`/`cacosh*`/`casinh*`/`catanh*`を既存の`clog`/`csqrt`から構成し、float/double/long double complexの全18 entry pointを標準signatureの関数pointer tableへ格納する。代表的な実数・純虚数入力の既知値とcomplex値渡し・戻りABIをnative/WAT/objectで固定する |
| complex条件演算子のmixed幅・branch評価境界 | 🔧 | complex_conditional_width_boundaries | scalar・aggregate・voidの`?:`にはIR loweringがあったがcomplex結果だけ欠落し、float/double/long double complexの条件式が`E4007`になっていた。結果幅のcomplex一時領域を作り、選択branchだけを評価して対応する幅へ実部・虚部を変換・コピーするCFG loweringを追加する。usual arithmetic conversion後の結果型、両branch選択、side effect回数、関数戻りをnative/WAT/objectで固定する |
| complex条件のcontrol-flow真偽境界 | 🔧 | complex_control_flow_truth_boundaries | complex値はIRで一時領域pointerとして表現されるため、`if`/`while`/`do`/`for`/`&&`/`||`/各種`?:`が実部・虚部ではなく常にnon-nullなpointerを真と判定していた。全CFG入口を共通condition loweringへ通し、実部または虚部がnonzeroなら真、両方zeroなら偽へ正規化する。float/double/long double complex、虚部だけがnonzeroの値、zero、loop回数、短絡副作用、scalar/aggregate/void/complex結果の条件式をnative/WAT/objectで固定する |
| address・complex定数条件のCFG終端境界 | 🔧 | pointer_complex_constant_cfg_termination_boundaries / pointer_complex_constant_cfg_warning_boundaries | Clangが非void関数の終端を非到達とする`while (&object)`、文字列・関数address、nonzero complex cast、`I`/`CMPLX*`、複素算術・比較・`_Bool`変換・component・条件式、NaN・infinityを、HIR定数truthとしてtarget-independent CFGへ反映する。complex compound literalは同一一時objectの実部・虚部へ副作用なしの定数を代入する形だけを直接畳み、callを含むinitializerや添字付きaddressの評価は保持する。`side_effect() || 1`やcommaの既知truthは式を通常生成して副作用を1回実行し、CFG条件だけを確定する。null pointer・zero complex・符号付きzeroおよび偽となる比較・条件式はW3005をNative/Wasm双方で維持し、正負fixtureをClangの`-Wreturn-type`と照合してnative/WAT/objectで固定する |
| 標準scalar定数式・pointer加減算のCFG終端境界 | 🔧 | standard_constant_expression_cfg_termination_boundaries / standard_constant_expression_cfg_warning_boundaries | `sizeof`/`_Alignof`/enum/`_Generic`/`offsetof`とscalar compound literal、既知nonnull baseに定数offsetを加減するpointer式をHIR定数truthへ反映する。scalar compound literalは同一一時objectへの単一代入かつ非volatile/非atomicに限定し、initializerが定数ならstorage生成を省略する。comma等で副作用後の値だけが確定するinitializer・offsetは式を通常生成して副作用を1回実行し、後続のtarget幅付き整数加減算・shift・比較・cast、floating算術・比較、短絡演算、integer/floating/pointer結果の条件演算子にも確定値を伝播する。`?:`は既知条件で選ばれたbranchだけを評価し、短絡右辺も不要なら実行しない。runtime未知offset、zeroのinteger/floating/pointer compound literal、算術・比較・選択結果がzeroになる副作用付き条件、およびobservable loadを持つvolatile-qualified compound literalはW3005を維持し、Clang strict C11と照合してnative/WAT/objectで固定する |
| 副作用後に確定するcomplex条件のCFG終端境界 | 🔧 | complex_after_required_cfg_termination_boundaries / complex_after_required_cfg_warning_boundaries | comma・cast・単項演算・加減乗除・条件演算子・比較・component抽出・complex compound literalで、必要な副作用を実行した後の実部・虚部が定数へ確定する経路をHIR CFG評価へ伝播する。条件式本体は通常どおり1回生成して副作用を保持し、既知条件の未選択branchは実行しない。nonzero結果ではW3005を抑止し、zero結果、volatile-qualified compound literal、runtime complex callは到達可能性を保守的に残す。Clang strict C11との正負診断差分と、Native/WAT実行時の副作用回数、Wasm object診断を固定する |
| 既知object/subobject addressのCFG終端境界 | 🔧 | known_lvalue_address_cfg_termination_boundaries / known_lvalue_address_cfg_warning_boundaries | global objectのmember・nested member・union member、定数添字のglobal array/string要素、既知nonnull baseからのpointer memberを、既存object storageへ由来するnonnull addressとしてHIR CFG評価へ反映する。pointer型objectそのもののaddressも値ではなくstorage addressとして真を保つ。一方、runtime添字、未知pointer base、scalar/aggregate compound literal storageはClang同様に保守的なW3005を維持し、実行fixtureでは添字・initializerの副作用が1回だけ起きることをNative/WATで、正負診断をNative/Wasm objectで固定する |
| complex→scalar変換後のCFG終端境界 | 🔧 | complex_scalar_cfg_termination_boundaries / complex_scalar_cfg_warning_boundaries | complexからdouble/float/整数型への変換は実部だけを使い、`_Bool`変換だけは実部・虚部のいずれかがnonzeroなら真とする規則を、副作用後の定数CFG評価にも適用する。representableな正負値、狭いsigned/unsigned整数、fraction切捨て、complex compound literalと既知branch選択をtarget DataLayout準拠で評価し、条件式本体は副作用を1回だけ実行する。zero実部、1未満から整数zeroへの変換、zero complexの`_Bool`、volatile compound literal、runtime complex callはW3005を維持し、Clang strict C11の正負診断とNative/WAT/objectを固定する |
| `complex.h` component関数・虚数単位型境界 | 🔧 | complex_component_function_unit_boundaries | `creal*`/`cimag*`が関数形式macroだけで標準関数のaddressを取得できず、`_Complex_I`/`I`も標準のfloat complexではなくdouble complexだった。3幅6関数を標準signatureの実関数へ移し、global関数pointer呼出し、direct callの単一評価、符号付きzero保持を固定する。虚数単位はfloat complexへ直し、float/double/long doubleとの演算結果型をnative/WAT/objectで検証する |
| complexの静的初期化・aggregate leaf境界 | 🔧 | complex_static_initializer_boundaries | file-scopeの`_Complex` brace初期化を通常scalar listとして1要素へ制限し、compound literalを含む`I`/`CMPLX*`はdiagnostic無しのdirect Syntax拒否になっていた。static initializerを実部・虚部の2つのfloating leafとしてmaterializeし、複素定数のcast・単項演算・加減乗除・条件式を両成分で評価する。3幅のscalar、要素数推論付き配列、struct/unionのcomplex memberとcomplex配列memberをnative/WAT/objectで固定する |
| complex静的初期化のscalar変換・比較境界 | 🔧 | complex_static_scalar_conversion_boundaries | 複素定数から実数への明示・暗黙変換と`==`/`!=`がstatic HIR evaluatorで実部・虚部を扱わずE3116になっていた。実数・整数変換は実部、`_Bool`と論理演算は両成分のnonzero、等値比較は両成分の一致として評価する。3幅、complex対scalar、file/block static、aggregate memberをnative/WAT/objectで固定する |
| complex実行時値のscalar変換・mixed比較境界 | 🔧 | complex_runtime_scalar_conversion_boundaries | complex→scalarのlocal初期化・関数引数経路が複素値の一時領域pointerを通常scalarへcoerceしてE4007になり、complexと実数scalarの`==`/`!=`も両operandを共通complex型へmaterializeせずE4007になっていた。実数・整数変換は実部、`_Bool`は両成分、mixed比較はscalarの虚部をzeroとして扱う。local/関数戻り、struct member、配列要素、引数変換とmixed比較をnative/WAT/objectで固定する |
| complex実行時値のscalar変換文脈境界 | 🧪 | complex_scalar_conversion_context_boundaries | complex→scalarの共通coercionがlocal初期化・関数引数以外にも適用されることを固定する。scalar関数return、通常代入とその式の値、bit-field・volatile・atomicへの代入、commaとmixed conditionalの結果変換をClang strict C11と照合し、実部の変換と虚部を含む`_Bool`真偽をnative/WAT/objectで検証する |
| scalar実引数からcomplex仮引数への暗黙変換境界 | 🔧 | scalar_to_complex_call_conversion_boundaries | prototyped関数のcomplex仮引数はassignment conversionにより全実数算術型の実引数を受け取れるが、call loweringが実引数もcomplexであることを先に要求して`E4007`になっていた。既存のscalar→complex materializationへ接続し、`_Bool`・各種整数・float/double/long doubleから3幅complexへの変換、虚部zero、direct/関数pointer/struct callback、複数complex仮引数、整数引数とのABI混在、単一評価をnative/WAT/objectで固定する |
| scalarからcomplex戻り値への暗黙変換境界 | 🔧 | scalar_to_complex_return_conversion_boundaries | complex戻り型のreturn文にはassignment conversionが適用されるが、HIR→IR loweringが変換元もcomplexであることを要求して、標準Cで有効な`double _Complex f(void) { return 1; }`を`E4007`にしていた。returnでも既存のscalar→complex materializationを許可し、全実数算術型からfloat/double/long double complexへの変換、実部・虚部zero、direct/関数pointer/struct callbackの戻りABI、単一評価をClang strict C11およびnative/WAT/objectで固定する |
| scalarからcomplex objectへの初期化・代入変換境界 | 🔧 | scalar_to_complex_storage_conversion_boundaries | complexは内部の静的data計画では実部・虚部の2 leafを持つが、aggregateのpositional initializer cursorが実数算術式をcomplex subobject全体ではなく先頭componentだけの初期化として消費し、後続値を虚部やpaddingへ連続配置していた。complex自身の`{re, im}`拡張は維持しつつ、struct/array/union配下では算術式1個をcomplex object全体のassignment conversionとして解決する。file/local scope、aggregate leaf、通常・pointer/member/subscript・連鎖・volatile・atomic代入、代入式の値と単一評価をClang strict C11およびnative/WAT/objectで固定する |
| complex aggregate subobjectの幅変換・nested cursor境界 | 🔧 | complex_aggregate_conversion_boundaries | aggregate initializerのwhole-object判定がcomplex subobjectと同一TypeIdの式だけを認識していたため、double complexからfloat complex等の標準算術変換も実部leafだけへ適用し、虚部喪失と後続memberのoffsetずれを起こしていた。float/double/long double complex間の双方向変換をfile/local/static local、要素数推論配列、union先頭member、多重struct/array、positional/designated initializerで固定し、runtime initializerの評価回数もnative/WAT/objectで検証する |
| qualified complex aggregate subobjectの初期化境界 | 🔧 | qualified_complex_aggregate_initializer_boundaries | complex whole-object spanへ付随する`const`/`volatile`/`_Atomic`を保ったまま、実数および異幅complex initializerを適用する。file/local/static localのpositional/designated struct member、atomic complex配列、前後canary、runtime式の単一評価を固定し、読み出し時はvolatile loadと8/16-byte atomic object経路をnative/WAT/objectで通す |
| complexのIEEE特殊値scalar変換・比較境界 | 🧪 | complex_special_scalar_boundaries | NaN・正負infinity・符号付きzeroを実部と虚部へ配置し、complex→`_Bool`がNaN/無限を真、両成分zeroを偽とすること、complex→実数が実部のNaN・infinity・zero符号を保持すること、NaNを含む`==`/`!=`とsigned zero比較をstatic初期化とruntimeの両方でClang strict C11に照合する。native/WAT/objectで同じ結果を固定する |
| `stdio.h` 固定引数APIの関数pointer・stream状態境界 | ✅ | stdio_function_pointer_state_boundaries | 文字・file・binary I/O、position、error状態を扱う全固定引数APIの`const`・`size_t`・`fpos_t`を含む標準signatureを関数pointer代入で固定する。native/objectではtemporary streamへNUL・高bit byteを間接出力し、`fgetpos`/`fsetpos`、pushback、EOF/clear、zero-size I/Oを間接入力で検証する。ファイルstoreを持たないstandalone WATでは同じ関数pointer経路から明示的なunavailable-file stub契約を検証する |
| `stdio.h` のtarget別`fpos_t`型・書込み幅境界 | 🔧 | stdio_position_type_abi_boundaries | Apple SDKの`fpos_t`は`__darwin_off_t`由来の`long long`だが、同梱headerはWasm runtime用の`long`をnativeにも公開していた。両者が同じ8-byteでも別のC型であることを`_Generic`でtarget別に固定し、`fgetpos`/`fsetpos`の標準signature、position値、前後canaryをnative/WAT/objectで検証する |
| `stdio.h` / `wchar.h` のopaque `FILE`型境界 | 🔧 | stdio_opaque_file_type_boundaries / compile_fail: stdio_file_pointer_argument | `FILE`を`void`のtypedefにしていたため`FILE *`と`void *`が同一型になり、任意のobject二重pointerをstream APIへ暗黙に渡せていた。Wasmではprivate incomplete tag、nativeではDarwinの`struct __sFILE` tagへ揃え、wchar→stdioのinclude順、`_Generic`での型区別、stream APIの関数pointerと実呼出しをnative/WAT/objectで固定し、`int *`から`FILE *`への不正な引数変換をE3120で拒否する |
| POSIX file descriptor APIの関数pointer・seek/append境界 | 🔧 | posix_file_descriptor_function_pointer_boundaries | `open`/`read`/`write`/`lseek`/`close`/`fstat`を標準signatureの関数pointer経由で呼び、NUL・高bit byte、sparse gap、`SEEK_SET/END`、append、`O_EXCL`、二重closeと無効fdをnative/objectで固定する。同梱`sys/stat.h`の16-byte Wasm layoutをnativeでも使っていたためApple `_fstat`の144-byte書込みがstackを破壊しておりtarget別layoutへ分離し、`L_tmpnam`もnative 1024/Wasm 32へ分離した。永続file storeを持たないstandalone WATは同じsignatureで失敗しcaller buffer/statを変更しない契約を持つ。WAT stub同期検査のheader集合へ`fcntl.h`/`unistd.h`/`sys/stat.h`も追加する |
| POSIX `off_t` / `blkcnt_t`のtarget別型・wide seek境界 | 🔧 | posix_offset_type_abi_boundaries / compile_fail: posix_off_t_pointer_identity | Apple SDKの`off_t`/`blkcnt_t`は`long long`だが、同梱headerはWasm runtime用の`long`をnativeにも公開していた。`sys/types.h`を型の正本にして`unistd.h`と`sys/stat.h`で共有し、Wasmの`long`とnativeの`long long`を`_Generic`、size/alignment、`struct stat` member型・offset、関数pointerで固定する。4GiB超の`lseek`値をnative/objectで往復させ、nativeではsparse fileの`st_size`書込みまで検証し、nativeの`long *`から`off_t *`への不正な初期化をE3099で拒否する |
| `sys/types.h`の公開typedef・`struct stat` member型境界 | 🔧 | sys_types_stat_member_identity_boundaries | `sys/types.h`を直接includeしたときに`size_t`/`ssize_t`と、`blksize_t`、`dev_t`、`gid_t`、`ino_t`、`mode_t`、`nlink_t`、`pid_t`、`uid_t`が利用できることを固定する。各typedefのC型同一性と幅、native `struct stat`の対応memberが同じtypedefを使うことをnative/WAT/objectで検証し、Apple arm64の144-byte layoutを維持する |
| wide formatted I/Oの変換指定・幅・切り詰め境界 | 🔧 | wide_format_scan_boundaries, wasm32/wchar_input_ops, wasm32/wchar_swprintf_ops | `swscanf`の`%s/%c`は狭文字出力、`%ls/%lc`はwide出力としてWAT/object runtimeを分離し、`%ld/%lu/%lld/%llu`は64-bit格納。入力field widthを整数・狭/wide文字列・複数文字`%c`へ適用し、`*`代入抑制が入力だけを消費して後続引数とassignment countをずらさないことも固定。WATの固定2出力stubへ可変引数が1個だけ渡された場合は不足slotをi32 nullで補う。`swprintf`へ通常幅・左寄せ・負数zero padding、同じlength modifier、20桁unsigned値を追加し、バッファ不足とsize=0では`snprintf`式の必要長ではなく負値を返すC規格動作をnative/WAT/objectで固定 |
| wide formatted outputの整数・count length modifier | 🔧 | wide_format_output_boundaries | `swprintf`/`vswprintf`の`hh/h/j/z/t`を解析し、`%hhd/%hhu/%hd/%hu`のdefault-promotion後の縮小、`%jd/%ju/%zu/%td`の8-byte値、`%hhn/%hn/%jn/%zn/%tn`の1/2/8-byte count格納をnative/WAT/objectで固定する。sub-int count格納は隣接canaryを保持し、va_list経路では5個の値と3個のcount pointerを順に消費する。さらに`d/i/u`の整数精度、`+`/space sign、精度0のzero-value抑制を検証する。WATはdirect wrapperとva_list APIを共通wide formatterへ接続し、object runtimeはnarrow formatterと同じ型別va_arg/count helperを使用する |
| wide formatted outputの整数基数・代替形式 | 🔧 | wide_format_integer_base_output_boundaries | `swprintf`/`vswprintf`の`%x/%X/%o/%p`、`#`、幅、左寄せ、zero padding、整数精度をnative/WAT/objectで固定する。`hh/h/j/z`による縮小と64-bit値、精度指定時の`0` flag無効化、pointerの`0x`表現と左右paddingも共通wide formatterで検証する |
| wide formatted outputの文字列・文字境界 | 🔧 | wide_format_string_character_output_boundaries | `swprintf`/`vswprintf`の`%s/%ls/%c/%lc`を区別し、固定・可変幅、左寄せ、文字列精度、精度0、複数のwidth/precision/value引数によるva_list cursorをnative/WAT/objectで固定する |
| wide formatted outputの浮動小数点境界 | 🔧 | wide_format_floating_output_boundaries | `swprintf`/`vswprintf`の`f/F/e/E/g/G/a/A`へ精度、幅、左寄せ、zero padding、`+`/space sign、`#`、`L`を実装する。10進・16進の丸め繰上がり、10進tieのnearest-even、`g`の固定小数/指数表記切替、負のゼロ、`inf`/`nan`、負の動的精度による既定精度復帰をnative/WAT/objectで固定する |
| wide formatted inputのva_list cursor | 🔧 | wide_format_input_va_list_boundaries | C11の`vswscanf`宣言とWAT/object runtime経路を追加し、`hh/h/j/z/t`を含む5個の出力pointerを順に消費する。代入抑制`%*d`はva_listを進めず、assignment countへ含まれない`%zn`はpointerだけを消費して後続`%d`の出力先をずらさないことをnative/WAT/objectで固定する。WATのdirect `swscanf`と`vswscanf`は独立したva-list cursorを持つ共通scannerへ接続する |
| wide formatted stream output | 🔧 | wide_format_stream_output_boundaries | C11の`wprintf`/`fwprintf`/`vwprintf`/`vfwprintf`宣言とruntime経路を追加する。direct/va_list双方で`hh/h/j/z/t`と`%n`を消費し、`%s/%ls/%lc`の幅・精度および`%f/%e`の浮動値も同じ共通formatterへ通す。native/objectではwide-oriented temporary streamへ出力した内容を読み戻し、standalone WATでは共通wide formatterによる文字数とcount格納を固定する |
| wide formatted stream input | 🔧 | wide_format_stream_input_boundaries | C11の`wscanf`/`fwscanf`/`vwscanf`/`vfwscanf`宣言とruntime経路を追加する。native/objectではwide-oriented temporary streamからdirect/va_list双方で`hh/h/j/z`と`%n`を読み、変換が消費したwide文字数に対応するbyte位置だけstreamを進める。空formatはstdinを読まず0を返し、standalone WATの入力不可contractは非空formatで`WEOF`を返す |
| `aligned_alloc`のalignment/size契約 | 🔧 | wasm32/stdlib_alloc_abs_ops | 有効なpower-of-two alignmentと、その倍数sizeだけを成功条件にする。WAT stubが非power-of-two alignmentや非倍数sizeを受理していたためobject runtimeと同じ検査を追加し、16/32-byte整列と不正引数拒否を両Wasm経路で固定 |
| VLA（1D / 2D / 関数引数 / sizeof 全体） | ✅ | literal_vla_boundaries, vla_2d_param_and_row_sizeof, pointer_to_vla | 1D/2D local、parameter、pointer-to-VLA、runtime `sizeof`、bound副作用をnative/Wasmで固定 |
| VLA boundのassignment-expression・typedef捕捉 | 🧪 | vla_bound_assignment_expression | block scopeのarray declaratorで`extent = 4`、`extent += 3`、括弧付きcomma式をboundとして各1回評価し、得られたruntimeサイズと要素アクセスをnative/WAT/objectで固定する。VLA typedefの宣言時に代入式boundを1回評価し、その後に元変数を変更しても2個のtypedef objectが捕捉済みサイズを保つことも検証する |
| array parameterの外側runtime bound評価 | 🔧 | array_parameter_static_bound_evaluation | function definitionの`int values[static note(extent)]`はarray-to-pointer調整で外側boundが型配置から消えるが、C11では関数entryでbound式の副作用を実行する。parameter pipelineで正しいlookup pointから得たTyped HIR expression IDを関数HIRの`VLA_DIMENSION` edgeへ残し、parameter bind後・body前に1回評価する。`static restrict`と通常array parameterの関数呼出しboundを呼出しごとに1回実行し、bound内の先行parameter代入がbodyへ反映されることをnative/WAT/objectで固定する |
| array parameterの内側runtime bound一般式 | 🔧 | array_parameter_inner_bound_evaluation | 多次元array parameterの内側boundを先行parameter識別子のframe offsetだけで表現していたため、`int values[rows][note(columns)]`のcall・assignment・comma式がoffset 0の第1parameterへ誤解決され、副作用を落として誤strideを生成していた。各dimensionのTyped HIR expression IDをparameter metadataへ保持し、単純parameter参照と一般式を明示sentinelで区別する。一般式をparameter HIRの`VLA_DIMENSION` childとしてAST解放後も所有し、関数entryで宣言順に1回評価した値からstrideを計算することをnative/WAT/objectで固定する |
| VLA runtime boundのtarget幅 | 🔧 | vla_wide_runtime_bound | VLA parameterとローカルpointer-to-VLAのdimension・suffix byte sizeを一律i32へ切り詰めていたため、64-bit targetで`size_t` boundが`INT_MAX`を超えるとruntime `sizeof`が誤った。DataLayoutのpointer sizeに合わせてdimensionをsigned/unsigned拡張し、target幅でstrideとsuffixを積算する。nativeでは2^31要素、Wasm32では65537要素のparameter/pointer-to-VLAを実メモリ確保なしの`sizeof`で固定する |
| 2D VLA の行の `sizeof(a[0])` / 内側次元が第1パラメータの 2D VLA 引数 | 🔧 | vla_2d_param_and_row_sizeof | A4: `sizeof(a[0])` が要素サイズ→行ストライドスロットを runtime ロード。A5: 内側次元が第1パラメータ (offset 0) の `int a[n][n]` で emit_vla_row_stride_for_params が src_offset==0 を未設定扱いし行ストライド計算を飛ばし SIGSEGV→frame_off 判定に変更 |
| pointer-to-VLA `int (*p)[m]` | 🔧 | pointer_to_vla, pointer_arithmetic_comparison_boundaries, overaligned_pointer_to_vla_typedef_boundaries, overaligned_vla_parameter_qualifier_xtu, overaligned_vla_global_callback_xtu, overaligned_vla_callback_factory_xtu / object: global_vla_callback_{,factory_}element_const_mismatch_{main,other} | ローカルは E3064 で拒否、仮引数 `int (*a)[n]` はコンパイル通過するが行ストライドを実行時 n で計算せずサイレント miscompile していた。行ストライド (extent*elem) を保持する隠しスロット (vla_row_stride_frame_off) をローカル (宣言子で VLA 次元を式捕捉し 16B 確保＋init_chain にストライド store 注入) と仮引数 (既存 register_vla_array_param 機構を再利用、関数 entry で *[rs]=*[n]*elem) の双方で確保。subscript は既存 vla_rsf 経路、ポインタ算術 (+/-) と inc/dec (++/--) はスロットから実行時ストライドを load してスケール。追加監査で`sizeof *(p + i++)`だけdirect type-query resolverがVLA baseをたどれずE0006になり、subscript indexと同様にpointer add/subのinteger operandをVLA `sizeof`の評価prefixとして保持するよう修正。int/double/float/plain structに加え、runtime-bound typedefで捕捉した64-byte整列aggregate行についてread/write、`sizeof *p`、行pointerの加減算・増減・byte距離、nested pointer、VLA仮引数のdirect callとprototype-scope `[*]`型を使うindirect call、128-byte整列matrixをNative/Wasmで固定する。別TUのarray parameter宣言とpointer-to-VLA定義についても、`static restrict`の調整とinner runtime boundを保ったままdirect/indirect callで互換にlinkし実行する。外部global callback dataに格納した同型についても、Wasm objectのcanonical data signatureとfunction-pointer relocation後にruntime strideを復元する。factory戻りfunction pointerへ一段再帰した型もfunction/data canonical署名とtable relocationを経てruntime strideを復元する。同じ低水準pointer ABIでもnested VLA elementの`const`有無が異なるglobal callback dataと、そのcallbackを返すfactory dataは`data signature mismatch`で拒否する |
| **3D VLA** `int t[n][m][k]` | 🔧 | vla_3d | register_vla_lvar_and_append_alloc が 1D/2D のみ対応で、3 段目を parse_decl_skip_constexpr_array_suffixes で消費しようとして非定数を拒否し E3064。32B descriptor slot ([base][byte_size][outer_stride][mid_stride]) に拡張し outer=m*k*elem を既存 rsf 経路で、mid=k*elem を別 STORE 注入で初期化。lvar_t に vla_mid_stride_frame_off 追加。build_subscript_deref が ND_LVAR (3D VLA) の subscript 結果 ND_DEREF に vla_row_stride_frame_off=mid_slot を立て、次 subscript が runtime mid stride で動作するように。make_subscript_scaled_offset が ND_DEREF からも vla_rsf を読む。subscript_base_address_of が vla_row_stride_frame_off>0 の deref を address として返す (これがないと t[i] が 1 バイト load されて SIGSEGV)。sizeof(vla3d[i][j]) は vla_mid slot を読む特別経路を追加。all-VLA `int t[n][m][k]`・first-dim VLA `int t[n][3][4]`・double 要素・read/write・3 段 sizeof を網羅 |
| **混在 const/VLA dim** `int t[2][n][4]` 等 | 🔧 | vla_mixed_dims | 第 1 dim が const でも後の dim が VLA なら配列全体は VLA (C11 6.7.6.2)。const 第 1 dim の場合は register_multidim_array_lvar 経由で parse_decl_constexpr_array_suffix_product_n が VLA dim を非定数として E3064 で拒否していた。decl_peek_trailing_array_dims_have_vla で後続 `[...]` を token peek し、TK_IDENT (enum 定数以外) があれば VLA 経路へ redirect。const 第 1 dim を ND_NUM ノードに包んで size_node として register_vla_lvar_and_append_alloc に渡す。2D `int a[2][n]`・3D `[C][n][C]`・`[C][C][n]`・全 VLA・enum 定数 dim を網羅。enum 定数は psx_ctx_find_enum_const で識別して定数扱いし、誤検出を避ける (誤検出した場合も VLA 経路の const-inner 分岐で正しく動く) |
| **4D+ VLA** `int t[n][m][k][l]...` (最大 8 次元) | 🔧 | vla_4d_and_higher | 続き30 の 3D 用 vla_mid_stride_frame_off を汎用 vla_strides_remaining + 連続 stride スロットへ置換。descriptor slot = 16 + 8*(N-1) バイト、stride[k] = dim[k+1]*...*dim[N-1]*elem を slot+16+8*k に保存。level 0 は VLA_ALLOC の rsf 経路で、level 1..N-2 は init_chain への STORE 注入。lvar_t / node_mem_t に vla_strides_remaining を追加し subscript chain で carry。inner_deref_size に elem を立てて連鎖伝播することで、最終 runtime stride 消費後も subscript_base_address_of が「中間配列」を認識し続ける。sizeof(vlaN[i][j]...[d]) は連続 [...] を D 段 peek して slot+16+(D-1)*8 を読む統一経路。4D 全 VLA、4D mixed const/VLA、5D 全 VLA、4 段 sizeof を fixture で網羅 |
| **3D+ VLA 仮引数** `int t[n][m][k]` / `int t[n][m][k][l]` | 🔧 | vla_3d4d_param | parse_param_declarator_name が内側 dim を 2 個までしか捕捉せず 3D 以上は silently 切り捨てられ miscompile していた。修正: (1) 内側 dim を最大 7 個 g_param_inner_dim_consts/idents に保存、(2) lvar_t に vla_param_inner_dim_consts/src_offsets/count を追加、(3) register_vla_array_param で N-D 用 stride スロット (n_inner*8 バイト) を anon lvar `__rs_<name>` として確保し vla_strides_remaining = n_inner - 1、(4) emit_vla_row_stride_for_params で各 level の stride を後ろから 1 MUL で計算して slot+8*level に store。subscript chain は local N-D VLA と同じ機構 (vla_row += 8 / strides_remaining -= 1) を共用。3D 全 VLA・4D 全 VLA・4D mixed const/VLA・3D 全 const 内側を fixture で網羅 |
| **局所 VLA のタグ carry** `struct P arr[n]; arr[i].m` | 🔧 | vla_struct_local | register_vla_lvar_and_append_alloc の呼び出し元が psx_decl_set_var_tag を呼んでおらず、tag_kind が EOF のままで `arr[i].m` の `.` が E3005 で弾かれていた。VLA 1D の `!size_ok` 分岐 + mixed const/VLA redirect 分岐の両方で psx_decl_set_var_tag(var, ..., 0) を追加。struct 直接アクセス・union・ポインタ取得経由・2D mixed `struct P arr[2][n]` を fixture で網羅 |
| **extern global の GOT 経由参照** `extern T v; v;` | 🔧 | extern_global_got | `extern T var;` で宣言のみのグローバル変数 (典型は libc の `__stderrp`) が @PAGE/@PAGEOFF 直参照されてリンク時に "does not have address" で失敗。続き4 の関数アドレス GOT 化と同じパターン。修正: ir_builder に共通ヘルパ emit_load_sym_for_gvar を追加し、psx_find_global_var で gv_ent を引いて is_extern_decl が立っていれば IR_LOAD_SYM の is_got_funcref を立てる (codegen は @GOTPAGE/@GOTPAGEOFF を使う)。LOAD_SYM 発行サイト 5 箇所をヘルパに置き換え。stdio.h に stdin/stdout/stderr (= Apple libc の __std{in,out,err}p) を追加 |
| **`struct N **` 仮引数の `(*root)->m`** | 🔧 | struct_pp_param_arrow | 多段の struct ポインタ仮引数 `struct N **root` で `(*root)->m` が E3005 で弾かれていた (ローカル `struct N **root` は動作)。register_param_lvar の struct ポインタ分岐 (param_ptr_levels>=2) で pointer_qual_levels が立っていなかったため、build_unary_deref_node の `*root` で is_tag_pointer 伝播が pql>=2 を要求して 0 にクリアされ、続く `->` が base_is_ptr=0 で弾かれていた。修正: param_ptr_levels>=2 のとき var->pointer_qual_levels = param_ptr_levels を設定 |
| **ファイルスコープの `T *p = (T[]){...}`** | 🔧 | file_scope_ptr_from_array_compound | ポインタ変数を配列複合リテラルで初期化すると SIGBUS。apply_toplevel_object_initializer の strip heuristic が `(T){...}` を無条件で剥がして `T *p = {...}` (複数値で初期化) と解釈し、先頭要素値がポインタスロットに書き込まれていた。修正: 集約 (配列 / struct 値 / union 値) のときだけ strip し、ポインタ・スカラ var では式経路 (psx_expr_assign) で compound literal を hidden gvar に materialize させる。ただし `char *p = (char[N]){"str"}` の単一文字列形は等価なので peek で例外的に strip 許可。int*/unsigned char*/sized array/char* + 単一文字列を fixture で網羅 |
| **関数の宣言と定義でcanonicalシグネチャ照合** (C11 6.7p4) | 🔧 | function_redecl_signature, incomplete_array_bound_signature_xtu_{main,other} / same_object_array_signature_refinement, nested_incomplete_array_signature_refinement / array_bound_signature_mismatch_{main,other} / compile_fail: nested_{array_inner_bound_function,callback_return_array_bound}_{redeclaration,pointer}_mismatch, func_redef_different_ret, function_parameter_{int_long,int_unsigned,char_signed_char,float_double,pointer_base,pointee_qualifier,nested_qualifier}_mismatch | 旧実装の戻り型・粗粒度parameterカテゴリ照合は廃止済み。同名関数の再宣言では`PSX_TYPE_FUNCTION`が保持する戻り値、固定引数数、可変長性、各canonical parameter型からcomposite typeを構成し、互換でなければ宣言を拒否する。Clang差分で戻り型、`int`/`long`、signedness、plain/signed char、float/double、pointer基底型、pointee/nested qualifierまで一致を確認し、8件のshould_reject正本をNative/Wasm object compile-failへ登録する。parameter最上位qualifier、array→pointer、function→pointer調整の合法形は維持。Wasm object間のcanonical署名では`int (*)[]`/`int (*)[count]`と`int (*)[3]`を再帰位置（parameter、return pointer、callback parameter）で互換とし、両側が定数境界で異なる`[2]`と`[3]`は同じWasm `i32`へlowerされても拒否する。同一object内で不完全境界の関数参照と後続の定数境界定義が現れる場合は、両宣言順およびreturn/parameterの異なる位置を補完するcomposite typeを含め、より完全な署名を保持する。多次元`int (*)[][3]`と`int (*)[2][3]`についてもparameter・return・保存済みfunction pointer・条件演算子、およびconsumer parameter内にあるfactory callbackのreturn typeまで外側boundを再帰的に補完し、内側boundが`[3]`対`[4]`なら直接functionとnested callbackの再宣言・pointer初期化を拒否する |
| **関数の重複定義検出** (C11 6.9p3) | 🔧 | function_duplicate_def | 同名関数の本体定義を 2 度書く `int f(){...} int f(){...}` が silently 通過し、後段でアセンブラ/リンカが duplicate symbol を出すまで気づけなかった。修正: func_name_t に is_defined フラグを追加し、funcdef の本体パース直前 (proto `;` を弾いた後) で psx_ctx_track_function_defined を呼ぶ。初回は記録、2 度目なら E3064。プロトタイプは何度書いても合法 (本フラグは立たない)。proto+def 混在・static 関数・複数 proto + 1 つの定義の合法形を fixture で網羅 |
| **declaration-specifier 順序自由 / storage class 重複 / グローバル重複定義** | 🔧 | decl_spec_order_and_dup | 3 件の関連バグ: (1) `int static x` (型 → storage class 順) が E3016 で拒否されていた。C11 6.7p1 で declaration-specifiers の順序は任意。psx_consume_type_kind のループ内で型指定子の後ろに出現する storage class / qualifier を消費し flag を立てるよう拡張。(2) `int static static`、`static int static`、`static int extern` 等の interleaved storage class 重複 / 併用が見逃されていた (skip_cv_qualifiers の storage_count は単発呼び出し内のみ)。同じくループ内でグローバル状態 (g_last_decl_is_static / is_extern) をチェックして 2 度目で E3064。(3) グローバル `int g=1; int g=2;` の重複定義 / `int g; double g;` の型違いが silently 通過し後段でアセンブラの duplicate symbol で気づくのみだった。register_toplevel_global_decl で同名既存を merge (extern と同様、型互換チェック付き)、apply_toplevel_object_initializer で `=` 消費時に既存 has_init を検出して E3064。tentative def 同型 (`int g; int g;`) は合法 merge、qualifier 重複 (`const const int x`、C11 6.7.3p5) は合法、各種順序の混在を fixture で網羅 |
| **識別子の名前空間衝突** (C11 6.2.3 / 6.7p4) | 🔧 | name_namespace_collision | 続き39 で global var redef は検出したが、(a) `extern int g; double g = 1.5;` (extern と定義の型不一致)、(b) `int foo(int){...} int foo;` (関数→変数)、(c) `int bar; int bar(int){...}` (変数→関数)、(d) `typedef int T; int T = 5;` (typedef→変数) はそれぞれ silently 通過。register_toplevel_global_decl の merge ロジックを extern 経由でも走るように一本化 (extern と非 extern の両方で型互換チェック)、加えて psx_ctx_has_function_name と psx_ctx_find_typedef_name で名前衝突を検出。funcdef では find_global_var_by_name で同名グローバル変数の有無を確認。合法形 (同型 extern+定義、proto+def、typedef を変数の型として使用) を fixture で網羅 |
| **追加の識別子診断** (関数代入 / enum 衝突 / implicit function declaration) | 🔧 | identifier_diagnostics | 続き40 の延長として 3 件: (1) `f = 5;` の関数識別子への代入が "ir build/emit failed" 粗エラーで止まっていたのを、assign 関数の前で ND_FUNCREF を check し parser 段で明確な E3064 を出すように。(2) `enum E{A=5}; int A=10;` および逆順 `int B=10; enum E{B=5};` の enum 定数と通常 identifier の名前空間衝突を検出 (register_toplevel_global_decl と enum_const.c の双方で全 4 名前種を check)。(3) C99/C11 で禁止されている implicit function declaration `undecl_func()` が silently 通過していたのを W3001 warning として警告 (build_unqualified_call で psx_ctx_has_function_name / psx_find_global_var に見つからない場合)。fixture では合法形 (関数 proto + 呼び出し / アドレス取得、enum 定数を含む switch) の回帰確認 |
| **タグ再定義検出 / 非 void 関数の return なし** | 🔧 | tag_redef_and_return | (1) `struct S { int x; }; struct S { int y; };` のタグ再定義が silently 通過。psx_ctx_define_tag_type_with_layout で同一スコープに既存の完全型 (member_count > 0) があり今回も完全型なら C11 6.7.2.1p1 違反 E3005。前方宣言→定義 / 内側スコープ shadow は従来挙動を保持。enum タグも同じ機構で検出。(2) `int get(int){ x = x+1; }` のように非 void 関数で値を返さずに終端するのが silently 通過 (C11 6.9.1p12 未定義動作)。emit_implicit_return_if_missing で main 以外の非 void 関数では W3001 warning (main は C11 5.1.2.2.3 で暗黙 return 0 が標準化されているので例外)。副次: ps_program_from 冒頭で psx_ctx_reset_tag_diag_state / reset_function_diag_state を呼び、ユニットテスト用のソフトリセットを追加 (実コンパイルは 1 ファイル 1 プロセスなので影響なし) |
| **コンパイル時 UB 警告 / 怪しい書き方** | 🔧 | undefined_behavior_warnings | 4 件の W3001 warning を追加: (1) 0 リテラルでの除算・剰余 `1 / 0` / `1 % 0` (C11 6.5.5p5 UB)。mul() の ND_DIV / ND_MOD 構築時に rhs が ND_NUM(0) なら警告。(2) シフト量が型の幅を超える `1 << 32` / `1 << -1` (C11 6.5.7p3 UB)。shift() で rhs が ND_NUM で int=32/long=64 以上または負なら警告。(3) 自己代入 `x = x` (clang -Wself-assign 相当)。assign() の TK_ASSIGN 分岐で両辺が同じ ND_LVAR offset なら警告。(4) 空 if 本体 `if (cond);` (clang -Wempty-body 相当)。parse_stmt_if で `)` 直後が TK_SEMI なら警告。fixture では非ゼロ除算 / 適切なシフト / 別変数への代入 / 本体ありの if で false-positive なしを確認 |
| **`const struct` / `const struct *` / const struct 配列要素のメンバ代入拒否** `const struct S s; s.x=2` / `const struct S a[1]; a[0].x=2` / `const struct S (*p)[1]; (*p)[0].x=2` | 🔧 | compile_fail: const_struct_member_assign_rejected, const_struct_array_member_assign_rejected, global_const_struct_array_member_assign_rejected, const_struct_pointer_to_array_member_assign_rejected | stmt.c の tag-keyword fast path が `const struct S s` / `struct S const s` / inline tag の const を after_type に渡さず、さらに `s.x` の ND_DEREF 代入で const を見ていなかった。tag 経路で const/volatile を保存し、メンバ deref に親 const を伝播、const 付き ND_DEREF への代入を E3077 にする。続き103で配列 decay の `ND_ADDR` と subscript 結果 `ND_DEREF` に const/volatile が伝播しない漏れを修正し、ローカル/static local/global の const struct 配列要素メンバ代入も E3077 にする。続き104で単項 `*` の結果に pointee const/volatile が伝播しない漏れを修正し、pointer-to-array 経由の const 配列要素メンバ代入も E3077 にする。非 const struct メンバ代入と `struct S * const p` 経由の mutable member 代入は許可 |
| **縮小変換と自己比較** | 🔧 | narrowing_and_self_compare | 2 件の W3001 warning を追加: (1) 整数変数を浮動小数点リテラルで初期化 `int x = 1.5;` (clang -Wliteral-conversion 相当)。psx_decl_parse_initializer_for_var のスカラ非ポインタ非タグ分岐で init_expr が ND_NUM かつ fp_kind != NONE かつ fval に小数部 (fval != (long long)fval) があれば警告。整数値の暗黙変換 (`int x = 2.0;`) は値が等価なので警告しない。(2) 自己比較 `x == x` / `x != x` (clang -Wtautological-compare 相当)。equality() で両辺が同じ ND_LVAR offset または同じ ND_GVAR 名なら警告。fixture では整数代入 / float 代入 / 明示キャスト経由 / 異なる変数同士の比較で false-positive なしを確認 |
| **代入を条件式 / 整数オーバーフロー / dangling pointer** | 🔧 | assign_overflow_dangling | 3 件の W3001 warning を追加: (1) `if (x = 10)` / `while (x = 0)` の代入を条件として使う形 (clang -Wparentheses 相当)。parse_stmt_if / parse_stmt_while で条件式 top が ND_ASSIGN なら警告。意図的形 `while ((c=getchar()) != EOF)` は top が ND_NE なので発火しない。(2) 整数リテラルが型サイズを超える初期化 `char c = 300;` (clang -Wconstant-conversion 相当)。decl.c のスカラ初期化分岐で var->elem_size < 4 かつ ND_NUM の値が型範囲外なら警告。`unsigned char uc = -1;` は全ビット 1 のイディオムとして除外。(3) ローカル変数アドレスの return `return &x;` (clang -Wreturn-stack-address 相当)。parse_stmt_return で ND_ADDR(ND_LVAR) かつ static でなければ警告。static ローカル / グローバル / 引数渡しは合法 |
| **HIR-to-IR builderの固定容量境界** 513 locals / 33 loops / 17 switches / 129 labels | 🔧 | hir_ir_dynamic_capacity_boundaries | 正当なCプログラムを内部配列の固定上限でE4007拒否していた。local storage、loop target、switch target、label targetをoverflow検査付きの動的領域へ移し、nested switchでは再確保後も外側targetのaddressが安定する個別所有にした。旧上限を1件ずつ超える単一fixtureをnative/WAT/objectで固定し、設計invariantで固定長配列の再導入を拒否する |
| **cold Wasm self-hostの深い二項式** 129項の左結合加算 | 🔧 | deep_binary_expression_pipeline | 有効なscalar式木をSyntax preflight、Typed HIR emission、HIR-to-IR loweringが再帰走査し、cold状態のself-host compilerでJavaScriptのcall stackを使い切っていた。3段階を明示stackで走査し、同一fixtureをNative/WAT/Wasm objectとJS resource-limit suiteの最初のcompileで固定する |
| **cold Wasm self-hostの深い関数呼び出し式** 192段の直接呼び出し | 🔧 | deep_call_expression_pipeline | 関数引数の構文解析、call型のpreflight、Typed HIR構築、HIR-to-IR loweringがcall treeを再帰走査し、cold状態のself-host compilerでJavaScriptのcall stackを使い切っていた。4段階を動的な明示stackへ移し、callee・引数の評価順、default argument promotion、複素数・aggregate引数表現を維持したままNative/WAT/Wasm objectとfresh self-host instanceで固定する |
| **cold Wasm self-hostの深い明示cast式** 192段の`(int)` | 🔧 | deep_cast_expression_pipeline | source castの型preflight、Typed HIR構築、HIR-to-IR loweringがoperand鎖を再帰走査し、cold状態のself-host compilerでJavaScriptのcall stackを使い切っていた。3段階を動的な明示stackへ移し、value decay、aggregate cast temporary、void・complex・scalar変換を共通の内側からの構築順で維持したままNative/WAT/Wasm objectとfresh self-host instanceで固定する |
| **cold Wasm self-hostの深いcomma式** 192項の副作用付き左結合comma | 🔧 | deep_comma_expression_pipeline | HIR-to-IR loweringでcommaと暗黙castが交互に現れる式木を相互に再帰し、cold状態のself-host compilerでJavaScriptのcall stackを使い切っていた。commaとcastを共通の動的な明示stackへ移し、左から右への副作用順序とscalar・void・complex・aggregateの結果表現を維持したままNative/WAT/Wasm objectとfresh self-host instanceで固定する |
| **cold Wasm self-hostの深い整数定数式** 257項の左結合加算 | 🔧 | deep_integer_constant_expression | `_Static_assert`の有効operand検査と整数定数値評価が式木を再帰走査し、257項でJavaScriptのcall stackを使い切っていた。短絡・三項・commaの評価規則を維持したままeager binary部分を明示stackへ移し、Native/WAT/Wasm objectとfresh self-host instanceのcold compileで固定する |
| **cold Wasm self-hostの深い三項式** 256段の右結合`?:` | 🔧 | deep_ternary_expression_pipeline | Syntax preflightを反復化した後もTyped HIR生成がcondition/then/elseを再帰走査し、cold状態のself-host compilerでJavaScriptのcall stackを使い切っていた。三分岐の構築状態を動的な明示stackへ移し、bitfield昇格・null pointer constant判定・value decayの順序を維持したままNative/WAT/Wasm objectとfresh self-host instanceで固定する |
| **深い短絡整数定数式の指数的再検査** 257項の左結合`&&` | 🔧 | deep_short_circuit_constant_expression | 整数定数式の再帰評価ごとに同じ部分木の有効operand検査をやり直し、33項でもnative compileに18秒超を要する指数的退行になっていた。有効operand検査を一度だけ行い、選択済み部分木を再検査なしで評価する段階へ分離して、短絡規則を保ったままNative/WAT/Wasm objectとfresh self-host instanceで固定する |
| **cold Wasm self-hostの深い単項定数式** 256段の`!` | 🔧 | deep_unary_constant_expression | Syntax preflight、Typed HIR生成、整数定数式のoperand検査と値評価が単項鎖を再帰してJavaScriptのcall stackを使い切っていた。4段階を動的な明示stackへ移し、Native/WAT/Wasm objectとfresh self-host instanceのcold compileで固定する |
| **cold Wasm self-hostの階層関数形式マクロ** 32段の三項式 | 🔧 | nested_function_macro_expansion | 関数形式マクロの引数prescanで、参照されないhidesetを括弧・演算子・数値を含む全置換tokenへ複製し、階層的な置換でcold compileが極端に遅くなっていた。hideset付与を識別子へ限定し、Native/WAT/Wasm objectとfresh self-host instanceで固定する |
| **virtual header macroの条件式内hover** `player_x > GAME_SCREEN_WIDTH - PLAYER_SIZE` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor位置で識別子を除いたrecovery sourceが比較演算子`<`/`>`の直後を補完せず`if (player_x > )`となり、nativeはfatal recovery、selfhost Wasmは`unreachable`へ進んでいた。比較演算子にも仮operand `0`を補い、実starter構造の`GAME_SCREEN_WIDTH`/`GAME_SCREEN_HEIGHT`それぞれで識別子の先頭・途中・末尾、同一instance連続実行・fresh instance、hover/completionのmacro replacementとvirtual header宣言元をnative/Wasmで固定する。Wasm fatalはJS境界で`AgcLanguageAnalysisError`と構造化diagnosticsへ変換しraw trapを公開しない |
| **enum定数の宣言位置hover** `enum { PLAYER_SIZE = 12 }` | 🔧 | test_language_analysis (native/Wasm JS API) | cursorで切ったrecovery sourceが列挙子名とinitializerを失い、不完全なenum bodyとしてselfhost Wasm trapへ進んでいた。宣言位置では選択中の列挙子名と整数定数式までを保持してenum宣言を完結し、先頭・途中・末尾から使用位置と同じkind/type/signature/declaration/valueを返す。暗黙値、直前値からの増加、先行enum定数を使う整数定数式、宣言位置と使用位置の交互解析、native/Wasm snapshot parityを固定する |
| **objectの宣言位置hover** `static int player_x;` | 🔧 | test_language_analysis (native/Wasm JS API) | cursorで切ったrecovery sourceが宣言子名を空白へ置換してE3016となり、selfhost Wasmでは構造化結果の代わりにtrapしていた。宣言位置では選択中のobject宣言子と、その宣言子に属する初期化子をtop-levelの`,`/`;`まで保持して宣言を完結する。file-scope static/非static、明示initializer、block-scope local、複数宣言子について、宣言名の先頭・先頭+1・途中・末尾、使用位置とのhover parity、同一instance交互解析、fresh instance、native/Wasm snapshot parityを固定する |
| **inline tag定義直後のobject・typedef宣言位置hover** `struct { int member; } object` / `typedef enum { VALUE } Type` | 🔧 | test_language_analysis (native/Wasm JS API) | object宣言prefix scannerがinline `struct/union/enum` bodyの`{}`をfunction/block scopeとして数え、memberの`;`と閉じ`}`で宣言開始位置を捨てるため、最初のdeclaratorやinitializer付き・block localがE3016/E3064の解析例外になっていた。comment・quote・preprocessor lineを除外する既存反復scanner内でtag keyword、任意のtag名、対応bodyを字句的に区別し、tag body内のnested braceとmember終端を外側scope計数から除外する。anonymous/named struct・union・enum、nested inline recordとfunction-pointer member、initializer、複数宣言子、block local、prototype parameter、anonymous/named record typedef、enum typedef、comment内の`}`について、Nativeは名前の先頭・中央・末尾と同一/fresh session、Wasmは小型専用sourceの全形中央と代表2形の境界を最初のfresh instanceから同一instance再利用でNative snapshotと完全一致させる。正確なkind/type/signature/declaration range、後続object非可視、diagnostics空・`partial:false`を固定し、意味推測・再帰探索・深いtag入れ子は追加しない |
| **`for`初期宣言のobject宣言位置hover** `for (int index = 0; ... )` | 🔧 | test_language_analysis (native/Wasm JS API) | object宣言prefix scannerが現在blockの`{`直後からcursorまでを調べて`for`を非宣言keywordと判定し、object専用回復へ入れなかった。汎用回復は選択中の宣言名を除去して`for (int = 0; ...)`相当にし、valid sourceを構造化snapshotではなく解析例外にしていた。既存のcomment・quote・preprocessor・inline tag対応反復scannerで、通常の`for` keyword直後の`(`を第1節の開始位置として保持し、その括弧depthにある最初の`;`より前だけを初期宣言候補にする。object回復は元宣言子を`,`または`;`まで保持して残り2節とloop bodyを字句的に閉じ、body内へcursor markerを置く。通常/initializerなし、複数宣言子の第1・第2、typedef型、inline anonymous record、function pointer、括弧付き宣言子、comment gapの9形について、Nativeは名前の先頭・中央・末尾と同一/fresh session、Wasmは全形中央・代表3形の境界・隣接するtypedef/条件式使用を小型専用sourceでNative snapshotと完全一致させる。正確なkind/type/signature/declaration range、先行parameter/object可視、後続local/file object非可視、diagnostics空・`partial:false`を固定し、式評価・再帰探索・深い制御構造は追加しない |
| **prototype parameter配列境界operandのhover** `int f(int count, int values[count]);` | 🔧 | test_language_analysis (native/Wasm JS API) | parameter宣言専用recoveryがprototype全体を保持してmarkerを宣言後のfile scopeへ置くため、後続parameterの配列境界から先行parameterをlookupできず、diagnostics空・`partial:false`でもhoverがnullになっていた。さらにblock-scopeの通常function prototype/function typedefは候補scannerがfile scopeの`brace_depth == 0`だけを認めていたため、外側local enum/objectとparameter宣言位置はgreenでも同じprototype内の先行parameterだけを失っていた。直接function parameter listの対応する`(`/`)`だけをcomment・quote・preprocessor line・LF/CRLF splice対応の反復scannerで調べ、選択識別子を含む配列宣言子の直後かつ後続parameterより前に一時marker parameterを挿入する。block内では選択位置を実際に含む候補まで絞ってから既存の宣言prefix classifierと現在のbrace depthを一致させ、宣言の`;`でsourceを切って外側blockを閉じる。これによりcall列へ反復prefix scanを追加せず、record bodyとblock内definition候補は除外する。通常/static/qualified/式/括弧付き/多次元/pointer-to-VLA/comment/splice、file/block function typedef、function definition、block-scope通常/nested/typedef戻り型prototypeの先行parameterと、file/block object・enum定数・macro operandをNative/Wasm snapshot parityで固定する。正確なkind・宣言range・enum値・macro replacement/documentation、後続parameter/local/file objectとdefinition body localの非可視、diagnostics空・`partial:false`を確認し、nested callback parameter list・意味評価・再帰探索・深い宣言子には広げない |
| **直接function-pointer objectのprototype parameter配列境界hover** `void (*callback)(int count, int values[count], int later);` | 🔧 | test_language_analysis (native/Wasm JS API) | 通常function prototypeとfile function typedefはgreenだった一方、file/block scopeのfunction-pointer objectではgeneric function parameter recoveryが先頭の`void (`を外側function declaratorと誤認し、先行parameterをprototype scopeから失ってhover/completionがdiagnostics空・`partial:false`のままnullになっていた。comment・quote・preprocessor line・LF/CRLF splice対応の既存scannerを、直前の完結したparenthesized declaratorがtop-levelの`*`を含みnested括弧を含まない直接`(*name)(...)`の場合だけ共有し、選択配列parameterの直後へmarker parameterを挿入する。file先頭からscanする場合は先頭行をpreprocessor候補として初期化し、未使用macroの不均衡な括弧を宣言delimiterに数えない。現在宣言の`;`でsourceを切り、block scopeでは外側braceだけを閉じることで、先行parameterを可視にしながら後続parameter・local/file objectを読まない。file/local object、`static` array parameter、comment、LF/CRLF spliceの6失敗形、括弧を含む未使用macro noise、file function typedefの既存green比較を、既存prototype-bound焦点targetへ統合してNative/Wasm snapshot parityを固定する。外側function parameter内のcallback、factory-return callback、nested declarator、複合bound式、深い式、意味評価、再帰探索には広げない |
| **block-scope `_Static_assert` operandのhover** `_Static_assert(sizeof(LocalType) >= 1, "ok")` | 🔧 | test_language_analysis (native/Wasm JS API) | block内の完全なstatic assertionでcursor識別子まで切った汎用recoveryが、開いている`sizeof`/`_Alignof`とassertionの括弧を閉じる一方、元statement後半の比較式・メッセージ引数を捨てていたため、`,`不足のE2006で構造化snapshotを返せなかった。keyword直後のassertion括弧を既存のcomment・quote・LF/CRLF splice対応delimiter stackへ記録し、元sourceに完全な対応`)`と`;`がある場合だけstatement全体を保持して直後へmarkerを置く。block-local typedef/tag/enum、macro、`sizeof`/`_Alignof`、type-name array bound、direct enum/macro operand、cast、delimiterを含むcomment/message、keyword comment、line splice、nested blockについて、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表4形の境界をNative snapshotと完全一致させる。正確なkind・宣言range・enum値・macro replacement/documentation、後続block/nested/file object非可視、diagnostics空・`partial:false`を固定し、不完全assertion、式評価、再帰探索、深い式には広げない |
| **`do` body内operandのhover** `do { object--; } while (object)` / `do object--; while (object)` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor位置で切った汎用recoveryがcompound bodyや単一statementを閉じても、`do` statementに必須の後続`while`節を作らずE3065となり、完全なvalid sourceから構造化snapshotを返せなかった。既存のcomment・quote・preprocessor・LF/CRLF splice対応反復scannerで、`do`直後の直接compound braceをdelimiter frameへ記録し、recoveryでbraceを閉じる際に最小`while (0);`を補う。浅い単一statementは同じstatement境界内にある`do`だけを追跡し、現在bodyを閉じてから最小while節とmarkerを置く。object/parameter/enum/macro/function call、body-local・nested block、comment/splice、単一expression・単一`if`、nested direct `do`、元while条件、完了済みdo後の通常statementの18形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表5形の境界をNative snapshotと完全一致させる。正確なkind・宣言range・enum値・macro replacement/documentation、後続body/block/file object非可視、diagnostics空・`partial:false`を固定し、別のcontrol statement配下にcompound bodyを持つ深い単一statement、式評価、再帰探索には広げない |
| **`__builtin_offsetof`・`offsetof`第1型引数のhover** `__builtin_offsetof(struct Item, member)` / `offsetof(Item, member)` | 🔧 | test_language_analysis (native/Wasm JS API) | member引数はsemantic record identityからhoverできた一方、cursorで第1型引数のtag/typedefを除いた汎用recoveryは通常call同様に仮式operandへ置換し、builtinへrecord/union型でない引数を渡してE3064「型名が必要」にしていた。既存delimiter frameで識別子境界付きの`__builtin_offsetof`または標準`offsetof`直後の`(`と、第1top-level commaまでの状態を記録する。元sourceに完全な対応`)`がありcursorが第1引数内にある場合だけ、型引数からcall終端までを原文で保持し、その後を現在scopeのmarkerへ閉じる。struct/union tag、typedef、qualified型、nested member、標準macro、comment、LF/CRLF splice、enum initializer、block-local tag/typedef、外側function argumentの13形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表5形の境界をNative snapshotと完全一致させる。正確なkind・宣言range、後続local/file object非可視、diagnostics空・`partial:false`を固定し、member引数の既存semantic hoverを変更せず、不完全call、任意macro wrapper、深い式、再帰探索には広げない |
| **aggregate initializer内のcompound literal型名・直接copy operand hover** `Item value = (Item){...}` / `Item first=p, second=first` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なvalid initializerでもcursor識別子を除いた汎用recoveryが式位置へ整数`0`を補い、aggregate objectへintを代入する一時sourceを作ってrejection 38「代入型不一致」の`AGC_PARTIAL_SEMANTIC`にしていた。さらにtypedef aggregateの複数declaratorでは、最初のinitializer内にあるparameter名までtypedef候補として数え、後続declaratorをobject宣言として再確認できなかったため、通常blockでは不要なpartial診断を残し、nested blockと`for`初期化子ではhoverも失っていた。完全な対応`)`と`}`を持つcompound literal型名は、既存type-name scannerでtypedef/tagを保持して最小`{0}`へ閉じる。block/file/for第1節の直接copy operandは、同じ反復delimiter scannerで宣言levelの単純`=`と直前declarator候補を記録し、選択識別子後が空白/commentから`,`/`;`へ直結する場合だけ元識別子を保持する。typedef宣言の確定時は最初のtop-level `=`より前にある`typedef候補 + 宣言子`だけを数え、initializer内の識別子を型候補から除外する。typedef/tag/union/scalar、file `sizeof`、comment、LF/CRLF splice、外側call、parameter/local copy、同一宣言copy/address、nested block、`for`初期化子について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表形の境界・fresh instanceを完全一致させる。正確なkind・宣言range、同一宣言の後続declaratorと後続local/file object非可視、diagnostics空・`partial:false`を固定し、不完全compound literal、複合initializer式、深い式、意味評価、再帰探索、security監査系には広げない |
| **`typedef`宣言子のpoint-of-declaration hover/completion** `typedef Base Copy;` / `typedef int Alias, Array[sizeof(Alias)];` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なvalid sourceでも既存recoveryが現在のtypedef宣言子まで完結してmarkerをその後へ置くため、宣言指定子側の`Base`や後続宣言子内の先行`Alias`のhover自体は返る一方、現在宣言中の`Copy`・`Array`・callback名までpoint of declarationより早くcompletionへ公開していた。同名を内側でshadowする`typedef Base Base;`では、先頭`Base`のhoverも外側typedefではなく未到達の内側宣言へ誤解決していた。宣言指定子側は、選択aliasより前がtop-level `typedef`とqualifier/comment/LF spliceだけで、直後が単純なobject・pointer・array・function・直接function-pointer宣言子として既存宣言prefix classifierでも確定する場合に限定し、現在宣言全体を除いてその直前へmarkerを置く。後続宣言子側は、top-level `typedef`と先行comma、comma後の現在宣言子名をcomment・quote・preprocessor line・LF/CRLF splice対応で確認し、そのcommaを`;`へ置き換えて先行宣言子の直後へmarkerを置く。file/block/nested scopeのplain/qualified/pointer/array/function/function-pointer、複数宣言子、同名shadow、record typedef callbackについて、Nativeは名前境界と再利用/fresh session、Wasmは代表境界・fresh instanceを専用0.62秒gateでNative snapshotと完全一致させる。正確なkind・宣言range、外側shadow先の保持、現在/後続typedef宣言子と後続nested/block/file object非可視、宣言名自身の既存hover、diagnostics空・`partial:false`を固定し、tag/`_Atomic`型指定子、attribute、宣言子内部の先行alias参照、深い宣言子、意味評価、再帰探索、security監査系には広げない |
| **直接aggregate operandのhover** `return value` / `target = value` / `sink(value)` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なvalid sourceでもcursor識別子を除いた汎用recoveryが整数`0`を補い、aggregateのreturn・assignment・call argumentをそれぞれrejection 52・38・44の`AGC_PARTIAL_SEMANTIC`にしていた。選択識別子後をcomment・LF/CRLF splice対応で走査し、現在開いている非brace delimiterに対応する`)`/`]`だけを順に閉じて直ちに`;`へ達する場合だけ元識別子を保持する。struct/union return、local/parameter assignment、union assignment、最後のcall argument、2重closing call、comment、LF/CRLF spliceと、expression statement/address/sizeof/memberの既存green比較14形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表5形の境界を完全一致させる。正確なkind・宣言range、後続local/file object非可視、diagnostics空・`partial:false`を固定し、途中のcall argument、演算tail、conditional/comma式、深い式、意味評価、再帰探索には広げない |
| **単純な後続引数を持つcall operandのhover** `sink(value, 1)` / `sink(1, value, 2)` / `sink(value, &object)` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なvalid callでも汎用recoveryがcursor識別子を整数`0`へ置換した後に後続引数を捨ててcallを閉じるため、aggregate/scalar/enum/macroのいずれでも引数不足のrejection 43 `AGC_PARTIAL_SEMANTIC`になっていた。cursorがpostfix callの直接引数位置にあり、直後が`,`、残りが単一primary、1個だけの`& * ! ~ + -`または`sizeof` prefix、1段だけの`.`／`->` member suffixで構成され、対応するcallと外側delimiterを閉じて直ちに`;`へ達する場合だけ、選択識別子から文末までの原文を保持する。struct/union/scalar parameter、enum定数、macro、先頭・中間引数、複数後続引数、comment、LF/CRLF splice、直接2重closing call、return context、文字／文字列／数値literal、address・dereference・`sizeof`・member access・logical/bitwise not・単項正負の30形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表7形の境界を完全一致させる。正確なkind・宣言range、後続local/file object非可視、diagnostics空・`partial:false`を固定し、複数単項operator、二項式、subscript、call、括弧式、`sizeof(...)`、conditional/comma式、深い式、意味評価、再帰探索には広げない |
| **local recordのbit-field幅にあるblock-local enum hover** `enum { WIDTH=4 }; struct Bits { unsigned field : WIDTH; };` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なvalid sourceをrecord member専用recoveryが原文のまま解析しても、cursor markerをtranslation unit末尾へ追加していたため、block-local enum定数がlookup pointから外れ、diagnostics空・`partial:false`のままhover/completionだけnullになっていた。選択識別子が最内の直接struct/union bodyにあり、直前の有意tokenが`:`、直後が`;`の場合だけ、幅識別子までのprefixを保持してrecordをsynthetic objectとして閉じ、markerを直後の外側scopeへ置いて残りsourceを読まない。named struct/union、anonymous record、nested block、file enum、macro、comment、LF/CRLF splice、local shadowingの10形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表5形の境界を完全一致させる。正確なkind・定数値・macro replacement・宣言range、後続enum/local/file object非可視、diagnostics空・`partial:false`を固定し、nested record body、複合bit-field幅式、意味評価、再帰探索には広げない |
| **local recordのmember配列境界にあるblock-local enum hover** `enum { BOUND=5 }; struct Item { int values[BOUND]; };` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なvalid sourceでもrecord member専用recoveryがgeneric配列境界recoveryより先に原文を保持し、cursor markerをtranslation unit末尾へ追加していたため、block-local enum定数がlookup pointから外れ、diagnostics空・`partial:false`のままhover/completionだけnullになっていた。選択識別子が最内の直接struct/union bodyにあり、直前の有意tokenが`[`、直後が`]`から空白/commentを挟んで`;`へ直結する場合だけ、境界識別子までのprefixを保持してmember配列とrecordをsynthetic objectとして閉じ、markerを直後の外側scopeへ置いて残りsourceを読まない。named struct/union、anonymous record、nested block、file enum、macro、comment、LF/CRLF splice、local shadowingと、既存greenのmember `_Alignas`／`_Atomic`比較を含む12形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表5形の境界を完全一致させる。正確なkind・定数値・macro replacement・宣言range、後続enum/local/file object非可視、diagnostics空・`partial:false`を固定し、多次元tail、複合境界式、nested record body、深い式、意味評価、再帰探索には広げない |
| **local recordの関数ポインタmember parameter配列境界にあるblock-local enum hover** `enum { BOUND=5 }; struct Item { void (*callback)(int [BOUND]); };` | 🔧 | test_language_analysis (native/Wasm JS API) | 単純member配列、多次元member配列、pointer-to-array memberはgreenだった一方、関数ポインタmemberのparameter配列境界だけはrecord member専用recoveryが完全source末尾へmarkerを置くため、block-local enumのhover/completionをdiagnostics空・`partial:false`のまま失っていた。最内の直接record bodyで、選択識別子がtop-level callback parameter listの直接配列境界にあり、parameter list直前が完結したparenthesized declarator、直後がmember終端`;`の場合だけを反復scannerで分類する。選択bound直後へsynthetic parameter markerを挿入し、現在memberでsourceを切ってrecordと外側blockを閉じることで、正しい外側scopeを保ちながら後続parameter・enum・local/file objectを可視にしない。named struct/union、anonymous record、nested block、file enum、macro、comment、LF/CRLF splice、local shadowing、後続named parameterの11形について、Nativeは全名前境界と再利用/fresh session、Wasmは全形中央と代表3形の名前境界をsnapshot完全一致させる。正確なkind・定数値・macro replacement・宣言range、diagnostics空・`partial:false`を固定し、nested callback declarator、複合bound式、nested record body、深い式、意味評価、再帰探索には広げない |
| **local recordのmember `_Static_assert` にあるblock-local enum hover** `enum { READY=1 }; struct Item { _Static_assert(READY, "ready"); int value; };` | 🔧 | test_language_analysis (native/Wasm JS API) | local record static assertionのdirect operandはrecord member専用recoveryがgeneric static-assert recoveryより先に完全sourceを保持し、cursor markerをtranslation unit末尾へ追加していたため、block-local enumはhover/completionがnull、同名file/local enumはfile側を誤選択していた。コンパイラ本体のlocal record static assertion対応後、選択識別子の直前の有意tokenが`(`、直後が`,`で、その開き括弧直前のidentifierがcomment・LF/CRLF spliceを除いて`_Static_assert`の場合だけrecord専用経路を辞退し、既存のcomplete static-assert recoveryへ渡す。named struct/union、anonymous record、nested block、file enum、macro、operand/keyword comment、LF/CRLF splice、local shadowingと、既存greenの`sizeof(LocalType)`比較を含む12形について、Nativeは名前先頭・中央・末尾と再利用/fresh session、Wasmは全形中央・代表5形の境界を完全一致させる。正確なkind・定数値・macro replacement・宣言range、後続enum/local/file object非可視、diagnostics空・`partial:false`を固定し、複合assertion式、不完全assertion、nested record body、深い式、意味評価、再帰探索には広げない |
| **object hoverの表示用C宣言signature** `static int player_x` | 🔧 | test_language_analysis (native/Wasm JS API) | objectだけ`signature`が空で、hostのhover code blockへ表示できる宣言がなかった。canonical semantic typeのdeclarator placeholderへ識別子を挿入する共通formatterを追加し、storage-classは記憶域期間を推測せずdeclaration metadataから前置する。file/block scopeのstatic、extern、register、automatic、const/volatile、pointer、array、function pointer、typedef使用、複数宣言子を宣言位置/使用位置とnative/Wasmで固定し、pointer/array/function pointer/typedef由来のsignatureをC宣言として再解析する |
| **functionの宣言・定義位置hover** `static inline int move_and_draw(...) { ... }` | 🔧 | test_language_analysis (native/Wasm JS API) | function名のcursor位置で汎用recoveryが識別子を除去し、不完全なdeclaratorをselfhost Wasmのraw trapへ送っていた。prototypeは`;`、definitionは対応するbody終端`}`までをcomment・文字列・preprocessor line・括弧深度を考慮して保持する専用recoveryへ分離する。宣言名の先頭・先頭+1・途中・末尾、static/extern/inline、parameter/return type、prototype→definition→useの宣言元range、同一instance交互解析、fresh instance、Native/Wasm snapshot parity、diagnostics空・`partial:false`を固定する |
| **旧形式・括弧付きdefinition・複数function declaratorのhover** `int sum(a,b) int a,b; {...}` / `int (wrapped)(int x) {...}` / `int (*(factory(void)))(int) {...}` / `int first(void), second(void);` | 🔧 | test_language_analysis (native/Wasm JS API) | function専用recoveryが旧形式parameter宣言の最初の`;`をprototype終端と誤認し、同一宣言の先頭function後にあるtop-level`,`では復旧を放棄して、valid sourceをNative/Wasmとも構造化snapshotではなく解析例外にしていた。またcursorが括弧付きdeclarator内にあると復旧対象外になっていた。初期identifier-listを字句的に識別し、parameter宣言をfunction body終端まで保持する。comma declaratorは同じ宣言の`;`まで追い、後続objectのbrace initializerをfunction bodyと区別する。宣言prefixの括弧・配列depthを引き継ぎ、括弧付きfunction definitionとfunction pointerを返すdefinitionも同じscannerで完結する。comma付きparameter宣言、nested callback declarator、inline tag定義、quoted array bound、複数functionとbrace initializer混在、typedef-nameだけのunnamed prototype parameterについて、関数名の先頭・中央・末尾、再利用/fresh instance、Native/Wasm snapshot parity、diagnostics空・`partial:false`を固定する |
| **function parameter宣言位置のhover** `int add(int left, const int *right)` / `int old(left) int left; {...}` | 🔧 | test_language_analysis (native/Wasm JS API) | definitionのparameter名ではfunction宣言専用recoveryがmarkerをbody終端後のfile scopeへ置くためparameter scopeが見えず、prototype parameterはscope graphへ登録済みでも同じglobal lookup pointから除外されていた。cursorがdirect function definitionのheaderまたは旧形式parameter宣言列にある場合は元bodyの先頭へ一時markerを挿入し、それでもmarkerから見えないprototypeや括弧付き・function pointer戻りdefinitionのnamed parameterでは、source rangeがcursor identifierと一致するparameter declarationのlookup pointを使う。通常・const pointer・function parameter調整、prototype、旧形式identifier listと宣言行、`register`、aggregate値、array-to-pointer調整、UTF-8 identifier、括弧付きdefinition、function pointerを返すdefinitionについて、名前の先頭・byte中央・末尾、再利用/fresh instance、Native/Wasm snapshot完全一致、canonical型、宣言range、diagnostics空・`partial:false`を固定する |
| **prototype-scope tag・enum定数とaggregate member宣言位置のhover** `struct Item { int field; };` / `int probe(enum State { READY } value);` | 🔧 | test_language_analysis (native/Wasm JS API) | prototype parameter内で定義したtagと列挙子はScopeGraphへ登録されていたが、解析markerがprototype終了後のfile scopeに置かれるためlookup対象から外れ、tagは`hover:null`になっていた。さらにenum専用recoveryが外側のfunction declaratorを閉じずに切るため、列挙子では構造化snapshotの代わりに解析例外になっていた。record memberはrecord scopeへ登録されてもsource rangeを持たず、language analysisから一律除外されていたためprototype内の宣言位置は`hover:null`だった。file-scope recordでは汎用object recoveryがmember名を除去し、外側のrecordを`;`なしで閉じるためvalid sourceまで解析例外にしていた。function header内のidentifierは完全なprototype/definitionを保持するrecoveryへ先に通し、tag・parameter・enum定数は元source rangeがcursorと一致する宣言自身のscope/orderへlookup pointを戻す。完全なdirect member名とparenthesized pointer declarator名を含むsourceはcomment・文字列・preprocessor lineを無視したdelimiter balanceを確認して元translation unitを保持する。memberは登録時にidentifier tokenのsource rangeを記録し、そのrangeとcursorが完全一致する1件だけを公開して、別recordの同名memberを混同せず通常のmember補完範囲も広げない。file-scope struct/unionのpointer・array・bit-field・function pointer、direct parameterとnested callback parameterのstruct/union/enum tag、同名member、明示値・暗黙増分を持つ列挙子をscope depthで区別し、名前の先頭・中央・末尾、再利用/fresh instance、Native/Wasm snapshot完全一致、canonical型・定数値・正確な宣言range、diagnostics空・`partial:false`を固定する |
| **label宣言・`goto`使用位置のhover** `goto done; ... done:` | 🔧 | test_language_analysis (native/Wasm JS API) | labelはScopeGraphの専用namespaceへ登録済みでもlanguage-analysis symbol kindとsource rangeがなく、通常のcompletion列挙から除外されたままだった。さらにcursor用recoveryがvalidな`goto`名を除去してE3065とし、self-host Wasmではraw trapへ進んでいた。jump ASTへlabel名tokenとparser時点のlookup pointを保持し、semantic登録時に宣言rangeを記録する。完全でbalancedな`goto name;`と`name:`は元sourceを保持し、cursorと一致するjumpだけstatement ASTを反復走査してfunction scopeのlabel namespaceへlookupする。labelは通常のcompletion範囲へ広げずexact hover時だけ`kind:label`・`signature:name:`として公開する。前方・後方`goto`、別functionの同名label、同名ordinary objectとのnamespace分離、nested blockについて、名前の先頭・中央・末尾、再利用/fresh instance、Native/Wasm snapshot完全一致、正確な宣言range、diagnostics空・`partial:false`を固定する |
| **同名aggregate member使用位置のhover** `first.value` / `second->value` | 🔧 | test_language_analysis (native/Wasm JS API) | member使用位置のcursor recoveryが完全なmember名と`.`/`->`を除去し、valid sourceにも`AGC_PARTIAL_SEMANTIC`を残していた。また基底objectの型から正しいmember型を選んでも、宣言rangeはsource先頭の同名identifierを文字列検索するため別recordのmemberを指していた。完全なmember accessではcursorまでの字句状態を確認してtokenを保持し、後続をcomma式の`0`で閉じて同じblock内のmarker lookup pointを維持する。基底record identityからScopeGraphのmember declaration IDを取得し、そのsource metadataをrangeへ使用する。旧形式parameterのdirect member、pointerによるarrow access、struct/union間の同名memberと異なるcanonical型について、名前の先頭・中央・末尾、再利用/fresh instance、Native/Wasm snapshot完全一致、正確な宣言range、diagnostics空・`partial:false`を固定する |
| **macroと同名のC namespace symbolのhover** `#define field() 1` / `struct S { int field; }` | 🔧 | test_language_analysis (native/Wasm JS API) | 一般hover選択が同名macroを常に最優先したため、function-like macroの名前が呼び出されずobject・typedef・enum constant・tag・member tokenとして使われる合法なsourceでもmacroを返していた。またrecord宣言用recoveryが完全なtranslation unitを保持する経路では、member宣言より後のobject-like macroまで一覧に入り、過去のmember宣言hoverを上書きしていた。object-like macroは通常どおり優先し、function-like macroはcursor名の直後にcomment/空白を挟んでも`(`が続く直接呼び出しだけを優先する。さらにcursorがScopeGraphの宣言token、label、または`.`/`->`に続くmember tokenと確定した場合は、そのC namespaceに対応するsymbol kindを最終選択する。非呼び出しfunction-like macroと同名のobject・typedef・enum constant・tagの使用、member宣言・使用、後続object-like macroと同名のmember宣言、および実際のfunction-like macro呼び出しについて、名前の先頭・中央・末尾、再利用/fresh instance、Native/Wasm snapshot完全一致、正確な宣言range、diagnostics空・`partial:false`を固定する |
| **header guard内prototypeのproject hover** `#ifndef MOVE_H ... void move_and_draw(void); ... #endif` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor用recovery sourceがprototype直後で入力を切り、元sourceに対応する`#endif`があってもE1053としていた。cursor以降は通常tokenを空白化し、条件付きdirectiveだけをvalidation tailとしてpreprocessorへ渡す。current sourceとvirtual headerが同名でも二重解析せず、各project sourceのmacro/conditional/diagnostic stateをrequestごとに初期化する。識別子の先頭・先頭+1・中央・末尾、prototype前のUTF-8、異なるguardを持つ複数header、別TU、同revision再利用・新revision再構築、Native/Wasm snapshot parityを固定し、本当に未終端のguardはE1053付きpartial resultとして維持する |
| **`_Generic`の制御式・association型hover** `_Generic(value, Score: 1, default: 0)` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor上の完全識別子を除いたrecovery sourceが、制御式では`_Generic()`、association型では`_Generic(value,)`となり、正常なsourceをE2006/E3064で解析失敗にしていた。delimiter stackで`_Generic`の括弧、top-level comma、association colonを追跡し、制御式には仮のdefault association、型位置には選択中の完全なtypedef名と仮の値を補って一時sourceだけを完結する。object、function callと引数、enum、macro、typedef association、association値について識別子の先頭・中央・末尾、同一/fresh instance、Native/Wasm snapshot parity、diagnostics空・`partial:false`を固定する。通常parserと元sourceは変更しない |
| **型名必須括弧のtypedef hover** `_Alignof /* query */ (const Score *)` / `_Atomic /* type */ (Score)` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor上のtypedef名を除いた汎用recovery sourceが`_Alignof(0)`または`_Atomic(0)`を合成し、式を受け取らないC11の型名文脈をE3064にしていた。delimiter stackで`_Alignof`と`_Atomic`の型名必須括弧を共通に識別し、その直下だけ仮の`int`型名を補う。keywordと括弧間のcomment、qualifier、pointerを含むsourceでtypedef名の先頭・中央・末尾を固定し、`_Alignof(int [1 + MODE])`の配列boundは従来どおり式として回復する。同一/fresh instance、Native/Wasm snapshot parity、diagnostics空・`partial:false`を固定し、通常parserと元sourceは変更しない |
| **型名文脈のtag名hover** `sizeof(const struct /* tag */ Player)` / `(struct Player){ 0 }` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor上の完全なtag名を除いたrecovery sourceが`sizeof(struct)`、`(enum)`、`(struct){...}`等となりE3065で失敗し、tag symbolの宣言rangeも名前ではなく`struct`/`union`/`enum`キーワードを指していた。tag keywordに直接続く完全名だけを元のtokenで一時復元し、compound literalには空initializer、scalar enum castとpointer castには仮operandを補う。tag後のabstract declaratorはcomment・文字列と`()`/`[]`/`{}`を字句的に釣り合わせ、外側の`)`または`_Generic` associationの`:`直前まで原形を保持する。これによりqualified pointer、pointer chain、pointer-to-array、function pointer、array compound literalを同じ経路で扱い、切り落としたassociation後方にしか`default`がない場合は一時sourceだけへ補う。保存済みkeyword位置からcommentと空白を越えてtag名rangeを正規化する。sizeof、`_Alignof`、cast、compound literal、generic association、struct/union/enum、識別子の先頭・中央・末尾、同一/fresh instance、Native/Wasm snapshot parity、diagnostics空・`partial:false`を固定する |
| **回復sourceから除いた完全識別子のpartial判定** `bef\nthis is invalid syntax after the cursor` | 🔧 | test_language_analysis (native/Wasm JS API) | 完全なtokenがcursor後にも存在するという字句情報だけでpartialを解除したため、未解決識別子と後続の壊れた構文を切り落として`diagnostics:[]`・`partial:false`にしていた。一方、関数use hoverでは完全tokenを一時除去しても宣言へ解決できるためnon-partialを維持する必要がある。recovery状態を「書きかけ識別子」と「完全だが除去した識別子」に分け、後者はsnapshotのexact hover symbolへ解決できた場合だけcompleteとする。`main`を使うfixtureでmissing-return警告による偶然のpartialを排除し、先行symbol保持とNative/Wasm同一結果を固定する |
| **宣言のないprimary sourceのEOF解析** 空/空白/comment/directive/include-only | 🔧 | test_language_analysis, wasm JS compile+link pipeline | 実tokenのないrecovery sourceにも先頭`;`を合成し、EOF位置へE3088を誤生成していた。非preprocessor tokenが一つもない場合は不要な`;`を追加せずmarker宣言だけを置き、diagnostics空・`partial:false`で完了する。virtual headerのcompletion/macro replacement/dependenciesをsnapshotに保持し、実在する`value;`のE3088と書きかけ`int`のpartial diagnosticは維持する。include-only TUを有効なmain TUとobject compile/linkする経路も固定する |
| **function-like macro直接呼び出しのhover** `CALL /* gap */ ()` / `CALL \\` 改行 `()` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor recoveryが完全な呼び出し名以降を切り落として有効な`return CALL();`を値なし`return;`へ変え、人工的な`AGC_PARTIAL_SEMANTIC`を残していた。またhover選択の空白走査がtranslation phase 2の行継続を越えず、同名objectがあるとmacroではなくobjectを返していた。元source上で対応する`)`まで完結した直接呼び出しであり、解析時点のpreprocessor状態にも同名function-like macroが存在すると確認できた場合だけ、syntheticな不完全statementを除いた一時sourceで再解析してmarkerとscopeを構築する。直接呼び出し判定ではliteral backslashとphase 1でbackslashへ置換されるslash trigraphのLF/CRLF行継続を空白相当として走査する。同名objectを持つfunction-like macroのcomment区切り・両表記のLF/CRLF行継続呼び出しについて、名前の先頭・中央・末尾、再利用/fresh instance、Native/Wasm snapshot完全一致、正確なmacro宣言range、diagnostics空・`partial:false`を固定し、閉じていない呼び出しはNativeのpartial snapshotまたはWasmの構造化analysis errorとしてcomplete扱いしない |
| **macro定義直前doc commentのhover/completion** `/// size` / `#define PLAYER_SIZE 12` | 🔧 | test_language_analysis (native/Wasm JS API) | documentation indexがC宣言だけを関連付け、macro symbolへはdocumentationを転記していなかった。また定義名上のcursor recoveryはdirectiveを途中で切って定義位置hoverを安定して返せなかった。doc comment直後が同じsourceの完全な`#define` directiveである場合だけ、その原文UTF-8 rangeを候補として保存し、preprocessorが選んだ現在有効なmacroの宣言rangeと一致した候補をhover/completionへ転記する。object-like/function-like、複数行replacement、定義/use位置の名前先頭・中央・末尾、CRLF、`#undef`後の再定義、inactive branch、空行・通常comment・別directiveによる遮断、primary source・virtual header・project revision、文字列/symbol/snapshot上限後の再利用について、正規化済みdocumentationと正確な`documentationRange`、Native/Wasm snapshot完全一致、同一/fresh instanceを固定する。predefined macroと文書なしmacroは空文字列・null rangeを維持する |
| **macro定義名のhover recovery** `#define BOARD_COLUMNS (...)` | 🔧 | test_language_analysis (native/Wasm JS API) | macro定義名上のcursorでもparameter/enum/record/function/object宣言用recoveryを先に試すため、後続のenum・配列・関数を含む有効なsourceをdeclarator途中と誤認してE3016を生成していた。元sourceのlogical line先頭からliteral/trigraph `#`、spliceを含む`define`、必須水平空白、macro名を確認し、完全なdefinition logical lineを保持した時点で宣言専用recoveryを迂回する。条件directiveだけをvalidation tailとして残すため、inactive branchは選ばず、`#undef`前のdefinition上ではその時点のreplacementを返す。不正なdefinitionがfatal recoveryだけを残した場合は元macro名range付き`AGC_PARTIAL_MACRO_DEFINITION`を補う。コメント付きスネーク全体、object/function/empty macro、`# define`、LF/CRLF、replacement・directive keyword・identifierのliteral/trigraph splice、定義/use位置、active/inactive、再定義、後続の完全/未完成/不正source、通常directive、primary/header/project revision、source/snapshot上限後の再利用について、documentationを含むNative/Wasm snapshot完全一致と同一/fresh instanceを固定する |
| **enumタグ・enum定数のdocumentation** `/// direction` / `enum Direction { DIRECTION_LEFT }` | 🔧 | test_language_analysis (native/Wasm JS API) | documentation転記がobject/function/macroだけに限定され、tagとenumConstantはindexにcommentがあっても空fieldになっていた。enum宣言全体の包含lookupをそのまま使うとtag説明や前の定数説明が後続定数へ漏れるため、named enum tagはcomment直後の`enum`から空白/commentを越えたtag名開始位置、enum constantはcomment直後の宣言開始位置と元source上のsymbol rangeを厳密一致させる。functionのprototype/definition fallbackはenumへ適用しない。スネーク全体、宣言/use位置、completion、tag-only/constant-only、anonymous enum、コメントなし定数、暗黙値/明示値、LF/CRLF、tab、日本語、複数段落、空行・通常comment・directive遮断、同名tag/定数のblock scope、virtual header/project revision、文字列/entry/snapshot上限後の再利用について、正確なUTF-8 documentationRange、同一/fresh instance、Native/Wasm snapshot一致を固定する |
| **translation phase正規化後のlanguage-analysis range** trigraph / backslash-newline後方の宣言 | 🔧 | test_language_analysis (native/Wasm JS API) | tokenizer内部のtoken rangeはphase 1/2正規化済みUTF-8を基準にするため、3 byteのtrigraph置換や行継続除去より後ろにあるmacro・object・functionの公開宣言rangeがeditorから渡された原文byte offsetより短くなっていた。language-analysisのsnapshot生成時だけrequest原文を同じphase順で走査し、正規化offsetのrange開始は直前の削除byte後、range終端は直後の削除byte前へ写像する。recovery sourceの浅いdelimiter走査も行継続を除去し、行頭の`#` trigraphをdirectiveとして扱う。trigraph directive、継続macro、その後方のmacro/object/functionについて原文のoffset・line・column、diagnostics空・`partial:false`、再利用/fresh instance、Native/Wasm snapshot完全一致を固定する。compiler内部tokenの正規化range契約は変更しない |
| **行継続で分断されたordinary object identifierのhover** `generic_literal_lf_\` 改行 `value` | 🔧 | test_language_analysis (native/Wasm JS API) | tokenizerはtranslation phase 2で行継続を除去して一つのidentifierを生成する一方、language-analysisのcursor名抽出は原文の連続byteだけを走査し、継続前後の断片名でglobal objectのhoverを失っていた。さらにcursorが継続byte内にあるとrecovery sourceをbackslashまたはtrigraph途中で切断し、E2028/E3016を合成していた。原文上のidentifier spanをLF/CRLFのliteral backslashおよびslash trigraph越しに連結し、symbol名との照合時だけspliceを除いた論理綴りを使う。recovery sourceではcursorが途中にある行継続を末尾まで保持し、identifier文字だけをelideする。global objectの宣言位置・加算式中のuse位置について、4表記それぞれの先頭・前半・splice直前/内部/直後・末尾、行を跨ぐ原文宣言range、diagnostics空・`partial:false`、再利用/fresh instance、Native/Wasm snapshot完全一致を固定する |
| **type-name cast直後のoperand hover** `(unsigned int)MAX_SNAKE_LENGTH` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor識別子を除いたrecovery sourceでは直前の有意文字が`)`となるため汎用の仮operandが入らず、完全に有効なcast式がoperand欠落のE3064またはself-host Wasm trapになっていた。cursor直前に空白・comment・LF/CRLF行継続だけを挟んで隣接する括弧範囲を、builtin/typedef候補、qualifier、pointer、tag、abstract declarator、配列boundを扱うbounded scannerでtype-name候補と確認した場合だけ仮operandを補う。function call、`sizeof`、`_Alignof`、`_Generic`、grouped expression、compound literalの括弧は専用context情報で除外する。実際のsnake fixtureとmacro/object/parameter/enum constant、simple/nested cast、binary RHS、argument、conditional、subscript、typedef・pointer・tag、comment・spliceについて名前の先頭・中央・末尾、project revision、source/snapshot上限後の再利用、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定し、不正・未完了type-nameと真のoperand欠落は構造化diagnosticを維持する |
| **括弧なし`sizeof`式operandのhover** `sizeof value` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor識別子を一時的に除くrecovery sourceで直前の有意文字が`sizeof`末尾の`f`となり、汎用の仮operandが入らず有効な式をpartial snapshotへ落としていた。delimiter走査が保持する直前tokenの`sizeof`状態を式operand補完条件へ加え、object/parameter/enum constant/macro、comment、LF/CRLF行継続について名前の先頭・中央・末尾、正確な宣言range・型・値・macro documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。型名を取る`sizeof(type)`の既存context判定は変更しない |
| **statement keyword直後のoperand hover** `return value` / `case ENUM:` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor識別子を一時的に除くrecovery sourceで`return`/`case`だけが残り、有効なstatementへ人工的なdiagnosticと`partial:true`を付けていた。直前tokenが式を要求する状態をdelimiter走査で保持し、`return`には仮operandを補う。`case`は単純な`case 0:`では既存labelと重複し得るため完全なcursor識別子を復元してlabelと空statementを閉じる。object/parameter/enum constant/macro、comment、LF/CRLF行継続、名前の先頭・中央・末尾、正確な宣言range・型・値・macro documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。後続が`(`の呼び出しは専用回復へ残し、未完了function-like macro invocationのpartial診断を維持する |
| **statement keyword直後の完結したcall operand hover** `return function()` / `case MACRO():` | 🔧 | test_language_analysis (native/Wasm JS API) | 未完了function-like macro invocationのpartial診断を守るため後続`(`を一律補完対象外にすると、完結した通常関数callとcase定数macro callにも人工的なdiagnosticが残っていた。quote/comment/括弧・角括弧・波括弧・行継続を反復走査する既存bounded scannerで対応`)`を確認し、完結した`return` callは仮operandへ、完結した`case` callは元のidentifierと呼出しsuffixを復元してlabelを閉じる。通常関数とfunction-like macro、comment、LF/CRLF行継続、名前の先頭・中央・末尾、function metadata、macro parameter/replacement/documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。閉じていないcallは引き続きpartial診断を維持する |
| **浅い`case`定数式内のoperand hover** `case 1 + ENUM:` / `case (ENUM):` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor識別子が`case`直後でないと汎用recoveryはlabel終端を補えず、さらに識別子直後が`:`の形ではjump-label専用経路が式中のenum定数を通常labelと誤分類して、有効なsourceにも`partial:true`を残していた。前方字句状態で未完了のcase式を保持してlabel誤分類を止め、quote/comment、括弧・角括弧・波括弧、条件演算子のcolon、LF/CRLF行継続を反復走査して実在するcase-label colonを確認した場合だけ、元のcase式を一時sourceへ復元する。単項・二項・括弧式・式中2個目の識別子・浅い条件演算子・object-like macro・comment・行継続について、名前の先頭・中央・末尾、enum値・macro replacement/documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。式の評価や再帰的な深度探索は追加しない |
| **列挙子initializer内のoperand hover** `DERIVED = BASE + 1` | 🔧 | test_language_analysis (native/Wasm JS API) | enum専用recoveryはcursor識別子が列挙子の宣言名である場合だけ選択中のenumeratorを保持し、initializer中の先行enum定数やmacroでは汎用recoveryへ落ちてenum本体を閉じず、valid sourceをE2006の解析例外にしていた。現在のenumerator区間でtop-level `=`がcursorより前にあり、対応する`,`または`}`が実在する場合だけ、そのenumerator末尾まで原文を保持してenum宣言を閉じる。file/block scope、単項・二項・括弧・式中2個目の識別子・浅い条件演算子・object-like macro・comment・LF/CRLF行継続について、名前の先頭・中央・末尾、enum値・macro replacement/documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。initializerの意味評価や再帰的な深度探索は追加しない |
| **未終端enum initializerのpartial hover** `DERIVED = BASE` / `DERIVED = BA` (末尾`,` / `}`なし) | 🔧 | test_language_analysis (native/Wasm JS API) | 完成したoperandの直後で入力が終わると、enum専用recoveryは実在するterminatorを必須として汎用経路へ落ち、Native/Wasm APIが構造化snapshotを返せなかった。現在のenumerator区間でtop-level `=`がcursorより前にあり、quote/commentと内側delimiterが閉じている場合だけEOFを未終端initializer境界として認識し、一時sourceではenumと外側blockを閉じる。EOF識別子はまず一時sourceからelideして仮operandで安全に候補scopeを構築し、可視enum定数またはobject-like macroへ完全一致する`BASE`だけ元operandを保持して再解析するため、hoverと派生enum値を維持する。未解決の`BA`は仮operand解析のまま、正確なrangeの`AGC_PARTIAL_IDENTIFIER`を付ける。識別子欠落とは独立したincomplete-source状態も保持するため、どちらも`partial:true`を維持する。file/block scope、virtual header、式中1個目/2個目、enum定数・macro、comment、LF/CRLF行継続、名前の先頭・中央・末尾、header上のdeclaration/documentation range、dependencies、completion metadata、lookup point、同一/fresh instance、失敗後再利用を固定する。同一sessionでvirtual headerをv1→v2→documentationなし→v1へ差し替え、enum値、macro replacement、documentationとrange、派生値、prefix候補が旧preprocessor/ScopeGraph状態を持ち越さないrevision境界を確認する。さらに旧名→rename→旧名の往復で、完全名operandがresolved hoverから正確な`AGC_PARTIAL_IDENTIFIER`へ遷移し、現revisionの別名は解決され、復元時には逆向きに切り替わることを固定する。同じ完全名をenum定数→object-like macro→enum定数および逆方向へ切り替える往復では、完全名hoverとprefix候補のsymbol kind、initializer/macro metadata、documentation/range、派生値が現revisionへ切り替わり、旧kindの同名候補を残さないこと、およびNative/Wasm snapshot完全一致も確認する。演算子で途切れた式を推測で補わず、式の意味評価や再帰的な深度探索は追加しない |
| **未終端enum initializer末尾のbare function-like macro** `DERIVED = FUNCTION_MACRO` | 🔧 | test_language_analysis (native/Wasm JS API) | EOF identifierを一度elideした安全な解析ではfunction-like macroをenum operandとして再解析しない判定自体は正しかったが、最終hover選択が同名macro候補を通常の完全一致として再採用し、派生列挙子はsynthetic operand由来の0なのにhoverあり・diagnostics空という不整合を返していた。初回のbounded判定結果を最終hover選択まで保持し、enum定数またはobject-like macroとして解決できないambiguous EOF identifierは候補一覧を維持したままhoverを抑止し、identifier全体へ`AGC_PARTIAL_IDENTIFIER`を付ける。同一sessionで同名macroをobject-like→2引数function-like→object-likeへ差し替え、bare完全名をresolved→partial→resolved、prefixをpartialのまま遷移させる。現revisionのfunction-like flag、parameter名、replacement、documentation/range、dependency、object-like時の派生値、Native/Wasm snapshot完全一致を固定する。候補構築、preprocessor本体、式評価や再帰的な深度探索は変更しない |
| **同名function-like macroとenum定数のEOF hover** `DERIVED = COLLISION` / `DERIVED = COLLISION(1)` | 🔧 | test_language_analysis (native/Wasm JS API) | macro namespaceとordinary namespaceに同じ綴りが共存するvirtual headerで、bare EOF identifierは二段階recoveryのScopeGraph lookupによりenum定数を保持し、完結したinvocationはfunction-like macro hoverを選ぶ。両経路でenum定数とmacroの同名候補を同時に公開し、それぞれ独立したdeclaration/documentation range、enum値73、macro parameter/replacement、bare派生値73、invocation派生値71を固定する。名前の先頭・中央・末尾、同一/fresh instance、dependency、diagnostics空・`partial:true`、Native/Wasm snapshot完全一致を確認し、namespace間の候補統合やhover kindを混線させない。式の意味評価や再帰的な深度探索は追加しない |
| **同名macro/enum候補のheader revision削除・復元** `DERIVED = COLLISION` / `DERIVED = COLLISION(1)` | 🔧 | test_language_analysis (native/Wasm JS API) | 同名function-like macroとenum定数が共存するvirtual headerをenum-only、macro-only、両方へ差し替えると、bare名は残存enumまたは`AGC_PARTIAL_IDENTIFIER`へ、呼び出し形は残存macroまたはenum hover付きE3102へ遷移する。enum-onlyの完結呼び出し形を未終端enum initializerの回復sourceへそのまま残すと、Nativeはfatal recoveryでsnapshotを返す一方、self-host WasmはE3102後にraw trapへ進んでいた。名前の先頭・中央・末尾では呼び出しを一度内部列挙子へ置換してheaderとScopeGraphを安全に構築し、同名function-like macroまたは通常の呼び出し対象なら元式を再解析する。残存enum定数へ解決した場合だけinvalid derived enumeratorを公開せず、カーソルまでに到達したE3102を元の`(`のrangeで構造化する。両方→enum-only→macro-only→両方の同一instance往復、候補消去、metadata/documentation/range、bare/invocation hover、派生値、dependency、Native/Wasm snapshot完全一致を固定し、深い式・資源stressは扱わない |
| **project source enumとvirtual header macroの更新順** `PROJECT_COLLIDING_SYMBOL` | 🔧 | test_language_analysis (native/Wasm JS API) | project indexは外部関数の定義情報だけを統合し、別TUのenum定数を可視化しないため、編集中の`main.c`自身にordinary namespaceのenum定数、virtual headerに同名function-like macroを置いてrevision境界を確認する。両方からsourceだけ削除・復元してからheaderを削除・復元する順と、headerを先に削除してsourceを削除し、header→sourceの順に戻す逆順を同一session/compilerで通す。両方・enum-only・macro-only・neitherの各状態で削除済み候補を残さず、現revisionのenum値101/202、macro replacement `+110`/`+220`、documentation/range、dependencyへ切り替える。bare名は残存enumまたは正確な`AGC_PARTIAL_IDENTIFIER`、呼び出し形は残存macroまたはenum hover付きE3102となり、有効な経路だけ派生enumerator値を持つ。名前の先頭・中央・末尾、project revision、再利用Native/Wasm instance、各revisionを先頭から再生するfresh Native snapshotの完全一致を固定する。同じrevisionを再指定したとき再構築しない既存cache契約と、深い式・資源stressは変更しない |
| **project source enum renameとheader macro shape更新** `PROJECT_COLLIDING_SYMBOL` ↔ `PROJECT_RENAMED_SYMBOL` | 🔧 | test_language_analysis (native/Wasm JS API) | 同じproject revision列を延長し、source enumを旧名からrename先へ移動して旧名をmacro-onlyにし、header macroのparameter/replacement/documentationを`value/+220`→`entry/+330`→`restored/+440`→`final_value/+550`へ更新しながら、旧名enumを値303・505で二度復元する。rename中は旧名enum候補が消えてrename先の値303/404・現documentation/rangeだけが残り、復元時はrename先候補が消えて新しい旧名enum identityへ切り替わる。bare旧名はrename中に正確な`AGC_PARTIAL_IDENTIFIER`、復元後は現enum hoverと派生値を返し、呼び出し形は常に現header macroのparameter/replacement・派生値331/441/551を返す。名前の先頭・中央・末尾、16 revisionの同一Native/Wasm instance、各revisionを先頭から再生するfresh Native snapshot完全一致を固定し、削除済み候補・旧値・旧documentationを復元後へ持ち越さない。各revisionではproject indexを再構築せずcurrent editing sourceだけをbare→invocation→bare→invocationと往復し、index revisionが不変なままhover kind、派生候補、`AGC_PARTIAL_IDENTIFIER`/E3102とrangeが現在のsource形へ戻ること、およびWasmの2巡目snapshotが1巡目と完全一致することも固定する |
| **未終端enum initializerのoperand直前cursor** `DERIVED = |OPERAND` | 🔧 | test_language_analysis (native/Wasm JS API) | cursorがoperand識別子直前の空白にあるとenum専用recoveryは識別子を選べず、汎用recoveryがenum内へcursor marker宣言を置く不正な一時sourceを作っていた。NativeはE2006後のfatal retryでpartial snapshotを返したが、self-host Wasmは同じ診断後にraw trapへ進んだ。空白/commentを越えた直後tokenが、現在のenumerator区間のtop-level `=`に続くoperandである場合だけ、そのenumeratorを内部placeholderへ置換してenumと外側blockを閉じる。cursor直前ではhover・diagnostic・派生enumeratorを公開せず、候補metadataとdependencyは現revisionを維持する。直前→名前の先頭→中央→末尾→直前を、bare/invocation、enum+macro・enum-only・macro-only・neither、rename/復元を含む16 project revisionで往復し、project index revision不変、同一Native/Wasm instance、fresh Native snapshot完全一致、戻り時のWasm snapshot完全一致を固定する。式の意味評価、深い式、資源stressは追加しない |
| **未終端enum initializerの直接call右側cursor** `DERIVED = MACRO(1)|` | 🔧 | test_language_analysis (native/Wasm JS API) | direct callの安全なelision/reparse判定はcursor識別子自身をcalleeと仮定していたため、cursorが`(`直後・引数末尾・`)`直後へ移るとcalleeのmacro/enum identityを再判定できなかった。特に`)`直後はenum専用recoveryから汎用recoveryへ落ち、Nativeでも有効なmacro派生値を失ってcursor位置へE2006を付けていた。現在のenumerator区間でinitializer全体が単一の完結したidentifier callである場合だけ、top-level `=`の直後からcallee spanと対応`)`をboundedに分類する。cursor位置と独立したcallee spanでfunction-like macroまたはenum定数を判定し、macroなら元callを再解析して`)`直後まで現派生値を維持し、enum-onlyなら元`(`のE3102を構造化して派生enumeratorを公開しない。名前末尾→`(`直後→引数末尾→`)`直後→名前末尾を16 project revisionの同一Native/Wasm instanceで往復し、名前外のnull hover、診断range、project revision不変、各位置のfresh Native snapshot完全一致、戻り時snapshot完全一致を固定する。複合式・深い式・資源stressへ分類を広げない |
| **空白/comment付きenum直接macro callのcursor回復** `DERIVED = MACRO /* gap */ ( /* before */ 1 /* after */ )` | 🔧 | test_language_analysis (native/Wasm JS API) | calleeと`(`、引数の前後に空白/commentがある完結callでcursorを`(`直後へ置くと、現在位置に識別子がないため次token探索がcommentを越えて数値`1`へ進み、識別子内部判定が数字も許すことから「operand直前」と誤分類してenumerator全体をplaceholderへ置換し、有効なmacro派生値を失っていた。現在のenumerator initializer全体が既存のbounded classifierで単一の完結したidentifier callと確定した場合は、次token探索より先にcallee spanを回復対象へ選び、元callを再解析する。代表6 revisionでcompact/comment付きsourceを同一Native/Wasm instance内で往復し、名前末尾→実際の`(`直後→引数末尾→実際の`)`直後→名前末尾について、現macro派生値、enum-only時の実際の`(`に対するE3102、null hover、候補metadata・documentation/range・dependency、project revision不変、fresh Native snapshot完全一致、戻り時snapshot完全一致を固定する。複合式・深い式・巨大入力・資源stressへ分類を広げない |
| **enum直接macro callの識別子引数hover** `DERIVED = MACRO(ENUM_ARGUMENT)` / `MACRO(OBJECT_MACRO_ARGUMENT)` | 🧪 | test_language_analysis (native/Wasm JS API) | 単一の完結した1引数callについて、calleeのbounded elision/reparseと引数自身のhover lookupが競合しない境界を固定する。enum定数引数とobject-like macro引数をcurrent editing sourceへ宣言し、引数直前→名前先頭→中央→末尾→直後、およびcallee名・`(`直後・`)`直後を往復する。引数上ではkind、定数値またはreplacement、declaration/documentation rangeを引数自身から返し、前後ではnull hover、function-like macro有効時は現revisionの派生値、enum-only時は実際の`(`に対するE3102と派生enumeratorなしを維持する。enum+macro・enum-only・macro-only・最終shape更新後の4 project revisionで、index revision不変、同一Native/Wasm instance、fresh Native snapshot完全一致、同じsource modeへ戻ったsnapshot完全一致を確認する。複合式・深い式・巨大入力・資源stressへ広げない |
| **enum直接2引数macro callのcomma境界hover** `DERIVED = MACRO(FIRST, SECOND)` | 🧪 | test_language_analysis (native/Wasm JS API) | initializer全体が単一の完結した2引数callである通常サイズのsourceについて、calleeのbounded elision/reparseがtop-level commaで途切れず、各引数のhoverと競合しない境界を固定する。同名enum定数と2引数function-like macroを共存させ、第1引数の先頭・中央・末尾・直後、comma位置と直後、第2引数の先頭・中央・末尾・直後、callee名・`(`直後・`)`直後を往復する。各引数上ではenum定数自身の値・declaration/documentation range、delimiter上ではnull hover、macro有効時はparameter/replacementと派生値103、enum-only時は実際の`(`に対するE3102と派生enumeratorなしを維持する。同一Native/Wasm instanceでmacro+enum→enum-only→macro+enum→enum-onlyを往復し、fresh Native snapshotおよび復元後snapshotの完全一致を確認する。複合式・深い式・巨大入力・資源stressへ広げない |
| **2引数macro callのcomment・line splice境界** `FIRST /* gap */ , /* gap */ SECOND` / `FIRST \\⏎ , \\⏎ SECOND` | 🧪 | test_language_analysis (native/Wasm JS API) | 上記2引数direct callのtop-level comma前後を、通常空白、block comment、LF line splice、CRLF line spliceの4 variantで固定する。各variantの実byte offsetを独立に求め、第1・第2引数、comma前後、第2引数直前に加え、block comment内部、各spliceのbackslash位置と改行側へcursorを動かす。comment/splice上ではnull hoverを返しつつ、calleeのbounded分類、各引数のdeclaration/documentation range、macro有効時の派生値103、enum-only時の実際の`(`に対するE3102を維持する。同一Native/Wasm instanceで全variantのmacro有効→enum-only→通常/CRLFへの復帰を行い、fresh Native snapshotと復帰snapshotの完全一致を確認する。式の複雑さ、深い式、巨大入力、資源stressは追加しない |
| **2引数macro callの片側object-like macro引数** `CALL(OBJECT_MACRO, SECOND)` / `CALL(FIRST, OBJECT_MACRO)` | 🧪 | test_language_analysis (native/Wasm JS API) | 上記comment・LF/CRLF line splice variantの第1・第2引数を片側ずつ通常サイズのobject-like macroへ切り替え、引数上のhover kindとmetadataがcalleeのfunction-like macroへ混線しない境界を固定する。引数macroはsource上の独立したdeclaration/documentation range、空parameter列、replacement 1/2を返し、反対側は元のenum定数を維持する。calleeはheader上の2 parameterとreplacementを保持し、macro有効時の派生値103、代表的なenum-only状態では実際の`(`に対するE3102と派生enumeratorなしを維持する。同一Native/Wasm instanceで両引数位置・全token間隔・代表enum-only状態を往復し、fresh Native snapshotと復帰snapshotの完全一致を確認する。複合式、深い式、巨大入力、資源stressは追加しない |
| **片側object-like macro引数のreplacement更新・復元** `1/2` → `11/12` → `1/2` | 🧪 | test_language_analysis (native/Wasm JS API) | 上記片側macro引数のsource定義だけを通常サイズで更新し、callee headerと反対側enum定数を固定したまま、comment・LF/CRLF line splice variant間を往復する。引数macroのreplacement、documentation/rangeを現revisionの1/2または11/12へ切り替え、macro有効時の派生enumerator値を103→113→103へ戻す。代表enum-only状態では更新後の引数macro metadataを維持しながら元`(`のE3102と派生enumeratorなしを固定する。両引数位置について同一Native/Wasm instanceの更新・復元列、各revisionのfresh Native snapshot、同一source復帰snapshotを完全一致させ、古いreplacement・documentation・派生値を残さない。複合式、深い式、巨大入力、資源stressは追加しない |
| **片側object-like macro引数のrename・復元** `FIRST_MACRO` ↔ `FIRST_RENAMED_MACRO` | 🧪 | test_language_analysis (native/Wasm JS API) | 上記片側macro引数のsource定義名とcall上の使用名だけを通常サイズの別名へ同時に切り替え、replacement 1/2とcallee headerを固定したままcomment・LF/CRLF line splice variant間を往復する。rename中は新名だけを候補・hoverとして公開し、元名候補を除去して新しいdeclaration/documentation rangeへ追従する。復元後はrename名候補を除去して元名のidentityとrangeへ戻し、反対側enum定数、calleeの2 parameter/replacement、派生値103を維持する。代表enum-only状態では現引数macro候補と元`(`のE3102、派生enumeratorなしを固定する。base・replacement更新・rename・復元を同一Native/Wasm instanceで連続解析し、候補集合全体、fresh Native snapshot、同一source復帰snapshotを完全一致させる。複合式、深い式、巨大入力、資源stressは追加しない |
| **片側object-like macro引数の定義削除・復元** `CALL(MACRO, SECOND)` with `#define MACRO` removed | 🔧 | test_language_analysis (native/Wasm JS API) | 完結したenum direct callを安全にelideした後、callee function-like macroが存在するだけで元callを再解析すると、未定義になった単一識別子引数でNativeはfatal recoveryからpartial snapshotを返す一方、self-host WasmはE3066後にraw trapしていた。各引数が単一識別子であるbounded callだけを走査し、object-like macro表とScopeGraph ordinary lookupの双方にない最初の引数を検出した場合は元callを再解析せず、既存macro診断契約どおり欠落名をmessageに保持したE3066をcallee invocation rangeへ構造化する。数値literal、宣言済みordinary識別子、複合引数は従来経路へ残す。第1・第2引数macroの定義削除・復元をcomment・LF/CRLF variantで往復し、削除時は全argument macro候補と派生enumeratorを除去、macro有効時はE3066、enum-only時は元`(`のE3102、復元時はhover・metadata・派生値103を回復する。同一Native/Wasm instance、fresh Native snapshot、同一source復帰snapshotを完全一致させ、深い式・巨大入力・資源stressは追加しない |
| **両側object-like macro引数の片側定義削除・復元** `CALL(FIRST_MACRO, SECOND_MACRO)` | 🧪 | test_language_analysis (native/Wasm JS API) | 上記bounded recoveryが片側だけmacroであることに依存しない境界を固定する。両引数を通常サイズのobject-like macroへした同一callで第1・第2定義を片側ずつ削除・復元し、欠落側候補と派生enumeratorを除去しながら、残存側macroのreplacement 1/2、declaration/documentation rangeとhoverを維持する。macro有効時は欠落側名をmessageに保持したE3066をcallee invocation rangeへ返し、復元後は両候補と派生値103を回復する。代表enum-only状態では残存定義のmetadataと元`(`のE3102を維持する。comment・LF/CRLF variantを同一Native/Wasm instanceでbase→第1削除→base→第2削除→baseと往復し、fresh Native snapshotと同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **両側object-like macro引数の同時定義削除・復元** `CALL(FIRST_MACRO, SECOND_MACRO)` with both definitions removed | 🧪 | test_language_analysis (native/Wasm JS API) | 両argument macro定義を同時に削除してもbounded recoveryが複数の未解決候補を重複診断せず、source順で最初の第1引数名だけをmessageに保持した1件のE3066をcallee invocation rangeへ返す決定性を固定する。削除中は両macro候補、両hover、派生enumeratorを除去し、第2引数名が診断本文へ混入しない。復元後は両macroのreplacement 1/2、declaration/documentation range、hoverと派生値103を回復する。代表enum-only状態では両macro候補なし、元`(`のE3102、派生enumeratorなしを維持する。comment・LF/CRLF variantで同時削除→復元を往復し、同一Native/Wasm instance、fresh Native snapshot、同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **両側object-like macro引数の段階復元** both missing → one restored → both restored | 🧪 | test_language_analysis (native/Wasm JS API) | 両定義欠落から第1だけを復元した場合はE3066対象がsource順の第2引数へ進み、両定義欠落から第2だけを復元した場合は第1引数を維持する診断遷移を固定する。各中間状態で復元済み側だけをmacro候補・hoverとして公開し、replacement 1/2とdeclaration/documentation rangeを維持しながら、未復元側候補と派生enumeratorを除去する。最終的な両定義復元では診断なし、両macro metadata、派生値103へ戻る。同じcommentまたはCRLF variant内で`3→2→0`と`3→1→0`を共有Native/Wasm instanceへ連続投入し、各revisionのfresh Native snapshotと同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **両側object-like macro引数の双方向削除・復元** both present → one missing → both missing → other missing → both present | 🧪 | test_language_analysis (native/Wasm JS API) | 両定義がある状態から片側削除、両側削除、反対側だけ復元、両側復元までを連続解析し、bounded recoveryが過去の未解決引数や候補集合に依存しないことを固定する。comment variantの`0→1→3→2→0`ではE3066対象が第1→第1→第2→なし、LF variantの`0→2→3→1→0`では第2→第1→第1→なしと現在sourceに従って遷移する。各状態のmacro候補、hover、replacement、declaration/documentation range、派生enumeratorの有無を検査し、最終状態は初回snapshotへ完全復帰する。同一Native/Wasm instance、fresh Native snapshot、同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **片側object-like macro rename中の反対側定義削除・復元** renamed first / second with the other definition removed | 🧪 | test_language_analysis (native/Wasm JS API) | 両側macro状態から片側だけを長い別名へrenameし、反対側を削除・復元してからbase名へ戻す列を両引数方向で固定する。rename中は元名候補を除去して新名だけを公開し、replacement 1/2、新しいdeclaration/documentation rangeとhoverを維持する。反対側削除時はrename側metadataを保ったまま欠落側名のE3066をcallee invocation rangeへ返し、派生enumeratorと欠落側候補を除去する。復元時は派生値103を回復し、最終base復帰ではrename名候補を除去して初回snapshotへ戻す。representative enum-only状態ではrename側候補と元`(`のE3102を維持する。comment・CRLF variantを同一Native/Wasm instanceで往復し、fresh Native snapshotと同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **片側object-like macro replacement更新中の反対側定義削除・復元** updated first / second with the other definition removed | 🧪 | test_language_analysis (native/Wasm JS API) | 両側macro状態から片側のreplacementとdocumentationだけを1→11または2→12へ更新し、反対側を削除・復元してからbaseへ戻す列を両引数方向で固定する。更新中は同じmacro identityでupdated replacement、declaration/documentation range、hoverを維持する。反対側削除時は更新側metadataを保ったまま欠落側名のE3066をcallee invocation rangeへ返し、派生enumeratorと欠落側候補を除去する。復元時は派生値113、最終base復帰ではreplacement 1/2と派生値103、初回snapshotへ戻す。representative enum-only状態では更新側候補と元`(`のE3102を維持する。comment・CRLF variantを同一Native/Wasm instanceで往復し、fresh Native snapshotと同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **片側object-like macro rename＋反対側replacement更新/削除・復元** renamed first with updated second, and vice versa | 🧪 | test_language_analysis (native/Wasm JS API) | 一方を長い別名へrenameし、反対側を2→12または1→11へ更新してから、そのupdated側を削除・復元し、最後に両側baseへ戻す列を両方向で固定する。rename側は旧名候補を除去して新名のdeclaration/documentation rangeとhoverを公開し、updated側は元identityのままreplacement 11/12とupdated documentation/rangeを保持する。updated側削除時はrename側metadataだけを残して欠落元名のE3066をcallee invocation rangeへ返し、復元時は両側metadataと派生値113を回復する。最終base復帰ではrename名とupdated metadataを除去し、元名2件、replacement 1/2、派生値103、初回snapshotへ戻す。representative enum-only状態ではrename側候補と元`(`のE3102を維持する。comment・CRLF variantを同一Native/Wasm instanceで往復し、fresh Native snapshotと同一source復帰snapshotを完全一致させる。深い式・巨大入力・資源stressは追加しない |
| **array designator index内のoperand hover** `{ [INDEX] = value }` | 🔧 | test_language_analysis (native/Wasm JS API) | cursor識別子がarray designatorの最初のoperandだと汎用recoveryは`]`だけを補って`[0]`とし、initializer要素に必須の`= value`とbrace終端を保持しないため、valid sourceをE2006の解析例外にしていた。prefixをquote/comment、preprocessor directive、trigraph directive、LF/CRLF行継続を除外しながら反復走査し、cursorを含む完全な`[...]`から後続の`.member` / `[...]` designator chainをたどり、最終`=`と、それを含む完全なobject brace initializerの対応`}`がsource中に実在する場合だけinitializer末尾まで原文を保持する。file/block scope、nested initializer、`[INDEX].member` / `[INDEX][INDEX]`、同一宣言の後続declarator非可視、単項・二項・括弧・式中2個目の識別子・浅い条件演算子・object-like macro・comment・行継続について、名前の先頭・中央・末尾、正確なlookup point、enum値・macro replacement/documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。式の意味評価や再帰的な深度探索は追加しない。compound literal内はmarker位置が異なるため別経路とする |
| **compound literal designator index内のoperand hover** `(int[8]){ [INDEX] = value }` | 🔧 | test_language_analysis (native/Wasm JS API) | object宣言initializer専用回復は直前が`=`のbraceだけを対象にするため、type-nameの`)`に続くcompound literal braceを選ばず、有効なsourceをE2006の解析例外にしていた。main delimiter stackでbounded type-name分類済み`)`に空白・comment・行継続だけを挟んで直結する`{`をcompound literalと記録し、cursorを含む完全な`[...]`、後続member/array designator chain、最終`=`、対応するcompound literal `}`、浅い式statementの実在する`;`またはtop-level comma境界をすべて確認した場合だけ原文を保持する。comma境界では後続declarator/式を取り込まず`;`へ閉じ、file scopeまたは元block scopeへmarkerを置く。file/block scope、postfix selection、function argument、builtin/typedef type-name、nested initializer、member/array chain、同一宣言の後続declarator非可視、単項・二項・括弧・浅い条件演算子、macro・comment・LF/CRLF行継続について、名前の先頭・中央・末尾、正確なdeclaration/lookup point、enum値・macro replacement/documentation、失敗後再利用、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。不完全なdesignator/compound literalはcomplete扱いせず、式の意味評価や再帰的な深度探索は追加しない |
| **object/compound literal brace initializer内のvalue operand hover** `int values[2]={{VALUE}}` / `(struct S){.x=VALUE}` | 🔧 | test_language_analysis (native/Wasm JS API) | brace initializer回復がarray designator indexだけに限定されていたため、通常のinitializer value上では汎用回復が対応するnested `}`と宣言終端を保持せずE3064、compound literalのmember/array value上では完全なliteralと後続postfix/statementを保持せずE2006にしていた。既存のquote/comment/directive/行継続対応frame scannerで、元sourceに完全な最外object initializer `}`が実在するときはそこまで保持して元scopeへmarkerを置く。main delimiter scannerがtype-name直後のcompound literal braceを先に識別し、完全なliteral `}`と浅い式statementまたはtop-level comma境界までを保持した後で通常object initializer回復へ進むため、compound literal内のnested braceをblock initializerと誤認しない。scalar/nested aggregate、単項・二項値、macro・enum・parameter、member initializer、後続postfix、file/block scope、同一宣言の後続declarator/local非可視について、名前の先頭・中央・末尾、正確なdeclaration range、enum値・macro replacement/documentation、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。不完全なinitializerは元の診断を維持し、式評価や深い探索は追加しない |
| **initializer member designator・`offsetof` member hover** `{ .member = 1 }` / `__builtin_offsetof(struct S, member)` | 🔧 | test_language_analysis (native/Wasm JS API) | member hoverは通常の`object.member`だけが基底objectからrecord型を解決し、initializer designatorと`offsetof`は有効なsourceでもmember候補を追加できずnull hoverになっていた。構文上の同名探索ではなく、flat initializerと`offsetof`の意味解析が確定したrecord identityからmember declaration IDをsemantic contextへtoken range付きで記録し、language analysisがcursor位置の解決済み参照だけを候補・hoverへ追加する。診断位置に使う既存designator tokenは維持し、識別子用`member_tok`を分離する。file-scope object、incomplete arrayの`[index].member`、nested member chain、block local、compound literal、direct/nested `offsetof`を対象に、同名で型が`int`/`long`の別recordを正しいdeclaration rangeへ分離し、名前の先頭・中央・末尾、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。式の深度探索や推測的な名前解決は追加しない |
| **`#if`条件内のmacro operand hover** `#if TARGET` / `#if defined(TARGET)` | 🔧 | test_language_analysis (native/Wasm JS API) | cursorまでで切った解析用sourceが`#if`を空または`defined`を未完了にし、完全な通常sourceでもE1008/E1010の解析例外にしていた。cursorが通常の`#if`論理行のcomment・quote外にある識別子の場合だけ、回復開始点をその論理行先頭へ戻し、元scopeのcursor markerとC delimiter終端を条件評価より前へ置いてから、既存のconditional validation tailで元の`#if` / `#else` / `#endif`を保持する。式を評価・書換えず、`#elif`・macro replacement listの既存経路は変更しない。直接条件、括弧あり/なし`defined`、block comment、LF行継続、真/偽macro、file/block scopeについて、Nativeは名前の先頭・中央・末尾と同一/fresh session、Wasmは全文脈の中央と代表的な真/偽/file/block境界、fresh instance、Native/Wasm完全snapshot一致、正確なmacro declaration range、後続object非可視、diagnostics空・`partial:false`を固定する。深い条件式、巨大入力、fuzz、資源stress、security監査系へは広げない |
| **`#undef`・`#ifndef` operandのmacro hoverとcomplete snapshot** `#undef TARGET` / `#ifndef TARGET` | 🔧 | test_language_analysis (native/Wasm JS API) | 通常の`#undef`行をcursor位置から回復するとdirectiveが処理されて直前まで有効なmacroがcompletion/hoverから消え、定義済みmacroの`#ifndef`では条件が偽のため行後方へ置いたmarkerが除去され、hoverは得られても完全なsourceを`partial:true`にしていた。`#if`専用だった論理行先頭回復を、directive名をidentifier境界付きで厳密照合する`if` / `ifdef` / `ifndef` / `undef` operand回復へ一般化する。`ifdef` / `ifndef` / `undef`はdirective直後の空白・block comment・LF行継続だけを許す直接operandに限定し、markerをdirectiveより前へ置く。conditional tailは`ifdef`/`ifndef`を保持し、`undef`をblankにするためcursor時点のmacro状態を公開する。通常/comment/行継続`#undef`、真`#ifdef`、定義済みで偽になる通常/comment/行継続`#ifndef`、未定義`#undef`/`#ifndef`について、Nativeは名前の先頭・中央・末尾と同一/fresh session、Wasmは代表境界・全形・fresh instance、Native/Wasm完全snapshot一致、正確なreplacement/declaration range、後続object非可視、diagnostics空・`partial:false`を固定する。未定義名にはmacro候補・hoverを作らない。深いdirective入れ子、巨大入力、fuzz、資源stress、security監査系へは広げない |
| **`#elif`条件内のmacro operand hover** `#elif TARGET` / `#elif defined(TARGET)` | 🔧 | test_language_analysis (native/Wasm JS API) | 先行branchまたは現在の条件が偽になる`#elif`では、directive行から始めたcursor recoveryのmarkerがpreprocessで除去されて`partial:true`となり、`defined(...)`形はcursor位置で切れてE1010になっていた。通常`#`から始まる行を後方へ線形走査し、閉じた内側conditional groupを数えながら現在のgroupに対応する`#if/#ifdef/#ifndef`へ回復開始点を戻す。条件式の評価や再帰探索は行わず、論理行形式のgroup境界は次項で扱い、trigraph hashは既存fallbackに残す。真/偽macro、未定義名、括弧付き`defined`、comment、LF行継続、file/block scope、閉じた内側group 1段について、Nativeは名前の先頭・中央・末尾と同一/fresh session、Wasmは全形の代表位置・代表境界・代表fresh instance、Native/Wasm完全snapshot一致、正確なreplacement/declaration range、後続object非可視、diagnostics空・`partial:false`を固定する。深いdirective入れ子、巨大入力、fuzz、資源stress、security監査系へは広げない |
| **論理行形式のconditional group内macro hover** `#i\` LF `f 0` / `# /*gap*/ if 0` / `#end\` LF `if` | 🔧 | test_language_analysis (native/Wasm JS API) | `#elif FALSE_MACRO`の対応opening directiveまたは閉じた内側groupの`#endif`が行継続・commentを含むと、物理行だけの後方scannerがgroup境界を見失い`partial:true`、split `#endif`ではE1053になっていた。conditional directive分類を既存のLF/CRLF・trigraph mode対応line-splice helperとblock-comment spacingを使う論理行scannerへ統一し、opener探索とvalidation tailの両方で共有する。directive keywordのidentifier境界を維持し、通常`#`だけを対象に、式評価や再帰探索は行わない。split opening keyword、`#`後comment、CRLF行継続gap、閉じた内側group 1段のsplit `#endif`、split current `#elif`について、Nativeは名前の先頭・中央・末尾と同一/fresh session、Wasmは元の4失敗形を小型専用sourceでNative snapshotと完全一致させ、正確なmacro declaration range、後続object非可視、diagnostics空・`partial:false`を固定する。深いdirective入れ子、巨大入力、fuzz、資源stress、security監査系へは広げない |
| **type-name配列bound内のoperand hover** `sizeof(int[BOUND])` / `(int (*)[BOUND])value` | 🔧 | test_language_analysis (native/Wasm JS API) | cursorが配列boundの最初の識別子だと汎用recoveryは残りのtype-nameを捨てて`0`を補い、zero-length arrayのE3096、cast/compound literalのE3064、`_Generic` associationのE2006にしていた。さらにfile-scope initializer中の`sizeof(`をfunction-parameter回復が関数宣言と誤認し、bracket内のenum名をobject回復がdeclarator名として扱って後続declaratorまで可視にしていた。top-level initializer区間をparameter候補から除外し、bracket内識別子をobject declarator候補から外す。delimiter stackと既存type-name classifierで完全な`]`および外側の`)` / association `:`を確認した場合だけ元のtype-name tailを復元する。cast/compound literalは`,`より先に完結する浅いstatementなら`;`まで保持して後続postfixの意味を維持し、それ以外は型に応じたsynthetic operand/initializerへ閉じる。file/block scope、`sizeof`、`_Alignof`、cast、array compound literal、`_Generic` association、後続postfix、単項・二項・括弧・浅い条件演算子、macro・comment・LF/CRLF行継続について、名前の先頭・中央・末尾、正確なdeclaration/lookup point、後続declarator/local非可視、invalid zero-boundと未完了boundの診断維持、失敗後再利用、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。意味評価や再帰的な深度探索は追加しない |
| **object declarator配列bound内のoperand hover** `int values[BOUND]` / `Element count=4, values[count]` | 🔧 | test_language_analysis (native/Wasm JS API) | cursorがobject declaratorの配列bound内にあると、object宣言回復はbracket内識別子を宣言名候補から除外し、汎用回復がboundを`0`へ置換して有効なfile-scope arrayをE3096、block VLAをE3064、record memberをE3016にしていた。さらに同一宣言の先行objectをboundから参照する場合、typedef宣言の字句分類がinitializerの数値tokenまでtypedef候補に数えて宣言判定を失い、`for`初期化子ではdeclaratorだけを閉じるsuffixが未完了の`for (`を残してE3064にしていた。明示型または字句的に`typedef候補 + 宣言子`と確定でき、cursor位置で未閉鎖の`[`がある場合だけ同じdeclaratorの`,` / `;`までを保持してmarkerを元scopeへ置く。identifier開始tokenだけをtypedef候補に数え、最初のtop-level comma以前で完結したtypedef declaratorを認識する。`for`初期化子ではcontrolとsynthetic bodyまで閉じて元block scopeへmarkerを置く。record memberは既存のmember回復で直後の`]`を許可する。builtin/typedef宣言、file/block/record scope、同一宣言・nested block・pointer-to-VLA・`for`初期化子、macro・enum・parameter bound、通常のsubscript式について、名前の先頭・中央・末尾、正確なdeclaration range、enum値・macro replacement/documentation、後続declarator/local非可視、同一/fresh instance、Native/Wasm snapshot完全一致、diagnostics空・`partial:false`を固定する。subscriptを宣言と推測せず、複合bound式、意味評価、深い探索、security監査系には広げない |
| **隣接cast chain内のoperand hover** `(int)(long)value` / `(void)(int[BOUND]){ 1 }` | 🔧 | test_language_analysis (native/Wasm JS API) | delimiter走査は閉じたtype-name castを式終端として扱うため、その直後の`(`をpostfix call括弧に固定し、2個目のcast operandやarray type-name bound上のcursorをE3064へ落としていた。閉じた前項と次の完全な括弧範囲を既存bounded type-name scannerで分類し、少なくとも片側にbuiltin/tag/qualifierがあってtype-nameと確定できる隣接対だけをcast chainとして扱う。builtin・typedefを両側に置く二重cast、array compound literal、後続postfix、pointer-to-array cast、comment・LF/CRLF行継続について、名前の先頭・中央・末尾、正確なdeclaration range、enum値・macro replacement/documentation、同一/fresh instance、Native/Wasmを固定する。`(function)(argument)`、`(pointer)(argument)`、`(*pointer)(argument)`、`(&function)(argument)`はpostfix callのまま維持する。両側がtypedef候補だけで字句的にcallと区別できない形を推測でcast扱いせず、意味評価や再帰的な深度探索は追加しない |

| **両側object-like macro引数の同時rename中の片側定義削除・復元** both arguments renamed with either definition removed | 🧪 | test_language_analysis (native/Wasm JS API) | 両引数の定義名とcall上の使用名を同時に長い別名へ切り替え、その状態で第1・第2定義を片側ずつ削除・復元してから両側baseへ戻す列を固定する。rename中は両旧名候補を除去して両新名だけを公開し、replacement 1/2、新しいdeclaration/documentation rangeとhoverを維持する。片側削除時は残存するrename名metadataを保ったまま欠落したrename名のE3066をcallee invocation rangeへ返し、欠落候補と派生enumeratorを除去する。復元時は両rename名と派生値103、最終base復帰では元名2件と初回snapshotへ戻す。representative enum-only状態では残存rename名候補と元`(`のE3102を維持する。comment・CRLF variantを同一Native/Wasm instanceで往復し、名前長変更後のcursor byte offset、fresh Native snapshot、同一source復帰snapshotを完全一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |

| **両側rename済みobject-like macro引数の同時定義削除・復元** both renamed definitions removed together | 🧪 | test_language_analysis (native/Wasm JS API) | 両引数を長い別名へrenameした通常サイズの2引数callで、両rename定義を同時に削除してから同時復元する履歴を固定する。同時欠落時は両rename候補・hover・派生enumeratorを除去し、source順で最初の第1rename名だけをmessageに保持した1件のE3066をcallee invocation rangeへ返して、第2rename名を診断本文へ混入させない。復元後は両rename名のreplacement 1/2、declaration/documentation range、hoverと派生値103を回復する。representative enum-only状態では両rename候補なし、元`(`のE3102、派生enumeratorなしを維持する。comment・CRLF variantで同時削除→同時復元を共有Native/Wasm instanceへ投入し、各revisionのfresh Native snapshotと復元後snapshotを完全一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |

| **両側rename済みmacro引数の段階復元** both renamed definitions missing → one restored → both restored | 🧪 | test_language_analysis (native/Wasm JS API) | 両rename定義欠落から第1だけを復元した場合はE3066対象が残る第2rename名へ進み、第2だけを復元した場合はsource順の第1rename名を維持する診断遷移を固定する。各中間状態では復元済みrename名だけを候補・hoverとして公開し、replacement 1/2、新しいdeclaration/documentation rangeを維持しながら、未復元rename候補と派生enumeratorを除去する。最終的な両定義復元では診断なし、両rename metadata、派生値103へ戻る。comment variantの`3→2→0`とCRLF variantの`3→1→0`を共有Native/Wasm instanceへ連続投入し、名前長変更後のcursor byte offset、各revisionのfresh Native snapshot、同一source復帰snapshotを完全一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |

| **両側rename済みmacro引数の双方向削除・復元** both present → one missing → both missing → other missing → both present | 🧪 | test_language_analysis (native/Wasm JS API) | 両rename定義がある状態から片側削除、両側削除、反対側だけ復元、両側復元までを連続解析し、過去の欠落順序ではなく現在sourceだけから未解決引数と候補集合を決める境界を固定する。comment variantの`0→1→3→2→0`ではE3066対象が第1rename名→第1rename名→第2rename名→なし、CRLF variantの`0→2→3→1→0`では第2rename名→第1rename名→第1rename名→なしと遷移する。各状態で定義済みrename名だけの候補・hover・replacement 1/2・declaration/documentation rangeを維持し、欠落候補と派生enumeratorを除去する。最終状態は両rename metadata、派生値103、初回snapshotへ完全復帰する。同一Native/Wasm instance、fresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct macro callの中央・末尾object-like macro欠落回復** `CALL(FIRST, MIDDLE, LAST)` | 🧪 | test_language_analysis (native/Wasm JS API) | bounded enum call recoveryの単一識別子引数走査が2引数に依存せず、通常サイズの3引数callでもsource順に未解決引数を選ぶ境界を固定する。3個のobject-like macroを引数に置き、中央定義だけまたは末尾定義だけを削除・復元するほか、CRLF line splice variantで末尾欠落→中央＋末尾欠落→中央欠落→全復元を共有instanceへ連続投入する。欠落時は定義済みmacroだけの候補・hover・replacement 1/2/3・declaration/documentation rangeを維持し、欠落候補と派生enumeratorを除去する。中央＋末尾欠落では中央名だけをmessageに保持した1件のE3066をcallee invocation rangeへ返し、末尾名を混入させない。全復元時は診断なし、3 macro metadata、派生値106へ戻る。callee、3引数の先頭・中央・末尾、2個のcomma、comment内部またはCRLF splice位置について、同一Native/Wasm instance、fresh Native snapshot、復帰snapshotを完全一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct callのordinary enum＋object-like macro混在回復** `CALL(FIRST_ENUM, MIDDLE_MACRO, LAST_ENUM)` | 🧪 | test_language_analysis (native/Wasm JS API) | 3引数bounded recoveryが全引数をmacroと仮定せず、ScopeGraph ordinary namespaceで解決する両端enum定数を有効な単一識別子引数として扱う境界を固定する。第1・第3引数を値4/5のenum定数、第2だけをreplacement 2のobject-like macroにし、comment・CRLF line splice variantで中央macro定義を削除・復元する。削除時は中央macro候補と派生enumeratorだけを除去し、中央名をmessageに保持した1件のE3066をcallee invocation rangeへ返す一方、両端enumの候補・hover・値・declaration/documentation rangeを維持する。復元時は中央macro metadataと派生値111へ戻る。callee、3引数、2個のcomma、comment/splice位置について、共有Native/Wasm instance、fresh Native snapshot、復帰snapshotを完全一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct callの両端macro＋中央ordinary enum混在回復** `CALL(FIRST_MACRO, MIDDLE_ENUM, LAST_MACRO)` | 🧪 | test_language_analysis (native/Wasm JS API) | ordinary enumを挟む2個のobject-like macroについて、現在sourceだけからsource順の未解決引数を選び直す境界を固定する。値6の中央enumを維持したまま、comment variantは両macroあり→第1欠落→両欠落→第3欠落→両復元、CRLF line splice variantは逆方向に遷移させる。両欠落時は第1macro名だけをmessageに含む1件のE3066をcallee invocation rangeへ返し、第1だけ復元すると第3macro名へ切り替える。各状態で中央enumと定義済みmacroの候補・hover・値/replacement・declaration/documentation rangeを維持し、欠落候補と派生enumeratorを除去する。全復元時は派生値110と初回snapshotへ戻る。共有Native/Wasm instance、各revisionのfresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct callの中央ordinary enum rename往復回復** `MIDDLE_ENUM` ↔ `RENAMED_MIDDLE_ENUM` | 🧪 | test_language_analysis (native/Wasm JS API) | 両端object-like macroの片側／同時欠落履歴を跨いで中央ordinary enumを長さの異なる名前へ変更・復帰しても、現在sourceだけからordinary候補とsource順E3066を作り直す境界を固定する。comment variantはrename後に第1欠落→両欠落→第3欠落→両復元、CRLF variantは逆方向に遷移させ、旧名comment/CRLFへも往復する。各revisionで現在名だけをenum候補・hoverとして公開し、非アクティブ名を除去しつつ値6、documentation、更新後のdeclaration rangeを維持する。定義済みmacroだけがreplacement 1/3を保持し、欠落時は派生enumeratorを除去、復元時は派生値110へ戻す。共有Native/Wasm instance、fresh Native snapshot、旧名sourceへの復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct callの中央ordinary enum metadata更新・復帰** value 6→9、documentation更新 | 🧪 | test_language_analysis (native/Wasm JS API) | rename済み中央enumの名前を保ったまま、両端macro欠落中に値とdocumentationを更新・復帰しても、現在revisionだけのmetadataと派生値を公開する境界を固定する。comment variantでは第1macro欠落中、CRLF variantでは第3macro欠落中に値6／旧documentationから値9／更新documentationへ切り替え、両macro欠落と反対側欠落を経て全復元する。中央enumは全状態で候補・hover、現在値、documentation、documentation rangeを維持し、古いmetadataを残さない。全macro復元時は派生値113となり、旧metadata sourceへ戻すと値6・旧documentation・派生値110へ復帰する。定義済みmacro候補、source順E3066、共有Native/Wasm instance、fresh Native snapshot、更新中／復帰後の同一source snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct callの中央enum・両端macro metadata交差更新** replacements 1/3→7/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 値9・更新documentationの中央enumを維持したまま、片側macro欠落中に両端macro replacementを1/3から7/11へ更新・復帰しても、現在sourceだけの候補metadata・E3066・派生値を返す境界を固定する。comment variantは第1macro欠落中、CRLF variantは第3macro欠落中にreplacement revisionを切り替え、両macro欠落と反対側欠落を経て同一欠落sourceへ戻してから全復元する。各状態で中央enumの値9・documentation・rangeと、定義済みmacroだけの現在replacement・declaration/documentation rangeを維持し、欠落候補と派生enumeratorを除去する。全復元時は派生値127、旧replacement sourceへ戻すと1/3と派生値113へ復帰する。共有Native/Wasm instance、fresh Native snapshot、更新中／復帰後の同一source snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |

| **3引数enum direct callの中央ordinary enum宣言削除・復元** outer replacements 7/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 更新済み両端macroを残したまま中央ordinary enum定義ブロックだけを削除・復元し、macro表ではなくordinary namespaceの欠落を同じbounded回復経路が扱う境界を固定する。comment/CRLF両variantを全定義→中央enum欠落→全復元し、欠落時は中央enum候補と派生enumeratorだけを除去して中央名を含む1件のE3066をcallee invocation rangeへ返す。両端macroは候補・hover、replacement 7/11、declaration/documentation rangeを維持する。復元時は中央enumの値9・更新documentation・rangeと派生値127へ戻る。共有Native/Wasm instance、fresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの中央enum＋片側macro同時欠落順序回復** outer replacements 7/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央ordinary enumと片側object-like macroを同時に欠落・復元し、異なるnamespaceを跨ぐ複数欠落でも現在source順の先頭だけをE3066へ選ぶ境界を固定する。comment variantはmask 0→1→3→2→6→4→0、CRLF variantは0→4→6→2→3→1→0と遷移させ、E3066対象を第1macro→中央enum、または中央enum→第3macroへ切り替える。定義済み引数だけが候補・hover、現在値/replacement、declaration/documentation rangeを維持し、欠落候補と派生enumeratorを除去する。全復元時は派生値127と初回snapshotへ戻る。共有Native/Wasm instance、各revisionのfresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全引数欠落・段階復元** missing mask 7 | 🧪 | test_language_analysis (native/Wasm JS API) | 両端object-like macroと中央ordinary enumの3引数をすべて欠落させても、現在source順の先頭だけをE3066へ選び直す境界を固定する。comment variantはmask 0→1→3→7→6→4→0として第1macro→中央enum→第3macroの順に診断対象を移し、CRLF variantは0→4→6→7→3→1→0として末尾側から全欠落へ到達・復元する。mask 7では3候補・hover・派生enumeratorをすべて除去し、第1macroだけをmessageへ含む1件のE3066をcallee invocation rangeへ返す。段階復元中は定義済み候補だけの値/replacement、declaration/documentation rangeを維持し、全復元時は中央enum値9、両端replacement 7/11、派生値127と初回snapshotへ戻る。共有Native/Wasm instance、各revisionのfresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落中macro revision切替** replacements 1/3→7/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 全引数欠落mask 7中に両端object-like macroのrevisionを1/3から7/11へ切り替え、宣言除去後に同一となるsourceがrevision由来のstale metadataを持たない境界を固定する。comment/CRLF両variantで旧revision全定義→全欠落→更新revisionの同一全欠落source→段階復元→更新revision全定義→旧revision全定義と遷移する。旧/更新の全欠落sourceを明示的に同一assertし、全cursorでrevision横断Wasm snapshotも完全一致させる。復元済み候補だけが現在replacement 7/11または中央enum値9とdocumentation/rangeを持ち、全復元時は派生値127、旧revisionへ戻すとreplacement 1/3と派生値113へ復帰する。fresh Native snapshotとの完全一致も維持し、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落中enum metadata revision切替** value 6→9、documentation更新 | 🧪 | test_language_analysis (native/Wasm JS API) | 全引数欠落mask 7中に中央ordinary enumの値・documentation revisionを6・旧documentationから9・更新documentationへ切り替え、宣言除去後に同一となるsourceがrevision由来のstale enum metadataを持たない境界を固定する。comment/CRLF両variantで旧revision全定義→全欠落→更新revisionの同一全欠落source→段階復元→更新revision全定義→旧revision全定義と遷移する。全欠落sourceのidentity assertをvariant 6〜11へ拡張し、全cursorのrevision横断Wasm snapshotも完全一致させる。中央enum復元後だけ値9・更新documentation/rangeが現れ、更新全復元時は両端replacement 1/3と派生値113、旧revisionへ戻すと値6・旧documentation・派生値110へ復帰する。fresh Native snapshotとの完全一致も維持し、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落中全metadata同時revision切替** enum value 6→9、macro replacements 1/3→7/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 全引数欠落mask 7中に中央ordinary enumの値・documentationと両端object-like macroのreplacementを同時に旧revisionから更新revisionへ切り替え、全宣言除去後のsourceとsnapshotが複数metadata revisionへ依存しない境界を固定する。variant 6/10・7/11の全欠落source identityを直接assertし、comment/CRLF両variantで旧全定義→旧全欠落→更新全欠落→両方向の段階復元→更新全定義→旧全定義と遷移する。全cursorのrevision横断Wasm snapshotとfresh Native snapshotを完全一致させる。復元済み候補だけが値9・更新documentation/rangeまたはreplacement 7/11を持ち、更新全復元時は派生値127、旧revisionへ戻すと値6・旧documentation・replacement 1/3・派生値110へ復帰する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落中中央enum rename・段階復元** `MIDDLE_ENUM`→`RENAMED_MIDDLE_ENUM` | 🧪 | test_language_analysis (native/Wasm JS API) | 全引数欠落mask 7中に中央ordinary enumを長さの異なる名前へ変更し、先行する第1macro欠落がある間はE3066対象を変えず、先行欠落の復元後だけ現在の中央名へ診断を切り替える境界を固定する。comment variantは旧名全定義→旧名全欠落→rename済み全欠落→mask 6→4→0、CRLF variantは旧名全定義→旧名全欠落→rename済み全欠落→mask 3→1→0と遷移し、旧名全定義へ戻す。全欠落時は現在のcall名をcursor anchorに使いつつ第1macro名だけのE3066をcallee invocation rangeへ返す。第1macro復元後はrename済み中央名へ診断が移り、中央enum復元後はrename済み候補だけが値6・documentation/rangeを持つ。全復元時は派生値110、旧名sourceへ戻すとrename済み候補を除去する。Native/Wasm共有instance、fresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落中中央enum rename＋metadata更新** name変更、value 6→9 | 🧪 | test_language_analysis (native/Wasm JS API) | 全引数欠落mask 7中に中央ordinary enumを長さの異なる名前へ変更しながら、値・documentationも6・旧documentationから9・更新documentationへ切り替える境界を固定する。comment variantは旧名全定義→旧名全欠落→rename＋metadata更新済み全欠落→mask 6→4→0、CRLF variantは同じrevisionをmask 3→1→0で復元し、最後に旧名全定義へ戻す。全欠落中は現在名へcall sourceを更新しても第1macro名だけのE3066を維持し、第1macro復元後にrename済み中央名へ切り替える。中央enum復元後はrename済み候補だけが値9・更新documentationと現在offsetのrangeを持ち、全復元時は両端replacement 1/3と派生値113へ戻る。旧名source復帰時は値6・旧documentation・派生値110へ戻り、更新名・metadataを除去する。Native/Wasm共有instance、fresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落中rename＋全metadata更新** name変更、value 6→9、replacements 1/3→7/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 全引数欠落mask 7中に中央ordinary enumのrename・値・documentationと両端object-like macroのreplacementを同時更新し、復元した定義だけが現在sourceの名前とmetadataを公開する境界を固定する。comment variantは旧全定義→旧全欠落→更新全欠落→mask 6→4→0、CRLF variantは同じrevisionをmask 3→1→0で復元し、最後に旧sourceへ戻す。全欠落中は第1macro名だけの1件のE3066をcallee invocation rangeへ返し、第1macro復元後はrename済み中央名へ診断を移す。中央enum復元後は新名候補だけが値9・更新documentationと現在offsetのrangeを持ち、両端macroは復元後だけreplacement 7/11を持つ。更新全復元時は派生値127、旧source復帰時は旧名・値6・旧documentation・replacement 1/3・派生値110へ戻る。Native/Wasm共有instance、fresh Native snapshot、復帰snapshotを完全一致させ、深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全欠落・全定義atomic往復回復** old variant 4/5 ↔ updated variant 10/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央enumのrename・値・documentationと両端macro replacementを同時更新した全欠落sourceから、3定義を一度に復元・再削除しても、共有instanceが全定義snapshotと全欠落snapshotを現在revisionだけから再構築する境界を固定する。comment/CRLF両variantで旧全定義→旧全欠落→更新全欠落→更新全定義のatomic復元→更新全欠落のatomic再削除→旧全欠落→旧全定義と往復する。更新全定義は新名・値9・更新documentation/range・replacement 7/11・派生値127、全欠落は3候補と派生値を除去した第1macroだけのE3066、旧全定義は旧名・値6・旧documentation/range・replacement 1/3・派生値110へ戻る。同じvariant/mask/cursorのWasm完全snapshot、revision横断の全欠落snapshot、fresh Native snapshotを比較し、Native/Wasm差とstale状態を検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのfull-source revision直接往復** old variant 4/5 ↔ updated variant 10/11 | 🧪 | test_language_analysis (native/Wasm JS API) | 欠落状態やE3066を介さず、中央ordinary enumの名前・値・documentationと両端object-like macroのreplacementがすべて異なる全定義sourceを直接往復しても、共有instanceが現在revisionだけの完全snapshotを返す境界を固定する。comment variantは4→10→4→10、CRLF variantは5→11→5→11と切り替える。旧revisionは旧中央名・値6・旧documentation/range・replacement 1/3・派生値110、更新revisionは新中央名・値9・更新documentation/range・replacement 7/11・派生値127を公開し、非アクティブな中央名とmetadataを除去して診断0件を維持する。同じvariant/mask/cursorのWasm完全snapshotとfresh Native snapshotを比較し、候補・hover・dependency・declaration/documentation rangeを含むNative/Wasm差とstale状態を検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout full revision往復** old comment/CRLF ↔ updated CRLF/comment | 🧪 | test_language_analysis (native/Wasm JS API) | 欠落状態を挟まず、comment配置の旧全定義sourceとCRLF line splice配置の更新全定義source、またはCRLF配置の旧sourceとcomment配置の更新sourceを直接往復し、layoutと中央enumのrename・値・documentation、両端macro replacementを同時変更しても現在sourceの完全snapshotへ追随する境界を固定する。variant 4→11→4→11と5→10→5→10を対称に実行する。旧revisionは旧中央名・値6・旧documentation・replacement 1/3・派生値110、更新revisionは新中央名・値9・更新documentation・replacement 7/11・派生値127を公開し、診断0件と非アクティブ名の除去を維持する。callee、3引数、comma、commentまたはCRLF splice cursorごとに現在offsetのdeclaration/documentation rangeとhoverを検査し、同じvariant/mask/cursorのWasm完全snapshotとfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout全欠落・atomic復元** old full → updated all-missing → updated full → old full | 🧪 | test_language_analysis (native/Wasm JS API) | 旧全定義sourceからlayoutと全metadata revisionを同時変更した更新全欠落sourceへ直接切り替え、3定義をatomic復元してから旧全定義へ戻しても、共有instanceが各状態を現在sourceだけから再構築する境界を固定する。旧comment variant 4→更新CRLF variant 11/mask 7→11/mask 0→4、旧CRLF variant 5→更新comment variant 10/mask 7→10/mask 0→5を対称に実行する。更新全欠落は3候補・hover・派生値を除去し、第1macroだけの1件のE3066をcallee invocation rangeへ返す。更新fullは新中央名・値9・更新documentation/range・replacement 7/11・派生値127、旧fullは旧中央名・値6・旧documentation/range・replacement 1/3・派生値110へ戻る。同じvariant/mask/cursorのWasm完全snapshot、同layoutのrevision横断全欠落snapshot、fresh Native snapshotを比較し、現在layoutのcommentまたはCRLF splice cursor/rangeを検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのreverse cross-layout全欠落・atomic復元** updated full → old all-missing → old full → updated full | 🧪 | test_language_analysis (native/Wasm JS API) | 更新全定義sourceからlayoutと全metadata revisionを同時に旧側へ戻した全欠落sourceへ直接切り替え、旧3定義をatomic復元してから更新全定義へ復帰しても、共有instanceが各状態を現在sourceだけから再構築する境界を固定する。更新comment variant 10→旧CRLF variant 5/mask 7→5/mask 0→10、更新CRLF variant 11→旧comment variant 4/mask 7→4/mask 0→11を対称に実行する。旧全欠落は3候補・hover・派生値を除去し、第1macroだけの1件のE3066を現在layoutのcallee invocation rangeへ返す。旧fullは旧中央名・値6・旧documentation/range・replacement 1/3・派生値110、更新full復帰時は新中央名・値9・更新documentation/range・replacement 7/11・派生値127へ戻る。同じvariant/mask/cursorのWasm完全snapshotとfresh Native snapshotを比較し、commentまたはCRLF splice cursor/rangeを検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout all-missing直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 7 | 🧪 | test_language_analysis (native/Wasm JS API) | 全定義sourceを挟まず、旧・更新revisionの全欠落sourceだけをlayoutも変えながら直接往復して、共有instanceの診断・候補除去・hover・rangeが現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 7→更新CRLF variant 11/mask 7→4/mask 7→11/mask 7、旧CRLF variant 5/mask 7→更新comment variant 10/mask 7→5/mask 7→10/mask 7を対称に実行する。各状態で3引数候補・hover・派生enumeratorを除去し、第1macro名だけの1件のE3066を現在layoutのcallee invocation rangeへ返す。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshot、更新variantでは同layoutのrevision横断全欠落snapshot、各keyのfresh Native snapshotを比較する。名前・metadataは欠落中に公開せず、旧／更新revisionやlayout由来のstale状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout first-only直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 6 | 🧪 | test_language_analysis (native/Wasm JS API) | 第1macroだけを残し、旧・更新revisionをlayoutも変えながら直接往復して、定義済みmacro metadataと欠落中の中央enum診断が現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 6→更新CRLF variant 11/mask 6→4/mask 6→11/mask 6、旧CRLF variant 5/mask 6→更新comment variant 10/mask 6→5/mask 6→10/mask 6を対称に実行する。第1macroは旧replacement 1または更新replacement 7と現在offsetのdeclaration/documentation rangeを公開し、中央・末尾候補、hover、派生enumeratorは除去する。1件のE3066は現在revisionの旧／rename済み中央enum名だけを現在layoutのcallee invocation rangeへ返し、非アクティブな中央名や後続の末尾macro名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshotと各keyのfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout middle-only直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 5 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央enumだけを残し、旧・更新revisionをlayoutも変えながら直接往復して、存続enum metadataと欠落中の第1macro診断が現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 5→更新CRLF variant 11/mask 5→4/mask 5→11/mask 5、旧CRLF variant 5/mask 5→更新comment variant 10/mask 5→5/mask 5→10/mask 5を対称に実行する。中央enumは旧名・値6・旧documentationまたはrename済み名・値9・更新documentationと現在offsetのdeclaration/documentation rangeを公開し、そのhoverも現在declarationへ追随する。第1・末尾macro候補とそれらのhover、派生enumeratorは除去する。1件のE3066は第1macro名だけを現在layoutのcallee invocation rangeへ返し、存続中・非アクティブな中央enum名や後続の末尾macro名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshotと各keyのfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout last-only直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 3 | 🧪 | test_language_analysis (native/Wasm JS API) | 末尾macroだけを残し、旧・更新revisionをlayoutも変えながら直接往復して、存続macro metadataと欠落中の第1macro診断が現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 3→更新CRLF variant 11/mask 3→4/mask 3→11/mask 3、旧CRLF variant 5/mask 3→更新comment variant 10/mask 3→5/mask 3→10/mask 3を対称に実行する。末尾macroは旧replacement 3または更新replacement 11と現在offsetのdeclaration/documentation rangeを公開し、そのhoverも現在declarationへ追随する。第1macro・中央enum候補とそれらのhover、派生enumeratorは除去する。1件のE3066は第1macro名だけを現在layoutのcallee invocation rangeへ返し、欠落中・非アクティブな中央enum名や存続中の末尾macro名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshotと各keyのfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout last-missing直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 4 | 🧪 | test_language_analysis (native/Wasm JS API) | 末尾macroだけを欠落させ、旧・更新revisionをlayoutも変えながら直接往復して、存続する第1macro・中央enum metadataと末尾macro診断が現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 4→更新CRLF variant 11/mask 4→4/mask 4→11/mask 4、旧CRLF variant 5/mask 4→更新comment variant 10/mask 4→5/mask 4→10/mask 4を対称に実行する。旧revisionは第1macro replacement 1、旧中央名・値6・旧documentation、更新revisionは第1macro replacement 7、rename済み中央名・値9・更新documentationと現在offsetのdeclaration/documentation rangeを公開し、両者のhoverも現在declarationへ追随する。末尾macro候補・hoverと派生enumeratorは除去する。1件のE3066は末尾macro名だけを現在layoutのcallee invocation rangeへ返し、存続する第1macro・現在／非アクティブな中央enum名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshotと各keyのfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout middle-missing直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 2 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央enumだけを欠落させ、旧・更新revisionをlayoutも変えながら直接往復して、存続する第1・末尾macro metadataと中央enum診断が現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 2→更新CRLF variant 11/mask 2→4/mask 2→11/mask 2、旧CRLF variant 5/mask 2→更新comment variant 10/mask 2→5/mask 2→10/mask 2を対称に実行する。旧revisionは第1・末尾macro replacement 1/3、更新revisionはreplacement 7/11と現在offsetのdeclaration/documentation rangeを公開し、両者のhoverも現在declarationへ追随する。中央enum候補・hoverと派生enumeratorは除去する。1件のE3066は現在revisionの旧／rename済み中央enum名だけを現在layoutのcallee invocation rangeへ返し、存続する第1・末尾macro名や非アクティブな中央enum名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshotと各keyのfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout first-missing直接往復** old comment/CRLF ↔ updated CRLF/comment、mask 1 | 🧪 | test_language_analysis (native/Wasm JS API) | 第1macroだけを欠落させ、旧・更新revisionをlayoutも変えながら直接往復して、存続する中央enum・末尾macro metadataと第1macro診断が現在sourceへ復帰する境界を固定する。旧comment variant 4/mask 1→更新CRLF variant 11/mask 1→4/mask 1→11/mask 1、旧CRLF variant 5/mask 1→更新comment variant 10/mask 1→5/mask 1→10/mask 1を対称に実行する。旧revisionは旧中央名・値6・旧documentationと末尾macro replacement 3、更新revisionはrename済み中央名・値9・更新documentationと末尾macro replacement 11を現在offsetのdeclaration/documentation range付きで公開し、両者のhoverも現在declarationへ追随する。第1macro候補・hoverと派生enumeratorは除去する。1件のE3066は第1macro名だけを現在layoutのcallee invocation rangeへ返し、存続する現在／非アクティブな中央enum名や末尾macro名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、同じvariant/mask/cursorのWasm完全snapshotと各keyのfresh Native snapshotを比較する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout単独欠落mask回転** mask 1 → 2 → 4 | 🧪 | test_language_analysis (native/Wasm JS API) | revision・layout・単独欠落位置を毎回同時に変更しても、共有instanceが現在sourceの診断対象と存続metadataへ追随する境界を固定する。旧comment variant 4/mask 1→更新CRLF variant 11/mask 2→4/mask 4→11/mask 1→4/mask 2→11/mask 4→4/mask 1、旧CRLF variant 5と更新comment variant 10でも対称に実行する。E3066は第1macro→rename済み／旧中央enum→末尾macroへ現在layoutのcallee invocation range付きで切り替わり、messageは診断対象だけを含む。各状態で存続するmacro replacement、中央enum名・値・documentation、declaration/documentation rangeとhoverを現在revisionから再構築し、欠落候補・hoverと派生enumeratorを除去する。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotと同じvariant/mask/cursorの初回Wasm完全snapshotへ再訪結果を一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout単独欠落mask逆回転** mask 4 → 2 → 1 | 🧪 | test_language_analysis (native/Wasm JS API) | revision・layout・単独欠落位置を毎回同時に変更する逆順でも、共有instanceが現在sourceの診断対象と存続metadataへ追随する境界を固定する。旧comment variant 4/mask 4→更新CRLF variant 11/mask 2→4/mask 1→11/mask 4→4/mask 2→11/mask 1→4/mask 4、旧CRLF variant 5と更新comment variant 10でも対称に実行する。E3066は末尾macro→rename済み／旧中央enum→第1macroへ現在layoutのcallee invocation range付きで切り替わり、messageは診断対象だけを含む。各状態で存続するmacro replacement、中央enum名・値・documentation、declaration/documentation rangeとhoverを現在revisionから再構築し、欠落候補・hoverと派生enumeratorを除去する。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへ再訪結果を一致させる。順回転と同mask直接往復を合わせ、単独欠落mask間の全9遷移を両方向で検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout二重欠落mask回転** mask 3 → 6 → 5 | 🧪 | test_language_analysis (native/Wasm JS API) | revision・layout・二重欠落位置を毎回同時に変更しても、共有instanceが現在sourceの唯一の存続operandと診断対象へ追随する境界を固定する。旧comment variant 4/mask 3→更新CRLF variant 11/mask 6→4/mask 5→11/mask 3→4/mask 6→11/mask 5→4/mask 3、旧CRLF variant 5と更新comment variant 10でも対称に実行する。唯一の存続operandは末尾macro→第1macro→旧／rename済み中央enumへ切り替わり、replacementまたはenum名・値・documentation、declaration/documentation rangeとhoverを現在revisionから再構築する。欠落2候補・hoverと派生enumeratorを除去する。1件のE3066はmask 3/5で第1macro、mask 6で現在revisionの中央enumだけを現在layoutのcallee invocation rangeへ返し、後続欠落名、唯一の存続名、非アクティブな中央enum名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへ再訪結果を一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout二重欠落mask逆回転** mask 5 → 6 → 3 | 🧪 | test_language_analysis (native/Wasm JS API) | revision・layout・二重欠落位置を毎回同時に変更する逆順でも、共有instanceが現在sourceの唯一の存続operandと診断対象へ追随する境界を固定する。旧comment variant 4/mask 5→更新CRLF variant 11/mask 6→4/mask 3→11/mask 5→4/mask 6→11/mask 3→4/mask 5、旧CRLF variant 5と更新comment variant 10でも対称に実行する。唯一の存続operandは旧／rename済み中央enum→第1macro→末尾macroへ切り替わり、replacementまたはenum名・値・documentation、declaration/documentation rangeとhoverを現在revisionから再構築する。欠落2候補・hoverと派生enumeratorを除去する。1件のE3066はmask 5/3で第1macro、mask 6で現在revisionの中央enumだけを現在layoutのcallee invocation rangeへ返し、後続欠落名、唯一の存続名、非アクティブな中央enum名をmessageへ混入させない。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへ逆順再訪結果を一致させる。順回転と同mask直接往復を合わせ、二重欠落mask間の全9遷移を両方向で検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout 3-bit欠落mask Gray cycle** 0 → 1 → 3 → 2 → 6 → 7 → 5 → 4 → 0 | 🧪 | test_language_analysis (native/Wasm JS API) | 欠落maskを1bitずつ変更しながらrevision・layoutも切り替え、全8 maskを一巡して完全状態へ戻る共有instance境界を固定する。旧comment variant 4/mask 0→更新CRLF variant 11/mask 1→4/mask 3→11/mask 2→4/mask 6→11/mask 7→4/mask 5→11/mask 4→4/mask 0、旧CRLF variant 5と更新comment variant 10でも対称に実行する。各stepで現在sourceの存続macro replacement、中央enum名・値・documentation、declaration/documentation rangeとhoverだけを公開し、欠落候補・hoverを除去する。派生enumeratorはmask 0で現在revisionの値へ復帰し、mask 1〜7では除去する。非zero maskの1件のE3066は最初の欠落operandだけを現在layoutのcallee invocation rangeへ返し、後続欠落名、存続名、非アクティブな中央enum名をmessageへ混入させず、mask 0では診断を除去する。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへcycle再訪結果を一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout 3-bit欠落mask逆Gray cycle** 0 → 4 → 5 → 7 → 6 → 2 → 3 → 1 → 0 | 🧪 | test_language_analysis (native/Wasm JS API) | 同じ3-bit Gray cycleを逆向きに辿り、全8 maskを1bit差で一巡して完全状態へ戻る共有instance境界を固定する。旧comment variant 4/mask 0→更新CRLF variant 11/mask 4→4/mask 5→11/mask 7→4/mask 6→11/mask 2→4/mask 3→11/mask 1→4/mask 0、旧CRLF variant 5と更新comment variant 10でも対称に実行する。各stepで現在sourceの存続macro replacement、中央enum名・値・documentation、declaration/documentation rangeとhoverだけを公開し、欠落候補・hoverを除去する。派生enumeratorはmask 0で現在revisionの値へ復帰し、mask 1〜7では除去する。非zero maskの1件のE3066は最初の欠落operandだけを現在layoutのcallee invocation rangeへ返し、後続欠落名、存続名、非アクティブな中央enum名をmessageへ混入させず、mask 0では診断を除去する。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへ逆cycle再訪結果を一致させ、同じ8本のedgeを両方向で検査する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout 3-bit欠落mask phase-swapped Gray cycle** 1 → 3 → 2 → 6 → 7 → 5 → 4 → 0 → 1 | 🧪 | test_language_analysis (native/Wasm JS API) | Gray cycleのrevision/layout位相を入れ替え、全8 maskを続く1bit差で一巡して開始状態へ戻る共有instance境界を固定する。旧comment variant 4/mask 1→更新CRLF variant 11/mask 3→4/mask 2→11/mask 6→4/mask 7→11/mask 5→4/mask 4→11/mask 0→4/mask 1、旧CRLF variant 5と更新comment variant 10でも対称に実行する。これにより旧側でmask 1/2/4/7、更新側でmask 0/3/5/6を通し、先行cycleとは反対側のrevision/layoutへ各maskを配置する。各stepで現在sourceの存続macro replacement、中央enum名・値・documentation、declaration/documentation rangeとhoverだけを公開し、欠落候補・hoverを除去する。派生enumeratorはmask 0で現在revisionの値へ復帰し、mask 1〜7では除去する。非zero maskの1件のE3066は最初の欠落operandだけを現在layoutのcallee invocation rangeへ返し、後続欠落名、存続名、非アクティブな中央enum名をmessageへ混入させず、mask 0では診断を除去する。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへphase-swapped cycle再訪結果を一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcross-layout 3-bit欠落mask phase-swapped逆Gray cycle** 1 → 0 → 4 → 5 → 7 → 6 → 2 → 3 → 1 | 🧪 | test_language_analysis (native/Wasm JS API) | phase-swapped Gray cycleを逆向きに辿り、全8 maskを続く1bit差で一巡して開始状態へ戻る共有instance境界を固定する。旧comment variant 4/mask 1→更新CRLF variant 11/mask 0→4/mask 4→11/mask 5→4/mask 7→11/mask 6→4/mask 2→11/mask 3→4/mask 1、旧CRLF variant 5と更新comment variant 10でも対称に実行する。各maskを先行の非phase-swapped cycleとは反対側のrevision/layoutへ配置したまま、phase-swapped cycleの同じ8本のedgeを両方向で通す。各stepで現在sourceの存続macro replacement、中央enum名・値・documentation、declaration/documentation rangeとhoverだけを公開し、欠落候補・hoverを除去する。派生enumeratorはmask 0で現在revisionの値へ復帰し、mask 1〜7では除去する。非zero maskの1件のE3066は最初の欠落operandだけを現在layoutのcallee invocation rangeへ返し、後続欠落名、存続名、非アクティブな中央enum名をmessageへ混入させず、mask 0では診断を除去する。callee、3引数、comma、commentまたは両CRLF splice cursorを検査し、各keyの既存fresh Native snapshotおよびWasm完全snapshotへ逆cycle再訪結果を一致させる。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのcallee header metadata revision復帰** | 🧪 | test_language_analysis (native/Wasm JS API) | 旧headerのparameter `first/middle/last`・定数項`+100`・documentationと、更新headerの`left/center/right`・`+200`・更新documentationを、旧comment variant 4→更新CRLF variant 11→4→11→4および旧CRLF variant 5→更新comment variant 10→5→10→5で交互に切り替える。同じ遷移を完全callのmask 0と中央operand欠落のmask 2で実行し、calleeのfunction-like/variadic/parameter/replacement、header側declaration/documentation range、documentation、dependency、hoverを現在headerから再構築する。mask 0では派生enumeratorを旧headerの値`110/127`または更新headerの値`210/227`へ切り替え、mask 2では派生を除去して現在sourceの中央operandだけを示す1件のE3066とcallee invocation rangeを維持する。snapshot keyとNative parity CLIへheader revisionを追加し、各keyのfresh Native JSONとWasm完全snapshotを一致させた後、再訪時に旧header metadata・値・rangeが残らず復帰することを確認する。従来のrevision省略CLIは旧headerとして互換維持する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callのbyte-identical全欠落sourceに対するcallee header-only復帰** | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask 7によってsource bytesが同一になるcomment variant 6/10とCRLF variant 7/11で、headerだけを0→1→1→0→0と切り替える。commentは6/header 0→10/header 1→6/header 1→10/header 0→6/header 0、CRLFは7/header 0→11/header 1→7/header 1→11/header 0→7/header 0を同一instanceで実行する。calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰し、派生enumeratorは全stepで除去する。1件のE3066は最初の欠落operandだけを同一sourceのcallee invocation rangeへ返し、後続欠落名と非アクティブな中央enum名をmessageへ混入させない。Wasmのall-missing revision snapshot keyにもheader revisionを含め、異なるheader結果を分離しつつ同一header内のbyte-identical variant結果、各keyのfresh Native JSON、再訪Wasm完全snapshotを一致させ、header-only cache invalidationを固定する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの固定完全sourceに対するcallee header-only semantic復帰** | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callのsource variant 6・7・10・11をそれぞれ固定したまま、callee headerだけを0→1→0で往復する。comment variant 6/10とCRLF variant 7/11の各source bytesをstep間で変えず、calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する。全operand候補と派生enumeratorを維持し、診断は全stepで除去する。派生値はvariant 6/7で`110→210→110`、variant 10/11で`127→227→127`へ切り替え、header replacementの`+100↔+200`だけによるsemantic invalidationを固定する。header revisionを含む各keyのfresh Native JSON、再訪Wasm完全snapshotを一致させ、sourceが不変でも旧header metadata・range・派生値が残留しないことを確認する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの固定完全sourceに対するcallee metadata-only復帰とsemantic不変** | 🧪 | test_language_analysis (native/Wasm JS API) | 第3header revisionとしてparameter `lhs/mid/rhs`、metadata-only documentationと対応するreplacement表記を追加する一方、定数項は旧headerと同じ`+100`に保つ。完全callのcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、header 0→2→0で往復する。calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverは現在headerへ更新・復帰するが、全operand候補、診断除去、派生enumerator値はvariant 6/7で`110→110→110`、variant 10/11で`127→127→127`を維持する。Native CLIのrevision上限とNative bundle数をheader配列長から導き、revision 2を含む各keyのfresh Native JSON、再訪Wasm完全snapshotを一致させ、metadata-only invalidationとsemantic不変を分離して固定する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの中央operand欠落固定sourceに対するcallee metadata-only復帰と診断semantic不変** | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、第3header revisionを0→2→0で往復する。calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverは現在headerへ更新・復帰する一方、外側2 operandの候補を維持し、中央operand `ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`の候補と派生enumeratorは全stepで除去する。E3066は全stepで1件とし、messageは現在sourceの中央operandだけを示して外側operand名と非アクティブな旧中央enum名を含めず、rangeは同一sourceのcallee invocationを維持する。revision 2を含む各keyのfresh Native JSON、再訪Wasm完全snapshotを一致させ、metadata-only invalidation中の診断semantic不変を固定する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの両端単一operand欠落固定sourceに対するcallee metadata-only復帰と診断semantic不変** | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`を、comment variant 6/10、CRLF variant 7/11の各固定sourceで第3header revision 0→2→0へ往復する。calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverは現在headerへ更新・復帰する一方、それぞれ残存する2 operand候補を維持し、欠落operand候補と派生enumeratorは全stepで除去する。E3066は全stepで1件とし、mask 1では`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 4では`ENUM_THREE_ARGUMENT_LAST_MACRO`だけをmessageへ含め、残存operand名と非アクティブな旧中央enum名を含めず、rangeは同一sourceのcallee invocationを維持する。revision 2を含む各keyのfresh Native JSON、再訪Wasm完全snapshotを一致させ、metadata-only invalidation中のprefix/suffix診断semantic不変を固定する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの2 operand欠落固定sourceに対するcallee metadata-only復帰と診断順序不変** | 🧪 | test_language_analysis (native/Wasm JS API) | 2 operand欠落mask `3`・`5`・`6`を、comment variant 6/10、CRLF variant 7/11の各固定sourceで第3header revision 0→2→0へ往復する。calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverは現在headerへ更新・復帰する一方、各maskの残存1 operand候補を維持し、欠落2 operand候補と派生enumeratorは全stepで除去する。E3066は全stepで1件とし、mask 3/5では最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 6では最初の`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけをmessageへ含め、後続欠落operand名・残存operand名・非アクティブな旧中央enum名を含めず、rangeは同一sourceのcallee invocationを維持する。revision 2を含む各keyのfresh Native JSON、再訪Wasm完全snapshotを一致させ、metadata-only invalidation中の複数欠落診断順序とsemantic不変を固定する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全operand欠落固定sourceに対するcallee metadata-only復帰とbyte-identical結果不変** | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`を、comment variant 6/10、CRLF variant 7/11の各固定sourceで第3header revision 0→2→0へ往復する。calleeのparameter・replacement・documentation・header側declaration/documentation range、dependency、hoverは現在headerへ更新・復帰する一方、全operand候補と派生enumeratorは全stepで除去する。E3066は全stepで1件とし、最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`だけをmessageへ含め、後続2 operand名と非アクティブな旧中央enum名を含めず、rangeは同一sourceのcallee invocationを維持する。revision 2を含む各keyのfresh Native JSON、header 0再訪Wasm完全snapshotを一致させ、all-missing専用keyでvariant 6/10および7/11のbyte-identical source結果もheader revisionごとに一致させる。これにより固定variant 6/7/10/11のmissing mask `0`〜`7`をmetadata-only往復で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの固定完全sourceに対する非基底callee header連続遷移** 0→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callのcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+100`へ切り替え、header 1から2へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新し、全operand候補と診断除去を維持する。派生値はvariant 6/7で`110→210→110→110`、variant 10/11で`127→227→127→127`とし、semantic変更後のmetadata-only変更を分離して検査する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision間のstale metadata・range・派生値を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの中央operand欠落固定sourceに対する非基底callee header連続遷移** 0→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+100`へ切り替え、header 1から2へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、外側2 operandの候補・metadata・range・hoverを維持し、中央operand候補と派生enumeratorは全stepで除去する。E3066は現在sourceの`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、外側operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision間で診断semanticやoperand状態を変えない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの両端単一operand欠落固定sourceに対する非基底callee header連続遷移** 0→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+100`へ切り替え、header 1から2へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る2 operandの候補・metadata・range・hoverを維持し、欠落operand候補と派生enumeratorは全stepで除去する。E3066はmask 1では`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 4では`ENUM_THREE_ARGUMENT_LAST_MACRO`だけを示す1件とし、残存operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision間で診断semanticやoperand状態を変えない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの2 operand欠落固定sourceに対する非基底callee header連続遷移** 0→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 2 operand欠落mask `3`・`5`・`6`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+100`へ切り替え、header 1から2へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る1 operandの候補・metadata・range・hoverを維持し、欠落2 operand候補と派生enumeratorは全stepで除去する。E3066はmask 3/5では最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 6では最初の`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、後続欠落名・残存名・非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision間で診断順序やoperand状態を変えない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全operand欠落固定sourceに対する非基底callee header連続遷移** 0→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+100`へ切り替え、header 1から2へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、全operand候補・hoverと派生enumeratorは全stepで除去する。E3066は最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`だけを示す1件とし、後続2 operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、all-missing専用keyでvariant 6/10および7/11のbyte-identical source結果もheader revisionごとに一致させる。これにより固定variant 6/7/10/11のmissing mask `0`〜`7`を非基底revision 1→2で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの固定完全sourceに対する逆非基底callee header連続遷移** 0→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callのcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`へ切り替え、header 2から1へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰し、全operand候補と診断除去を維持する。派生値はvariant 6/7で`110→110→210→110`、variant 10/11で`127→127→227→127`とし、metadata-only変更後のsemantic変更を分離して検査する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision 2→1間のstale metadata・range・派生値を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの中央operand欠落固定sourceに対する逆非基底callee header連続遷移** 0→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`へ切り替え、header 2から1へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、外側2 operandの候補・metadata・range・hoverを維持し、中央operand候補と派生enumeratorは全stepで除去する。E3066は現在sourceの`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、外側operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision 2→1間で診断semanticやoperand状態を変えない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの両端単一operand欠落固定sourceに対する逆非基底callee header連続遷移** 0→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`へ切り替え、header 2から1へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る2 operandの候補・metadata・range・hoverを維持し、欠落operand候補と派生enumeratorは全stepで除去する。E3066はmask 1では`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 4では`ENUM_THREE_ARGUMENT_LAST_MACRO`だけを示す1件とし、残存operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision 2→1間で診断semanticやoperand状態を変えない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの2 operand欠落固定sourceに対する逆非基底callee header連続遷移** 0→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 2 operand欠落mask `3`・`5`・`6`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`へ切り替え、header 2から1へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る1 operandの候補・metadata・range・hoverを維持し、欠落2 operand候補と派生enumeratorは全stepで除去する。E3066はmask 3/5では最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 6では最初の`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、後続欠落名・残存名・非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、非基底revision 2→1間で診断順序やoperand状態を変えない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全operand欠落固定sourceに対する逆非基底callee header連続遷移** 0→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`へ切り替え、header 2から1へ基底revisionを挟まず直接遷移する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、全operand候補・hoverと派生enumeratorは全stepで除去する。E3066は最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`だけを示す1件とし、後続2 operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、all-missing専用keyでvariant 6/10および7/11のbyte-identical source結果もheader revisionごとに一致させる。これにより固定variant 6/7/10/11のmissing mask `0`〜`7`を非基底revision 2→1で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの固定完全sourceに対する非基底callee header往復再訪** 0→2→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callのcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→2→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`→`+100`へ切り替え、header 2→1→2を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰し、全operand候補・metadata・range・hoverと診断除去を維持する。派生値はvariant 6/7で`110→110→210→110→110`、variant 10/11で`127→127→227→127→127`とする。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 2再入場時にrevision 1由来のstale metadata・range・派生値を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの中央operand欠落固定sourceに対する非基底callee header往復再訪** 0→2→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→2→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`→`+100`へ切り替え、header 2→1→2を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、外側2 operandの候補・metadata・range・hoverを維持し、中央operand候補と派生enumeratorは全stepで除去する。E3066は現在sourceの`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、外側operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 2再入場時にrevision 1由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの両端単一operand欠落固定sourceに対する非基底callee header往復再訪** 0→2→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→2→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`→`+100`へ切り替え、header 2→1→2を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る2 operandの候補・metadata・range・hoverを維持し、欠落operand候補と派生enumeratorは全stepで除去する。E3066はmask 1では`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 4では`ENUM_THREE_ARGUMENT_LAST_MACRO`だけを示す1件とし、残存operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 2再入場時にrevision 1由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの2 operand欠落固定sourceに対する非基底callee header往復再訪** 0→2→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 2 operand欠落mask `3`・`5`・`6`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→2→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`→`+100`へ切り替え、header 2→1→2を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る1 operandの候補・metadata・range・hoverを維持し、欠落2 operand候補と派生enumeratorは全stepで除去する。E3066はmask 3/5では最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 6では最初の`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、後続欠落名・残存名・非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 2再入場時にrevision 1由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全operand欠落固定sourceに対する非基底callee header往復再訪** 0→2→1→2→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→2→1→2→0と連続遷移する。parameterは`first/middle/last`→`lhs/mid/rhs`→`left/center/right`→`lhs/mid/rhs`→`first/middle/last`、replacement定数項は`+100`→`+100`→`+200`→`+100`→`+100`へ切り替え、header 2→1→2を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、全operand候補・hoverと派生enumeratorは全stepで除去する。E3066は最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`だけを示す1件とし、後続2 operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、all-missing専用keyでvariant 6/10および7/11のbyte-identical source結果もheader revisionごとに一致させる。これにより固定variant 6/7/10/11のmissing mask `0`〜`7`を非基底revision 2→1→2で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの固定完全sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callのcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰し、全operand候補・metadata・range・hoverと診断除去を維持する。派生値はvariant 6/7で`110→210→110→210→110`、variant 10/11で`127→227→127→227→127`とする。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・派生値を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの中央operand欠落固定sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、外側2 operandの候補・metadata・range・hoverを維持し、中央operand候補と派生enumeratorは全stepで除去する。E3066は現在sourceの`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、外側operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの両端単一operand欠落固定sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る2 operandの候補・metadata・range・hoverを維持し、欠落operand候補と派生enumeratorは全stepで除去する。E3066はmask 1では`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 4では`ENUM_THREE_ARGUMENT_LAST_MACRO`だけを示す1件とし、残存operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの2 operand欠落固定sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 2 operand欠落mask `3`・`5`・`6`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る1 operandの候補・metadata・range・hoverを維持し、欠落2 operand候補と派生enumeratorは全stepで除去する。E3066はmask 3/5では最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 6では最初の`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、後続欠落名・残存名・非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの全operand欠落固定sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`のcomment variant 6/10、CRLF variant 7/11をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、全operand候補・hoverと派生enumeratorは全stepで除去する。E3066は最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`だけを示す1件とし、後続2 operand名と非アクティブな旧中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、all-missing専用keyでvariant 6/10および7/11のbyte-identical source結果もheader revisionごとに一致させる。これにより固定variant 6/7/10/11のmissing mask `0`〜`7`を非基底revision 1→2→1で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの残存中央enum世代完全sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callの旧中央enum variant 4/5と更新中央enum variant 8/9をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰し、全operand候補・metadata・range・hoverと診断除去を維持する。派生値はvariant 4/5で`110→210→110→210→110`、variant 8/9で`113→213→113→213→113`とする。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・派生値を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの残存中央enum世代・中央operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`の旧中央enum variant 4/5と更新中央enum variant 8/9をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、外側2 operandの候補・metadata・range・hoverを維持し、中央operand候補と派生enumeratorは全stepで除去する。E3066はvariant 4/5では`ENUM_THREE_ARGUMENT_MIDDLE_ENUM`、variant 8/9では`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、外側operand名と非アクティブな中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの残存中央enum世代・両端単一operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`の旧中央enum variant 4/5と更新中央enum variant 8/9をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る2 operandの候補・metadata・range・hoverを維持し、欠落operand候補と派生enumeratorは全stepで除去する。E3066はmask 1では`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 4では`ENUM_THREE_ARGUMENT_LAST_MACRO`だけを示す1件とし、残存operand名と非アクティブな中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの残存中央enum世代・2 operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 2 operand欠落mask `3`・`5`・`6`の旧中央enum variant 4/5と更新中央enum variant 8/9をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る1 operandの候補・metadata・range・hoverを維持し、欠落2 operand候補と派生enumeratorは全stepで除去する。E3066はmask 3/5では最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`、mask 6ではvariant 4/5の`ENUM_THREE_ARGUMENT_MIDDLE_ENUM`またはvariant 8/9の`ENUM_THREE_ARGUMENT_RENAMED_MIDDLE_ENUM`だけを示す1件とし、後続欠落名・残存名・非アクティブな中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの残存中央enum世代・全operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`の旧中央enum variant 4/5と更新中央enum variant 8/9をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、全operand候補・hoverと派生enumeratorは全stepで除去する。E3066は最初の`ENUM_THREE_ARGUMENT_FIRST_MACRO`だけを示す1件とし、後続2 operand名と非アクティブな中央enum名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、variant 8/10および9/11のbyte-identical all-missing source結果もheader revisionごとに一致させる。これによりvariant 4/5/8/9のmissing mask `0`〜`7`を順非基底revision 1→2→1で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの初期macro/outer-enum世代完全sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 完全callのmacro 3 operand variant 0/1と外側enum + 中央macro variant 2/3をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰し、全operand候補・種別・metadata・range・hoverと診断除去を維持する。派生値はvariant 0/1で`106→206→106→206→106`、variant 2/3で`111→211→111→211→111`とする。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・派生値を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの初期macro/outer-enum世代・中央operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 中央operand欠落mask `2`のmacro 3 operand variant 0/1と外側enum + 中央macro variant 2/3をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、外側2 operandの候補・種別・metadata・range・hoverを維持し、中央operand候補・hoverと派生enumeratorを全stepで除去する。E3066は`ENUM_THREE_ARGUMENT_MIDDLE_MACRO`だけを示す1件とし、外側operand名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの初期macro/outer-enum世代・両端単一operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭operand欠落mask `1`と末尾operand欠落mask `4`のmacro 3 operand variant 0/1と外側enum + 中央macro variant 2/3をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、各maskで残る2 operandの候補・種別・metadata・range・hoverを維持し、欠落operand候補・hoverと派生enumeratorを全stepで除去する。E3066はvariant 0/1では該当する先頭または末尾macro、variant 2/3では該当する先頭または末尾enumだけを示す1件とし、残存operand名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの初期macro/outer-enum世代・2 operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭+中央operand欠落mask `3`と中央+末尾operand欠落mask `6`のmacro 3 operand variant 0/1と外側enum + 中央macro variant 2/3をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、mask 3では末尾、mask 6では先頭operandの候補・種別・metadata・range・hoverを維持し、欠落2 operandの候補・hoverと派生enumeratorを全stepで除去する。E3066はmask 3では先頭macro/enum、mask 6では中央macroだけを示す1件とし、後続欠落名と残存operand名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの初期macro/outer-enum世代・両外側operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 先頭+末尾operand欠落mask `5`のmacro 3 operand variant 0/1と外側enum + 中央macro variant 2/3をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。variant 2/3では共有`EnumThreeArgumentMixedValues` block全体を除去して空enumを作らない。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、中央macroの候補・metadata・range・hoverを維持し、外側2 operandの候補・hoverと派生enumeratorを全stepで除去する。E3066は先頭macro/enumだけを示す1件とし、後続欠落名と中央macro名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。深い式・巨大入力・fuzz・資源stressは追加しない |
| **3引数enum direct callの初期macro/outer-enum世代・全operand欠落sourceに対する順非基底callee header往復再訪** 0→1→2→1→0 | 🧪 | test_language_analysis (native/Wasm JS API) | 全operand欠落mask `7`のmacro 3 operand variant 0/1と外側enum + 中央macro variant 2/3をそれぞれ固定し、callee headerだけを0→1→2→1→0と連続遷移する。variant 2/3では共有`EnumThreeArgumentMixedValues` block全体を除去した後に中央macroも除去して空enumを作らない。parameterは`first/middle/last`→`left/center/right`→`lhs/mid/rhs`→`left/center/right`→`first/middle/last`、replacement定数項は`+100`→`+200`→`+100`→`+200`→`+100`へ切り替え、header 1→2→1を基底revisionを挟まず往復する。calleeのdocumentation、header側declaration/documentation range、dependency、hoverを現在headerへ更新・復帰する一方、全operand候補・hoverと派生enumeratorを全stepで除去する。E3066は先頭macro/enumだけを示す1件とし、後続2欠落名をmessageへ含めず、rangeは同一sourceのcallee invocationを維持する。各keyのfresh Native JSONと既存key再訪時のWasm完全snapshotを一致させ、revision 1再入場時にrevision 2由来のstale metadata・range・診断状態を残さない。これによりvariant 0/1/2/3のmissing mask `0`〜`7`を順非基底revision 1→2→1で被覆する。深い式・巨大入力・fuzz・資源stressは追加しない |

### リンケージ / 複数 TU（extern / static）
複数ファイルをリンクする差分ハーネス（各 .c を ag_c で .s 化→clang で個別アセンブル→
まとめてリンク、clang -I include 直ビルドと exit code 比較）で確認。
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| extern グローバル変数 int/double/struct/`arr[]`/ポインタ の TU 跨ぎ参照 | ✅ | — | 値・型とも clang と一致 |
| extern 関数の TU 跨ぎ呼び出し（int/double 戻り） | ✅ | — | |
| tentative definition `int t;` を複数 TU | ✅ | — | `.comm` で正しくマージ |
| **ファイルスコープ `static` 変数/関数の内部リンケージ** | 🔧 | static_internal_linkage | C11 6.2.2p3 違反。ag_c が `static` に `.global` を出し、無初期化 static を `.comm`（common=外部）にしていたため、同名 static を持つ別 TU と `duplicate symbol` 衝突／共有。codegen で static は `.global` 抑制＋無初期化を `.zerofill __bss`（ローカル）に。グローバル/関数/関数内 static の 3 経路。is_static フラグを global_var_t / ir_func_t / node_func_t に追加し parser から伝播 |
| file-scope const objectのexternal linkage | 🧪 | file_scope_const_external_linkage_xtu | CではC++と異なり、file-scopeの`const` objectも`static`がなければexternal linkageを持つ。別TUの`extern const`宣言からscalar/arrayの値とaddress identityを参照し、NativeとWasm object linkerが定義symbol・data relocation・const qualifierを同じobjectへ解決することを固定する |
| qualified external objectの一致受理 | 🧪 | qualified_external_objects_xtu | 別TUの宣言と定義で一致する`volatile` objectとfile-scope `restrict` pointerを同じ外部data symbolとして受理する。pointerの静的初期化data relocation、別TUからのread/write、target address identityをNativeとWasm object link/runで固定し、qualifier不一致を拒否する既存negative coverageを正方向から補完する |
| **`_Thread_local` objectのcross-TU identity / internal linkage** | ✅ | thread_local_xtu_boundaries | 定義TUのexternal TLSを別TUのfile/block-scope `extern _Thread_local`から同一objectとして読書きし、address identityと`_Alignas(16)` aggregate更新を確認する。同名file-scope `static _Thread_local`はTUごとに別objectとして保持する。NativeのTLV relocationとWasm object linkerのsymbol/data relocationを2-TU実行で固定 |
| **extern 宣言＋同一TU定義（tag/typedef 基底）** `extern struct S es; struct S es={7};` | 🔧 | extern_then_def_same_tu | C11 6.9.2。builtin (`extern int v; int v=5;`) は動作。2 つの独立バグ: (1) storage class フラグ (g_*_is_extern/static) が宣言間でリセットされず前の extern が次の bare-struct 定義に漏れ→finalize の extern 分岐が brace を scalar 式として食べ E3064。reset_toplevel_decl_spec_state と parse_toplevel_tag_decl で宣言ごとに 0 クリア。(2) typedef object 経路 (apply_toplevel_typedef_prefix_flags) が extern を無条件 0 にし `extern T et;` が tentative 定義 (.comm) になり本定義 data と重複 ASSEMBLE_FAIL→extern/static を伝播 (漏れは (1) の reset で防止)。static の漏れ (`static struct S a; struct S b;` で b が内部リンケージ化) も解消。逆順 (定義→extern)・別 TU は元から動作。tag/typedef/ポインタ/配列・複数宣言子を網羅 |
| **block-scope `extern` の同一scope衝突と字句的可視範囲** | 🔧 | block_scope_extern_visibility / should_reject / compile_fail: local_extern_after_automatic, automatic_after_local_extern, block_{extern,function}_after_{typedef,enum_constant}, block_function_after_automatic, block_extern_object_{out_of_scope,leaks_to_later_function} | `int x; extern int x;`、逆順、typedef/enum定数とのordinary namespace衝突を受理していた。さらにbackend用に作ったtranslation-unit symbolを通常の名前検索にも公開したため、`{ extern int hidden; } return hidden;`や別関数から`hidden`を参照できた。scope graphに通常lookupから隠すbacking declarationとblock内の専用linkage aliasを分離し、後続のfile-scope宣言時だけbackingを通常lookupへ公開する。9件をClang strict、Native/Wasm should_reject、Native/Wasm object compile-failのE3064/E3066/E3067診断ID parityで固定する。nested externが外側のautomatic objectを隠す合法形、block宣言後のfile-scope定義、object/function双方と同一scope再宣言を回帰確認する |
| **block-scope `extern` のinitializer・alignment再宣言境界** | 🧪 | block_scope_extern_alignment_redeclaration / should_reject 4件 / compile_fail: block_scope_extern_{initializer,alignment_conflict,alignment_missing_definition}, alignas_block_scope_function_declaration | block-scope `extern` objectはinitializerを持てず、alignment指定は同じ外部objectの定義・他宣言と一致する必要がある。initializer違反をE3064で診断した後もdeclaration pipelineが成功を返して後続のglobal登録へ進む内部不整合を即時失敗へ変更する。合法な一致alignment宣言をNative・WAT・Wasm objectで実行し、initializer、異なるalignment、定義側でのalignment欠落、block-scope functionへの`_Alignas`をClang strict同様に拒否する。 |
| **block-scope linkage aliasとordinary namespaceの宣言順・shadow境界** | 🔧 | block_function_nested_typedef_shadow, nested_ordinary_identifier_shadow_boundaries, nested_linkage_declaration_shadow_boundaries / should_reject 12件 | block-scope function/objectの`extern`宣言はtranslation-unit宣言へのscope-local linkage aliasを作る。typedef/enum resolverがalias自体のkindを未知宣言として扱い、同一scopeで後続するtypedefが通常診断なしの`E0006`へ落ち、enum constantも衝突種別を失っていた。alias先のfunction/object宣言を解決してordinary namespace衝突を分類し、direct typedef preflightはinitializer禁止とresolver失敗の両方でE3064を発行する。function・object・typedef・enum定数・parameterの同一scopeにおける12通りの逆順衝突をClang strictおよびNative/Wasm object compile-failへ登録する。一方、内側blockではautomatic object・typedef・enum定数が外側のlinkage宣言を、内側のlinkage宣言が外側のno-linkage宣言を合法に隠し、block終了後に元の宣言へ戻ることをNative・WAT・Wasm objectで固定する。 |
| **ordinary identifierの同一scope重複宣言** local object・typedef・enum constant | 🧪 | should_reject / compile_fail: dup_{local_var,typedef_conflict,enum_name} | ordinary identifier namespaceでは同一scopeのlocal object、互換でないtypedef再宣言、enum constantの重複名を受理しない。3件をClang strictと照合し、E3064/E3067のNative/Wasm object診断ID parityを固定する。異なる内側scopeでの合法なshadowは既存fixtureで維持する |
| **storage class / function specifier の適用文脈とenum完全性** | 🔧 | storage_class_constraints, function_specifier_constraints, standalone_tag_storage_class_specifiers / should_reject 37件 / compile_fail: empty_enum, incomplete_enum_{object,member} | file/block/parameter/member/typedef/type-name文脈をClang strict C11と差分比較する。`auto`/`register`のfile-scope適用、parameter/member/functionへの不正storage class、block-scope `_Thread_local`、thread-local再宣言、競合・重複storage class、standalone typedef tagとtype-name内storage classを固定する。識別子を伴わない`static/extern/auto/register/_Thread_local struct S;`はstorage classが無視される合法宣言なので、file/block scopeの10形をpositive fixtureで受理し、tag identityを後続の定義とobject利用まで検査する。名前のない`typedef struct S;`だけは拒否する。`inline`/`_Noreturn`はfunction identifier宣言だけに限定し、object、typedef、function-pointer object/parameter/member、function type-name、functionとobjectの混在宣言を拒否する。storage class/function specifier 33件をshould_reject正本からNative/Wasm object compile-failへ登録する。enumでは空の列挙子リスト、未完了enumのobject/member、定義中の`sizeof(enum E)`を受理する漏れを修正し、E3064/E3117診断parityを固定する。空enumと未完成enum object/memberの3件もshould_reject正本からNative/Wasm object compile-failへ登録する。enumのcanonical型は整数ABI表現を保ちつつ、`enum_decl_id`からtag完成状態を照会してobject確保・member配置・type queryだけを拒否する。既存c-testsuite互換の前方enumポインタ拡張は維持 |
| **external/internal linkageの再宣言制約** `extern int value; static int value;` | 🧪 | inherited_static_linkage_xtu / should_reject 7件 / compile_fail: object_{extern_then_static,static_then_plain,plain_then_static}_linkage, function_{extern_then_static,plain_then_static}_linkage, block_extern_then_file_static_linkage, thread_local_extern_then_static_linkage | 同一識別子のlinkageをexternalとinternalの間で変更する再宣言を、object/function/TLS、file-scope宣言順、block-scope `extern`から後続file-scope `static`までClang strict C11と照合する。一方、先行する`static`宣言が可視な場合、後続の明示`extern`はobject/functionとも既存のinternal linkageを継承する。別TUに同名のexternal object/functionを置き、誤ってexternalへ切り替わるとsymbol衝突または実値不一致になる正例をNative・WAT・Wasm object link/runで固定する。7件の不正例はshould_reject正本からNative/Wasm object compile-failへ登録し、E3064診断parityを固定する |
| **tentative definition・C11宣言完全性** `static int values[];`, `int invalid(int)`, implicit int | 🔧 | static_tentative_incomplete_array_completion / should_reject 7件 / compile_fail: static_tentative_incomplete_record, unresolved_tentative_incomplete_record, funcdef_unnamed_param, c11_{implicit_int_objects,implicit_return_type,block_implicit_int}, old_style_typedef_name_parameter | internal-linkageの未指定長array tentative definitionもC11 6.9.2p5によりtranslation unit終了時に1要素へ補完されるが、ag_cはexternal-linkage arrayだけを遅延しE3064で過剰拒否していた。static arrayも共通のfinalizationへ通し、未完成配列で有効なelement accessと、補完後storageのzero initialization・読書きをNative/WAT/Wasm objectで固定する。`sizeof`は使用位置ではまだ未完成型のためpositive境界に含めない。一方、internal-linkageの未完成record tentative definitionは宣言時に完全型を要求し、external-linkageの未完成recordはtranslation unit終了までに完成しなければならない。function definitionのparameterはidentifierを持ち、C11ではfile/block-scope object・function returnのimplicit intを許可しない。old-style identifier listでもvisible typedef名をparameter識別子として再解釈しない。7件をshould_reject正本からNative/Wasm object compile-failへ登録し、日本語message依存だった境界をE3064/E3065/E3037/E3088診断IDへ置き換える |
| **old-style function definitionのidentifier list・prototype互換制約** | 🧪 | old_style_function_definition_boundaries / should_reject 33件 / compile_fail: old_style_{parameter_*,typedef_name_parameter,identifier_list_declaration,nested_identifier_list,incompatible_{float,bool,complex}_prototype,array_pointee_qualifier_prototype,function_parameter_prototype,variadic_function_parameter_prototype,declaration_variadic_prototype,signed_enum_unsigned_prototype,unsigned_enum_signed_prototype,distinct_record_prototype,record_kind_prototype,atomic_int_plain_prototype,plain_int_atomic_prototype,atomic_short_int_prototype,atomic_pointer_plain_prototype}, incompatible_prototype_oldstyle_definition, oldstyle_float_prototype, upgraded_prototype_argument_count | identifier listの全parameterに一意な宣言を要求し、余分・重複・initializer・static storage・visible typedef名を拒否する。parameter宣言内で導入されたenum定数・tagは関数bodyへ漏らさず、array boundからは後続parameterや同じ宣言内の後続declaratorを参照できない。identifier listはoutermost function definitionだけで許可する。先行prototypeとはdefault argument promotion後の型・parameter数を比較し、float/_Bool/complex、array element修飾、nested callback prototype/variadicness、signednessの異なるenum、distinct record/record kind、plain/Atomic integer・pointerを区別する。33件をshould_reject正本からNative/Wasm object compile-failへ登録する |
| **array declarator / flexible array / bit-field制約境界** | 🔧 | should_reject fixture群 / should_reject: static_array_comma_bound, flexible_array_{in_union,without_prior_member,nested_struct_member,nested_union_member,pointer_to_array,parameter_array,compound_literal_array}, member_after_flexible_array, array_of_flexible_record / compile_fail: array_{bound_signed_overflow,of_function,of_void,qualifier_outside_parameter,static_bound_outside_parameter}, array_parameter_{duplicate_static,incomplete_element,nested_static,nested_qualifier,nested_volatile,nested_restrict,nested_atomic,static_without_bound,star_definition}, {extern,file_scope,static_local}_vla, extern_pointer_to_vla, vla_{star_outside_parameter,static_star}, pointer_to_{array,vla}_incomplete_element, pointer_to_array_incomplete_element_type_name, function_returning_{array,function}, atomic_bitfield_{qualifier,specifier,typedef,unnamed}, bitfield_{comma_width,pointer_type,array_type,function_type,negative_width,named_zero_width,floating_type,nonconstant_width,width_signed_overflow,derived_switch_duplicate_case}, bool_bitfield_width_exceeds_one, incomplete_enum_bitfield | arrayの要素完全性、関数return型、`static`/qualifier適用文脈、file/static/externのvariably modified type、signed overflowまたはcomma式を使うbound、flexible arrayの配置、bit-field型・幅・`sizeof`をClang strict C11と差分比較する。file-scope array boundのcomma式はICEでないためshould_reject正本から登録する。flexible arrayはstructの末尾かつ先行named memberを要求し、union、包含struct/union、配列要素、pointer-to-array、parameter array、compound literal arrayの9境界をshould_reject正本から両compile-fail経路へ登録する。前方enum拡張で未完了の`enum E`をbit-fieldの整数ABI表現だけ見て受理する漏れは、aggregate member resolutionでも`enum_decl_id`の完成状態を確認して未完了型だけを拒否する。bit-fieldは整数・`_Bool`・許可された実装型に限定し、Atomic/array/function/pointer/floating/incomplete型、非ICE・負・型幅超過・named zero幅を拒否する。Native/WasmのE3060/E3064/E3117診断parityまで固定する |
| **flexible array memberを持つ構造体の代入** `*destination = *source` | 🧪 | flex_array/assignment_copies_fixed_prefix | C11の構造体代入はflexible array memberより前の固定部分だけをコピーする。`malloc(sizeof(struct)+N*sizeof(element))`で別々に確保したsource/destinationについて、通常memberは更新される一方、destination固有の末尾要素は変化しないことをClang strictとの比較およびnative/WAT/objectで固定する |
| **flexible array memberを持つ構造体の値渡し** `struct F id(struct F value)` | 🧪 | flexible_array_value_context_boundaries | flexible array memberを含む完成struct型も、引数・return・間接call・初期化・代入式と結果値・条件式・comma式では`sizeof(struct)`の固定prefixをaggregate値として扱う。small direct objectとlarge indirect object、選択branchだけの単一評価、return結果をmalloc済みdestinationへ代入しても末尾dataを上書きしないことをClang strictおよびnative/WAT/objectで固定する |
| **over-aligned固定prefixとflexible array memberのlayout** `_Alignas(32) unsigned char tag; unsigned long long values[]` | 🧪 | overaligned_flexible_array_layout_boundaries | FAMのoffsetは直前memberとelement alignmentから決まり、record alignmentによる末尾paddingを含む`sizeof(struct)`より小さくなり得る。16/32-byte整列recordについて`_Alignof`・固定member/FAMの`offsetof`・`sizeof`を固定し、global/local/static local objectの実アドレス整列と初期値、手動整列した動的storage上でFAMがmember offsetから始まること、sub-int/64-bit payloadの読書きをClang strict・Native・WAT・Wasm objectで照合する |

---

## バグではない（仕様 / 既知の差異、追わない）
- **ag_c は標準C のみ対象。GNU 拡張は新規に実装しない。差分テストで clang が GNU 拡張として受理するものは「バグ/未対応」として扱わない。** ISO C11 strictで診断されるstatement expressionと配列範囲designatorは`gnu_{statement_expression,array_range_designator}` should-reject fixtureでNative/Wasmの拒否を固定する。
  - `, ##__VA_ARGS__`（GNU 可変長マクロのカンマ削除）— 未対応のまま。
  - statement expression `({...})`、`typeof`、範囲指定子 `[2 ... 5]=v`、二重波括弧スカラ初期化 — 標準外。
  - 認識済みの未対応 GNU 拡張は `W3024` で「このコンパイラでは使用できない」旨を警告して読み飛ばす。
    `#pragma push_macro` / `pop_macro` は意味実装せず行を skip、範囲指定子 `[lo ... hi]=v` は先頭 `lo`
    の単一 designator として処理するのみ (GCC/clang 互換の範囲 fill ではない)。ゼロ長配列 `[0]` は
    0 バイトとして処理する。
  - （既に入っている GNU 拡張 `__real__`/`__imag__` は complex.h が依存するため残置。新規は入れない。）
- 過剰初期化子 `struct S s={{1,2},{3,4}}`（メンバ1個に2グループ）等は ag_c は意図的に E3064（厳格）。
- `s07.c`（深さ 10 万の再帰）の SIGSEGV はスタックオーバーフロー（誤コンパイルではない）。
- 評価順序が未規定/UB のもの（`a[i++]=i` 等）。

## 未チェックの着手候補（⬜ を埋める）
（現時点で未着手候補なし。下記「既存バグ」も含めすべて対応済み。）

### ストリーミング前方先読み境界バグ（既存バグ・修正済み）
| 領域 | 状態 | fixture / commit | メモ |
|------|------|------------------|------|
| `_Generic` 型照合の `t->next` 先読みがストリーミング窓境界で NULL を踏む | 🔧 | generic_streaming_lookahead | `_Generic` の型照合はカーソルを進めず `t->next` で型全体を先読みする。ストリーミング生成器はカーソル前方を一定窓だけ materialize し、`set_curtok` のジャンプが補充起点 `refill_at` を飛び越えると補充がチェーン末尾到達まで起きない。複雑なグローバル funcptr/ネスト宣言 + 複数の `assert(_Generic)`（`#expr` 文字列化で大量トークン）+ 末尾の cast 制御式の深いネスト型が同居すると、窓境界が型の途中に来て先読みが未生成境界(NULL)を踏み、有効な型を誤却下し「':' が必要 (実際 '(')」E2006。**72b23c2 でも再現する既存バグ**で型シグネチャ照合とは独立。チャンク再利用は無関係(無効化しても再現)。修正: 深い前方先読みの直前に `tk_ensure_lookahead()`（プリプロセッサが登録するフック経由で窓を満たす、parser↔preprocess は疎結合のまま）を呼ぶ |

## チェック済みだが miscompile でなかった領域（再探索不要）
- compound literal: ネスト / `&(compound)` / 配列 / 関数引数 (probe cl1, cl3)。
- `static p = &(struct){..}`（非定数 static init）は clang が拒否、ag_c は受理（寛容な差異）(probe cl2)。
- 浮動小数の `printf` 出力書式・丸め（`%f`/`%g`/`%e`/`%E`、精度・幅・フラグ、`%.0f` の偶数丸め、
  `0.0`/`-0.0`/Inf/NaN、`float`→`double` 可変長昇格、int/float 混在のレジスタ/スタック境界、
  `snprintf`）を stdout 比較で確認。書式・丸めは libc 任せで ag_c は引数を正しく渡しており全て一致。

### 2026-06-22 セッションの探索（~84 probe、すべて clang 一致 = miscompile なし）
2D ポインタ配列 4 象限を修正した後の網羅探索。下記領域は stdout/exit code 比較で全 green。
**再探索不要**（バグは依然「宣言子・型の複雑な組合せ」に集中し、式・制御フロー・ABI・数値変換は堅牢）:
- **複合代入**: `%= <<= |= ^= &= += -= /=` を int/ポインタ/`unsigned char`/`long`・負値で。ポインタ複合代入 `p+=3; *p+=10`。
- **ビット演算の幅**: `unsigned<<long`、`(long)x<<40`、`u>>31`、補数。
- **enum × bitfield**: `enum 値 | で初期化した unsigned:3` 等。signed bitfield 抽出（`int a:4=-1`）、バイト跨ぎ bitfield（`:6,:6,:6`）、bitfield 代入オーバーフロー（`:4 に 20`）。
- **整数昇格/変換**: char/short 実引数→int、float 実引数→double、narrowing（long→int→short→char 連鎖）、signed↔unsigned、`(int)(char)(long)double` キャスト連鎖、戻り値の暗黙切り詰め（`char f(){return 0x141;}`）。
- **ABI**: >8 整数引数（スタックスピル）、int/float 混在引数、>8 float 引数、struct 引数と scalar 混在、浮動小数レジスタ枯渇スピル。
- **三項/論理**: ternary がポインタ/struct を返す、ネスト三項、`!`/`!!`、短絡、`_Bool` 論理。
- **ポインタ/配列境界**: 配列⇄ポインタ仮引数、負の添字 `p[-1]`、2D の flat 添字 `(&a[0][0])[4]`、ポインタ差/比較/等価。
- **宣言子/typedef**: VLA（1D/2D、`sizeof`、多次元 VLA 引数）、typedef の fp/配列/多段ポインタ/qualified、`int(*p)[3]` 仮引数、const ポインタ vs ポインタ to const、関数を返す関数ポインタ、`int(**pp)(void)=t` 経由呼び出し。
- **集約初期化**: 指示初期化子 `[n]=`（飛び/混在）、複合リテラル `(struct){.x=1,.z=3}`・`(int[5]){[2]=..}`、部分初期化、ネスト struct/union、匿名 struct/union メンバ、flexible array member の sizeof。
- **グローバル初期化**: アドレス+オフセット `&arr[2]`/`arr+3`、定数式 `{1*1,2*2,..}`、struct 内アドレス `{&g,7}`、グローバル 2D 配列。
- **その他**: const struct コピー、static ローカルのアドレス返し、再帰（accumulator/相互/Ackermann は別途確認済み）、連鎖代入 `a=b=c=5`、深いメンバ連鎖 `d.c.b.a.v`、struct 配列メンバのループ、ネスト関数呼び出し引数、volatile 再読み込み、文字列/文字エスケープ（`\t \\ \" \x41 \101 \0`）、文字列連結、除算/剰余の符号全組合せ、シフト端、octal/hex/binary リテラル、`unsigned long long` サフィックス、プリプロセッサ stringize/paste、`#include <string.h>`(strcmp/strlen/memset)。

### 2026-06-22 セッション 続き: 大きめプログラム・libc 連携の探索（全 green、1 件のみ別途修正済み）
最適化が絡む大きめプログラム・libc 連携・レジスタ圧迫を狙った探索。下記は全一致で**再探索不要**
（唯一 global_struct_char_array_member だけ miscompile で別途 🔧 修正済み）:
- **アルゴリズム**: 3x3 行列積、bubble/qsort（int 配列・struct 配列）、popcount ループ、文字列反転、
  二分木（malloc/再帰 sum）、ハッシュテーブル（線形探索 put/get）、bignum 桁加算、状態機械（switch）、
  Duff's device、gcd 二重ループ、ポインタ追跡（idx[]）、固定小数点乗算（`(a*b)>>16`）。
- **libc 連携**: malloc/free、qsort+比較関数、strtok/strlen、memmove（重複領域）、math（1/i² 累積）。
- **レジスタ圧迫**: 16 整数ローカルの混合演算、10 double の混合演算、4 重ネストループ、2D 転置。
- **グローバル集約**: 混在メンバ struct `{int,int,double,char[8],int[4]}`（char[] メンバ含む形が唯一のバグ）。

### 2026-06-22 セッション 続き17: 探索 round 2 (タグ shadowing 完了後)
タグ shadowing 応用形修正後、未探索の角度で probe を 19 件流して下記領域は全て clang 一致 (=
**再探索不要**)。新規 2 件は別途記録 (HANDOFF.md「次セッションの最優先タスク」参照):
- **libc string**: strcpy/strcat/strcmp/memset/memcpy/strlen の組合せ
- **libc math**: sqrt/sin/cos/floor/ceil/fabs/pow/log
- **libc malloc/free**: malloc + 関数引数渡し、strcpy(malloc(16),...)
- **libc qsort**: 関数ポインタ callback (cmp_int)
- **可変長関数**: va_list で int 列 / double 列を sum
- **typedef + 関数ポインタ**: シンプルな `typedef int (*BinOp)(int,int); BinOp ops[3] = {...}` は OK。
  ただし「関数ポインタ配列へのポインタ」(`BinOp (*pa)[3]` で要素が関数ポインタ) は 🆕 (未修正)。
- **bitfield + union**: union { unsigned int v; struct { :8, :8, :16 } } のパッキング
- **文字列リテラル連結**: `"hello " "world"` の concatenation + sizeof
- **16進浮動小数リテラル**: `0x1.8p1` / `0x1p-3` / `0x1.fffp10`
- **VLA + 多次元**: 関数引数 `int a[n][m]` + sum_n
- **三項 + struct/struct ポインタ/配列**: `cond ? a : b` で struct 値・ポインタ・配列
- **static ローカル struct**: 関数呼び越し永続化 (`static struct Counter c = {0, 0.0}` + 累積)
- **再帰 struct (リスト)**: malloc でリスト構築 + sum 累積
- **const struct**: ポインタ経由のアクセス、グローバル const struct
- **offsetof**: char/int/double/配列メンバの offsetof マクロ
- **配列の関数引数 decay**: `int arr[10]` 仮引数の sizeof
- **short ポインタ算術**: short 配列の `p+2`/`p[4]`/`p-arr`
- **function-local static counter**: `static int id = 0; return ++id;`
- **ネスト struct + union メンバ designator (int)**: `{ .n = 99 }` は OK。
  ただし `.f = 2.5f` (float メンバ) は 🆕 (未修正、ネスト union の fp 初期化バグ)。

### 2026-06-22 セッション 続き23: 探索 round 3 (12 probe、新規 2 件発見・修正)
タグ shadowing 完了後、未探索の角度で probe 12 件流して下記領域は全て clang 一致 (=
**再探索不要**)。新規発見の 2 件は続き23 で同時修正:
- **C11 機能**: _Noreturn (stdnoreturn.h)、_Thread_local (TLS 初期化と書き込み)、
  _Static_assert (関数内・トップレベルは元から OK)
- **言語機能**: enum + ビット演算組合せ、部分初期化 (残り 0 埋め、global/local/struct)、
  多次元 const 配列、多引数呼び出し (>8 整数/fp 混在 stack spill)、struct 戻り関数のチェーン
  (`add(make(1,2), make(10,20))`)、グローバルアドレス算術 (`data + 5`、`&data[9]`)、
  libc string 関数の strchr 経路、bitfield + _Alignas 配置と offsetof (非 bitfield メンバ)
- **新規発見 (続き23 で修正済み)**:
  - `typedef IP IPA[3]; IPA arr = {&x, &y, &z}` (pointer-element 配列 typedef の宣言)
    →続き23 で base_is_pointer + 配列 typedef のとき is_pointer=0 リセットして配列宣言経路に。
  - `struct S { _Static_assert(...); int x; };` (struct メンバ位置の static_assert)
    →続き23 で struct_layout に TK_STATIC_ASSERT 分岐追加。
- **C 仕様外で probe から除外**: offsetof on bitfield (clang もエラー、未定義動作)。

### 2026-06-22 セッション 続き24: 探索 round 4 (12 probe、新規 2 件発見・修正 + 3 件残課題)
未探索角度 12 件流して新規 2 件発見・修正 + 3 件残課題を HANDOFF 記録:
- **C11/言語機能 (green、再探索不要)**: typedef of struct (self-referential)、_Generic 細形
  (char*/const char*/int* 区別)、複合代入 `<<=` `>>=` `|=` `&=` 各種 + struct メンバ配列、
  snprintf format flags (`%05d %-8s %+d %.3f %x %o %*d`)、realloc + 動的配列、
  深いネスト (3 重 for + 条件 + 集約)、長い二項演算子チェーン (20 項加算/シフト)、
  struct 配列内 char 配列 + 文字列初期化 (`{5, "hello"}` × 3)
- **修正 (続き24)**:
  - `void *p = (void*)0xdeadbeefL` (明示キャスト経由のポインタ初期化) → node_num_t に
    from_pointer_cast フラグを追加し apply_cast でスタンプ、init check で skip。
  - グローバル `struct P *parr[3]; parr[i]->x` (struct ポインタ配列の subscript + ->)
    → try_build_global_var_node で is_tag_pointer のとき pql=1/bds=struct サイズを立て、
    emit を scalar 経路に流す。
- **残課題** (続き 25/26/27 で全て対応済み):
  - VLA `sizeof(arr)` ランタイム計算: 続き25 で vla_sizeof_direct として修正済み。
    印付き変数経由・variadic 引数経路を網羅。
  - グローバル struct ポインタ配列の init slot 計算: 続き26 で global_struct_ptr_array_subscript
    拡張として修正済み (gbrace_ctx_t.is_tag_pointer を追加)。
  - `(*pp)->x` (struct P** の単独 deref からの ->): 続き27 で *(ptr-to-array) tag carry
    の文脈で解決済み。

### 2026-06-22 セッション 続き32: 探索 round (20 probe、全 green)
3D VLA + 混在 const/VLA 対応後の網羅探索。下記領域は全て clang 一致 = **再探索不要**:
- 関数ポインタ戻り値 / 関数ポインタ配列メンバ + 集約初期化 (FI (*get_picker())(int))
- qsort 複雑 comparator (struct R + tie-break)
- 複合代入チェーン (`s.a[2] += s.a[0] * 3`、構造体メンバ × 配列要素 × shift)
- snprintf format flags 細形 (`#`/`0` フラグ、precision、`e`/`g`/`a` 浮動小数)
- ポインタ配列 + 負添字 + 文字列長合計
- unsigned long ビット演算 (64bit 64bit 混在 long * 0x9E3779B97F4A7C15UL)
- 可変長引数 double 8 個交互加減算
- bitfield + cast (`(unsigned)(signed bitfield)`)
- 関数ポインタ配列 struct メンバ + 集約初期化 (`struct Engine { int(*ops[3])(int,int); }`)
- 混在幅比較 (short+ushort、char==255)
- 再帰 struct list (malloc/free chain + sum)
- switch fallthrough (4-way fallthrough + default)
- designator init array gap (`[1]=1,[3]=3,[5]=5` + positional 混在)
- extern + 同一 TU 定義 (int/double)
- マクロ stringize / paste の組合せ (`VAR(1)`, `MAKE_STR_PAIR(name, val)`)
- volatile + post-increment 副作用
- const 関数ポインタ typedef (`typedef int (*const Cmp)(...)`)
- goto + label の複雑な制御フロー
- ternary が struct 値を返す (`b ? (struct V){...} : (struct V){...}`)
- 大きい struct 値渡し + scalar 混在 (struct{8int+3double} × 2 + int/double)
- **GNU 拡張で probe 除外**: inline 関数の暗黙的 linkage 解釈の細形 (probe `u_inline.c` は
  clang もエラー)。
